[
  {
    "index": 0,
    "verdict": "CONFIRMED",
    "reason": "Reproduced all three sub-claims on the HEAD binary: `batten init --from <doc>` produced a batten.yaml byte-identical to plain `batten init` except the project name; `unit.plan` was never emitted even though the scanner already has a UnitPlan field and an emitter for it; and a nonexistent path was accepted with exit 0. The `--from` value is also NOT forwarded through `--scan-json`, so it does not reach the /batten-init skill either — it is a pure stdout echo, not a deliberate delegation.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v00/run-init/fromtest/context\" \"$SB/sandbox/v00/run-init/flagtest\"\ncd \"$SB/sandbox/v00/run-init/fromtest\" && git init -q . && git config user.email t@t.io && git config user.name tester\ncat > context/planeacion_proyecto.md <<'EOF'\n# Planeacion del proyecto\nNuestro flujo tiene cinco fases: research, plan, build, verify, close.\nLa unidad de trabajo se llama HISTORIA y su patron es HU-\\d+.\nDominios: backend, frontend, datos.\nGates: qa_gate, security_gate.\nEOF\nexport BATTEN_DB=\"$SB/sandbox/v00/state.db\"\n\"$BAT\" init --from context/planeacion_proyecto.md; echo \"EXIT=$?\"\ncd \"$SB/sandbox/v00/run-init/flagtest\" && git init -q .\nexport BATTEN_DB=\"$SB/sandbox/v00/state.db\"\n\"$BAT\" init\ndiff \"$SB/sandbox/v00/run-init/fromtest/batten.yaml\" \"$SB/sandbox/v00/run-init/flagtest/batten.yaml\"\nmkdir -p \"$SB/sandbox/v00/run-init/ghosttest\" && cd \"$SB/sandbox/v00/run-init/ghosttest\" && git init -q .\nexport BATTEN_DB=\"$SB/sandbox/v00/state.db\"\n\"$BAT\" init --from does-not-exist.md; echo \"EXIT=$?\"\ncd \"$SB/sandbox/v00/run-init/fromtest\"\nexport BATTEN_DB=\"$SB/sandbox/v00/state.db\"\n\"$BAT\" init --scan-json --from context/planeacion_proyecto.md > /tmp/sj_from.json\n\"$BAT\" init --scan-json > /tmp/sj_plain.json\ndiff /tmp/sj_from.json /tmp/sj_plain.json",
    "evidence": "$ diff fromtest/batten.yaml flagtest/batten.yaml\n3c3\n< project: fromtest\n---\n> project: flagtest\n(that is the ENTIRE diff — 1 hunk, the directory name)\n\n$ sed -n '/^unit:/,/^$/p' fromtest/batten.yaml\nunit:\n  name: TASK\n  pattern: 'TASK-\\d+'\n(no `plan:`, no `locator:`)\n\n$ batten.exe init --from does-not-exist.md\nwrote batten.yaml — a working draft in report mode (gates warn, don't block).\n  project=\"ghosttest\" unit=\"TASK\", 0 domain(s) detected, graphify found, engram found\nNext:\n  1. fill the invariants (the TODOs) — the highest-value part of the file\n  2. reconcile against your prose workflow: does-not-exist.md\n  2. run: batten doctor\nEXIT=0\n\n$ diff <(batten init --scan-json --from context/planeacion_proyecto.md) <(batten init --scan-json)\nSCAN-JSON IDENTICAL WITH AND WITHOUT --from\n$ grep unit_plan sj_from.json\n  \"unit_plan\": \"\",\n...yet the same scan already SAW the file:\n  \"purpose\": [\n    \"context/planeacion_proyecto.md\"\n  ],\n\nHEAD source, cmd/batten/main.go:1430-1476 — `from` captured at 1437, its ONLY other\noccurrence is the Printf at 1475. Meanwhile internal/scan/scan.go:25 declares\n`UnitPlan string `json:\"unit_plan\"`` and scan.go:583-587 emits `  plan: `+f.UnitPlan`\nwhenever it is non-empty. The field, the emitter and the flag all exist; the one\nassignment between them does not.",
    "positive_control": "Two controls, both needed because a byte-identical file could also mean init is inert in my sandbox. (1) CONTENT-SENSITIVITY control: same binary, same DB, in a sibling dir with go.mod + Makefile + backend/ + frontend/ on branch feature/US-034-thing — `batten init` produced a 37-line diff against fromtest's yaml (stack header `go, make, node`, `name: US` / `pattern: 'US-\\d{3}'`, a `backend:` domain with check commands lifted from the Makefile). So init's output IS driven by what it finds; the byte-identity above is caused by `--from` being inert, not by init being inert. (2) BY-DESIGN control: I tested whether `--from` is deliberately delegated to the /batten-init skill via the declared `--scan-json` handoff (main.go:1448-1449 comment says exactly that) — `--scan-json` output is byte-identical with and without `--from`, so no delegation exists in the binary. n/a on ALLOW/DENY — this finding makes no hook claim.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:1441-1443 — after `facts, err := scan.Scan(cwd)` and BEFORE the `--scan-json` marshal at 1451, add: `if from != \"\" { if _, err := os.Stat(from); err != nil { return fmt.Errorf(\"--from %s: %w\", from, err) }; facts.UnitPlan = from }`. That one insertion satisfies expected (b) and (c) at once — scan.go:583 then emits `unit.plan`, and the path also rides along in `unit_plan` to the /batten-init skill, closing (a)'s handoff. Also retire the misleading `  2. reconcile against your prose workflow: %s` line at 1475 (which additionally duplicates the '2.' index) in favour of an honest 'recorded as unit.plan; the binary does not mine prose — run /batten-init to do that'."
  },
  {
    "index": 1,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary in my own sandbox: renaming `enforcement:` to `enforcment:` flipped `batten doctor` from '● enforcement: REPORT — gates WARN' to '✓ enforcement: enforce — gates block' with EXIT=0 and not one warning. I also confirmed the general case — `bogus_top_level_key: 42`, `domians:` (1 domain silently became 0) and `budgt:` (declared tokens_per_run/on_exceed ceilings silently vanished) all pass with a green ✓. internal/spec/spec.go:206 is still non-strict `yaml.Unmarshal`, and no Go file references batten.schema.json at all (grep for `schema.json` over *.go: no matches), so the root `\"additionalProperties\": false` at batten.schema.json:13 is documentation the binary never enforces. Not Trap 1: this is not fail-open-with-a-warning — there is no warning, and batten found and parsed the spec fine.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v01/typotest\" && cd \"$SB/sandbox/v01/typotest\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\necho hello > README.md && git add -A && git commit -q -m init\nexport BATTEN_DB=\"$SB/sandbox/v01/state.db\"\n\"$BAT\" init < /dev/null\n\"$BAT\" doctor            # baseline: '● enforcement: REPORT'\nsed -i 's/^enforcement: report/enforcment: report/' batten.yaml\n\"$BAT\" doctor; echo \"EXIT=$?\"   # now: '✓ enforcement: enforce — gates block'\nsed -i 's/^enforcment: report/enforcement: report/' batten.yaml\necho 'bogus_top_level_key: 42' >> batten.yaml\n\"$BAT\" doctor; echo \"EXIT=$?\"   # green ✓, no warning\n# fail-open variants, in $SB/sandbox/v01/failopen with a hand-written spec\n# (domains: api/path: src ; budget: tokens_per_run 1000 / on_exceed block):\nexport BATTEN_DB=\"$SB/sandbox/v01/state2.db\"\nsed -i 's/^domains:/domians:/' batten.yaml && \"$BAT\" doctor   # '1 domains' -> '0 domains', still ✓\nsed -i 's/^domians:/domains:/;s/^budget:/budgt:/' batten.yaml && \"$BAT\" doctor  # ceilings gone, still ✓",
    "evidence": "$ grep -n '^enforcment' batten.yaml\n6:enforcment: report\n\n$ \"$BAT\" doctor; echo \"EXIT=$?\"\n✓ ...\\typotest\\batten.yaml — project \"typotest\", unit \"TASK\", 3 phases, 0 domains\n✓ close gate: phase \"close\" requires verdict \"ok\" on any gate\n⚠ gate \"qa\" declares no checks — it verifies NOTHING and approves on the agent's word. ...\n✓ enforcement: enforce — gates block\n✓ store: .../sandbox/v01/state.db\nEXIT=0\n\n--- same file, key spelled correctly (baseline) ---\n● enforcement: REPORT — gates WARN, they do not block yet. Set enforcement: enforce (or remove it) when the team trusts the gates.\nEXIT=0\n\n--- bogus top-level key ---\n$ echo 'bogus_top_level_key: 42' >> batten.yaml && \"$BAT\" doctor | head -4; echo EXIT=...\n✓ ...\\batten.yaml — project \"typotest\", unit \"TASK\", 3 phases, 0 domains\n...\n● enforcement: REPORT — gates WARN, they do not block yet. ...\nEXIT=0\n\n--- fail-open variants (richer spec) ---\n# domains: -> domians:\n✓ ...\\failopen\\batten.yaml — project \"failopen\", unit \"US\", 3 phases, 0 domains      (was: 1 domains)\nEXIT=0\n# budget: -> budgt:   (tokens_per_run: 1000, on_exceed: block declared)\n✓ ...\\failopen\\batten.yaml — project \"failopen\", unit \"US\", 3 phases, 1 domains\nEXIT=0                                                     (no ceiling warning; ceilings silently dropped)\n\n--- code ---\ninternal/spec/spec.go:206:\tif err := yaml.Unmarshal(b, &s); err != nil {\nbatten.schema.json:13:  \"additionalProperties\": false,\ngrep 'schema\\.json' over **/*.go -> No matches found   (schema is never loaded by the binary)",
    "positive_control": "The claim is 'doctor stays silent', so I proved doctor's validation/warning path is live on the very same file in the very same sandbox, three independent ways: (1) it emitted '⚠ gate \"qa\" declares no checks — it verifies NOTHING...' on the unmodified spec, so the warning channel fires; (2) with `budget.on_exceed: deny` it emitted '✗ invalid spec: budget.on_exceed must be block|warn|downgrade_effort, got \"deny\"', so bad VALUES are rejected; (3) with `gates:` renamed to `gate:` it emitted '✗ invalid spec: phase \"verify\" references unknown gate \"qa\"', so cross-reference checks fire. The path is fully engaged — it simply has no unknown-KEY check, which is exactly the gap. (Controls 2 and 3 also bound the blast radius honestly: a typo'd `gates:` IS caught, indirectly, because phases reference it.)",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/spec/spec.go:206 — swap `yaml.Unmarshal(b, &s)` for `dec := yaml.NewDecoder(bytes.NewReader(b)); dec.KnownFields(true); dec.Decode(&s)`; if a hard load error is too aggressive for specs in the wild, keep Load permissive and decode into a `map[string]any` inside doctor to emit `⚠ unknown key %q at top level` per offender."
  },
  {
    "index": 2,
    "verdict": "REFUTED",
    "reason": "ALREADY-FIXED at HEAD by commit 3122d29 'fix: init reads the backlog for the unit, not just branch names'. I rebuilt the finding's conditions (single `main` branch, context/planeacion_proyecto.md with 44 `### US-0NN — ...` headings, AGENTS.md, .claude/skills/portal-git-workflow/SKILL.md) and the HEAD binary emitted all four values the finding says are missing: name US, pattern 'US-\\d{3}', plan context/planeacion_proyecto.md, locator '### {id}'.",
    "repro": "SB=.../scratchpad/ft; BAT=$SB/bin/batten.exe\nmkdir -p $SB/sandbox/v02/proyecto_ui/context && cd $SB/sandbox/v02/proyecto_ui\n# write context/planeacion_proyecto.md with 44 '### US-%03d — Historia' headings + AGENTS.md + README.md + package.json + src/index.ts\ngit init -q -b main . && git config user.email t@t.io && git config user.name tester && git add -A && git commit -q -m initial\ngit branch --list            # -> * main   (only branch, as in the finding's target)\ngrep -c '^### US-' context/planeacion_proyecto.md   # -> 44\nexport BATTEN_DB=$SB/sandbox/v02/state.db\n$BAT init --scan-json | head -20\nexport BATTEN_DB=$SB/sandbox/v02/state.db; $BAT init && cat -n batten.yaml | sed -n '12,20p'\nexport BATTEN_DB=$SB/sandbox/v02/state.db; $BAT doctor",
    "evidence": "init --scan-json:\n  \"project\": \"proyecto_ui\",\n  \"unit_pattern\": \"US-\\\\d{3}\",\n  \"unit_name\": \"US\",\n  \"unit_plan\": \"context/planeacion_proyecto.md\",\n  \"unit_locator\": \"### {id}\",\n\nbatten.yaml (lines 12-16):\n    12  unit:\n    13    name: US\n    14    pattern: 'US-\\d{3}'\n    15    plan: context/planeacion_proyecto.md\n    16    locator: '### {id}'\n\nbatten doctor:\n  ok batten.yaml — project \"proyecto_ui\", unit \"US\", 3 phases, 1 domains",
    "positive_control": "Two controls prove the values are derived from the doc, not constants. (A) Same repo shape with NO backlog doc: unit_pattern \"TASK-\\\\d+\", unit_name \"TASK\", unit_plan \"\", unit_locator \"\" — i.e. the finding's old output is still exactly what a repo with nothing to read produces, so my sandbox is genuinely exercising the new doc-reading path. (B) Backlog with a different prefix (12 '### HU-NN' headings in context/plan.md): unit_pattern \"HU-\\\\d{2}\", unit_name \"HU\", unit_plan \"context/plan.md\", unit_locator \"### {id}\" — the prefix and digit width track the file's content.",
    "already_fixed_at_head": true,
    "severity_adjusted": "not-a-defect",
    "fix_hint": ""
  },
  {
    "index": 3,
    "verdict": "CONFIRMED",
    "reason": "On the HEAD binary, `batten init --from <file>` prints the Next: list as 1, 2, 2, 3 — the inserted \"reconcile against your prose workflow\" line hardcodes \"  2. \" and the following \"run: batten doctor\" line also hardcodes \"  2. \", so nothing renumbers. The same init without --from prints a correct 1, 2, 3, proving the duplication is caused solely by the inserted line.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v03/fromtest/context\"\ncd \"$SB/sandbox/v03/fromtest\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\nprintf '# Planeacion\\n\\n## Fase 1 research\\n## Fase 2 plan\\n## Fase 3 build\\n' > context/planeacion_proyecto.md\ngit add -A && git commit -qm init\n\"$BAT\" version\n\"$BAT\" init --from context/planeacion_proyecto.md; echo \"EXIT=$?\"",
    "evidence": "$ batten version\nbatten 0.1.0\n\n$ batten init --from context/planeacion_proyecto.md\nwrote batten.yaml — a working draft in report mode (gates warn, don't block).\n  project=\"fromtest\" unit=\"TASK\", 0 domain(s) detected, graphify found, engram found\nNext:\n  1. fill the invariants (the TODOs) — the highest-value part of the file\n  2. reconcile against your prose workflow: context/planeacion_proyecto.md\n  2. run: batten doctor\n  3. flip enforcement: enforce when you trust the gates\nEXIT=0",
    "positive_control": "n/a — no ALLOW claim. Ran the negative-control variant instead: `batten init` (same sandbox recipe, no --from) in .../sandbox/v03/plain printed the list correctly as \"  1. fill the invariants ... /  2. run: batten doctor /  3. flip enforcement ...\" — so the numbering path works when the optional line is absent, isolating the duplicate \"2.\" to the --from branch.",
    "already_fixed_at_head": false,
    "severity_adjusted": "polish",
    "fix_hint": "cmd/batten/main.go:1473-1478 — replace the hardcoded \"  1. \"/\"  2. \"/\"  3. \" prefixes with a running step counter (n := 1; print with %d and n++ after each line) so the optional --from line renumbers the ones after it."
  },
  {
    "index": 4,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: `batten claim` only checks collisions inside its own run_id (store.go:490 `WHERE run_id=? AND path=?`), so B1 in run US-011 was told it owns a file A1 already owns in US-010 (rc=0, 'B1 owns 1 file(s); any other agent writing them is now denied'), while the identical claim inside one run is rejected rc=1. The cross-run guard (hooks.go:459) then hard-DENIES both declared owners; I isolated causality by showing A1 could edit its own claimed file (silent ALLOW) until B1 claimed it, after which the same payload was denied.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"; R=\"$SB/sandbox/v04\"\nmkdir -p \"$R\" && cd \"$R\" && git init -q . && git config user.email t@t.io && git config user.name tester\nmkdir -p backend/app && echo 'print(1)' > backend/app/main.py && git add -A && git commit -qm init\nexport BATTEN_DB=\"$R/state.db\"; \"$BAT\" init\n# batten.yaml edited in MY sandbox only: enforcement: enforce, unit.name US, unit.pattern 'US-\\d+'\nexport BATTEN_DB=\"$R/state.db\"; \"$BAT\" phase US-010 build\nexport BATTEN_DB=\"$R/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"A1\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\nexport BATTEN_DB=\"$R/state.db\"; \"$BAT\" claim A1 \"$R/backend/app/main.py\"; echo rc=$?\n# positive control (same run):\nexport BATTEN_DB=\"$R/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"A2\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\nexport BATTEN_DB=\"$R/state.db\"; \"$BAT\" claim A2 \"$R/backend/app/main.py\"; echo rc=$?\nexport BATTEN_DB=\"$R/state.db\"; \"$BAT\" phase US-011 build\nexport BATTEN_DB=\"$R/state.db\"; echo '{\"session_id\":\"s2\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"B1\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\nexport BATTEN_DB=\"$R/state.db\"; \"$BAT\" claim B1 \"$R/backend/app/main.py\"; echo rc=$?\nexport BATTEN_DB=\"$R/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"A1\",\"tool_input\":{\"file_path\":\"'\"$R\"'/backend/app/main.py\"}}' | \"$BAT\" hook PreToolUse\nexport BATTEN_DB=\"$R/state.db\"; echo '{\"session_id\":\"s2\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"B1\",\"tool_input\":{\"file_path\":\"'\"$R\"'/backend/app/main.py\"}}' | \"$BAT\" hook PreToolUse\n# causality isolation on a fresh path backend/app/svc.py:\nexport BATTEN_DB=\"$R/state.db\"; \"$BAT\" claim A1 \"$R/backend/app/svc.py\"\nexport BATTEN_DB=\"$R/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"A1\",\"tool_input\":{\"file_path\":\"'\"$R\"'/backend/app/svc.py\"}}' | \"$BAT\" hook PreToolUse\nexport BATTEN_DB=\"$R/state.db\"; \"$BAT\" claim B1 \"$R/backend/app/svc.py\"\nexport BATTEN_DB=\"$R/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"A1\",\"tool_input\":{\"file_path\":\"'\"$R\"'/backend/app/svc.py\"}}' | \"$BAT\" hook PreToolUse",
    "evidence": "=== claim A1 backend/app/main.py ===\nA1 owns 1 file(s); any other agent writing them is now denied\nrc=0\n=== POSITIVE CONTROL: same-run second agent A2 claims the same file (must FAIL) ===\nbatten: write-set collision: backend/app/main.py is already owned by n-A1\nrc=1\n=== 6. claim B1 on the SAME file already owned by A1 in US-010 ===\nB1 owns 1 file(s); any other agent writing them is now denied\nrc=0\n=== 7. A1 (declared owner in US-010) edits the file it owns ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: backend/app/main.py is being worked by US-011 in another open session (run US-011-1785250426629974400-72e5288f).\\nEditing it now races that work. Coordinate, or use a worktree per unit so each has its own branch.\"}}\n=== 8. B1 (declared owner in US-011) edits the file it owns ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: backend/app/main.py is being worked by US-010 in another open session (run US-010-1785250404810982500-d293071d).\\nEditing it now races that work. Coordinate, or use a worktree per unit so each has its own branch.\"}}\n=== CAUSALITY on fresh file backend/app/svc.py ===\n-- A1 claims it (run US-010) --\nA1 owns 1 file(s); any other agent writing them is now denied\n-- A1 edits it BEFORE any other session claims: expect ALLOW (silence) --\nrc=0            <-- silent, and the same payload denies once B1 claims\n-- now B1 (run US-011) claims the very same file --\nB1 owns 1 file(s); any other agent writing them is now denied\nrc=0\n-- A1 edits it AGAIN, nothing else changed: --\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: backend/app/svc.py is being worked by US-011 in another open session (run US-011-1785250426629974400-72e5288f).\\n...\"}}",
    "positive_control": "Three controls. (1) CLAIM control: the identical claim inside ONE run is rejected -- `batten claim A2 backend/app/main.py` -> `write-set collision: backend/app/main.py is already owned by n-A1`, rc=1. So `batten claim` demonstrably has a working rejection path; it simply is not wired across runs. (2) DENY control (silence is not allow): A1's PreToolUse on the same path returned a JSON deny envelope, so the guard provably engages in this sandbox. (3) ALLOW control: the SAME A1 payload on `backend/app/svc.py` was silent before B1 claimed it and denied after -- the only variable changed was B1's claim, so the silence was a genuine allow and the deny is caused by the accepted cross-run claim.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:327 (cmdClaim) / internal/store/store.go:481 (ClaimWriteSet, whose lookup at :490 is scoped `WHERE run_id=? AND path=?`): before INSERT, call the already-existing store.WriteSetOwnerAcrossOpenRuns(project, path, runID) (store.go:1062) and return the same `write-set collision: %s is already owned by %s (run %s)` error, so the plan bug fails at claim time instead of detonating as a mutual deny at hooks.go:459. Not covered by Trap 1: this is a hard DENY of the recorded owner, not a fail-open warning, and it contradicts the guard's own doc comment at hooks.go:399-403 that only session B should be stopped."
  },
  {
    "index": 5,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary in a clean sandbox: with US-012 open (phase node + subagent node, canvas 4 nodes/1 edge), running `batten phase US-013 build` moved the single row `p-build` to US-013's run, so `batten show US-012` lost its phase node and `batten canvas US-012` fell to 1 node / 0 edges. It ping-pongs back when US-012's build phase is re-opened (US-012 3 nodes, US-013 1 node), and the same collision exists for subagent ids: a SubagentStart with agent_id C1 under US-013 stole `n-C1` too, leaving US-012's DAG completely empty. Node ids (`p-`+phase at cmd/batten/main.go:368, `n-`+agent_id at internal/hooks/hooks.go:479) carry no run scope while nodes.node_id is a global PRIMARY KEY (internal/store/store.go:84) written with INSERT OR REPLACE (store.go:404). Not by design: batten explicitly supports concurrent open runs (the cross-session guard at hooks.go:461 says a file 'is being worked by <unit> in another open session'), and this is data loss, not a fail-open advisory.",
    "repro": "SB=.../scratchpad/ft; BAT=$SB/bin/batten.exe; WD=$SB/sandbox/v05/b\nmkdir -p \"$WD\" && cd \"$WD\"\ngit init -q . ; git config user.email t@t.io ; git config user.name tester\necho hi > a.txt; git add -A; git commit -qm init\nexport BATTEN_DB=\"$WD/state.db\"\n\"$BAT\" init\n\"$BAT\" phase US-012 build\necho '{\"session_id\":\"s3\",\"cwd\":\"'\"$WD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"C1\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" show US-012 ; \"$BAT\" canvas US-012 --out before.canvas\n\"$BAT\" phase US-013 build\n\"$BAT\" show US-012 ; \"$BAT\" canvas US-012 --out after.canvas\n\"$BAT\" show US-013\n# escalation: same agent_id under the second unit\necho '{\"session_id\":\"s9\",\"cwd\":\"'\"$WD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"C1\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" show US-012 ; \"$BAT\" canvas US-012 --out after2.canvas\n# ping-pong (first sandbox, sandbox/v05): after US-013 stole it,\n\"$BAT\" phase US-012 build ; \"$BAT\" canvas US-012 --out pp-012.canvas ; \"$BAT\" canvas US-013 --out pp-013.canvas",
    "evidence": "=== BEFORE ===\nUS-012  run=US-012-1785250745258771800-f5ab3f00  status=running  phase=build  base=654496c\n  [phase] build                    running \n  [subagent] backend                  running \nwrote before.canvas (4 nodes, 1 edges)\n\n=== a second session opens ANOTHER unit: batten phase US-013 build ===\nanchor: US-013 base SHA = 654496c\nUS-013 -> phase build\n\n=== AFTER - US-012 DAG ===\nUS-012  run=US-012-1785250745258771800-f5ab3f00  status=running  phase=build  base=654496c\n  [subagent] backend                  running \nwrote after.canvas (1 nodes, 0 edges)\n\n=== US-013 (the thief) ===\nUS-013  run=US-013-1785250755091609300-468e79a0  status=running  phase=build  base=654496c\n  [phase] build                    running \n\n=== US-013 session also spawns an agent_id \"C1\" ===\n--- US-012 now ---\nUS-012  run=US-012-...-f5ab3f00  status=running  phase=build  base=654496c\n(no nodes at all)\n--- US-013 now ---\nUS-013  run=US-013-...-468e79a0  status=running  phase=build  base=654496c\n  [phase] build                    running \n  [subagent] backend                  running \nwrote after2.canvas (1 nodes, 0 edges)\n\n=== PING-PONG (sandbox/v05, after US-013 had stolen it) ===\nUS-012 -> phase build\nwrote pp-012.canvas (3 nodes, 0 edges)\nwrote pp-013.canvas (1 nodes, 0 edges)\n\n=== canvas diff proving the shared key ===\nbefore: {\"nodes\":[{\"id\":\"g-p-build\",...},{\"id\":\"p-build\",...},{\"id\":\"header\",...}]}\nafter : {\"nodes\":[{\"id\":\"header\",...}]}",
    "positive_control": "Not an ALLOW claim, but the renderer was proved live in the same DB rather than merely silent: (a) immediately before the theft, `show US-012` printed both nodes and `canvas` wrote 4 nodes/1 edge; (b) after the theft the identical commands against US-013 printed the phase node, so `show`/`canvas` still render phase nodes - only US-012's are gone; (c) re-running `batten phase US-012 build` brought the node back (1 -> 3 canvas nodes) while emptying US-013, so the emptiness tracks the last writer of the shared key, not a broken command. Control on the hook: the SubagentStart payload creates a visible [subagent] node when the cwd resolves (it silently no-ops with an MSYS-style cwd or when two unowned runs make activeUnit ambiguous), so the node's presence in BEFORE proves the hook engaged.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "Namespace node ids by run: cmd/batten/main.go:368 NodeID: run.RunID+\":p-\"+phaseID, and internal/hooks/hooks.go:479/494 for \"n-\"+AgentID and the AddEdge source; or make nodes PK (run_id,node_id) at store.go:84 and scope FinishNode/NodeByAgent by run_id."
  },
  {
    "index": 6,
    "verdict": "CONFIRMED",
    "reason": "On the HEAD binary, A2's Edit of A1's claimed file is denied, and the byte-equivalent write issued through Bash (shell redirect, `sed -i`, and the finding's own python heredoc) produces empty stdout and exit 0 — no deny, no advise(), no systemMessage. PostToolUse on the same command is silent too, so there is not even an after-the-fact warning. This is NOT the deliberate fail-open: batten has full attribution here (it names owner n-A1 and trespasser n-A2 one call earlier on the identical path), and ROADMAP.md:198 principle #3 is \"Fail open only out loud\" — this fails open silently. hooks.go:250-255 shows the `case \"Bash\"` branch calls only verdictGate; nothing in the hooks.go history fixes it.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v06\" && cd \"$SB/sandbox/v06\"\nexport BATTEN_DB=\"$PWD/state.db\"\nW=$(pwd -W)   # MUST be a Windows path: a Git-Bash /tmp/... cwd makes every hook a silent no-op\ngit init -q . && git config user.email t@t.io && git config user.name tester\nmkdir -p backend/app frontend && printf 'print(\"hi\")\\n' > backend/app/main.py && printf 'x\\n' > frontend/a.js\ngit add -A && git commit -qm init\n\"$BAT\" init\nsed -i 's/^enforcement: report/enforcement: enforce/' batten.yaml\n\"$BAT\" phase TASK-001 build\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PostToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"batten phase TASK-001 build\"}}' | \"$BAT\" hook PostToolUse\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"A1\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"A2\",\"agent_type\":\"frontend\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" claim A1 \"$W/backend/app/main.py\"\n\"$BAT\" claim A2 \"$W/frontend/a.js\"\n# 1) A2 Edit on A1's file -> DENY\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"A2\",\"tool_input\":{\"file_path\":\"'\"$W\"'/backend/app/main.py\"}}' | \"$BAT\" hook PreToolUse; echo \"EXIT=$?\"\n# 2) POSITIVE CONTROL: same session/agent/cwd, tool_name=Bash, a command that MUST deny\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"agent_id\":\"A2\",\"tool_input\":{\"command\":\"git commit -m x\"}}' | \"$BAT\" hook PreToolUse; echo \"EXIT=$?\"\n# 3) the identical write, via Bash\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"agent_id\":\"A2\",\"tool_input\":{\"command\":\"echo \\\"# rewritten by A2\\\" > backend/app/main.py\"}}' | \"$BAT\" hook PreToolUse; echo \"EXIT=$?\"\n# 4) sed -i variant\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"agent_id\":\"A2\",\"tool_input\":{\"command\":\"sed -i s/hi/bye/ backend/app/main.py\"}}' | \"$BAT\" hook PreToolUse; echo \"EXIT=$?\"\n# 5) the finding's own python heredoc\nprintf '%s' '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"agent_id\":\"A2\",\"tool_input\":{\"command\":\"python - <<PY\\nopen(\\u0027backend/app/main.py\\u0027,\\u0027w\\u0027).write(\\u0027# rewritten by A2\\\\n\\u0027)\\nPY\"}}' | \"$BAT\" hook PreToolUse; echo \"EXIT=$?\"\n# 6) no after-the-fact warning either\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PostToolUse\",\"tool_name\":\"Bash\",\"agent_id\":\"A2\",\"tool_input\":{\"command\":\"echo \\\"# rewritten by A2\\\" > backend/app/main.py\"}}' | \"$BAT\" hook PostToolUse; echo \"EXIT=$?\"",
    "evidence": "=== 1. A2 Edit on A1's file (must DENY) ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: write-set collision. backend/app/main.py belongs to another agent's write-set (n-A1); you are n-A2.\\nTwo agents must never write the same file — that is what makes the fan-out safe.\\nYour write-set:\\n  frontend/a.js\\nIf this file genuinely belongs to you, the plan is wrong: fix the plan, do not cross the fence.\"}}\nEXIT=0\n\n=== 2. POSITIVE CONTROL: Bash git commit (must DENY -> proves the Bash branch engages) ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-001 has no verdict envelope. Run the \\\"verify\\\" phase before committing.\\nTo proceed anyway (recorded in the audit log): batten override TASK-001 --reason \\\"...\\\"\"}}\nEXIT=0\n\n=== 3. A2 does the IDENTICAL write via Bash redirect ===\n(no output)\nEXIT=0\n\n=== 4. A2 via sed -i on A1's file ===\n(no output)\nEXIT=0\n\n=== 5. A2 via python heredoc (the finding's exact command) ===\n(no output)\nEXIT=0\n\n=== PostToolUse after the Bash write (any after-the-fact warning?) ===\n(no output)\nEXIT=0\n\n=== claim still in force? re-run the Edit ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: write-set collision. backend/app/main.py belongs to another agent's write-set (n-A1)...\n\nHEAD source, internal/hooks/hooks.go:248-264:\n\tswitch in.ToolName {\n\tcase \"Bash\":\n\t\t...\n\t\treturn h.verdictGate(in, bi.Command)      // <- write-set never consulted\n\tcase \"Write\", \"Edit\", \"NotebookEdit\":\n\t\t...\n\t\treturn h.writeSetGuard(in, wi.FilePath)\n\nplugin/claude-code/hooks/hooks.json: PreToolUse has a \"Bash\" matcher, so the payload does reach the binary.\nROADMAP.md:198 — principle #3: \"Fail open only out loud. If the gate cannot attribute the work, it does not block — and it says so.\" Here batten CAN attribute and still says nothing.",
    "positive_control": "Two controls, both passed. (a) Engagement control per Trap 2: the same session_id/agent_id/cwd with tool_name=Bash and command 'git commit -m x' returned a hard permissionDecision:deny (\"TASK-001 has no verdict envelope\"), proving the Bash branch of preToolUse is reached and can emit output for exactly this payload shape — so the silence on the write commands is a real ALLOW, not a dead hook. (b) Guard-liveness control: the same file via Edit AND via Write both returned deny naming owner n-A1 vs trespasser n-A2, before and after the Bash writes, proving the claim was in force throughout.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/hooks/hooks.go:250-255 — in `case \"Bash\"`, when verdictGate returns nil, scan bi.Command for write targets (`>`/`>>`/tee, sed -i, mv/cp, patch, python open(...,'w')) and route each resolved path through an advisory variant of writeSetGuard so a cross-fence Bash write warns instead of passing in silence."
  },
  {
    "index": 7,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary in enforce mode. `batten claim agent-fe-4 <root>/frontend/` is accepted and prints \"agent-fe-4 owns 1 file(s); any other agent writing them is now denied\", yet a foreign agent's Edit of <root>/frontend/pages/index.vue is silently allowed (empty output, rc=0) — while the identical payload against an exact-file claim is hard-denied. The claim is stored as the literal path `frontend`, so it only ever fences a write to `frontend` itself. The out-of-repo variant reproduces too: `batten claim agent-fe-4 C:/Windows/System32/drivers/etc/hosts` is accepted with the same 'now denied' assertion and stored as `c:/windows/system32/drivers/etc/hosts`, which writeSetGuard can never match because it returns early for any path whose repo-relative form starts with `../`. A path that does not exist at all is also accepted.",
    "repro": "SB=$SANDBOX\nmkdir -p \"$SB/sandbox/v07\" && cd \"$SB/sandbox/v07\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nmkdir -p frontend/pages ml && printf 'x\\n' > frontend/pages/index.vue && printf 'x\\n' > ml/train.py\ngit add -A && git commit -qm init\nR=\"$(pwd -W)\"                       # Windows-form path: the hook payload's cwd must NOT be the MSYS /tmp/... form\nexport BATTEN_DB=\"$R/state.db\"\nBAT=\"$SB/bin/batten.exe\"\n\"$BAT\" init\nsed -i 's/^enforcement: report$/enforcement: enforce/' batten.yaml\n\"$BAT\" phase TASK-1 build\nfor A in agent-fe-4 agent-be-4; do echo '{\"session_id\":\"sess-E\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"'\"$A\"'\",\"agent_type\":\"frontend\"}' | \"$BAT\" hook SubagentStart; done\n\n# --- POSITIVE CONTROL: exact-file claim must deny ---\n\"$BAT\" claim agent-fe-4 \"$R/ml/train.py\"\necho '{\"session_id\":\"sess-E\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"agent-be-4\",\"tool_input\":{\"file_path\":\"'\"$R\"'/ml/train.py\"}}' | \"$BAT\" hook PreToolUse\n\n# --- CASE A: directory claim ---\n\"$BAT\" claim agent-fe-4 \"$R/frontend/\"\necho '{\"session_id\":\"sess-E\",\"cwd\":\"'\"$R\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"agent-be-4\",\"tool_input\":{\"file_path\":\"'\"$R\"'/frontend/pages/index.vue\"}}' | \"$BAT\" hook PreToolUse; echo \"[END] rc=$?\"\n\n# --- CASE B: path outside the repo / CASE C: path that does not exist ---\n\"$BAT\" claim agent-fe-4 \"C:/Windows/System32/drivers/etc/hosts\"\n\"$BAT\" claim agent-fe-4 \"$R/does/not/exist/anywhere.txt\"\n\n# --- reveal the stored path form via the collision message ---\n\"$BAT\" claim agent-be-4 \"$R/frontend/\"\n\"$BAT\" claim agent-be-4 \"C:/Windows/System32/drivers/etc/hosts\"\n\"$BAT\" show TASK-1",
    "evidence": "=== POSITIVE CONTROL: claim a real FILE, foreign agent edits it ===\nagent-fe-4 owns 1 file(s); any other agent writing them is now denied\nrc=0\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: write-set collision. ml/train.py belongs to another agent's write-set (n-agent-fe-4); you are n-agent-be-4....\"}}\nrc=0\n\n=== CASE A: claim an existing DIRECTORY ===\nagent-fe-4 owns 1 file(s); any other agent writing them is now denied\nrc=0\n--- other agent edits a file UNDER the claimed directory\n[END-OF-OUTPUT] rc=0            <-- completely silent: ALLOWED\n\n=== CASE B: claim a path OUTSIDE the repo ===\nagent-fe-4 owns 1 file(s); any other agent writing them is now denied\nrc=0\n\n=== CASE C: claim a directory that does NOT exist at all ===\nagent-fe-4 owns 1 file(s); any other agent writing them is now denied\nrc=0\n\n=== what agent-fe-4 is recorded as owning ===\nTASK-1  run=TASK-1-1785250681867960700-34a8d234  status=running  phase=build  base=3eba0ed\n  [phase] build                    running\n  [subagent] frontend                 running  owns 4 file(s)\n  [subagent] frontend                 running\n\n=== reveal the stored path form via the collision message ===\nbatten: write-set collision: frontend is already owned by n-agent-fe-4\nbatten: write-set collision: c:/windows/system32/drivers/etc/hosts is already owned by n-agent-fe-4\n\n=== the directory claim only fences the literal path 'frontend' ===\n(Edit of <root>/frontend)\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: write-set collision. frontend belongs to another agent's write-set (n-agent-fe-4); you are n-agent-be-4....\"}}",
    "positive_control": "Ran twice, before and after the failing case, in the SAME sandbox, same run, same two agents, same payload shape — only file_path differs. `batten claim agent-fe-4 <root>/ml/train.py` followed by agent-be-4 editing <root>/ml/train.py yields permissionDecision=deny both times, so the write-set guard is demonstrably engaged. A third control shows the directory claim is not dead data either: an Edit of <root>/frontend (the literal claimed path, no trailing component) IS denied. The guard fires; the directory claim simply matches nothing under it. Silence in Case A is therefore a real ALLOW, not a hook that failed to engage.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:320-326 (cmdClaim): os.Stat each arg — reject a directory with the file-list form (or expand it / support an explicit `dir/**` prefix honoured by internal/hooks/hooks.go:404 writeSetGuard), reject args whose filepath.Rel is empty/starts with '..' instead of storing the absolute path, and print the claimed paths rather than only a count so 'owns N file(s); ... now denied' can never assert protection that does not exist."
  },
  {
    "index": 8,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: when a registered subagent that never ran `batten claim` trespasses on another agent's file, the deny reason prints 'Your write-set:' followed by a line containing only two spaces. The blank bullet is exactly strings.Join(mine, \"\\n  \") over an empty slice at internal/hooks/hooks.go:452-454; the code has no empty-slice guard at HEAD, so this is NOT already fixed.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v08\" && cd \"$SB/sandbox/v08\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\nmkdir -p backend/app frontend/src && echo x > backend/app/main.py && echo y > frontend/src/App.tsx\ngit add -A && git commit -qm init\n\"$BAT\" init\nsed -i 's/^enforcement: report/enforcement: enforce/' batten.yaml\ngit checkout -q -b TASK-1\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" phase TASK-1 build\nP=$(pwd -W)\nexport BATTEN_DB=\"$PWD/state.db\"\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$P\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"A1\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$P\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"A2\",\"agent_type\":\"frontend\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" claim A1 \"$P/backend/app/main.py\"\n# A2 deliberately never claims anything\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$P\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"A2\",\"tool_input\":{\"file_path\":\"'\"$P\"'/backend/app/main.py\"}}' | \"$BAT\" hook PreToolUse",
    "evidence": "$ ... | batten.exe hook PreToolUse   (EXIT=0, raw stdout)\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: write-set collision. backend/app/main.py belongs to another agent's write-set (n-A1); you are n-A2.\\nTwo agents must never write the same file — that is what makes the fan-out safe.\\nYour write-set:\\n  \\nIf this file genuinely belongs to you, the plan is wrong: fix the plan, do not cross the fence.\"}}\n\nRendered (note the line that is nothing but two spaces):\nbatten: write-set collision. backend/app/main.py belongs to another agent's write-set (n-A1); you are n-A2.\nTwo agents must never write the same file — that is what makes the fan-out safe.\nYour write-set:\n  \nIf this file genuinely belongs to you, the plan is wrong: fix the plan, do not cross the fence.\n\nHEAD source (unchanged, no empty guard) internal/hooks/hooks.go:448-454:\n  mine, _ := h.Store.WriteSet(myRun, myNode)\n  return h.gate(\"PreToolUse\", fmt.Sprintf(\n    ... \"Your write-set:\\n  %s\\n\" ...,\n    rel, owner, myNode, strings.Join(mine, \"\\n  \")))\nNo test asserts this string: `grep -rn \"Your write-set\" --include=*_test.go .` returns nothing.",
    "positive_control": "n/a for an ALLOW claim — this finding asserts a DENY message, and the deny demonstrably fired (permissionDecision=deny), so the guard engaged. I ran two contrast controls on the same payload with ONE field changed to prove the blank line is the empty-slice artifact and not a dead path:\n(A) `batten claim A2 <cwd>/frontend/src/App.tsx`, then the IDENTICAL trespass payload -> same deny, but the hint now renders content:\n    Your write-set:\n      frontend/src/app.tsx\n(B) A2 editing its own claimed file (only file_path changed) -> no output, exit 0, i.e. allowed. Together these show the guard is live, the deny is correct, and only the write-set hint degrades to a blank bullet when the trespasser has claimed nothing.",
    "already_fixed_at_head": false,
    "severity_adjusted": "polish",
    "fix_hint": "internal/hooks/hooks.go:448-454 — before the Sprintf, replace the empty case: `ws := strings.Join(mine, \"\\n  \"); if len(mine) == 0 { ws = \"(none — you have not claimed any files; run `batten claim \" + in.AgentID + \" <file>...` with the write-set from the plan)\" }`. Use in.AgentID (A2), not myNode (n-A2): `batten claim` takes the agent id.",
    "notes": "ALREADY-FIXED does not apply. Two adjacent observations, out of scope for this finding but visible in the evidence: (1) the message addresses the agent as `n-A2` while the `batten claim` CLI expects `A2`, so even the corrected hint must use the agent id; (2) `batten claim` stored `frontend/src/App.tsx` as `frontend/src/app.tsx` (lowercased), which the deny hint then echoes back lowercased. Isolation respected: BATTEN_DB was exported in every batten invocation, all work confined to sandbox/v08, no file in the repo under test was modified (git status clean)."
  },
  {
    "index": 9,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary with enforcement: enforce. `batten check US-013` writes its own source=\"batten\" verdict, and that single row satisfies BOTH gate conditions in verdictGate (the generic LatestVerdict at hooks.go:303 and the source-scoped LatestVerdictBySource at hooks.go:329). The commit that was denied a moment earlier is then allowed, on a unit whose diff since its own anchor is empty, whose phase is still `build` (verify never ran), and against which nothing judged any acceptance criterion. `batten close US-013 --status ok` also succeeds. The gate is one-sided: an agent verdict alone is denied (\"no batten-verified pass\"), but a batten verdict alone passes unchallenged.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nWSB='$SANDBOX'\nmkdir -p \"$SB/sandbox/v09\" && cd \"$SB/sandbox/v09\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n# batten.yaml: unit US-\\d+, enforcement: enforce, phases build / verify(gate qa, diff_from anchor) / close(requires_verdict ok),\n#              gates.qa.checks: ['git --version', 'git status --porcelain'], verdict: required, evidence: required\nmkdir -p backend && echo \"print('hello')\" > backend/app.py\ngit add -A && git commit -q -m \"base: repo exists before US-013\"   # -> 08e7837\ngit checkout -q -b feature/US-013-heavy-exports\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" phase US-013 build\n# A) positive control BEFORE any verdict -- must DENY\nexport BATTEN_DB=\"$PWD/state.db\"; printf '{\"session_id\":\"s3\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m x\"}}' \"$WSB\" | \"$BAT\" hook PreToolUse; echo \"[hook exit=$?]\"\n# B) the single command under test\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" check US-013\n# C) the SAME payload again -- now allowed\nexport BATTEN_DB=\"$PWD/state.db\"; printf '{\"session_id\":\"s3\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m x\"}}' \"$WSB\" | \"$BAT\" hook PreToolUse; echo \"[hook exit=$?]\"\n# D) US-013 changed nothing\ngit diff --stat 08e7837 HEAD\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" show US-013\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" close US-013 --status ok\n# E) asymmetry: agent verdict WITH evidence but no batten check -- denied\ngit checkout -q -b feature/US-015-asym\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" phase US-015 build\nprintf '{\"check_id\":\"US-015-qa\",\"gate\":\"qa\",\"result\":\"ok\",\"why\":\"looks good\",\"safe_next_step\":\"close\",\"evidence\":[\"I ran the tests and they pass\"]}' > /tmp/v09v.json\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" verdict --unit US-015 --file /tmp/v09v.json\nexport BATTEN_DB=\"$PWD/state.db\"; printf '{\"session_id\":\"s15\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m x\"}}' \"$WSB\" | \"$BAT\" hook PreToolUse; echo \"[hook exit=$?]\"",
    "evidence": "=== A) commit hook BEFORE any verdict (positive control: MUST deny) ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-013 has no verdict envelope. Run the \\\"verify\\\" phase before committing.\\nTo proceed anyway (recorded in the audit log): batten override US-013 --reason \\\"...\\\"\"}}\n[hook exit=0]\n\n=== B) batten check US-013 (US-013 has ZERO code; checks were green before it existed) ===\n  ✓ git --version\n  ✓ git status --porcelain\n\nUS-013: OK (batten-verified). all gate checks passed (batten ran them)\nEXIT=0\n\n=== C) SAME commit hook payload again ===\n[hook exit=0]           <-- ALLOW (no JSON emitted)\n\n=== D) diff since US-013 anchor 08e7837 ===\n(empty == US-013 changed nothing)\n\n=== batten show US-013 ===\nUS-013  run=US-013-1785250721902840500-45c5a961  status=running  phase=build  base=08e7837\n  [phase] build                    running\nverdict qa=ok (batten): all gate checks passed (batten ran them)\n  - git --version: PASS (exit 0, 114ms)\n  - git status --porcelain: PASS (exit 0, 124ms)\n   ^ the ONLY verdict on the run, and the phase never left `build` -- verify never ran.\n\n=== batten close US-013 --status ok ===\nUS-013 closed (ok). Write-set claims released -- files it held are free again.\nclose EXIT=0\n\n=== E) asymmetry -- agent verdict alone is NOT enough (the enforced direction) ===\nverdict recorded: US-015 qa=ok (1 evidence)\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-015 has no batten-verified pass. The gate's checks must be RUN, not asserted.\\nRun: batten check US-015\\nTo proceed anyway (recorded): batten override US-015 --reason \\\"...\\\"\"}}\n\nIntent that `check` was never meant to be the whole gate, from batten's own source:\n  cmd/batten/main.go:790  result, why, next = \"ok\", \"all gate checks passed (batten ran them)\", \"add your acceptance-criteria judgment, then close\"\n  internal/store/store_test.go:65  \"asserts LatestVerdictBySource separates the agent's claim from batten's evidence\"\n  docs/VERIFICATION.md:75  \"commit con checks declarados exige veredicto source='batten'\" -- only the agent-only direction was ever tested.",
    "positive_control": "Two controls, both passed. (1) Temporal: the IDENTICAL payload denied before `batten check` and was silent after it -- the gate demonstrably engages with this exact payload/cwd/session. (2) Simultaneous, one field changed: at the same moment the US-013 payload was silent, the same payload with session_id s9 on branch feature/US-014-control (phase build, no check run) returned an explicit deny for US-014. So the silence on US-013 is a real ALLOW, not a dead hook. Cross-checked with `batten show US-013` (verdict qa=ok source=batten present) and `batten close US-013 --status ok` succeeding.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/hooks/hooks.go:328-336 -- inside the `len(g.Checks) > 0` branch, also require a NON-batten verdict (result==closing.RequiresVerdict, non-empty evidence) alongside the source=\"batten\" one; today check's own row satisfies the generic LatestVerdict at hooks.go:303, so one command clears both halves of the gate.",
    "notes": "Not the deliberate fail-open of Trap 1: the advise() branch at hooks.go:337-352 only fires when the gate declares NO checks. Here the gate declares checks and returns a fully silent allow -- no warning at all -- for a unit with an empty diff still sitting in phase `build`. Sandbox: .../scratchpad/ft/sandbox/v09, BATTEN_DB exported to $PWD/state.db on every command; the repo under test and ~/.batten were never touched."
  },
  {
    "index": 10,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary (b38fd5a). After `batten override US-014`, the PreToolUse commit gate flips from a hard deny to silent-allow, yet `batten show US-014` still prints \"no verdict: the close gate will deny a commit\" — the literal opposite of the truth — `batten runs` shows VERDICT `—`, and the canvas contains no 'override' string. This is not by-design fail-open: two other surfaces built on the same store (`batten statusline` and the MCP `batten_verdict_status`) DO read HasOverride and correctly say \"no verdict (overridden)\". The CLI surfaces and the SessionStart context block are simply not asking.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"; D=\"$SB/sandbox/v10\"\nmkdir -p \"$D\" && cd \"$D\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$D/state.db\"\necho hello > README.md && git add -A && git commit -qm init\n\"$BAT\" init\nperl -pi -e 's/^enforcement: report/enforcement: enforce/; s/TASK-/US-/; s/  name: TASK/  name: US/' batten.yaml\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" doctor\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" phase US-014 build\n# POSITIVE CONTROL (before override) — the gate must deny:\nexport BATTEN_DB=\"$D/state.db\"; printf '{\"session_id\":\"s4\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m x\"}}' \"$D\" | \"$BAT\" hook PreToolUse; echo \"[hook exit=$?]\"\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" override US-014 --reason \"hotfix for the Friday demo; QA runs Monday, tracked in US-014-followup\"\n# same payload, one thing changed (the override now exists) — the gate must allow:\nexport BATTEN_DB=\"$D/state.db\"; printf '{\"session_id\":\"s4\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m x\"}}' \"$D\" | \"$BAT\" hook PreToolUse; echo \"[hook exit=$?]\"\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" show US-014\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" runs\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" canvas US-014 --out \"$D/us014.canvas\"; grep -c override \"$D/us014.canvas\"\nexport BATTEN_DB=\"$D/state.db\"; printf '{\"session_id\":\"s4\",\"cwd\":\"%s\",\"workspace\":{\"current_dir\":\"%s\"}}' \"$D\" \"$D\" | \"$BAT\" statusline\nexport BATTEN_DB=\"$D/state.db\"; printf '{\"session_id\":\"s5\",\"cwd\":\"%s\",\"hook_event_name\":\"SessionStart\"}' \"$D\" | \"$BAT\" hook SessionStart",
    "evidence": "=== POSITIVE CONTROL: commit hook BEFORE override (gate proves it fires) ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-014 has no verdict envelope. Run the \\\"verify\\\" phase before committing.\\nTo proceed anyway (recorded in the audit log): batten override US-014 --reason \\\"...\\\"\"}}\n[hook exit=0]\n\n=== override ===\noverride recorded for US-014 (gate *): hotfix for the Friday demo; QA runs Monday, tracked in US-014-followup\nthe gate will now allow the commit. This override is in the audit log.\n\n=== IDENTICAL payload AFTER override — now allows (silence, and the control above proves silence here means ALLOW) ===\n[hook exit=0]\n\n=== batten show US-014 — states the OPPOSITE of the truth ===\nUS-014  run=US-014-1785251132475331500-fedd88d6  status=running  phase=build  base=5fcc46b\n  [phase] build                    running\n\nno verdict: the close gate will deny a commit\n\n=== batten runs — no trace of the override ===\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-014  running  build  —        —       —\n\n=== canvas ===\nwrote .../us014.canvas (3 nodes, 0 edges)\n0        <- grep -c override\n\n=== SessionStart additionalContext (what the AGENT reads) — same false claim ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"## batten — v10 (US)\\n\\n- **US-014** — phase `build`, status `running`, **no verdict yet** — the close gate will deny a commit\\n\"}}\n\n=== batten statusline — the surface that DOES ask HasOverride, on the SAME db, same instant ===\nbatten US-014 build · no verdict (overridden)",
    "positive_control": "Ran the byte-identical PreToolUse payload twice against the same run, with the override as the ONLY difference. Before the override the hook emitted an explicit permissionDecision=deny JSON; after the override it emitted nothing. The deny proves the gate engages in this sandbox, so the post-override silence is a real ALLOW, not a dead hook. Independently corroborated by internal/hooks/hooks.go:289 (`if ok, _ := h.Store.HasOverride(...); ok { return nil, nil }`) and by `batten statusline` reporting \"(overridden)\" on the same database at the same moment.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:492 (cmdShow else-branch) must call st.HasOverride(run.RunID, gate) and print \"gate OPEN by override\" + reason + ts instead of \"the close gate will deny a commit\"; needs a store.OverrideDetail(runID,gate) (reason,ts) next to internal/store/store.go:935, which today returns only bool. Same one-line omission in cmd/batten/main.go:388 (cmdRuns VERDICT column -> `override`), internal/hooks/hooks.go:583 (the SessionStart context block the agent reads), and canvas.Render. internal/mcp/mcp.go:436 and internal/statusline/statusline.go:307 already do it correctly — copy them."
  },
  {
    "index": 11,
    "verdict": "REFUTED",
    "reason": "The finding's load-bearing claim -- 'no batten surface can show you the output it captured' -- is false on the HEAD binary. I reproduced the blocked gate, then printed the FULL captured pytest traceback from TWO surfaces: `batten canvas <unit> --out` (verdict node body) and the MCP tool `batten_verdict_status` (verdict.evidence[] verbatim, plus gate_checks[], deny_reason and how_to_fix). `batten show` also DOES name the failing check and its exit code ('.\\pytest.cmd backend/tests -q: FAIL (exit 1) …') -- only the multi-line tail is elided, contradicting the title 'a blocked gate never names the failing check'. The deny string itself is the short envelope by design and routes the agent onward via safe_next_step ('run batten check again', which re-prints the failing check with its tail) and via the MCP tool whose own description says to call it 'after a commit was denied ... to see exactly what the close gate requires'. Residual: the PreToolUse deny text does not inline the failing check name. That is a one-line UX polish, not a major defect, and it is not 'no surface can show you the output'.",
    "repro": "SB=$SANDBOX\nmkdir -p \"$SB/sandbox/v11\" && cd \"$SB/sandbox/v11\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$SB/sandbox/v11/state.db\"\n# batten.yaml: enforcement: enforce; unit US-\\d+; phases build/verify(gate qa)/close(requires_verdict ok);\n# gates.qa.checks: ['.\\pytest.cmd backend/tests -q', '.\\compileall.cmd -q backend']; evidence: required\n# pytest.cmd emits a 7-line pytest-style failure and `exit /b 1`; compileall.cmd `exit /b 0`\ngit add -A && git commit -q -m init --no-verify && git checkout -q -b US-011\n\"$SB/bin/batten.exe\" check US-011\nW=\"$SB/sandbox/v11\"\nP='{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m x\"}}'\necho \"$P\" | \"$SB/bin/batten.exe\" hook PreToolUse; echo \"EXIT=$?\"\n\"$SB/bin/batten.exe\" show US-011\n\"$SB/bin/batten.exe\" canvas US-011 --out \"$W/run.canvas\"   # verdict node = FULL evidence\npython mcpcall.py                                          # JSON-RPC: tools/call batten_verdict_status {unit: US-011}\n# negative control: make pytest.cmd exit 0, re-run check, re-fire the same hook payload",
    "evidence": "$ batten.exe check US-011\n  ✗ .\\pytest.cmd backend/tests -q (exit 1)\n    ============================= test session starts =============================\n    collected 3 items\n    backend/tests/test_auth.py F\n      File \"backend/tests/test_auth.py\", line 42, in test_login\n        assert resp.status_code == 200\n    E   assert 500 == 200\n    FAILED backend/tests/test_auth.py::test_login - assert 500 == 200\n    1 failed, 2 passed in 0.31s\n  ✓ .\\compileall.cmd -q backend\nUS-011: BLOCKED (batten-verified). one or more gate checks failed\n\n$ echo <payload> | batten.exe hook PreToolUse   # REPRODUCES the reported deny text\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-011 verdict is \\\"blocked\\\", not \\\"ok\\\". one or more gate checks failed\\nsafe_next_step: fix the failures, then run batten check again\\nTo proceed anyway (recorded): batten override US-011 --reason \\\"...\\\"\"}}\n\n$ batten.exe show US-011          # DOES name the failing check + exit code\nUS-011  run=US-011-1785251131227972400-3db8081f  status=running  phase=  base=\nverdict qa=blocked (batten): one or more gate checks failed\n  - .\\pytest.cmd backend/tests -q: FAIL (exit 1) …\n  - .\\compileall.cmd -q backend: PASS (exit 0, 36ms)\n\n=== REFUTATION 1: `batten canvas US-011 --out run.canvas` -> verdict node text ===\n## verdict: blocked\n**one or more gate checks failed**\n- .\\pytest.cmd backend/tests -q: FAIL (exit 1)\n============================= test session starts =============================\ncollected 3 items\nbackend/tests/test_auth.py F\n  File \"backend/tests/test_auth.py\", line 42, in test_login\n    assert resp.status_code == 200\nE   assert 500 == 200\nFAILED backend/tests/test_auth.py::test_login - assert 500 == 200\n1 failed, 2 passed in 0.31s\n- .\\compileall.cmd -q backend: PASS (exit 0, 36ms)\n\n=== REFUTATION 2: MCP tools/call batten_verdict_status {\"unit\":\"US-011\"} ===\n{\n  \"commit_denied\": true,\n  \"deny_reason\": \"US-011 verdict is \\\"blocked\\\", not \\\"ok\\\". one or more gate checks failed\",\n  \"gate\": \"qa\",\n  \"gate_checks\": [\".\\\\pytest.cmd backend/tests -q\", \".\\\\compileall.cmd -q backend\"],\n  \"has_verdict\": true,\n  \"how_to_fix\": \"fix the failures, then run batten check again\",\n  \"verdict\": {\n    \"evidence\": [\n      \".\\\\pytest.cmd backend/tests -q: FAIL (exit 1)\\n============================= test session starts =============================\\r\\ncollected 3 items\\r\\nbackend/tests/test_auth.py F\\r\\n  File \\\"backend/tests/test_auth.py\\\", line 42, in test_login\\r\\n    assert resp.status_code == 200\\r\\nE   assert 500 == 200\\r\\nFAILED backend/tests/test_auth.py::test_login - assert 500 == 200\\r\\n1 failed, 2 passed in 0.31s\\r\",\n      \".\\\\compileall.cmd -q backend: PASS (exit 0, 36ms)\"\n    ],\n    \"evidence_count\": 2, \"result\": \"blocked\", \"why\": \"one or more gate checks failed\",\n    \"safe_next_step\": \"fix the failures, then run batten check again\"\n  }\n}\n\nCode corroboration (two further full-evidence surfaces, not exercised): internal/tui/tui.go:509 renders each evidence item with wrapIndent (full string, wrapped, never truncated); internal/vault/vault.go:272-278 writes a '### Evidence' section with every item verbatim. Only cmd/batten/main.go:486 (firstLineOf) elides, and it is one surface of five.",
    "positive_control": "TWO controls. (1) Gate-fires control: with the pytest check failing, the same PreToolUse payload returned a JSON permissionDecision=deny -- the gate demonstrably engages in this sandbox, so this is not a silent no-op. (2) Discrimination control: I flipped pytest.cmd to `exit /b 0`, re-ran `batten check US-011` (-> 'US-011: OK (batten-verified)'), and re-fired the BYTE-IDENTICAL payload: output was empty, EXIT=0 -- the deny tracks the verdict state exactly. Trap-2 note: my first attempt WAS falsely silent because Git Bash's $PWD expands to a POSIX path (/tmp/claude/...) that spec.LoadFrom cannot resolve on Windows, so loadForHook errored and cmdHook exited 0 silently (cmd/batten/main.go:179-182). I caught it precisely because the control mattered; every result above uses the Windows-style cwd (C:/Users/...).",
    "already_fixed_at_head": false,
    "severity_adjusted": "polish",
    "fix_hint": "Not a defect as filed. Optional polish only: internal/hooks/hooks.go:318-322 could append the first line of the first FAIL evidence item to the deny string (v.Evidence[i] up to '\\n'), and cmd/batten/main.go:486 could gain a `--full` flag that skips firstLineOf."
  },
  {
    "index": 12,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary in a fresh sandbox. After the run for US-011 was closed through the real path (batten phase close + git commit + PostToolUse hook -> status=ok), `batten check US-011` silently INSERTed a second run with phase=NULL, base_sha=NULL, status=running, printed no warning and exited 0. `batten show US-011` then displays only that anchorless run (`phase=  base=`), so the closed run's anchor SHA and phase nodes become unreachable, and `batten runs`/`batten budget` show two rows for one unit. Not covered by any known-deliberate behaviour: this is neither a fail-open advisory nor a NOT-MEASURABLE report, and no doc describes `check` as a run-opening verb. cmd/batten/main.go:764 calls st.EnsureRun, whose contract (internal/store/store.go:269, internal/store/lifecycle_test.go:43-46) is deliberately 'after CloseRun the next EnsureRun opens a NEW run' -- correct for cmdPhase, wrong for an inspection/gate verb.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v12\" && cd \"$SB/sandbox/v12\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\n# edit batten.yaml: unit {name: US, pattern: 'US-\\d+'}, gates.qa.checks: ['exit 0']\nmkdir -p src && echo x > src/a.txt\ngit checkout -q -b US-011 && git add -A && git commit -q -m spec\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" phase US-011 build\n\"$BAT\" phase US-011 verify\n\"$BAT\" check US-011\n\"$BAT\" phase US-011 close\ngit commit -q --allow-empty -m \"US-011 work\"\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$PWD\"'\",\"hook_event_name\":\"PostToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\"US-011 work\\\"\"}}' | \"$BAT\" hook PostToolUse\n\"$BAT\" runs          # -> US-011  ok  close  ok*   (single row, run closed)\n\"$BAT\" check US-011  # <-- the defect\n\"$BAT\" runs\n\"$BAT\" show US-011\npython -c \"import sqlite3,os;[print(r) for r in sqlite3.connect(os.environ['BATTEN_DB']).execute('select run_id,unit_id,phase,status,base_sha from runs order by rowid')]\"",
    "evidence": "$ batten runs                       # after the close hook fired\nUNIT    STATUS  PHASE  VERDICT  TOKENS  IMPUTED\nUS-011  ok      close  ok*      -       -\n\n$ batten check US-011               # on the CLOSED unit\n  ✓ exit 0\n\nUS-011: OK (batten-verified). all gate checks passed (batten ran them)\nEXIT=0                              # no warning, no mention that a run was opened\n\n$ batten runs\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-011  running         ok*      -       -       <-- forked run: no phase\nUS-011  ok       close  ok*      -       -\n\n$ batten show US-011\nUS-011  run=US-011-1785251212404281100-12ae7d1f  status=running  phase=  base=\n   (the closed run's phase nodes and base=5850ec0 are gone from view)\n\n$ sqlite3 runs table\n('US-011-1785251182614307200-9e45a092', 'US-011', 'close', 'ok', '5850ec0', 1785251182, 1785251201)\n('US-011-1785251212404281100-12ae7d1f', 'US-011', None,    'running', None,   1785251212, None)\n\n$ batten budget US-011               # the phantom run duplicates the ledger line\nUS-011  0 tokens, $0.00 imputed\nUS-011  0 tokens, $0.00 imputed",
    "positive_control": "Not an ALLOW claim, but I ran the A/B control that isolates 'closed' as the trigger, in a separate DB (control.db), same binary, same spec:\n  A) after `batten phase US-020 build`      runs: [('75c7f3ce','build','running','37f1789')]\n  B) `batten check US-020` while run OPEN    runs: [('75c7f3ce','build','running','37f1789')]   <-- CONTROL: no fork, EnsureRun is idempotent\n  C) run closed                              runs: [('75c7f3ce','close','ok','37f1789')]\n  D) `batten check US-020` while run CLOSED  runs: [('75c7f3ce','close','ok','37f1789'), ('46d3e282', None, 'running', None)]   <-- forks\nThe identical command differs only in the run's status, so the hook/CLI path is demonstrably engaged in both arms and the fork is caused by the closed state, not by `check` always inserting. Separately, the close path itself was proven to engage: the PostToolUse commit payload flipped `batten runs` from status=running to status=ok.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:764 (and cmd/batten/main.go:266 in cmdVerdict): use st.ActiveRun instead of st.EnsureRun and return an error on sql.ErrNoRows -- e.g. \"no open run for US-011 (closed at 5850ec0); run `batten phase US-011 build` to reopen it\". Reserve EnsureRun for cmdPhase (main.go:351), the one verb whose job is to open a run and set the anchor."
  },
  {
    "index": 13,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: a check whose process dies with a negative NTSTATUS-style code renders as the unsigned 32-bit wraparound. `npm test` in a repo with no package.json printed `✗ npm test (exit 4294963238)` (= 2^32 - 4058) instead of -4058. HEAD still calls ee.ExitCode() raw at cmd/batten/main.go:826 with no int32 narrowing, and the garbage value is not merely displayed — it is persisted into the verdict evidence string, which `batten show` reads back as `npm test: FAIL (exit 4294963238)`.",
    "repro": "SB=$SANDBOX\nmkdir -p \"$SB/sandbox/v13\" && cd \"$SB/sandbox/v13\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" init\n# edit batten.yaml: gates.qa.checks: [\"exit 3\", \"npm test\"]   (no package.json in the repo)\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" check TASK-001\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" show TASK-001",
    "evidence": "$ batten.exe check TASK-001\n  ✗ exit 3 (exit 3)\n\n  ✗ npm test (exit 4294963238)\n    npm error code ENOENT\n    npm error syscall open\n    npm error path $HOME\\package.json\n    npm error errno -4058\n    npm error enoent Could not read package.json: Error: ENOENT: no such file or directory, open '$HOME\\package.json'\n\nTASK-001: BLOCKED (batten-verified). one or more gate checks failed\n\n$ batten.exe show TASK-001\nverdict qa=blocked (batten): one or more gate checks failed\n  - exit 3: FAIL (exit 3) …\n  - npm test: FAIL (exit 4294963238) …\n\n$ node -e \"console.log(4294967296-4058)\"\n4294963238        # i.e. the printed value is uint32(-4058)\n\nHEAD source, cmd/batten/main.go:824-828 (unchanged, no int32 narrowing):\n\tif err != nil {\n\t\tif ee, ok := err.(*exec.ExitError); ok {\n\t\t\treturn string(b), ee.ExitCode(), took\n\t\t}\n\t\treturn err.Error(), 1, took\n\t}",
    "positive_control": "Paired control in the SAME check run: the gate's first check `exit 3` — an ordinary small positive status — rendered correctly as `✗ exit 3 (exit 3)`. So the runCheck/print path demonstrably works and does report real exit statuses faithfully; the 4294963238 is specifically the missing signed-32-bit narrowing for abnormal-termination codes, not a broken or disengaged code path. (No ALLOW claim is involved, so Trap 2's hook-silence control does not apply.)",
    "already_fixed_at_head": false,
    "severity_adjusted": "polish",
    "fix_hint": "cmd/batten/main.go:826 — narrow before returning: `return string(b), int(int32(ee.ExitCode())), took`; optionally, at main.go:781-782, when code < 0 print `(terminated abnormally: exit -4058)` instead of a plain status."
  },
  {
    "index": 14,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary and proved at the storage layer, not the display layer. Phase node ids are `\"p-\" + phaseID` (cmd/batten/main.go:368) with `node_id TEXT PRIMARY KEY` (store.go:84) written via `INSERT OR REPLACE` (store.go:404). The key carries no run/unit/project scope, so when US-002 enters `plan` the existing `p-plan` ROW IS REWRITTEN: its run_id flips from US-001's run to US-002's run and its started_at is overwritten. Direct SQLite dumps before/after show US-001 going from 2 node rows to 1. A stronger variant empties the run record entirely: if US-001 is only in `build` and US-002 then enters `build`, `batten show US-001` lists ZERO phases while runs.phase still says `build`. This is silent, unrecoverable data loss in the artifact batten exists to keep.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v14\" && cd \"$SB/sandbox/v14\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\necho hello > README.md && git add -A && git commit -qm init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\n# edit batten.yaml: unit.pattern -> 'US-\\d+', prepend a `plan` phase (anchor: git_sha)\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" phase US-001 plan\n\"$BAT\" phase US-001 build\n\"$BAT\" show US-001\npython dump.py \"$PWD/state.db\"      # SELECT node_id, run_id, label, started_at FROM nodes\n\"$BAT\" phase US-002 plan\npython dump.py \"$PWD/state.db\"\n\"$BAT\" show US-001\n\n# --- second, worse variant: the run record is emptied outright ---\nexport BATTEN_DB=\"$PWD/spawn.db\"\n\"$BAT\" phase US-001 build\n\"$BAT\" phase US-002 build\n\"$BAT\" show US-001",
    "evidence": "$ batten show US-001            # BEFORE US-002 exists\nUS-001  run=US-001-1785251170919001200-3ed01d33  status=running  phase=build  base=258a68f\n  [phase] plan                     running \n  [phase] build                    running \n\n$ python dump.py state.db        # BEFORE — SQLite, not the renderer\n-- nodes --\n('p-plan',  'US-001-1785251170919001200-3ed01d33', 'phase', 'plan',  'running', 1785251170)\n('p-build', 'US-001-1785251170919001200-3ed01d33', 'phase', 'build', 'running', 1785251171)\n\n$ batten phase US-002 plan\nanchor: US-002 base SHA = 258a68f\nUS-002 -> phase plan\n\n$ python dump.py state.db        # AFTER — the p-plan ROW MOVED, it was not hidden\n-- runs --\n('US-001-1785251170919001200-3ed01d33', 'US-001', 'build', 'running')\n('US-002-1785251198398498500-cada87f5', 'US-002', 'plan',  'running')\n-- nodes --\n('p-build', 'US-001-1785251170919001200-3ed01d33', 'phase', 'build', 'running', 1785251171)\n('p-plan',  'US-002-1785251198398498500-cada87f5', 'phase', 'plan',  'running', 1785251198)\n\n  -> row count for US-001 fell 2 -> 1; p-plan.run_id was rewritten to US-002's run\n     and its started_at was overwritten (1785251170 -> 1785251198). No orphan row,\n     no second p-plan: the original tuple no longer exists anywhere in the file.\n\n$ batten show US-001             # AFTER — nothing else touched US-001\nUS-001  run=US-001-1785251170919001200-3ed01d33  status=running  phase=build  base=258a68f\n  [phase] build                    running \n\n--- ESCALATION: the run record can be emptied completely (spawn.db) ---\n$ batten phase US-001 build ; batten phase US-002 build\n-- nodes --\n('p-build', 'US-002-1785251253211428900-bc2c400b', 'phase', 'build', 'running', 1785251253)\n\n$ batten show US-001\nUS-001  run=US-001-1785251252841210100-3ade46ae  status=running  phase=build  base=258a68f\n\nno verdict: the close gate will deny a commit\n  -> US-001's run row still claims phase=build, but its phase graph has zero nodes.\n\n--- HEAD source (binary under test is HEAD; `batten version` -> batten 0.1.0) ---\ncmd/batten/main.go:367-369\n\t_ = st.AddNode(store.Node{\n\t\tNodeID: \"p-\" + phaseID, RunID: run.RunID, Kind: \"phase\", Label: phaseID, Status: \"running\",\n\t})\ninternal/store/store.go:84   node_id    TEXT PRIMARY KEY,\ninternal/store/store.go:404  _, err := s.db.Exec(`INSERT OR REPLACE INTO nodes ...\n\nBlast radius: `nodes` has no project column and dbPath() (cmd/batten/main.go:128-137)\ndefaults every project on the machine to the single ~/.batten/batten.db, so \"p-build\"\nis a MACHINE-GLOBAL key — the theft crosses projects, not just units.",
    "positive_control": "Two controls, both ran green.\n(1) NEGATIVE control isolating the cause — same sequence but the second unit enters a phase name that does NOT collide (`batten phase US-002 verify` instead of `plan`, control.db). US-001 kept BOTH rows and both `show` lines:\n    -- nodes --\n    ('p-plan',   US-001-...-35672fc9, 'phase', 'plan',   'running', 1785251218)\n    ('p-build',  US-001-...-35672fc9, 'phase', 'build',  'running', 1785251218)\n    ('p-verify', US-002-...-0559db19, 'phase', 'verify', 'running', 1785251218)\n  This proves the loss is caused specifically by the phase-name-derived primary key colliding, and rules out \"batten show filters by current phase\" or any generic multi-run display bug.\n(2) STORE-vs-RENDERER control demanded by the brief — every assertion above is read from the SQLite file with an independent read-only sqlite3 connection, never from batten's own output. The row is absent from the table, so this is not a display quirk. `batten show US-002` correctly renders `[phase] plan`, confirming the renderer is working and it is the DATA that moved.\nNo ALLOW/deny claim is involved in this finding, so the hook positive-control rule does not apply.",
    "already_fixed_at_head": false,
    "severity_adjusted": "blocker",
    "fix_hint": "cmd/batten/main.go:368 — scope the phase node id to the run: NodeID: run.RunID + \":p-\" + phaseID (any run-unique prefix). Must be changed in lockstep with internal/hooks/hooks.go:494, the only other site that reconstructs the id (\"p-\"+run.Phase) to hang spawn edges; add a UNIQUE(run_id,node_id) or migrate existing rows."
  },
  {
    "index": 15,
    "verdict": "CONFIRMED",
    "reason": "Reproduced twice on the HEAD binary. With gates.qa.checks non-empty and only an agent-source ok verdict on record, `batten hook PreToolUse` DENIES the git commit (\"no batten-verified pass. The gate's checks must be RUN, not asserted.\") while `batten close <unit> --status ok` closes the run ok and exits 0. gateReadyToClose (cmd/batten/main.go:698) calls st.LatestVerdict and never consults LatestVerdictBySource(...,\"batten\"), so it cannot see the source the hook gates on. Not by-design: the function's own doc comment says it \"mirrors the commit gate ... so `batten close` and a gated `git commit` cannot disagree about what 'done' means\". Amplifier: after the close the SAME denied payload goes silent, i.e. the CLI close also releases the commit gate, and unlike `batten override` it leaves no override record.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v15\" && cd \"$SB/sandbox/v15\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\n# then hand-edit batten.yaml: enforcement: enforce; unit.pattern 'US-\\d+';\n# phases build/verify(gate qa)/close(gate qa, requires_verdict ok);\n# gates.qa.checks: ['git --version']; evidence: required\nmkdir -p src docs && echo hello > src/a.txt && git add -A && git commit -q -m init\ngit checkout -q -b feature/US-004\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" phase US-004 build\ncat > v5.json <<'EOF'\n{\"check_id\":\"US-004\",\"result\":\"ok\",\"evidence\":[\"I ran the tests\",\"green\",\"done\"],\"why\":\"asserted\",\"safe_next_step\":\"commit\"}\nEOF\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" verdict --unit US-004 --file v5.json\nCWD=$(pwd -W)\nprintf '{\"session_id\":\"s4\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\\\"feat(US-004): y\\\\\"\"}}' \"$CWD\" > payload4.json\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" hook PreToolUse < payload4.json; echo \"EXIT=$?\"\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" close US-004 --status ok; echo \"EXIT=$?\"",
    "evidence": "$ batten doctor\n✓ close gate: phase \"close\" requires verdict \"ok\" on gate \"qa\"\n✓ enforcement: enforce — gates block\n\n$ batten verdict --unit US-004 --file v5.json\nverdict recorded: US-004 qa=ok (3 evidence)\n\n$ batten show US-001   (same shape as US-004)\nverdict qa=ok (agent): agent asserts the work is done\n  - all tests pass\n  - lint clean\n  - manual smoke ok\n\n$ batten hook PreToolUse < payload4.json\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-004 has no batten-verified pass. The gate's checks must be RUN, not asserted.\\nRun: batten check US-004\\nTo proceed anyway (recorded): batten override US-004 --reason \\\"...\\\"\"}}\nEXIT=0\n\n$ batten close US-004 --status ok\nUS-004 closed (ok). Write-set claims released — files it held are free again.\nEXIT=0\n\n(first run of the same divergence, on US-001, identical: hook denied, `batten close US-001` printed \"US-001 closed (ok)...\" EXIT=0, and `batten show US-001` then read status=ok with the verdict still marked (agent).)\n\nAmplifier — same payload.json, same cwd, only the run status changed:\n  before `batten close US-001` → permissionDecision:\"deny\" (verbatim above)\n  after  `batten close US-001` → no output, EXIT=0\n(hooks.go:284-287 returns nil,nil when ActiveRun errors: \"no run recorded: the gate has nothing to say\")\n\nCode at HEAD, cmd/batten/main.go:696-713 — the mismatch:\n  // gateReadyToClose enforces the close precondition (mirrors the commit gate) so `batten close`\n  // and a gated `git commit` cannot disagree about what \"done\" means.\n  v, err := st.LatestVerdict(run.RunID, closing.Gate)\n  if err != nil || v.Result != \"ok\" || len(v.Evidence) == 0 { ... }\nvs internal/hooks/hooks.go:328-336 which additionally requires\n  bv, err := h.Store.LatestVerdictBySource(run.RunID, gateName, \"batten\"); bv.Result == \"ok\"\nwhen len(sp.Gates[gateName].Checks) > 0.\n\nThe existing regression test cmd/batten/main_test.go:183 TestGateReadyToCloseAgreesWithTheHook does\nNOT catch this: its fixture gate declares `verdict: required` / `evidence: required` but no `checks:`,\nso it never enters the branch where the hook and the CLI diverge.",
    "positive_control": "Four controls, all showing the close gate and the commit gate genuinely engage in this sandbox, so neither ALLOW is silence-by-accident. (A) `batten close US-002` with NO verdict at all -> \"batten: cannot close US-002 as ok: needs a verdict with result=ok and cited evidence ...\" EXIT=1. (B) same unit after an agent verdict with result=blocked -> same refusal, EXIT=1. (C) an agent ok verdict with empty evidence[] is rejected at write time by SaveVerdict. (D) hook differential: the identical payload4/payload.json that returned permissionDecision:\"deny\" is the proof the hook fires; the batten-verified path (`batten check US-003`, which stored `verdict qa=ok (batten)`) then produced an allow, so the ONLY variable between deny and allow is the verdict's source. The close command's gate is therefore live and discriminating on result+evidence, and blind only to source.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:706 — in gateReadyToClose, after the LatestVerdict check add: if g, ok := sp.Gates[closing.Gate]; ok && len(g.Checks) > 0 { bv, err := st.LatestVerdictBySource(run.RunID, closing.Gate, \"batten\"); if err != nil || bv.Result != \"ok\" { return fmt.Errorf(\"...run `batten check %s`\", run.UnitID) } }. Better: extract one gateVerdictOK(spec,store,run) used by both hooks.go:328 and cmdClose, and give TestGateReadyToCloseAgreesWithTheHook a fixture whose gate declares checks."
  },
  {
    "index": 16,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary using the README's OWN example batten.yaml (README.md:143-146 declares gates.qa.checks). After `batten verdict --unit US-034 --file verdict.json` reported `ok (3 evidence)`, the PreToolUse commit hook still DENIED with \"has no batten-verified pass ... Run: batten check US-034\". The README (lines 58-66) claims the commit lands at that point, and the word `batten check` appears nowhere in README.md, nowhere in plugin/, and is not listed in `batten --help` — so the documented flow dead-ends on an undocumented command. The two halves of the README contradict each other: the flow only works for a gate with `checks: []` (what `batten init` writes), not for the gate the README itself showcases.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"; D=\"$SB/sandbox/v16\"\nmkdir -p \"$D\" && cd \"$D\" && git init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$D/state.db\"\n# batten.yaml transcribed from README.md:100-146 (unit US, phases plan/build/verify/close,\n# close requires_verdict: ok, gates.qa.checks: ['make check','make test'], enforcement: enforce)\n# verdict.json = {\"check_id\":\"US-034-qa\",\"result\":\"ok\",\"evidence\":[3 items],\"why\":...,\"safe_next_step\":\"close\"}\ngit checkout -q -b feature/US-034 && git add -A && git commit -q -m 'chore: base'\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" doctor\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" phase US-034 build\n# payload.json uses the WINDOWS-style cwd (a git-bash /tmp/... cwd makes the hook silently no-op):\n# {\"session_id\":\"s1\",\"cwd\":\"<D>\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\n#  \"tool_input\":{\"command\":\"git commit -m \\\"feat: add order rate limiting\\\"\"}}\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" hook PreToolUse < payload.json      # CONTROL A -> deny (no verdict)\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" verdict --unit US-034 --file verdict.json\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" hook PreToolUse < payload.json      # README says commit lands -> DENY\n# CONTROL B, in $D/b with gates.qa.checks: ['git --version'] so the checks can actually pass:\nexport BATTEN_DB=\"$D/b/state.db\"; \"$BAT\" verdict --unit US-034 --file verdict.json\nexport BATTEN_DB=\"$D/b/state.db\"; \"$BAT\" hook PreToolUse < payload.json    # deny\nexport BATTEN_DB=\"$D/b/state.db\"; \"$BAT\" check US-034\nexport BATTEN_DB=\"$D/b/state.db\"; \"$BAT\" hook PreToolUse < payload.json    # now allows",
    "evidence": "$ batten doctor\n✓ ...\\batten.yaml — project \"acme\", unit \"US\", 4 phases, 1 domains\n✓ close gate: phase \"close\" requires verdict \"ok\" on any gate\n✓ enforcement: enforce — gates block\n\n$ batten phase US-034 build\nanchor: US-034 base SHA = 53cb026\nUS-034 -> phase build\n\n$ batten hook PreToolUse < payload.json          # CONTROL A, no verdict yet\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-034 has no verdict envelope. Run the \\\"verify\\\" phase before committing.\\nTo proceed anyway (recorded in the audit log): batten override US-034 --reason \\\"...\\\"\"}}\n\n$ batten verdict --unit US-034 --file verdict.json\nverdict recorded: US-034 qa=ok (3 evidence)\n\n$ batten hook PreToolUse < payload.json          # README.md:64-65 says the commit lands HERE\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-034 has no batten-verified pass. The gate's checks must be RUN, not asserted.\\nRun: batten check US-034\\nTo proceed anyway (recorded): batten override US-034 --reason \\\"...\\\"\"}}\n\n--- the command the denial names is undocumented ---\n$ batten --help | grep -c 'check'   -> 0   (help lists init/doctor/hook/phase/claim/verdict/runs/show/canvas/budget/override/tui/mcp/statusline/ingest; NO `check`)\n$ rg 'batten check' README.md        -> no matches\n$ rg 'batten check' plugin/          -> No files found\n\n--- README.md at HEAD, verbatim ---\n58: A verdict that cites three real things gets through, and only then:\n61: $ batten verdict --unit US-034 --file verdict.json\n62: verdict US-034-qa: ok (3 evidence)\n64: $ git commit -m \"feat: add order rate limiting\"\n65: [feature/US-034 8f2a1c9] feat: add order rate limiting\n...\n143: gates:\n144:   qa:\n145:     checks: ['make check', 'make test']\n146:     verdict: required\n\n(Also wrong verbatim: README:62 shows `verdict US-034-qa: ok (3 evidence)`; the binary prints `verdict recorded: US-034 qa=ok (3 evidence)`.)",
    "positive_control": "TWO controls, both in-sandbox on the HEAD binary. (A) Same payload, before any verdict: DENY — `US-034 has no verdict envelope` — so the gate demonstrably engages for this payload/cwd, and the later deny is not an artifact of a dead hook. (B) Second sandbox ($D/b) with gates.qa.checks: ['git --version'] so the checks can pass: agent verdict alone -> the identical `no batten-verified pass` DENY; then `batten check US-034` -> `US-034: OK (batten-verified). all gate checks passed (batten ran them)`; then the IDENTICAL payload -> silent allow, exit 0. The deny->allow transition, driven by nothing but the added batten-source verdict, isolates the cause to the undocumented `batten check` step. (Note: a git-bash-style `/tmp/...` cwd makes the hook print nothing at all — an early silent no-op that would have looked like an ALLOW; caught it and switched to the Windows-style path before drawing any conclusion.)",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "README.md:58-66 — insert the missing step between the verdict and the commit: `$ batten check US-034` -> `US-034: OK (batten-verified)`, then the commit; and add `batten check <unit>  run the gate's checks and record a batten-verified verdict` to the usage block in cmd/batten/main.go (the `check` case at main.go:83 exists but is not printed by --help)."
  },
  {
    "index": 17,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary in my own sandbox. `batten --help` prints exactly 16 commands and none of them is `check` or `close` (grep exit=1), yet both are real dispatched subcommands that do real work (`batten check TASK-1` ran the declared checks and recorded a batten-verified OK verdict; `batten close TASK-1` closed the run and released its write-set claims). Worse, the commit gate's own deny message says literally \"Run: batten check TASK-2\" — a command `--help` denies exists. `measure`, `hook-debug` and `version` are likewise dispatched and likewise unlisted. Source at HEAD confirms: printUsage() (cmd/batten/main.go:99-118) has no entry for the `close`/`check`/`measure`/`hook-debug` cases of the dispatch switch (main.go:81-86). This is NOT the by-design fail-open path (Trap 1) — nothing is being warned instead of denied here, and there is no `hidden`/`internal` marker on those cases; the CHANGELOG (lines 99-101) and docs/VERIFICATION.md (R1/R2) both ship them as user-facing features.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v17\" && cd \"$SB/sandbox/v17\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\necho hello > README.md && git add -A && git commit -q -m init\n\"$BAT\" init\n\"$BAT\" --help 2>&1\n\"$BAT\" --help 2>&1 | grep -i 'check\\|close'; echo \"GREP_EXIT=$?\"\n# give the qa gate two real checks so `check` has something to run\npython -c \"p='batten.yaml';s=open(p,encoding='utf-8').read();s=s.replace('    checks: []','    checks:\\n      - echo qa-ok\\n      - echo qa-two');open(p,'w',encoding='utf-8').write(s)\"\n\"$BAT\" phase TASK-1 build\n\"$BAT\" check TASK-1;  echo \"EXIT=$?\"\n\"$BAT\" close TASK-1;  echo \"EXIT=$?\"\n# the other two unlisted-but-dispatched commands\n\"$BAT\" measure; \"$BAT\" hook-debug\n# dispatch proof: a genuinely unknown subcommand takes the default branch, these do not\n\"$BAT\" bogus-subcommand >/dev/null 2>&1; echo \"BOGUS=$?\"      # 2 (printUsage)\n\"$BAT\" check   >/dev/null 2>&1; echo \"CHECK_NOARG=$?\"          # 1 (cmdCheck ran)\n\"$BAT\" close   >/dev/null 2>&1; echo \"CLOSE_NOARG=$?\"          # 1 (cmdClose ran)\n\"$BAT\" help    >/dev/null 2>&1; echo \"HELPWORD=$?\"             # 2 -> printUsage is the ONLY help surface\n# and the deny message points at the command --help hides\npython -c \"p='batten.yaml';s=open(p,encoding='utf-8').read();s=s.replace('enforcement: report','enforcement: enforce');open(p,'w',encoding='utf-8').write(s)\"\ngit checkout -q -b TASK-2 && git add -A && git commit -q -m wip\n\"$BAT\" phase TASK-2 build && \"$BAT\" phase TASK-2 verify\nWIN=$(pwd -W)\necho '{\"unit\":\"TASK-2\",\"gate\":\"qa\",\"check_id\":\"TASK-2-qa-agent\",\"result\":\"ok\",\"evidence\":[\"I ran the suite and it was green\"],\"why\":\"looks fine\",\"safe_next_step\":\"close\"}' > v.json\n\"$BAT\" verdict --file v.json\necho \"{\\\"session_id\\\":\\\"s2\\\",\\\"cwd\\\":\\\"$WIN\\\",\\\"hook_event_name\\\":\\\"PreToolUse\\\",\\\"tool_name\\\":\\\"Bash\\\",\\\"tool_input\\\":{\\\"command\\\":\\\"git commit -m x\\\"}}\" | \"$BAT\" hook PreToolUse",
    "evidence": "$ batten version\nbatten 0.1.0\n\n$ batten --help\nbatten — procedural memory as data\n\n  batten init [--from <prose-doc>]   generate batten.yaml\n  batten doctor                      validate spec, report live capabilities\n  batten hook <event>                hook entrypoint (stdin JSON -> stdout JSON)\n  batten phase <unit> <phase>        advance a unit to a phase (records the anchor SHA)\n  batten claim <agent-id> <file>...  declare a subagent's write-set\n  batten verdict [--file v.json]     record a verdict envelope\n  batten runs                        list runs\n  batten show <unit>                 run detail\n  batten canvas <unit> [--out p]     emit the run DAG as JSON Canvas\n  batten budget [<unit>]             governor status (tokens / imputed $ / quota)\n  batten override <unit> --reason    audited escape from the gate\n  batten tui                         review runs in a terminal UI\n  batten mcp                         MCP server (exposes the run graph to the agent)\n  batten statusline [--install]      status line + the only subscription-quota sensor\n  batten ingest <unit> --transcript  price a transcript's tokens into the run ledger\n\n$ batten --help 2>&1 | grep -i 'check\\|close'\nGREP_EXIT=1                      <-- zero matches\n\n$ batten --help 2>&1 | grep -c 'measure\\|hook-debug\\|version'\n0                                <-- measure / hook-debug / version unlisted too\n\n$ batten phase TASK-1 build\nanchor: TASK-1 base SHA = 9ba1906\nTASK-1 -> phase build\n\n$ batten check TASK-1\n  ✓ echo qa-ok\n  ✓ echo qa-two\n\nTASK-1: OK (batten-verified). all gate checks passed (batten ran them)\nEXIT=0\n\n$ batten close TASK-1\nTASK-1 closed (ok). Write-set claims released — files it held are free again.\nEXIT=0\n\n$ batten hook-debug\nbatten: hook-debug: --tap | --off | --show      <-- dispatched, unlisted\n\n$ batten runs\nUNIT    STATUS   PHASE   VERDICT  TOKENS  IMPUTED\nTASK-1  ok       build   ok*      —       —\n\n--- and the gate's own deny names the hidden command ---\n$ echo '{...\"tool_input\":{\"command\":\"git commit -m x\"}}' | batten hook PreToolUse\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-2 has no batten-verified pass. The gate's checks must be RUN, not asserted.\\nRun: batten check TASK-2\\nTo proceed anyway (recorded): batten override TASK-2 --reason \\\"...\\\"\"}}\n\n--- source at HEAD (cmd/batten/main.go) ---\n 81:\tcase \"close\":\n 82:\t\terr = cmdClose(os.Args[2:])\n 83:\tcase \"check\":\n 84:\t\terr = cmdCheck(os.Args[2:])\n...\n 99: func printUsage() {          <-- 16 lines, none of them close/check/measure/hook-debug\ninternal/hooks/hooks.go:333:\t\t\t\t\t\"Run: batten check %s\\n\"+",
    "positive_control": "Two controls, both passed.\n(1) DISPATCH control — is `check`/`close` really dispatched, or is it just the default branch echoing usage? `batten bogus-subcommand` -> exit 2 and prints the usage block (the `default:` branch). `batten check` with no args -> exit 1 and prints its OWN usage: `batten: check: batten check <unit> [--gate <name>]`. `batten close` with no args -> exit 1, `batten: close: batten close <unit> [--status ok|failed|rolled_back]`. Different exit code, different text: cmdCheck/cmdClose genuinely ran. `batten help` and `batten --help` both -> exit 2 + the same usage block, proving printUsage() is the only help surface there is, so nothing else could be listing them.\n(2) GATE control (Trap 2) — my first commit-hook payload was silent, so I did NOT read that as ALLOW. Diagnosed it: I had passed a git-bash `/c/Users/...` cwd that the native Windows binary cannot resolve, so the hook bailed before the gate. Re-run with `cwd` = `$(pwd -W)` produced a mandatory DENY: `\"permissionDecision\":\"deny\" ... \"batten: TASK-2 has no verdict envelope. Run the \\\"verify\\\" phase before committing.\"` Only after the gate was proven to fire did I record an agent verdict and obtain the `Run: batten check TASK-2` deny quoted in the evidence.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:116 (end of printUsage's list) — add two lines to the usage string: `batten check <unit> [--gate <n>]   run the gate's checks, record a batten-verified verdict` and `batten close <unit> [--status ...]  end a run and release its write-set claims` (and, optionally, `measure`).",
    "notes": "NOT ALREADY-FIXED: the omission is still present in HEAD source, not just in the frozen binary. NOT Trap-1 by-design: no warn-instead-of-deny is involved and the cases carry no hidden/internal marker; CHANGELOG.md:99-101 ships both as user-facing. Severity lowered from major to minor because two other surfaces do name the commands (the commit deny at internal/hooks/hooks.go:333 and doctor's stale-run warning at cmd/batten/main.go:1324), so discoverability is degraded rather than lost, and no behaviour or data is wrong. Isolation note: BATTEN_DB was exported in every single call and pointed at sandbox/v17/state.db (batten doctor confirmed the store path); ~/.batten/batten.db was never targeted. One unintended side effect to disclose: while diagnosing the silent hook I ran `batten hook-debug --tap`, which writes its flag and log to ~/.batten/hook-taps.jsonl regardless of BATTEN_DB, so concurrent sessions appended a few lines to that pre-existing debug log before I ran `batten hook-debug --off` (tap is now OFF; no file was deleted or truncated, and the DB was untouched)."
  },
  {
    "index": 18,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary (batten 0.1.0). After advancing US-001 through research->plan->build->verify->close, passing the qa gate and closing the run ok, `batten show US-001` still prints all five phases as `running`. Direct SQLite inspection proves this is NEVER RECORDED, not a rendering artifact: every phase row has status='running' and ended_at=NULL (COUNT(*) WHERE ended_at IS NOT NULL = 0) while the run row is status='ok' with ended_at=1785251548. Confirmed in code: FinishNode has exactly one caller in non-test code (internal/hooks/hooks.go:508, SubagentStop); cmd/batten/main.go cmdPhase (line 367) only AddNode's the new phase and never finishes the outgoing one, and cmdClose (line 639) + store.CloseRun (store.go:375) touch only the runs table.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v18\" && cd \"$SB/sandbox/v18\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\necho hello > README.md && git add -A && git commit -q -m init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\n# edit sandbox batten.yaml: unit.pattern 'US-\\d+', add phases research+plan before build, gates.qa.checks: ['git --version']\nexport BATTEN_DB=\"$PWD/state.db\"\nfor p in research plan build verify close; do \"$BAT\" phase US-001 \"$p\"; done\n\"$BAT\" check US-001\n\"$BAT\" close US-001\n\"$BAT\" show US-001\npython -c \"import sqlite3;c=sqlite3.connect('state.db');print(list(c.execute('SELECT node_id,kind,label,status,ended_at FROM nodes ORDER BY started_at')));print(list(c.execute('SELECT run_id,status,ended_at FROM runs')))\"",
    "evidence": "$ \"$BAT\" check US-001\n  ✓ git --version\nUS-001: OK (batten-verified). all gate checks passed (batten ran them)\nEXIT=0\n\n$ \"$BAT\" close US-001\nUS-001 closed (ok). Write-set claims released — files it held are free again.\nEXIT=0\n\n$ \"$BAT\" show US-001\nUS-001  run=US-001-1785251511401107900-c5de0d92  status=ok  phase=close  base=7a5464b\n  [phase] research                 running \n  [phase] plan                     running \n  [phase] build                    running \n  [phase] verify                   running \n  [phase] close                    running \n\nverdict qa=ok (batten): all gate checks passed (batten ran them)\n  - git --version: PASS (exit 0, 73ms)\n\n$ sqlite nodes / runs (store level — proves NEVER RECORDED, not un-rendered)\nNODES: (node_id, kind, label, status, started_at, ended_at)\n('p-research', 'phase', 'research', 'running', 1785251511, None)\n('p-plan', 'phase', 'plan', 'running', 1785251511, None)\n('p-build', 'phase', 'build', 'running', 1785251511, None)\n('p-verify', 'phase', 'verify', 'running', 1785251511, None)\n('p-close', 'phase', 'close', 'running', 1785251511, None)\nRUNS: ('US-001-1785251511401107900-c5de0d92', 'US-001', 'close', 'ok', 1785251511, 1785251548)\ncount nodes with ended_at NOT NULL: (0,)\n\nCode at HEAD (cmd/batten/main.go:367, inside cmdPhase — no FinishNode of the outgoing phase):\n\t_ = st.AddNode(store.Node{\n\t\tNodeID: \"p-\" + phaseID, RunID: run.RunID, Kind: \"phase\", Label: phaseID, Status: \"running\",\n\t})\ninternal/store/store.go:375 CloseRun updates only `runs`:\n\t_, err := s.db.Exec(`UPDATE runs SET status=?, ended_at=? WHERE run_id=?`, status, t, runID)",
    "positive_control": "Ran the inverse control to rule out 'recorded but not rendered': in the same sandbox/DB I drove a subagent node through the one code path that DOES call FinishNode (hooks.go:508). `batten hook SubagentStart` then `batten hook SubagentStop` (agent_id=a1) on unit US-002. Result: `batten show US-002` prints `[subagent] worker                   ok` and the SQLite row becomes ('n-a1','subagent','worker','ok',1785251621) — status flipped and ended_at was stamped. So both the store write path and the renderer are capable of showing a terminal node; the phase nodes stay `running` purely because nothing ever calls FinishNode for them. (Note: the hook is silently a no-op if `cwd` is a Git-Bash-style /c/... path — the Windows-style path from `pwd -W` is required for it to engage; the first control attempt produced no node at all and I discarded it.)",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:367 (cmdPhase): before AddNode, finish the run's previous phase node; cmd/batten/main.go:686 / internal/store/store.go:375 (CloseRun): finish every still-running phase node of that run with the run's final status. Caveat: FinishNode (store.go:411) keys on node_id alone and phase ids are global ('p-build'), so the finish must be run-scoped (`WHERE run_id=? AND node_id=?`) or it will finish another run's node — I observed exactly that theft when US-002 entered `build` and stole US-001's p-build row.",
    "notes": "Not covered by Trap 1: this is not fail-open-with-a-warning and not NOT-MEASURABLE-vs-0. batten knows the phase advanced (it wrote runs.phase) and knows the run closed (runs.status='ok', runs.ended_at set); it asserts a positively false state ('running') for five phases of a finished run rather than declining to measure. Nothing in DESIGN or batten.yaml makes phase nodes intentionally immortal; docs/field-test/dimensions.json:797 records the same behaviour as an open finding."
  },
  {
    "index": 19,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary. hooks.go:177 is the ONLY production caller of LogEvent and it hardcodes runID=\"\", so store.go:916 nullable(\"\") writes NULL into events.run_id every time. StaleRuns' subquery `(SELECT MAX(ts) FROM events e WHERE e.run_id=r.run_id)` therefore always yields NULL and COALESCE falls back to r.started_at unconditionally: the 'no activity' half of the predicate is dead code. After three genuine hook events (SessionStart + two PreToolUse, all within 20s) and two phase transitions on the run, `batten doctor` still printed \"1 run(s) open >48h with no activity\". The advice it gives ('close or resume them') is unactionable — resuming the run can never clear it; only closing can. The message asserts something false about a run that was being actively worked.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v19\" && cd \"$SB/sandbox/v19\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\necho hello > README.md && git add -A && git commit -q -m init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\n\"$BAT\" phase TASK-001 build\n# backdate the open run's started_at by 100h (no batten command does this; sqlite directly)\npython -c \"import sqlite3; c=sqlite3.connect('state.db'); c.execute('update runs set started_at = started_at - ?', (100*3600,)); c.commit()\"\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" doctor | grep -A3 'open >48h'\n# --- now generate REAL activity on that run ---\nWCWD=\"$SB/sandbox/v19\"   # NOTE: Windows-style path; a git-bash /c/... cwd makes spec.LoadFrom fail and the hook no-ops silently\nexport BATTEN_DB=\"$PWD/state.db\"\nprintf '{\"session_id\":\"sessE\",\"cwd\":\"%s\",\"hook_event_name\":\"SessionStart\"}' \"$WCWD\" | \"$BAT\" hook SessionStart\nprintf '{\"session_id\":\"sessE\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"ls\"}}' \"$WCWD\" | \"$BAT\" hook PreToolUse\n\"$BAT\" phase TASK-001 verify\nprintf '{\"session_id\":\"sessE\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m x\"}}' \"$WCWD\" | \"$BAT\" hook PreToolUse   # positive control\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" doctor | grep -A3 'open >48h'\npython -c \"import sqlite3,time; c=sqlite3.connect('state.db'); print('now',int(time.time())); print(list(c.execute('select run_id,node_id,hook,ts from events order by ts desc'))); print(list(c.execute('select r.unit_id,(select max(ts) from events e where e.run_id=r.run_id),r.started_at from runs r')))\"",
    "evidence": "$ batten doctor        # run backdated 100h, zero activity\n⚠ 1 run(s) open >48h with no activity — close or resume them:\n    TASK-001 (phase build): batten close TASK-001 [--status ok|failed]\n\n$ printf '{\"session_id\":\"sessE\",\"cwd\":\"<repo>\",\"hook_event_name\":\"SessionStart\"}' | batten hook SessionStart\n{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"## batten — v19 (TASK)\\n\\n- **TASK-001** — phase `verify`, status `running`, **no verdict yet** — the close gate will deny a commit\\n\\n→ this session is working **TASK-001**.\\n\"}}\nEXIT=0\n\n$ batten phase TASK-001 verify\nTASK-001 -> phase verify\nthis phase must emit a verdict for gate \"qa\" (evidence required)\n\n$ batten doctor        # AFTER 3 hook events + 2 phase transitions, all <20s old\n⚠ 1 run(s) open >48h with no activity — close or resume them:\n    TASK-001 (phase verify): batten close TASK-001 [--status ok|failed]\n\n$ python - state.db\nnow= 1785251579\nRUN ('TASK-001-1785251518349317800-2d6e9618', 'TASK-001', 'verify', 'running', 1784891518, 'sessE')\nEV (None, None, 'PreToolUse', 1785251578)\nEV (None, None, 'SessionStart', 1785251564)\nEV (None, None, 'PreToolUse', 1785251564)\nnewest event per run join: [('TASK-001', None, 1784891518)]\n\n# ^ every events.run_id is NULL even though the run IS bound to session 'sessE'\n#   (runs.session_id went '' -> 'sessE' via AdoptSession), and the StaleRuns join\n#   subquery evaluates to None, so COALESCE always returns started_at (1784891518).\n\n# Static confirmation that this is systemic, not a sandbox artefact — the only\n# production writer to the events table:\n$ grep -rn \"INTO events|UPDATE events|LogEvent\\(\" --include=*.go .\n./internal/hooks/hooks.go:177:\t_ = h.Store.LogEvent(\"\", \"\", event, truncateBytes(raw, 4096))\n./internal/store/lifecycle_test.go:291:\tif err := s.LogEvent(a.RunID, \"\", \"PreToolUse\", big); err != nil {\n./internal/store/lifecycle_test.go:294:\tif err := s.LogEvent(a.RunID, \"\", \"PreToolUse\", nil); err != nil {\n./internal/store/store.go:915:\t_, err := s.db.Exec(`INSERT INTO events (run_id, node_id, hook, ts, payload) VALUES (?,?,?,?,?)`,\n\n# The tests pass a REAL a.RunID, which is exactly why no test catches the production\n# gap. store.go:144 even builds `CREATE INDEX idx_events_run ON events(run_id, event_id)`\n# — an index over a column that is NULL in 100% of production rows.\n\n# Intent is documented as event-based, so this is not by design:\n#   store.go:1080  \"StaleRuns lists runs still 'running' whose newest event is older than maxAge\"\n#   main.go:1320   \"48h with no event means abandoned or forgotten\"",
    "positive_control": "Two controls, both required because `batten hook` exits 0 silently for many reasons.\n(1) HOOK-IS-LIVE control: the same SessionStart payload printed a full hookSpecificOutput block naming TASK-001 and its phase — the handler definitely engaged and reached the LogEvent line (three rows appeared in `events` as a result).\n(2) MUST-GATE control: PreToolUse with tool_input.command='git commit -m x' returned {\"systemMessage\":\"batten (warning — not blocking): batten: TASK-001 has no verdict envelope. Run the \\\"verify\\\" phase before committing.\"} — one field changed, gate fires. So the silence of the benign 'ls' PreToolUse is a genuine allow, not a dead hook.\n(3) NEGATIVE-CONTROL note worth recording: my first attempt passed a git-bash cwd (/c/Users/...). Every hook then exited 0 with NO output AND wrote NO events row — spec.LoadFrom failed and cmdHook returned nil silently. That is precisely Trap 2; I discarded that run and redid it with a Windows-style path, which is what the evidence above is from.\n(4) STALE-WARNING-CAN-FIRE control: doctor printed nothing about stale runs before I backdated started_at, and printed the warning after — so the code path is reachable and I am observing it, not its absence.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "internal/hooks/hooks.go:177 — resolve the run before logging instead of hardcoding \"\": e.g. `rid := \"\"; if in.SessionID != \"\" { if r, err := h.Store.RunBySession(h.Spec.Project, in.SessionID); err == nil { rid = r.RunID } }; _ = h.Store.LogEvent(rid, in.NodeID, event, truncateBytes(raw, 4096))`. RunBySession already exists at internal/store/store.go:1021 and the run is already session-bound by AdoptSession. Secondary/belt-and-braces: reword cmd/batten/main.go:1322 to \"open >48h\" so the message stays true even when a run legitimately has no events."
  },
  {
    "index": 20,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary (b38fd5a). With 2+ open runs that no session owns, an unbound session's `git commit` PreToolUse payload produces ZERO bytes on stdout and exit 0 — the commit is allowed with no deny and no advisory. Two paired positive controls prove the gate is live in the same sandbox: changing only `session_id` to a bound session on the byte-identical payload flips it to a hard `permissionDecision: deny`. Trap 1 does not apply: the finding asks for the by-design advise() form, and got silence, not a warning. The `sessionStart makes this visible` mitigation (hooks.go:667) is conditional and I broke it: when the session starts before the runs exist, SessionStart, PreToolUse, PostToolUse and Stop are ALL silent (0 bytes each) — the user gets no signal at any surface.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v20\" && cd \"$SB/sandbox/v20\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\necho hi > README.md && git add -A && git commit -qm init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\nsed -i 's/^enforcement: report/enforcement: enforce/' batten.yaml\nCWD=$(pwd -W)\n\n# --- A) literal finding: three open, unowned runs; unbound session ---\n\"$BAT\" phase FOO-9 build; \"$BAT\" phase us-001 build; \"$BAT\" phase US-1 build\nprintf '{\"session_id\":\"newsess\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\\\"anything\\\\\"\"}}' \"$CWD\" | \"$BAT\" hook PreToolUse > out_A.txt; echo \"EXIT=$? BYTES=$(wc -c < out_A.txt)\"\n\n# --- B/C) paired control in a second db, built so one run is session-bound ---\nexport BATTEN_DB=\"$PWD/paired.db\"\nP() { printf '{\"session_id\":\"%s\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\\\"anything\\\\\"\"}}' \"$1\" \"$CWD\"; }\n\"$BAT\" phase TASK-1 build            # only run -> the next hook adopts it onto sPC\nP sPC | \"$BAT\" hook PreToolUse       # B: gate engages -> DENY (and binds sPC to TASK-1)\n\"$BAT\" phase TASK-2 build && \"$BAT\" phase TASK-3 build   # 3 open, 2 unowned\nP newsess | \"$BAT\" hook PreToolUse   # C1: unbound  -> 0 bytes, exit 0  (DEFECT)\nP sPC     | \"$BAT\" hook PreToolUse   # C2: bound    -> deny            (POSITIVE CONTROL)\n\n# --- D) same defect with the sessionStart mitigation removed by ordering ---\nexport BATTEN_DB=\"$PWD/nomit.db\"\nprintf '{\"session_id\":\"sX\",\"cwd\":\"%s\",\"hook_event_name\":\"SessionStart\",\"source\":\"startup\"}' \"$CWD\" | \"$BAT\" hook SessionStart   # 0 bytes: no runs yet\n\"$BAT\" phase TASK-7 build && \"$BAT\" phase TASK-8 build   # opened by other actors\nprintf '{\"session_id\":\"sX\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\\\"anything\\\\\"\"}}' \"$CWD\" | \"$BAT\" hook PreToolUse   # 0 bytes\n# D-control: same db, same payload, HEAD moved to a branch that names a unit\ngit checkout -q -b TASK-7\nprintf '{\"session_id\":\"sX\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\\\"anything\\\\\"\"}}' \"$CWD\" | \"$BAT\" hook PreToolUse   # -> DENY\ngit checkout -q main",
    "evidence": "A) three open unowned runs (FOO-9, us-001, US-1), session 'newsess':\n  EXIT=0\n  STDOUT_BYTES=0\n  [stdout]        <- empty\n  [stderr]        <- empty\n\nC) SAME db state (TASK-1 owned by sPC; TASK-2, TASK-3 unowned), payloads differing in exactly one field:\n  --- C1 session_id=newsess (unbound) ---\n  EXIT=0  STDOUT_BYTES=0\n  --- C2 session_id=sPC (bound to TASK-1) ---\n  EXIT=0  STDOUT_BYTES=283\n  {\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-1 has no verdict envelope. Run the \\\"verify\\\" phase before committing.\\nTo proceed anyway (recorded in the audit log): batten override TASK-1 --reason \\\"...\\\"\"}}\n  $ diff c1.json c2.json\n  < {\"session_id\":\"newsess\",\"cwd\":\".../sandbox/v20\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\"anything\\\"\"}}\n  > {\"session_id\":\"sPC\",\"cwd\":\".../sandbox/v20\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\"anything\\\"\"}}\n\nD) no signal at ANY surface when the session predates the runs:\n  SessionStart EXIT=0 BYTES=0\n  PreToolUse   EXIT=0 BYTES=0\n  PostToolUse  EXIT=0 BYTES=0\n  Stop         EXIT=0 BYTES=0\n  D-control (identical payload, HEAD on branch TASK-7):\n  {\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-7 has no verdict envelope. ...\"}}\n\nCode at HEAD (b38fd5a), unchanged by the five fixes:\n  internal/hooks/hooks.go:280-282\n    unit := h.activeUnit(in)\n    if unit == \"\" {\n        return nil, nil // cannot attribute this commit to a unit: do not block\n    }\n  internal/hooks/hooks.go:667\n    return \"\" // ambiguous: refuse to guess, and refuse to block (sessionStart makes this visible)\n\nSecondary contradiction observed in the SessionStart output that IS emitted in case C — it promises a denial the hook will not deliver, three lines above admitting the gates cannot act:\n  \"- **TASK-1** — phase `build`, status `running`, **no verdict yet** — the close gate will deny a commit\n   ...\n   \\u26a0 3 units are open (TASK-1, TASK-2, TASK-3) and this session isn't bound to one.\n      The gates can't act until you bind it: run `batten phase <unit> <phase>`.\"\n\nThe project's own matrix already flags this as unresolved: docs/field-test/TEST-MATRIX.md:229 (VG-E2) — \"activeUnit returns \\\"\\\" -> silence, no denial. Expect (README \\u00a7'fail open only out loud'): something says so. SessionStart does (hooks.go:601); the commit hook does not.\"",
    "positive_control": "Two, both in the same sandbox. (1) Payload-level, at an IDENTICAL db state (3 open runs, TASK-1 bound to sPC, TASK-2/TASK-3 unowned): the byte-identical commit payload with only `session_id` changed from `newsess` to `sPC` flipped 0 bytes/allow into `{\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-1 has no verdict envelope...\"}` (283 bytes). (2) State-level, in the nomit.db scenario where every surface was silent: moving HEAD onto branch `TASK-7` (so activeUnit step 2 can attribute the commit) made the same payload return a hard deny for TASK-7. The gate is demonstrably live in this sandbox; the silence in the ambiguous case is a real ALLOW, not a hook that never engaged.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/hooks/hooks.go:281-282 — replace `if unit == \"\" { return nil, nil }` with `return advise(\"PreToolUse\", ...)` naming the open unowned units and telling the user to bind: `batten phase <unit> <phase>`. Same one-line change pattern as the checkless-gate advise() at hooks.go:347."
  },
  {
    "index": 21,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: with unit.pattern = 'US-\\d{3}', `batten phase FOO-9|us-001|US-1|US-0001 build` are all accepted, exit 0, and each opens a run listed forever by `batten runs`. The very same command hard-rejects a bad phase id (`no phase \"deploy\"`, exit 1), so the asymmetry is real, not a load failure. cmdPhase never calls sp.MatchUnit on args[0]; no CLI subcommand exists to delete the resulting phantom run.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v21\" && cd \"$SB/sandbox/v21\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"\necho '# backlog' > README.md && git add -A && git commit -qm init\n\"$BAT\" init\n# edit batten.yaml: unit.name: US / unit.pattern: 'US-\\d{3}'\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" doctor          # -> store points at the sandbox db\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" phase FOO-9 build; echo EXIT=$?\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" phase us-001 build; echo EXIT=$?\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" phase US-1 build; echo EXIT=$?\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" phase US-0001 build; echo EXIT=$?\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" runs\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" phase US-001 deploy; echo EXIT=$?   # positive control\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" show FOO-9\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" budget FOO-9\n# harm demo (enforcement: enforce, branch feature/US-001, run US-001 in phase close)\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; \"$BAT\" override us-001 --reason \"approved by hand\"\nexport BATTEN_DB=\"$SB/sandbox/v21/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$SB/sandbox/v21\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\"US-001 done\\\"\"}}' | \"$BAT\" hook PreToolUse",
    "evidence": "$ batten doctor\n✓ ...batten.yaml — project \"v21\", unit \"US\", 3 phases, 0 domains\n✓ store: .../scratchpad/ft/sandbox/v21/state.db\n\n$ batten phase FOO-9 build\nanchor: FOO-9 base SHA = 6297013\nFOO-9 -> phase build\nEXIT=0\n$ batten phase us-001 build\nanchor: us-001 base SHA = 6297013\nus-001 -> phase build\nEXIT=0\n$ batten phase US-1 build\nanchor: US-1 base SHA = 6297013\nUS-1 -> phase build\nEXIT=0\n$ batten phase US-0001 build\nanchor: US-0001 base SHA = 6297013\nUS-0001 -> phase build\nEXIT=0\n$ batten runs\nUNIT     STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-0001  running  build  —        —       —\nUS-1     running  build  —        —       —\nus-001   running  build  —        —       —\nFOO-9    running  build  —        —       —\n\n--- the same command DOES validate its OTHER argument ---\n$ batten phase US-001 deploy\nbatten: no phase \"deploy\" in batten.yaml\nEXIT=1\n\n--- the phantom is durable and fully first-class ---\n$ batten show FOO-9\nFOO-9  run=FOO-9-1785251842524199600-7e925a1d  status=running  phase=build  base=6297013\nno verdict: the close gate will deny a commit\n$ batten budget FOO-9\nFOO-9  0 tokens, $0.00 imputed\n  · tokens       0 / 3.0M  [............]\n(`batten help` lists no rm/prune/delete subcommand; main.go's switch has no such case)\n\n--- downstream harm: batten makes a FALSE statement about the gate ---\n$ batten override us-001 --reason \"approved by hand\"\noverride recorded for us-001 (gate *): approved by hand\nthe gate will now allow the commit. This override is in the audit log.\nEXIT=0\n$ <commit payload on branch feature/US-001> | batten hook PreToolUse\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-001 has no verdict envelope. Run the \\\"verify\\\" phase before committing....\"}}\n\n--- code ---\ncmd/batten/main.go:346  unit, phaseID := args[0], args[1]\ncmd/batten/main.go:347  ph, ok := sp.Phase(phaseID)   // phaseID validated, unit never is\ncmd/batten/main.go:351  run, err := st.EnsureRun(sp.Project, unit, \"\")\nThe only MatchUnit call sites (main.go:255,259,662,740,1027; hooks.go:212,640) all take a\nbranch/commit-message string — never an argv unit id.",
    "positive_control": "Two. (1) CLI: `batten phase US-001 deploy` -> `batten: no phase \"deploy\" in batten.yaml`, EXIT=1 — same command, one field changed, proves the spec was loaded and the error path is alive, so the exit-0 on FOO-9 is a real accept and not a silent load failure. (2) Gate: with enforcement: enforce and a run for US-001, the commit payload returned permissionDecision=deny, while the identical payload with command changed to `ls -la` was silent — the hook machinery engages in this sandbox. (Note: an earlier silent run was caused by passing a Git-Bash POSIX $PWD as `cwd`; with a Windows-style cwd the gate fires. Silence there was 'no spec found', exactly Trap 2.)",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:346 (cmdPhase), plus cmdClose/cmdCheck/cmdOverride/cmdIngest/cmdShow/cmdBudget which also take argv unit ids: after loading the spec, reject when `sp.MatchUnit(unit) != unit` with `batten: %q does not match unit.pattern %q` and exit 1, gated by a `--force` escape (anchor the regexp with ^$ so US-0001 does not slide to US-000).",
    "notes": "NOT Trap 1: this is not the hooks' deliberate fail-open/advise() path (DESIGN principle #3 covers writes batten cannot attribute). Here the user explicitly names the id on the command line, the spec explicitly declares the pattern, and the sibling argument in the very same function gets a hard error. Nothing in DESIGN.md, README.md or batten.schema.json states that unit ids are intentionally free-form. Severity lowered from major to minor because the failure mode is fail-SAFE at the gate (the real unit is still denied) and the damage is a permanent but cosmetic phantom row plus one misleading override message — no approval is ever wrongly granted by this alone. Caveat for the fixer: the repo's own backlog carries both US-001 and US-UX-01 style ids (docs/field-test/ARTIFACT-PLAN.md:556), so a strict reject without --force would break real ids if unit.pattern is narrower than the tracker."
  },
  {
    "index": 22,
    "verdict": "CONFIRMED",
    "reason": "Reproduced byte-for-byte on the HEAD binary: with session sessA bound to US-001's run, `git commit -m \"feat(US-999): nope\"` is denied with a message that names US-001 and never mentions US-999 — and the denial text is IDENTICAL whether the commit message says US-999, US-001, or names no unit at all, proving the commit text is never read. The same root cause also flips a deny into an allow: once US-001 is batten-verified, sessA's byte-identical `feat(US-999)` commit sails through silently even though US-999 has no verdict at all, while the same command from a session bound to US-999 is denied. `activeUnit` (internal/hooks/hooks.go:626) has no commit-message path, yet batten.schema.json:26 and :40 explicitly promise the unit pattern is read 'out of branch names and commit messages' and 'is how batten answers \"which unit is this commit for?\"'. Not covered by the known-deliberate fail-open/advisory exceptions: this is a hard deny naming the wrong unit, plus a documented-behaviour contradiction.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"; CWD=\"$SB/sandbox/v22\"\nmkdir -p \"$CWD\" && cd \"$CWD\" && git init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$CWD/state.db\"\n\"$BAT\" init\n# edit batten.yaml: enforcement: enforce; unit.name US, unit.pattern 'US-\\d{3}';\n# phases build / verify(gate: qa) / close(gate: qa, requires_verdict: ok);\n# gates.qa.checks: ['echo ok'], verdict/evidence required; domains.core.path: src\necho x > README.md && git add -A && git commit -q -m init\n\"$BAT\" doctor\n\"$BAT\" phase US-001 build\n\"$BAT\" phase US-999 build\n# bind session sessA to US-001's run through the PostToolUse binder\nprintf '{\"session_id\":\"sessA\",\"cwd\":\"%s\",\"hook_event_name\":\"PostToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"batten phase US-001 build\"}}' \"$CWD\" | \"$BAT\" hook PostToolUse\n# agent-sourced verdict so the gate reaches the checks branch (matches the finding's observed text)\necho '{\"check_id\":\"US-001\",\"result\":\"ok\",\"evidence\":[\"I ran the tests\"],\"why\":\"looks fine\",\"safe_next_step\":\"commit\"}' > v001.json\n\"$BAT\" verdict --unit US-001 --gate qa --file v001.json\n# R1 - the finding: commit message names US-999, denial names US-001\nprintf '{\"session_id\":\"sessA\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\\\"feat(US-999): nope\\\\\"\"}}' \"$CWD\" | \"$BAT\" hook PreToolUse\n# R2 - same command naming US-001 -> byte-identical denial\n# R3 - same command naming no unit  -> byte-identical denial\n# S1 - escalation: make US-001 batten-verified, then commit 'for' unverified US-999\n\"$BAT\" check US-001\nprintf '{\"session_id\":\"sessA\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\\\"feat(US-999): unverified work lands\\\\\"\"}}' \"$CWD\" | \"$BAT\" hook PreToolUse\n# positive control for that ALLOW: same payload, session_id changed to sessB bound to US-999\nprintf '{\"session_id\":\"sessB\",\"cwd\":\"%s\",\"hook_event_name\":\"PostToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"batten phase US-999 build\"}}' \"$CWD\" | \"$BAT\" hook PostToolUse\nprintf '{\"session_id\":\"sessB\",\"cwd\":\"%s\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\\\"feat(US-999): unverified work lands\\\\\"\"}}' \"$CWD\" | \"$BAT\" hook PreToolUse",
    "evidence": "=== R1) sessA bound to US-001; commit message names US-999 (a REAL, separate open run) ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-001 has no batten-verified pass. The gate's checks must be RUN, not asserted.\\nRun: batten check US-001\\nTo proceed anyway (recorded): batten override US-001 --reason \\\"...\\\"\"}}\n\n=== R2) same session, commit message names US-001 (byte-identical denial) ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-001 has no batten-verified pass. The gate's checks must be RUN, not asserted.\\nRun: batten check US-001\\n...\"}}\n\n=== R3) commit message names NO unit at all (byte-identical denial) ===\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-001 has no batten-verified pass. ...\"}}\n\n# the three denials are identical strings -> the commit text is never consulted.\n# R1 matches the finding's `observed` field verbatim.\n\n=== S1) after `batten check US-001` (US-001 batten-verified); sessA commits 'for' US-999 ===\nEXIT=0 (no output = ALLOW)\n\n=== batten show US-999 ===\nUS-999  run=US-999-1785252013294809000-06a9315e  status=running  phase=build  base=8ebf42a\nno verdict: the close gate will deny a commit\n\n=== batten runs ===\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-999  running  build  —       —       —\nUS-001  running  build  ok*      —       —\n* batten-verified: the gate's checks were actually run\n\n=== source ===\ninternal/hooks/hooks.go:626 activeUnit() - priority is (1) session_id, (2) git branch, (3) single unowned open run. No branch reads the command text.\nbatten.schema.json:26  \"...batten never invents one - it reads yours out of branch names and commit messages.\"\nbatten.schema.json:40  \"...matches a unit id anywhere in free text - a branch name, a prompt, a commit message... This is how batten answers \\\"which unit is this commit for?\\\" without being told.\"",
    "positive_control": "Required, because I escalated to an ALLOW claim (S1). Control: the byte-identical PreToolUse payload with ONE field changed (session_id sessA -> sessB, sessB bound to US-999) produced a mandatory DENY: {\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-999 has no verdict envelope. Run the \\\"verify\\\" phase before committing....\"}. So the gate is live in this sandbox and the silence in S1 is a genuine allow, not a dead hook. A negative control (`ls -la`, a non-commit command) was correctly silent. For the finding's own claim (a DENY, not an ALLOW) no control is needed - the deny JSON is the output itself.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/hooks/hooks.go:280 (verdictGate) - run h.Spec.MatchUnit(cmd) on the commit command; if it yields a unit that differs from activeUnit(in), deny naming BOTH (\"this commit says US-999 but this session is bound to US-001\"), and fall back to it when activeUnit is empty. Also fix batten.schema.json:26,:40, which promise commit-message resolution that hooks.go:626 does not implement."
  },
  {
    "index": 23,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary, but SCOPED. The mechanical defect is real: `batten show <unit> --run <run-id>` discards both the flag and its value, prints the unit's LATEST run and exits 0 — it does so even when the run id does not exist, so the user is shown a run they did not ask for and told it succeeded. `batten runs` likewise has no RUN ID / STARTED / AGE column, and both commands accept any unknown flag silently (`--json`, `--help`, `--totally-bogus-flag` all exit 0 and change nothing). The finding's HEADLINE, however, is refuted: closed runs are NOT unreachable. `batten mcp` exposes them completely — `batten_runs` returns run_id/started_at/ended_at per run and `batten_run_graph {\"run_id\": \"<old>\"}` opened the exact closed run (internal/mcp/mcp.go:847 resolveRun -> store.Run). `batten tui` also lists every run (internal/tui/tui.go:148 ListRuns(project,100)) and renders a detail pane for whichever row is selected. So the gap is the two NON-INTERACTIVE CLI commands only, not the product. Not a Trap-1 by-design case: nothing here is fail-open-with-a-warning and nothing is a NOT MEASURABLE report — it is a typed argument being thrown away while reporting success. Not already fixed: none of the five landed fixes in CHANGELOG.md touch cmdShow/cmdRuns argument handling.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v23\" && cd \"$SB/sandbox/v23\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$SB/sandbox/v23/state.db\"\n\"$BAT\" init\ngit add -A && git commit -qm init\nexport BATTEN_DB=\"$SB/sandbox/v23/state.db\"\n\"$BAT\" phase US-001 build\n\"$BAT\" close US-001 --status failed\n\"$BAT\" phase US-001 build\n\"$BAT\" runs\n\"$BAT\" show US-001\ngrep -ao 'US-001-[0-9]\\{19\\}-[0-9a-f]\\{8\\}' state.db | sort -u   # -> OLD=US-001-1785251889456151800-b7e70c52\n\"$BAT\" show US-001 --run US-001-1785251889456151800-b7e70c52 ; echo EXIT=$?\n\"$BAT\" show US-001-1785251889456151800-b7e70c52 ; echo EXIT=$?\n\"$BAT\" show US-001 --json ; echo EXIT=$?\n\"$BAT\" show US-001 --totally-bogus-flag ; echo EXIT=$?\n\"$BAT\" runs --help ; echo EXIT=$?\n# positive controls\n\"$BAT\" phase US-002 build && \"$BAT\" show US-002 ; echo EXIT=$?\n\"$BAT\" show US-001 --run NO-SUCH-RUN-ID-9999 ; echo EXIT=$?\n\"$BAT\" bogusverb ; echo EXIT=$?\n# refutation of the 'unreachable' claim: drive the MCP server over stdio in the same sandbox\n# (PowerShell, BATTEN_DB set, cwd = sandbox), calling batten_runs then\n# batten_run_graph {\"run_id\":\"US-001-1785251889456151800-b7e70c52\"}",
    "evidence": "$ batten runs\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-001  running  build  —        —       —\nUS-001  failed   build  —        —       —\n   (no RUN ID / STARTED / AGE column: the two rows are separable only by status+phase)\n\n$ batten show US-001\nUS-001  run=US-001-1785251889635567000-ef775931  status=running  phase=build  base=e7c7f1f\n\n$ batten show US-001 --run US-001-1785251889456151800-b7e70c52\nUS-001  run=US-001-1785251889635567000-ef775931  status=running  phase=build  base=e7c7f1f\nEXIT=0\n   (asked for ...456151800-b7e70c52, got ...635567000-ef775931 — the OTHER run — exit 0)\n\n$ batten show US-001-1785251889456151800-b7e70c52\nbatten: no run recorded for US-001-1785251889456151800-b7e70c52\nEXIT=1\n\n$ batten show US-001 --json\nUS-001  run=US-001-1785251889635567000-ef775931 ...   EXIT=0\n$ batten show US-001 --totally-bogus-flag\nUS-001  run=US-001-1785251889635567000-ef775931 ...   EXIT=0\n$ batten runs --help\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED   ...   EXIT=0\n\nCODE: cmd/batten/main.go:60  `case \"runs\": err = cmdRuns()`  — os.Args[2:] is never passed, so\n      cmdRuns() (main.go:388) cannot see any flag at all.\n      cmd/batten/main.go:442 cmdShow(args) reads args[0] only and calls st.LatestRun(); args[1:]\n      is never examined. store.Run(runID) already exists at internal/store/store.go:322.\n\nWHY THE TITLE IS OVERSTATED — the same closed run, opened by id through `batten mcp`:\n  {\"id\":2,\"result\":... \"runs\":[{... \"run_id\":\"US-001-1785251889635567000-ef775931\",\"started_at\":\"2026-07-28T15:18:09Z\",\"ended_at\":\"\",\"status\":\"running\"...},\n                              {... \"run_id\":\"US-001-1785251889456151800-b7e70c52\",\"started_at\":\"2026-07-28T15:18:09Z\",\"ended_at\":\"2026-07-28T15:18:09Z\",\"status\":\"failed\"...}]}\n  {\"id\":3,\"result\":... \"run\":{\"run_id\":\"US-001-1785251889456151800-b7e70c52\",\"status\":\"failed\",\"ended_at\":\"2026-07-28T15:18:09Z\",...}}\n  -> batten_run_graph resolved the OLDER, CLOSED run by id. It is reachable; only the CLI is blind.",
    "positive_control": "Three controls, all passed, so the silence is genuinely the flag being discarded and not an inert binary. (A) `batten show US-002` after `batten phase US-002 build` printed a DIFFERENT run (US-002-1785252078780204400-15cf9bc5, exit 0) — cmdShow does read and react to its arguments. (B) THE DECISIVE ONE: `batten show US-001 --run NO-SUCH-RUN-ID-9999` must error if --run were honoured; instead it printed US-001's latest run and exited 0 — proving the value is discarded, not merely unmatched. (C) `batten bogusverb` printed the usage block and exited 2, so the binary does validate argv at the top level; the laxity is per-command, and the contrast rules out 'this build ignores everything'.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:442 cmdShow — parse `--run <id>` and resolve with the already-existing store.Run(id) (internal/store/store.go:322) instead of LatestRun, and return an error on an unrecognised flag; cmd/batten/main.go:60 pass os.Args[2:] into cmdRuns and add RUN ID + STARTED/AGE columns at main.go:403. Incidental, separate, and already recorded in docs/field-test/FINDINGS-RAW.md: `nodes.node_id TEXT PRIMARY KEY` (store.go:84) is global, so AddNode's `INSERT OR REPLACE` with the deterministic id \"p-\"+phase (main.go:367) moved run 1's phase node onto run 2 — observed live here when `batten phase US-002 build` stripped the `[phase] build` line from `batten show US-001`.",
    "notes": "Isolation held: BATTEN_DB was re-exported to <sandbox>/state.db in every single command (bash and PowerShell); ~/.batten was never created or touched; no file in the repo under test was modified; no git push/remote."
  },
  {
    "index": 24,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: with the anchor-bearing `build` phase skipped, `batten phase US-008 verify` (which declares `diff_from: anchor`) prints only the generic gate line and exits 0 — no mention that the anchor it diffs from does not exist. `.DiffFrom` has zero consumers anywhere in the Go tree at HEAD. This is not Trap 1: the absent anchor is not a normal state and the ask is a warning, not a deny. I proved the consequence — the exact command `/batten-verify.md:20-24` prescribes (`git diff --name-only <base-sha>` with the SHA from `batten show`) degenerates to a 1-file working-tree diff instead of the unit's 4 real files, so the QA gate silently grades the wrong scope. batten's own store test at internal/store/lifecycle_test.go:201 names this exact hazard (\"Losing it silently would make `diff_from: anchor` diff from nothing\"), and cmdPhase already announces the anchor when it IS recorded — there is simply no counterpart for its absence. Worse, I found a stronger static variant: a spec whose `verify` declares `diff_from: anchor` while NO phase declares `anchor: git_sha` (so the anchor can never be recorded) is reported by `batten doctor` as a clean `✓ ... 4 phases`.",
    "repro": "SB=$SANDBOX\nmkdir -p \"$SB/sandbox/v24\" && cd \"$SB/sandbox/v24\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" init\n# replace batten.yaml with the finding's shape: phases plan / build(anchor: git_sha, fanout) / verify(gate: qa, diff_from: anchor) / close(requires_verdict: ok), unit pattern 'US-\\d+', one domain core(path: src)\necho hello > README.md && git add -A && git commit -qm init\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" doctor\n# --- the finding ---\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" phase US-008 plan\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" phase US-008 verify; echo \"EXIT=$?\"\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" show US-008\n# --- consequence: the scope /batten-verify.md tells the agent to use ---\nmkdir -p src && echo a > src/a.txt && git add -A && git commit -qm 'unit work 1'\necho b > src/b.txt && git add -A && git commit -qm 'unit work 2'\nexport BATTEN_DB=\"$PWD/state.db\"; BASE=$(\"$SB/bin/batten.exe\" show US-008 | head -1 | sed 's/.*base=//')\ngit diff --name-only $BASE          # what the agent actually runs\ngit diff --name-only 840d8b0        # what it SHOULD have been\n# --- static variant: delete `anchor: git_sha` from the build phase, keep diff_from: anchor ---\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" doctor\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" phase US-010 verify",
    "evidence": "=== A) phase plan (anchor-bearing `build` skipped) ===\nUS-008 -> phase plan\nEXIT=0\n\n=== B) phase verify (declares diff_from: anchor; NO anchor recorded) ===\nUS-008 -> phase verify\nthis phase must emit a verdict for gate \"qa\" (evidence required)\nEXIT=0\n        <-- nothing about the missing anchor\n\n=== C) show ===\nUS-008  run=US-008-1785251887652212900-ba5a1cd4  status=running  phase=verify  base=\n  [phase] plan                     running\n  [phase] verify                   running\n\nno verdict: the close gate will deny a commit\n        <-- `base=` is a bare empty field, not \"no anchor recorded\"\n\n=== CONSEQUENCE: the scope /batten-verify.md:20-24 prescribes ===\nbase from 'batten show US-008' = []\n--- git diff --name-only $BASE  (BASE empty) ---\nstate.db\n(files listed above: 1)\n--- the CORRECT scope, had the anchor been recorded (840d8b0) ---\nbatten.yaml\nbatten.yaml.bak\nsrc/a.txt\nsrc/b.txt\nstate.db\n        <-- the QA gate reviews 1 unrelated file instead of the unit's real diff, silently\n\n=== STATIC VARIANT: `diff_from: anchor` with NO phase carrying `anchor: git_sha` ===\n$ grep -n anchor batten.yaml\n18:    diff_from: anchor\n$ batten doctor\n✓ ...batten.yaml — project \"v24\", unit \"US\", 4 phases, 1 domains\n✓ close gate: phase \"close\" requires verdict \"ok\" on any gate\n⚠ gate \"qa\" declares no checks — it verifies NOTHING ...\n● enforcement: REPORT ...\n✓ store: ...state.db\n        <-- an anchor that can NEVER be recorded validates clean\n$ batten phase US-010 verify\nUS-010 -> phase verify\nthis phase must emit a verdict for gate \"qa\" (evidence required)\nEXIT=0\n\n=== code at HEAD ===\n$ grep -rn \"\\.DiffFrom\" --include=\"*.go\" .   ->  exit=1 (zero consumers)\ncmd/batten/main.go:371-378  only `ph.Anchor == \"git_sha\"` is ever consulted; `ph.DiffFrom` never is.\ninternal/store/lifecycle_test.go:201  \"The anchor is what every later phase diffs from. Losing it silently would make `diff_from: anchor` diff from nothing.\"\ndocs/field-test/ARTIFACT-PLAN.md:166  names this failure mode: QA \"silently grades\" the wrong scope.",
    "positive_control": "Same four commands with `build` NOT skipped, in the same sandbox and DB — proves the anchor machinery is live here and that batten does speak when it has the anchor:\n  US-009 -> phase plan\n  anchor: US-009 base SHA = 840d8b0     <-- cmdPhase DOES announce the anchor when recorded\n  US-009 -> phase build\n  US-009 -> phase verify\n  this phase must emit a verdict for gate \"qa\" (evidence required)\n  US-009  run=...  phase=verify  base=840d8b0\nSo the silence in the US-008 case is not \"the command never engaged\" — the identical code path ran and printed a line one phase earlier. Note the control also shows the second half of the finding: even WITH the anchor present, entering `verify` prints no `diff scope: 840d8b0..HEAD`, because `ph.DiffFrom` is read nowhere.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:378 (after the anchor block, re-reading the run since EnsureRun cached BaseSHA before SetBaseSHA): if ph.DiffFrom == \"anchor\" — when BaseSHA is empty, warn naming the phase that carries `anchor: git_sha`; when set, print `diff scope: <base>..HEAD`. Also add to Spec.Validate (internal/spec/spec.go:225) a doctor warning when any phase declares diff_from: anchor and no phase declares anchor: git_sha."
  },
  {
    "index": 25,
    "verdict": "REFUTED",
    "reason": "The factual half reproduces at HEAD (base_sha is stored as 7 chars, cmd/batten/main.go:1487 still uses `--short=7`), but it is not a defect. `git rev-parse --short=N` is a MINIMUM, not a fixed width: git lengthens the prefix until it is unique, so batten can never persist an ambiguous anchor. I proved that empirically. And every consumer of BaseSHA in the binary is display/serialization only (main.go:458 show, canvas.go:215, mcp.go:185, tui.go:403, vault.go:154/204) — nothing computes a diff, and nothing compares it to a full SHA; the only comparisons in the tree are `== \"\"` / `!= \"\"`. The stored abbreviation resolves fine through `git rev-parse --verify`, `git diff <base>..HEAD` and `git log <base>..HEAD`, which is the one job the anchor has. The 7-char form is also the repo's declared provenance convention (plugin/claude-code/commands/batten-close.md:42: \"`{git_sha7}` is `git rev-parse --short=7 HEAD`\"). Not fixed at HEAD, but nothing to fix.",
    "repro": "SB=$SANDBOX\nmkdir -p \"$SB/sandbox/v25\" && cd \"$SB/sandbox/v25\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\necho hello > a.txt && git add -A && git commit -q -m c1\n\"$SB/bin/batten.exe\" init\ngit rev-parse HEAD\n\"$SB/bin/batten.exe\" phase US-001 build\n\"$SB/bin/batten.exe\" phase US-001 verify\n\"$SB/bin/batten.exe\" show US-001 | head -1\n\n# --- downstream-consumer test: does the abbreviation actually work? ---\nexport BATTEN_DB=\"$PWD/state.db\"\necho x >> a.txt && git commit -qam c2\necho y >> a.txt && git commit -qam c3\ngit rev-parse --verify 035c8fc\ngit diff --stat 035c8fc..HEAD\ngit log --oneline 035c8fc..HEAD\n\"$SB/bin/batten.exe\" phase US-001 build   # anchor must not move\n\"$SB/bin/batten.exe\" show US-001 | head -1\n\n# --- ambiguity test: is --short=N a fixed width or a minimum? ---\nmkdir -p \"$SB/sandbox/v25/abbrev-lab\" && cd \"$SB/sandbox/v25/abbrev-lab\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nmkdir -p objs; i=1; while [ $i -le 4000 ]; do echo \"$i\" > \"objs/f$i\"; i=$((i+1)); done\ngit add -A && git commit -q -m bulk\ngit cat-file --batch-all-objects --batch-check='%(objectname)' > all.txt\ncut -c1-4 all.txt | sort | uniq -d | head -1 > dup.txt   # a genuinely colliding 4-char prefix\ngit rev-parse --verify $(cat dup.txt)                    # what ambiguity looks like\nfor s in $(grep \"^$(cat dup.txt)\" all.txt | head -2); do echo \"$s -> $(git rev-parse --short=4 $s)\"; done\n\n# --- stress: 7-char anchor after the repo grows by 4000 objects ---\ncd \"$SB/sandbox/v25\"; export BATTEN_DB=\"$PWD/state.db\"\nmkdir -p bulk; i=1; while [ $i -le 4000 ]; do echo \"$i\" > \"bulk/f$i\"; i=$((i+1)); done\ngit add -A && git commit -q -m bulk\ngit rev-parse --verify 035c8fc && git diff --stat 035c8fc..HEAD | tail -1",
    "evidence": "# the finding's factual claim DOES reproduce at HEAD:\n$ git rev-parse HEAD\n035c8fcdf4b7e3b1c4bf6582d9026a0a1a0d8210\n$ batten.exe phase US-001 build\nanchor: US-001 base SHA = 035c8fc\n$ batten.exe show US-001 | head -1\nUS-001  run=US-001-1785252264521956500-525676a1  status=running  phase=verify  base=035c8fc\n   -> stored base_sha=[035c8fc] len=7   (cmd/batten/main.go:1487 still `--short=7`; NOT fixed at HEAD)\n\n# but every downstream use of the anchor works on the abbreviation:\n$ git rev-parse --verify 035c8fc\n035c8fcdf4b7e3b1c4bf6582d9026a0a1a0d8210\nEXIT=0\n$ git diff --stat 035c8fc..HEAD\n a.txt | 2 ++\n 1 file changed, 2 insertions(+)\nEXIT=0\n$ git log --oneline 035c8fc..HEAD\nc76ca23 c3\nb77d5c5 c2\nEXIT=0\n\n# and the anchor does not move on a phase re-run (TEST-MATRIX PH-A1):\n$ batten.exe phase US-001 build && batten.exe show US-001 | head -1\nUS-001 -> phase build\nUS-001  run=US-001-1785252264521956500-525676a1  status=running  phase=build  base=035c8fc\n\n# THE DECISIVE POINT — `--short=N` is a MINIMUM, git lengthens until unique.\n# 4003-object repo, a real 4-char collision:\ncolliding 4-char prefix: 01d6\n$ git rev-parse --verify 01d6\nerror: short object ID 01d6 is ambiguous\nhint: The candidates are:\nhint:   01d65e2 blob\n$ git rev-parse --short=4 01d65e21e04bc2d93100460b606f84f117090412\n01d65        <- 5 chars, NOT 4\n$ git rev-parse --short=4 01d6fdc12aab11b796a48b14e8b751e3675d72bd\n01d6f        <- 5 chars, NOT 4\n   -> batten can never persist an ambiguous anchor; git widens the prefix for it.\n\n# stress: anchor still resolves after the repo grows by 4000 objects\nobjects now: 4014\n$ git rev-parse --verify 035c8fc\n035c8fcdf4b7e3b1c4bf6582d9026a0a1a0d8210   EXIT=0\n 4004 files changed, 4057 insertions(+)\n\n# no consumer needs 40 chars — grep for any equality compare on BaseSHA:\ncmd/batten/main.go:372:  if ph.Anchor == \"git_sha\" && run.BaseSHA == \"\"\ninternal/canvas/canvas.go:214:  if run.BaseSHA != \"\"\ninternal/tui/tui.go:402:        if r.BaseSHA != \"\"\ninternal/vault/vault.go:203:    if r.BaseSHA != \"\"\n   (only emptiness checks; nothing compares the anchor to a full SHA, and no Go code\n    in the tree shells out to `git diff` with it — `diff_from: anchor` is a spec\n    declaration consumed by the agent, which uses git, which accepts prefixes.)\n\n# the 7-char form is the repo's DECLARED provenance convention:\nplugin/claude-code/commands/batten-close.md:42:\n  `{git_sha7}` is `git rev-parse --short=7 HEAD`.",
    "positive_control": "n/a — no ALLOW claim in this finding (it is a stored-value claim, not a hook-silence claim). The equivalent control I ran instead is the ambiguity control: I constructed a git repo where a 4-char prefix IS genuinely ambiguous, showed that git reports `error: short object ID 01d6 is ambiguous` for it, and then showed that `git rev-parse --short=4` on those same objects returns 5 characters rather than 4. That proves the ambiguity failure mode is real and detectable in my sandbox, and that `--short=N` structurally cannot produce it — which is what the finding needed in order to be a defect.",
    "already_fixed_at_head": false,
    "severity_adjusted": "not-a-defect",
    "fix_hint": ""
  },
  {
    "index": 26,
    "verdict": "REFUTED",
    "reason": "The observation reproduces byte-for-byte on the HEAD binary (12 nodes, 0 edges, identical node texts), but it is BY DESIGN, not broken. batten's edge vocabulary is fixed at spawn|depends_on|retry_of|supersedes|rollback (store.go:106, DESIGN.md 5.3) — there is no phase-sequence relation to record. Phase order is encoded POSITIONALLY: canvas.go:119 sorts phases by StartedAt and canvas.go:136 lays them out left-to-right at x = i*gapX, one group per phase, with the verdict card continuing the row at x = len(phases)*gapX. canvas.go:97-98 states this outright: 'Render lays the run out as columns ... Deliberately simple — a run is small, and the layered structure is already in the data.' The canvas's own acceptance contract (docs/field-test/ARTIFACT-PLAN.md, section G) requires 'every edge from the store' plus one group per phase, a verdict node and a header node — it never requires phase-to-phase edges nor a verdict edge, and its own reference observation is '8 nodes, 2 edges' where both edges are spawns. A run with no fan-out and no retries has zero non-positional structure to draw, so 0 edges is the correct render of an empty edge set, not a dropped one. 'verdict' and 'header' are synthetic canvas-only nodes with no row in the nodes table, so no store edge could ever point at them without inventing a relation the schema does not have. What remains is a wording gap, not a defect: the CLI help calls the artifact 'the run DAG' and internal/mcp/mcp.go:82 calls the edges 'the path actually taken', which sets up the reporter's expectation of arrows.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v26\" && cd \"$SB/sandbox/v26\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\n# rewrote batten.yaml: unit US-\\d{3}, 5 phases research/plan/build/verify/close (build fanout+anchor, verify gate qa, close requires_verdict ok), 1 domain app\necho hello > README.md && git add -A && git commit -qm init\nexport BATTEN_DB=\"$PWD/state.db\"\nfor p in research plan build verify close; do \"$BAT\" phase US-001 $p; done\ncat > v.json <<'JSON'\n{\"check_id\":\"US-001\",\"result\":\"ok\",\"evidence\":[\"go test ./... passed\"],\"why\":\"all checks green\",\"safe_next_step\":\"commit\"}\nJSON\n\"$BAT\" verdict --file v.json --unit US-001\n\"$BAT\" canvas US-001 --out us001.canvas\npython -c \"import json;d=json.load(open('us001.canvas'));print('nodes',len(d['nodes']),'edges',len(d['edges']));[print(' ',n['id'],'|',repr(n.get('text') or n.get('label'))) for n in d['nodes']]\"\n# --- positive control: prove the canvas edge pipeline engages at all in this sandbox ---\ngit checkout -q -b US-001\nexport BATTEN_DB=\"$PWD/state.db\"\necho '{\"session_id\":\"sfan2\",\"cwd\":\"'\"$SB/sandbox/v26\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"a1\",\"agent_type\":\"app\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"sfan2\",\"cwd\":\"'\"$SB/sandbox/v26\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"a2\",\"agent_type\":\"app\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" canvas US-001 --out us001_fanout.canvas\npython -c \"import json;d=json.load(open('us001_fanout.canvas'));print('nodes',len(d['nodes']),'edges',len(d['edges']));[print('  EDGE',e['id'],e['fromNode'],'->',e['toNode'],repr(e.get('label',''))) for e in d['edges']]\"",
    "evidence": "$ for p in research plan build verify close; do batten phase US-001 $p; done\nUS-001 -> phase research\nUS-001 -> phase plan\nanchor: US-001 base SHA = 2712f1f\nUS-001 -> phase build\nUS-001 -> phase verify\nthis phase must emit a verdict for gate \"qa\" (evidence required)\nUS-001 -> phase close\n\n$ batten verdict --file v.json --unit US-001\nverdict recorded: US-001 qa=ok (1 evidence)\n\n$ batten canvas US-001 --out us001.canvas\nwrote us001.canvas (12 nodes, 0 edges)\n\n$ python -c \"...\"\nnodes 12 edges 0\n  g-p-research | 'research'\n  p-research | '## research\\n`running`'\n  g-p-plan | 'plan'\n  p-plan | '## plan\\n`running`'\n  g-p-build | 'build'\n  p-build | '## build\\n`running`'\n  g-p-verify | 'verify'\n  p-verify | '## verify\\n`running`'\n  g-p-close | 'close'\n  p-close | '## close\\n`running`'\n  verdict | '## verdict: ok\\n\\n**all checks green**\\n\\n- go test ./... passed\\n'\n  header | '# US-001\\n\\nrun `US-001-1785252313624573700-d6388f99`\\nstatus `running`\\nphase `close`\\nbase `2712f1f`'\n\n# store confirms the render is faithful — the edge set really is empty, nothing was dropped:\nnodes: [('p-research','phase','research','running'), ('p-plan','phase','plan','running'), ('p-build','phase','build','running'), ('p-verify','phase','verify','running'), ('p-close','phase','close','running')]\nedges: []\n\n# the ONE production writer of edges, and it only ever writes 'spawn':\n$ grep -rn 'AddEdge' --include=*.go . | grep -v _test.go\n./internal/store/store.go:453:func (s *Store) AddEdge(runID, src, dst, rel string) error {\n./internal/hooks/hooks.go:494:\t\t_ = h.Store.AddEdge(run.RunID, \"p-\"+run.Phase, nodeID, \"spawn\")\n\n# the deliberate-design citation, internal/canvas/canvas.go:97-98:\n// Render lays the run out as columns: each phase is a group, its subagents stack below it.\n// Deliberately simple — a run is small, and the layered structure is already in the data.\n\n# and canvas.go:119 + :136-137 encode the phase ORDER positionally rather than with edges:\nsort.Slice(phases, func(i, j int) bool { return phases[i].StartedAt < phases[j].StartedAt })\nx := i * gapX   ... c.Nodes = append(c.Nodes, Node{ID: \"g-\"+p.NodeID, Type: \"group\", X: x - 30, ...})\n\n# docs/field-test/ARTIFACT-PLAN.md section G (the canvas's acceptance contract) requires\n# \"every edge from the store\" — no phase-sequence edge, no verdict edge — and records\n# \"Observed: `wrote …/US-015.canvas (8 nodes, 2 edges)`\" where both edges are spawns.",
    "positive_control": "REQUIRED and PASSED — the finding is an absence claim ('zero edges'), so I proved edges CAN reach the canvas in this same sandbox before accepting 0 as meaningful. Fired two SubagentStart hooks (agent_type=app, a declared domain) on branch US-001. First attempt with a POSIX cwd ('/tmp/claude/...') was silently a no-op (0 nodes, 0 edges added) — exactly the Trap-2 failure the brief warns about; re-running with an absolute Windows-style cwd made the hook engage. Store then held: edges: [('p-close','n-a1','spawn'), ('p-close','n-a2','spawn')], and `batten canvas US-001 --out us001_fanout.canvas` printed `wrote us001_fanout.canvas (14 nodes, 2 edges)` with `EDGE e0 p-close -> n-a1` / `EDGE e1 p-close -> n-a2`. So the render path, the store round-trip and the JSON writer all work; 0 edges on the no-fan-out run is a true empty edge set, not a dead code path. The control also settles the finding's own two 'expected' items: even WITH edges present, still no p-research->p-plan->... chain and still nothing pointing at the `verdict` card — because no such relation exists in the schema, which is the design, not a regression.",
    "already_fixed_at_head": false,
    "severity_adjusted": "polish",
    "fix_hint": ""
  },
  {
    "index": 27,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: an evidence[] whose items are JSON objects is rejected with the raw Go decoder error 'json: cannot unmarshal object into Go struct field Verdict.evidence of type string' appended to the envelope-shape wrapper. Not fixed at HEAD. The report's documentation claim is partly overstated, however: the string-item shape IS shown in two plugin files (skills/batten-verdict/SKILL.md:19 and commands/batten-verify.md:62) and in the MCP tool schema (internal/mcp/mcp.go:159, []string), though it is absent from README.md, from `batten --help`, and from batten.schema.json (which schematises batten.yaml, not the verdict envelope — so the report's fix hint points at the wrong file for that part). Real but cosmetic: severity polish.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v27\" && cd \"$SB/sandbox/v27\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$SB/sandbox/v27/state.db\"\n\"$BAT\" init\ncat > verdict-obj.json <<'EOF'\n{\"check_id\":\"TASK-001-qa\",\"result\":\"ok\",\"evidence\":[{\"kind\":\"command\",\"detail\":\"docker compose config -q -> exit 0\"}],\"why\":\"all checks pass\",\"safe_next_step\":\"commit\",\"requires_confirmation\":false}\nEOF\ncat verdict-obj.json | \"$BAT\" verdict --unit TASK-001; echo \"EXIT=$?\"",
    "evidence": "$ cat verdict-obj.json | batten.exe verdict --unit TASK-001\nbatten: verdict must be a JSON envelope {check_id, result, evidence[], why, safe_next_step, requires_confirmation}: json: cannot unmarshal object into Go struct field Verdict.evidence of type string\nEXIT=1\n\n(byte-identical to the finding's `observed`.)\n\nSame leak with a scalar of the wrong type:\n$ echo '{\"check_id\":\"TASK-001-qa\",\"result\":\"ok\",\"evidence\":[42],...}' | batten.exe verdict --unit TASK-001\nbatten: verdict must be a JSON envelope {...}: json: cannot unmarshal number into Go struct field Verdict.evidence of type string\nEXIT=1\n\nDocumentation check (repo under test, read-only):\n- README.md:46 shows only `\"evidence\":[]` (empty) — item shape never shown; the `--file verdict.json` example at README.md:58 never shows the file.\n- `batten --help`: \"batten verdict [--file v.json]     record a verdict envelope\" — no shape.\n- batten.schema.json mentions `evidence` only as the gate's `evidence: required` knob (lines 496-506); it does not schematise the verdict envelope at all.\n- The shape IS documented, with string items, in plugin/claude-code/skills/batten-verdict/SKILL.md:19 and plugin/claude-code/commands/batten-verify.md:62, and typed as []string in internal/mcp/mcp.go:159.\n\nSource at HEAD (cmd/batten/main.go:249-253) — the raw decoder error is wrapped with %w and printed as-is:\n\tvar v store.Verdict\n\tif err := json.Unmarshal(raw, &v); err != nil {\n\t\treturn fmt.Errorf(\"verdict must be a JSON envelope {check_id, result, evidence[], why, \"+\n\t\t\t\"safe_next_step, requires_confirmation}: %w\", err)\n\t}",
    "positive_control": "Three controls, all run in the same sandbox with the same BATTEN_DB, proving the verdict path engages and that it is specifically the item type that fails:\n(A) identical envelope with evidence[] as plain strings -> 'verdict recorded: TASK-001 qa=ok (1 evidence)', EXIT=0. So the command reaches the store and succeeds on the documented shape.\n(B) genuinely malformed JSON ('{not json') -> same wrapper + 'invalid character 'n' looking for beginning of object key string', EXIT=1. The wrapper is the generic parse path.\n(C) golden-rule control, result ok + empty evidence[] -> 'batten: result \"ok\" with an empty evidence[] is not allowed: an approval must cite something (command output, test counts, a criterion verified). Without evidence the result is \"blocked\"', EXIT=1 — showing batten CAN produce a human-authored, actionable message for an evidence problem, which is exactly what the type error does not do.",
    "already_fixed_at_head": false,
    "severity_adjusted": "polish",
    "fix_hint": "cmd/batten/main.go:250 — type-switch the unmarshal error: on *json.UnmarshalTypeError with Field==\"evidence\", return \"evidence[] items must be plain strings, e.g. \\\"make test: 142 passed, 0 failed\\\" — got a JSON \"+e.Value; and copy the string-item example from plugin/claude-code/skills/batten-verdict/SKILL.md:19 into README.md near line 58."
  },
  {
    "index": 28,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary, but the mechanism is DISPLAY-ONLY, not store loss: SaveVerdict is a plain INSERT (store.go:616) into an AUTOINCREMENT table and SQLite still holds both rows (agent + batten) after `batten check`; the commit gate reads both correctly via LatestVerdictBySource. What is wrong is that every read surface - show, runs, canvas, tui, mcp, statusline, export - calls LatestVerdict(runID,\"\") and renders exactly one verdict, so whichever producer wrote last erases the other's complementary evidence from view. The finding's own wording (\"still in the verdicts table but no longer shown anywhere\") is accurate, and I verified the 'nowhere' part on show, canvas and the MCP batten_verdict_status tool. I am raising it above 'polish': in the order `batten check` ITSELF recommends (safe_next_step = \"add your acceptance-criteria judgment, then close\"), the agent verdict lands last and `batten runs` then DROPS the '*' whose printed legend says \"batten-verified: the gate's checks were actually run\" - for US-002 they were run, and the table says otherwise. That is an active misstatement about verification status, which is the one thing batten exists to make trustworthy. Trap 1 does not apply: this is not warn-vs-deny and not NOT-MEASURABLE-vs-0.",
    "repro": "cd .../scratchpad/ft/sandbox/v28 && git init -q . && git commit -qm init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init                      # then edit sandbox batten.yaml: unit.pattern 'US-\\d+', gates.qa.checks: [echo check-ok, echo test-ok]\ncat > verdict.json  # agent envelope, result=ok, 3 AC citations incl. \"AC-3 ...: verified at backend/app/settings.py:5\"\n\"$BAT\" verdict --unit US-001 --file verdict.json\n\"$BAT\" show US-001               # CONTROL: prints all 3 agent AC citations\n\"$BAT\" check US-001              # batten runs the 2 gate checks, INSERTs a source=batten verdict\n\"$BAT\" show US-001               # DEFECT: only batten's 2 command results; all 3 AC citations gone\npython -c \"import sqlite3;print(list(sqlite3.connect('state.db').execute('select verdict_id,check_id,source,result from verdicts')))\"\n\"$BAT\" canvas US-001 --out c.json    # 'AC-3' absent, 'echo check-ok' present\n# reverse (batten's OWN recommended) order, which is worse:\n\"$BAT\" check US-002 && \"$BAT\" verdict --unit US-002 --file verdict2.json\n\"$BAT\" show US-002               # only the agent verdict; batten's PASS output gone\n\"$BAT\" runs                      # US-002 shows 'ok' with NO '*' though its checks DID run\n# MCP surface (what the agent reads):\n\"$BAT\" mcp  <<< tools/call batten_verdict_status {\"unit\":\"US-002\"}",
    "evidence": "$ \"$BAT\" show US-001          # CONTROL, before `batten check`\nverdict qa=ok (agent): all three acceptance criteria verified by reading the diff\n  - AC-1 (login returns 401 on bad password): verified at backend/app/auth.py:42\n  - AC-2 (password hashed with argon2): verified at backend/app/auth.py:88\n  - AC-3 (app must not start without DATABASE_URL): verified at backend/app/settings.py:5\n\n$ \"$BAT\" check US-001\n  ✓ echo check-ok\n  ✓ echo test-ok\nUS-001: OK (batten-verified). all gate checks passed (batten ran them)\n\n$ \"$BAT\" show US-001          # DEFECT: the 3 AC citations are gone\nverdict qa=ok (batten): all gate checks passed (batten ran them)\n  - echo check-ok: PASS (exit 0, 83ms)\n  - echo test-ok: PASS (exit 0, 47ms)\n\n$ sqlite verdicts             # STORE IS INTACT - nothing was overwritten\n(1, 'US-001-qa-agent',  'agent',  'ok', 1785252281)\n(2, 'US-001-qa-batten', 'batten', 'ok', 1785252290)\n(3, 'US-002-qa-batten', 'batten', 'ok', 1785252390)\n(4, 'US-002-qa-agent',  'agent',  'ok', 1785252390)\n\n$ \"$BAT\" canvas US-001 --out c.json\nAC-3 present in canvas: False\necho check-ok present in canvas: True\n\n# --- worse variant, in the order `batten check` ITSELF recommends\n# (safe_next_step = \"add your acceptance-criteria judgment, then close\", main.go:790) ---\n$ \"$BAT\" show US-002\nverdict qa=ok (agent): acceptance criteria verified\n  - AC-1 verified at a.py:1\n  - AC-2 verified at b.py:2\n  - AC-3 verified at c.py:3      <- batten's PASS output is the hidden one now\n\n$ \"$BAT\" runs\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-002  running         ok       -       -        <- checks DID run; star lost\nUS-001  running         ok*      -       -\n* batten-verified: the gate's checks were actually run\n\n$ MCP batten_verdict_status {\"unit\":\"US-002\"}   # what the agent itself reads\n \"verdict\": {\"check_id\":\"US-002-qa-agent\",\"evidence\":[\"AC-1 verified at a.py:1\",\n   \"AC-2 verified at b.py:2\",\"AC-3 verified at c.py:3\"],\"evidence_count\":3,...}\n (no `source` field exists on verdictInfo at all - mcp.go:155-165)",
    "positive_control": "Two controls, both required and both passed.\n(1) DISPLAY control - the same `batten show US-001` on the same DB one row earlier printed\nall three agent AC citations verbatim, so the renderer is not failing, the SELECT is: every\nread surface calls LatestVerdict(runID,\"\") (main.go:407,483,544,706; mcp.go:188,441;\ntui.go:178; export.go:58; statusline.go:302).\n(2) GATE control (for my \"no functional loss / commit still allowed\" claim, per Trap 2).\nFirst attempt was INVALID twice and I discarded it: reusing session_id \"s1\" made activeUnit\nresolve to the already-adopted run, and a git-bash \"/c/Users/...\" cwd meant the hook never\nengaged at all (events table empty, runs.session_id still ''). With a native Windows cwd\n(`pwd -W`) and a fresh session the gate fires:\n  US-003 (agent verdict only, NO batten check) -> MANDATORY DENY, verbatim:\n    {\"hookSpecificOutput\":{...,\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\n     \"batten: US-003 has no batten-verified pass. The gate's checks must be RUN, not asserted.\"}}\n  US-002 (batten check first, agent verdict LATEST) -> silent/EXIT=0 = ALLOW\n  US-001 (agent first, batten verdict LATEST)       -> silent/EXIT=0 = ALLOW\nSo the ALLOWs are real ALLOWs, not the silent-for-another-reason trap: the commit gate finds\nBOTH verdicts in either order via LatestVerdictBySource (hooks.go:329). Store and gate are\nunaffected; the defect is confined to what is DISPLAYED.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:483 (cmdShow): render BOTH LatestVerdictBySource(run.RunID,\"\",\"agent\") and (...,\"batten\"), each labelled by source, instead of the single LatestVerdict(run.RunID,\"\"). Same one-line swap at main.go:407-414 (cmdRuns must set '*' from the existence of a source=batten ok verdict, not from the LATEST verdict's source) and mcp.go:441/155-165 (verdictInfo has no `source` field, so the agent cannot tell whose claim it is reading)."
  },
  {
    "index": 29,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary (built 08:48, HEAD b38fd5a committed 08:44 same day). A transcript of 3 assistant records worth 291,000 tokens / $0.22, timestamped 20-30 min before the run's started_at, is read, parsed and PRICED by batten, then dropped by the store.RecordUsage time fence with no disclosure whatsoever: `batten ingest` prints `US-014: +0 requests, 0 tokens total, $0.00 imputed` and `batten budget` prints `· tokens 0 / 200.0k [............]` — a hard 0 with an empty progress bar, on the line directly above `· quota_pct NOT MEASURABLE`. The 200k ceiling that was in fact exceeded by 145% never trips. This is the Trap-1 defect side, not the design side: batten owns two honest idioms for an unmeasured value (`NOT MEASURABLE` in cmdBudget, `—` in cmdRuns main.go:416-420) and uses neither. cmdIngest even discloses the analogous case two lines later (`unpriced models (counted as $0, tokens still exact)`, main.go:1058-1061) but has no channel for fenced rows because RecordUsage returns only `added int`. The fence itself is correct and deliberate; the SILENCE is the defect, and the maintainers have already been bitten by it in their own fixtures (CHANGELOG.md:115-118 'every seeded row was silently discarded'; internal/mcp/mcp_test.go:126-131 'RecordUsage skips silently by design, and a seed that quietly seeds nothing is how three tests came to fail').",
    "repro": "SBW=$SANDBOX\nmkdir -p \"$SBW/sandbox/v29\" && cd \"$SBW/sandbox/v29\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\necho '# sandbox' > README.md && git add -A && git commit -q -m init\nexport BATTEN_DB=\"$SBW/sandbox/v29/state.db\"\n\"$SBW/bin/batten.exe\" init\n# edit batten.yaml: unit.pattern -> 'US-\\d{3}', budget -> tokens_per_run: 200000, imputed_usd_per_run: 2.00, quota_pct_per_run: 80.0\nexport BATTEN_DB=\"$SBW/sandbox/v29/state.db\"\n\"$SBW/bin/batten.exe\" phase US-014 build          # opens the run; started_at = now (2026-07-28T15:25Z)\n# write gen/ses-US-014.jsonl: 3 JSONL records, type=assistant, model=claude-sonnet-4-5-20250929,\n#   usage {input 1000, output 2000, cache_creation 4000, cache_read 90000} = 97,000 tok each = 291,000 total,\n#   timestamps 2026-07-28T14:55:39Z / 15:00:39Z / 15:05:39Z  (30/25/20 min BEFORE started_at)\nexport BATTEN_DB=\"$SBW/sandbox/v29/state.db\"\n\"$SBW/bin/batten.exe\" ingest US-014 --transcript \"$SBW/sandbox/v29/gen/ses-US-014.jsonl\"\n\"$SBW/bin/batten.exe\" budget US-014\n\"$SBW/bin/batten.exe\" runs\n\"$SBW/bin/batten.exe\" measure\n\"$SBW/bin/batten.exe\" show US-014\n# POSITIVE CONTROL — byte-identical file, ONLY the `timestamp` field rewritten to now+1..+3 min:\nsed 's/\"timestamp\": \"[^\"]*\"/<new ts>/' gen/ses-US-014.jsonl > gen/ses-US-014-NEW.jsonl\nexport BATTEN_DB=\"$SBW/sandbox/v29/state.db\"\n\"$SBW/bin/batten.exe\" ingest US-014 --transcript \"$SBW/sandbox/v29/gen/ses-US-014-NEW.jsonl\"\n\"$SBW/bin/batten.exe\" budget US-014\n\"$SBW/bin/batten.exe\" runs",
    "evidence": "=== ingest (transcript predates run) ===\nUS-014: +0 requests, 0 tokens total, $0.00 imputed\nEXIT=0\n\n=== budget US-014 ===\nUS-014  0 tokens, $0.00 imputed\n  · tokens       0 / 200.0k  [............]\n  · imputed_usd  $0.00 / $2.00  [............]\n  · quota_pct    NOT MEASURABLE — install the statusline (`batten statusline --install`)\nEXIT=0\n\n=== runs ===\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-014  running  build  —        —       —\nEXIT=0\n\n=== measure ===\nEXIT=0                       <-- prints NOTHING AT ALL\n\n=== show US-014 ===\nUS-014  run=US-014-1785252302271387400-2fe9907b  status=running  phase=build  base=fd0c34b\n  [phase] build                    running\nno verdict: the close gate will deny a commit\nEXIT=0\n\nNOTE the three different renderings of the SAME zero, in the same binary, seconds apart:\n  batten budget : `0 tokens` and `· tokens 0 / 200.0k [............]`   <-- a hard 0 + an empty bar\n  batten budget : `· quota_pct NOT MEASURABLE ...`                      <-- principle #1 applied correctly\n  batten runs   : `TOKENS —` / `IMPUTED —`                             <-- also honest (main.go:416-420)\nThe run's ledger holds NO row for 291,000 tokens that batten itself parsed and priced, and\n`ingest` said nothing about discarding them. Neither did anything else.",
    "positive_control": "DECISIVE, and it PASSED. I took the same transcript and changed exactly ONE field — the RFC3339 `timestamp` on each of the 3 records, moved from ~25 min before started_at to ~2 min AFTER it. requestIds, model and every usage number are byte-identical (verified with `diff <(sed 's/\"timestamp\": \"[^\"]*\"/TS/' old) <(sed ... new)` -> IDENTICAL apart from timestamp). Result:\n\n  $ batten ingest US-014 --transcript gen/ses-US-014-NEW.jsonl\n  US-014: +3 requests, 291.0k tokens total, $0.22 imputed\n  EXIT=0\n\n  $ batten budget US-014\n  US-014  291.0k tokens, $0.22 imputed\n    ! tokens       291.0k / 200.0k  [============]\n        OVER — on_exceed=warn\n    · imputed_usd  $0.22 / $2.00  [=...........]\n    · quota_pct    NOT MEASURABLE — install the statusline (`batten statusline --install`)\n\n  $ batten runs\n  UNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\n  US-014  running  build  —        291.0k  $0.22\n\nThis proves three things at once and closes every alternative explanation: (1) the parser DID read and price the file — 291.0k tokens and $0.22 are measurable, so the earlier `0` is an unreported drop, not an unparseable file; (2) store.RecordUsage's `if u.TS < fenceFor(u.RunID) { continue }` (store.go:720-722) is the sole cause; (3) the ceiling the fenced run reported as 0% consumed is in truth breached by 145% — the governor is silently disarmed, which matters because batten-night uses the budget ceilings as its only unattended tripwire.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/store/store.go:691 — change `RecordUsage(us []Usage) (added int, err error)` to also return the fenced-out rows (count + token sum + earliest TS), and have cmd/batten/main.go:1056 print them next to the existing unpriced-models disclosure at :1058, e.g. `+0 requests (3 requests / 291.0k tokens skipped: they predate this run, opened 15:25:02Z)`. One-line variant if the signature must not change: make cmdIngest compare len(rows) against `added` — it already holds both — and say so.",
    "notes": "ADVERSARIAL DUE DILIGENCE. (a) Not Trap 1 by-design: principle #1 says report NOT MEASURABLE rather than 0, and the brief states the converse — an UNMEASURED quantity rendered as a hard 0 — is a real defect. cmdBudget prints literal `0` plus a drawn empty bar, while its own next line prints NOT MEASURABLE and cmdRuns prints an em dash. batten demonstrably knows both honest idioms and used neither. (b) Not Trap 2: no ALLOW claim, and the positive control ran anyway and fired. (c) Not already fixed: reproduced on the HEAD binary; `git log` shows no commit after the finding touching the fence or cmdIngest's reporting. (d) WHERE I DISAGREE WITH THE REPORT, hence the downgrade blocker->major: the finding calls the `0` an 'invented number'. Strictly it is not — under the fence's deliberate definition ('usage before the run opened belongs to the session, not to this run', store.go:687-690, added after E0 showed a fresh run inheriting 176M tokens it never spent) the RUN really did own 0. The fence is right. What is indefensible is the total absence of disclosure, and the finding's own `expected` offers exactly that as the alternative fix ('or say what was dropped and why'), so the finding stands on its defensible half. It is a reporting defect on the DEFAULT path (real flow: session starts -> work happens -> `batten phase` opens the run -> Stop hook ingests; everything before `batten phase` is fenced), it disarms the only unattended tripwire, but it corrupts no data and blocks no work — major, not blocker. (e) SEPARATE DEFECT SPOTTED IN PASSING, NOT PART OF THIS FINDING: after the positive control, `batten measure` reported `claude-sonnet-4-5-20250929  3 req, 9.0k tokens, $0.22` for the very same rows `batten budget` and `batten runs` report as 291.0k. store.go:1135 sums only `input_tokens+output_tokens`, dropping all three cache buckets, and so contradicts Usage.Tokens() at store.go:679-681 which deliberately includes cache reads ('they are cheap, not free'). measure under-reports this run by 32x. Worth filing on its own. (f) Also observed: with tokens at 0, `batten measure` prints NOTHING and exits 0 — the Trap-2 silence shape, on a reporting command."
  },
  {
    "index": 30,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary. With gates.qa.checks empty and a run 7.8x over its token ceiling and 1.8x over its USD ceiling under on_exceed: block + enforcement: enforce, the hook returns only an advisory with no permissionDecision, so the commit proceeds. Adding one check line to the same spec, with the byte-identical payload and the same DB, produces permissionDecision=deny citing the budget; removing it again restores the pass-through. The else-branch return at hooks.go:347 consumes the return path the budget block at 354 lives on. Not by design: the advisory is deliberate (commit 24d8e4c) but that commit only ever discusses the unverified-approval hole, the budget comment calls itself an 'also' condition, being over a declared ceiling is a violation rather than the missing declaration Trap 1 carves out, and mcp.go:393 requires the MCP mirror to match - it has no such early return and does deny.",
    "repro": "# sandbox, HEAD binary, isolated DB\ncd $SB/sandbox/v30 && git init -q . && git checkout -b US-012 && git commit -qm seed\nexport BATTEN_DB=\"$PWD/state.db\"\n# batten.yaml: enforcement: enforce; phases close.requires_verdict: ok; verify.gate: qa\n#              gates.qa: {checks: [], verdict: required, evidence: required}\n#              budget: {tokens_per_run: 200000, imputed_usd_per_run: 2.00, on_exceed: block}\nbatten verdict --unit US-012 --gate qa --file v_ok.json      # result=ok, 2 evidence items\nbatten ingest US-012 --transcript \"$PWD/tr/sess.jsonl\"        # -> 1.6M tokens, $3.60\nbatten budget US-012                                          # -> ! OVER on BOTH ceilings\n\n# byte-identical payload used for every hook call below (md5 8233780e08d609a90ce200f94eb87449):\necho '{\"session_id\":\"s12\",\"cwd\":\"<sandbox>\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m US-012\"}}' > payload.json\n\n# A  : gates.qa.checks: []                 -> warning only, NO permissionDecision  (commit proceeds)\nbatten hook PreToolUse < payload.json\n# B1 : gates.qa.checks: ['echo verified']  -> deny (no batten-verified pass)  [gate fires at all]\nbatten hook PreToolUse < payload.json\nbatten check US-012                                           # -> OK (batten-verified)\n# B2 : same checks + batten pass           -> deny ON BUDGET  [the budget path IS reachable]\nbatten hook PreToolUse < payload.json\n# A2 : flip checks back to []              -> warning only again, identical DB state\nbatten hook PreToolUse < payload.json\n# MCP for the SAME run/DB, gate_checks: []  -> commit_denied: true (budget). Hook disagrees.\nbatten mcp  # tools/call batten_verdict_status {\"unit\":\"US-012\"}",
    "evidence": "--- batten budget US-012 (state of the run for EVERY call below) ---\nUS-012  1.6M tokens, $3.60 imputed\n  ! tokens       1.6M / 200.0k  [============]\n      OVER - on_exceed=block\n  ! imputed_usd  $3.60 / $2.00  [============]\n      OVER - on_exceed=block\n\n=== A: gates.qa.checks: []  === EXIT=0\n{\"systemMessage\":\"batten (warning - not blocking): batten: gate \\\"qa\\\" declares no checks, so US-012 was approved on the agent's word alone - nothing was run to verify it. Add gates.qa.checks to make this gate mean something.\",\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"batten warning:\n...\"}}\n  ^ NO permissionDecision key -> the commit proceeds, while both ceilings are blown.\n\n=== B1: gates.qa.checks: ['echo verified'], no batten pass === EXIT=0\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-012 has no batten-verified pass. The gate's checks must be RUN, not asserted....\"}}\n\n=== B2: checks declared + batten-verified pass, SAME payload bytes === EXIT=0\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: US-012 is over budget. budget.on_exceed=block.\n  ! tokens       1560000 of 200000\n  ! imputed_usd  $3.60 of $2.00\n\nTo proceed anyway (recorded): batten override US-012 --reason \\\"...\\\"\"}}\n\n=== A2: checks flipped back to [], IDENTICAL db (batten pass still on record) === EXIT=0\n{\"systemMessage\":\"batten (warning - not blocking): batten: gate \\\"qa\\\" declares no checks, ...\"}\n  ^ warning again. The ONLY variable between a hard budget DENY and a pass-through\n    warning is whether gates.qa.checks is non-empty.\n\n=== MCP batten_verdict_status, same run, same DB, gate_checks: [] ===\n{\"commit_denied\":true,\"deny_reason\":\"US-012 is over budget and budget.on_exceed=block: tokens 1560000.00 of 200000.00; imputed_usd 3.60 of 2.00\",\"gate_checks\":[],\"has_verdict\":true,...}\n  ^ The MCP tool denies; the hook - the actual enforcement point - does not.\n\n--- HEAD code, internal/hooks/hooks.go ---\n337  } else {\n347      return advise(\"PreToolUse\", fmt.Sprintf(\n348          \"batten: gate %q declares no checks, so %s was approved on the agent's word alone - \"...\n351  }\n353  // Budget is also a closing condition: a run that blew its ceiling should not quietly land.\n354  if h.Spec.Budget.OnExceed == \"block\" && h.Spec.Budget.Set() {\n  ^ the else-branch return at 347 is unconditional and precedes 354. mcp.go:393 states the\n    two decisions \"must stay faithful\" to each other; mcp.go has no such early return.",
    "positive_control": "Not an ALLOW-by-silence claim: state A is NOT silent - it emits the 'declares no checks' advisory, which is printed ONLY from hooks.go:347, inside verdictGate, after activeUnit/ActiveRun/LatestVerdict all succeeded. So the hook provably engaged and reached the line immediately above the budget block. Beyond that I ran two positive controls with the byte-identical payload and the same over-budget DB: B1 produced a deny (gate fires), and B2 produced the BUDGET deny specifically - permissionDecision=deny citing tokens 1560000 of 200000 - proving the budget path is live and reachable in this exact sandbox. Toggling gates.qa.checks back to [] (A2) returned to warning-only with the DB otherwise unchanged.",
    "already_fixed_at_head": false,
    "severity_adjusted": "blocker",
    "fix_hint": "internal/hooks/hooks.go:347 - don't return from the else branch: capture it (`pending := advise(...)`) and return it after the budget block at 354-363, so a budget deny still wins; or simply hoist the budget block above the checks test at 328. Add the paired hooks test: no checks + over budget must still DENY."
  },
  {
    "index": 31,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary in my own sandbox. After ingesting one transcript, `batten ingest`, `batten runs` and `batten budget` all report 348.3k tokens (five-bucket), while `batten measure` in the same DB reports 3.0k for the Opus rows whose ledger value is 323,000 (107.7x understatement) and 300 for the Haiku row whose ledger value is 25,300 (84.3x). The dollar column beside it is on the FULL basis: Opus shows $0.96, which is arithmetically only reachable by pricing all three cache buckets (input/output alone at Opus 5/25 per Mtok is $0.035), so the printed line implies ~$320 per Mtok. MeasureByModel at internal/store/store.go:1135 still reads SUM(u.input_tokens+u.output_tokens) at HEAD; nothing in the code or docs makes the omission deliberate, and there is no separate labelled column -- the header word is plain `tokens`. Worse, the SAME command is internally inconsistent: the headroom / code-graph sections below it come from measureByFlag (store.go:976) which averages runs.tokens_spent, i.e. the five-bucket basis, so one `batten measure` invocation prints two `tokens` columns on two different definitions. Not Trap 1: nothing was denied-vs-warned and nothing is reported as NOT MEASURABLE -- this is a measured quantity printed on the wrong basis, which is the defect side of principle #1, not the design side.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v31\" && cd \"$SB/sandbox/v31\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\necho x > README.md && git add -A && git commit -qm init\n\"$BAT\" init\n\"$BAT\" phase TASK-001 build\n# transcript: 2 Opus requests (1000 in / 500 out / 100000 cache_read / 50000 5m / 10000 1h each)\n# plus 1 Haiku request (200 in / 100 out / 20000 cache_read / 5000 5m).\n# NOTE: timestamps MUST be later than the run's started_at or RecordUsage fences the rows out\n# (store.go:720 `if u.TS < fenceFor(u.RunID) { continue }`) and ingest silently reports +0.\ncat > t.jsonl <<'EOF'\n{\"type\":\"assistant\",\"requestId\":\"req-a1\",\"timestamp\":\"2026-07-29T10:00:00Z\",\"message\":{\"id\":\"m1\",\"model\":\"claude-opus-4-5-20260401\",\"usage\":{\"input_tokens\":1000,\"output_tokens\":500,\"cache_read_input_tokens\":100000,\"cache_creation\":{\"ephemeral_5m_input_tokens\":50000,\"ephemeral_1h_input_tokens\":10000}}}}\n{\"type\":\"assistant\",\"requestId\":\"req-a2\",\"timestamp\":\"2026-07-29T10:01:00Z\",\"message\":{\"id\":\"m2\",\"model\":\"claude-opus-4-5-20260401\",\"usage\":{\"input_tokens\":1000,\"output_tokens\":500,\"cache_read_input_tokens\":100000,\"cache_creation\":{\"ephemeral_5m_input_tokens\":50000,\"ephemeral_1h_input_tokens\":10000}}}}\n{\"type\":\"assistant\",\"requestId\":\"req-b1\",\"timestamp\":\"2026-07-29T10:02:00Z\",\"message\":{\"id\":\"m3\",\"model\":\"claude-haiku-4-5-20251001\",\"usage\":{\"input_tokens\":200,\"output_tokens\":100,\"cache_read_input_tokens\":20000,\"cache_creation\":{\"ephemeral_5m_input_tokens\":5000,\"ephemeral_1h_input_tokens\":0}}}}\nEOF\n\"$BAT\" ingest TASK-001 --transcript \"$PWD/t.jsonl\"\n\"$BAT\" runs\n\"$BAT\" budget\n\"$BAT\" measure\npython -c \"import sqlite3;c=sqlite3.connect('state.db');[print(r) for r in c.execute('select model,count(*),sum(input_tokens+output_tokens),sum(input_tokens+output_tokens+cache_write_5m+cache_write_1h+cache_read),round(sum(imputed_usd),4) from usage group by model')]\"\n# --- isolating control: one more request with ZERO cache tokens ---\ncat > ctl.jsonl <<'EOF'\n{\"type\":\"assistant\",\"requestId\":\"req-c1\",\"timestamp\":\"2026-07-29T11:00:00Z\",\"message\":{\"id\":\"m9\",\"model\":\"claude-sonnet-4-5-20250929\",\"usage\":{\"input_tokens\":40000,\"output_tokens\":10000,\"cache_read_input_tokens\":0,\"cache_creation\":{\"ephemeral_5m_input_tokens\":0,\"ephemeral_1h_input_tokens\":0}}}}\nEOF\n\"$BAT\" ingest TASK-001 --transcript \"$PWD/ctl.jsonl\"\n\"$BAT\" measure",
    "evidence": "$ \"$BAT\" ingest TASK-001 --transcript \"$PWD/t.jsonl\"\nTASK-001: +3 requests, 348.3k tokens total, $0.97 imputed\n\n$ \"$BAT\" runs\nUNIT      STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nTASK-001  running  build  —        348.3k  $0.97\n\n$ \"$BAT\" budget\nTASK-001  348.3k tokens, $0.97 imputed\n  · tokens       348.3k / 3.0M  [=...........]\n\n$ \"$BAT\" measure\nspend by model (imputed, not billed):\n  claude-opus-4-5-20260401     2 req, 3.0k tokens, $0.96\n  claude-haiku-4-5-20251001    1 req, 300 tokens, $0.01\n\n$ python -c \"... select model,count(*),sum(in+out),sum(all five),sum(imputed_usd) ...\"\nmodel                        req  measure   ledger   ratio     usd\nclaude-haiku-4-5-20251001      1      300    25300    84.3x  $0.0089\nclaude-opus-4-5-20260401       2     3000   323000   107.7x  $0.96\n\nruns.tokens_spent = [(348300, 0.9689)]\n\nSo: same database, same rows, same instant. ingest/runs/budget say 348,300 tokens;\nmeasure's per-model column sums to 3,300. The $0.96 on the Opus line is the full-basis\nprice (input 2000*$5/Mtok + output 1000*$25/Mtok = $0.035; the remaining $0.925 is\n100k 5m-write @1.25x + 20k 1h-write @2x + 200k cache-read @0.1x), printed next to a\n3.0k token count -> an implied $320 per Mtok.\n\nSource at HEAD, internal/store/store.go:1135 (MeasureByModel):\n  SELECT u.model, COUNT(*), SUM(u.input_tokens+u.output_tokens), SUM(u.imputed_usd)\ncontrasted with internal/store/store.go:738 (RecordUsage, which sets runs.tokens_spent):\n  tokens_spent = (SELECT COALESCE(SUM(input_tokens+output_tokens+cache_write_5m+cache_write_1h+cache_read),0) ...\nand internal/store/store.go:976 (measureByFlag, the OTHER half of the same `batten measure` output):\n  COUNT(*), AVG(tokens_spent), AVG(imputed_usd_spent)\nbatten.schema.json:128 defines the project's token unit as \"all five buckets: input, output,\n5m cache write, 1h cache write, cache read\".",
    "positive_control": "Not an ALLOW claim, so Trap 2's hook control does not apply; instead I ran an ISOLATING control to rule out 'the column is simply dead/zeroed'. I ingested one extra request on claude-sonnet-4-5-20250929 with input=40000, output=10000 and ALL THREE cache buckets = 0. `batten measure` then printed:\n  claude-opus-4-5-20260401     2 req, 3.0k tokens, $0.96\n  claude-sonnet-4-5-20250929   1 req, 50.0k tokens, $0.27\n  claude-haiku-4-5-20251001    1 req, 300 tokens, $0.01\nThe Sonnet row reads 50.0k, exactly its five-bucket ledger value (40000+10000+0+0+0), and $0.27 is exactly 40000*$3/Mtok + 10000*$15/Mtok. So the column is live and correct precisely when there is no cache traffic, and diverges only by the omitted cache buckets. That isolates the cause to the missing three terms rather than to a broken query, a wrong project filter, or an empty table.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/store/store.go:1135 — change SUM(u.input_tokens+u.output_tokens) to SUM(u.input_tokens+u.output_tokens+u.cache_write_5m+u.cache_write_1h+u.cache_read) so MeasureByModel matches runs.tokens_spent (store.go:738) and batten.schema.json's five-bucket definition; add a test asserting SUM over MeasureByModel equals SUM(runs.tokens_spent) for the same rows."
  },
  {
    "index": 32,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: `batten measure` renders the unpriced model claude-opus-4-9-20260901 as `2 req, 57.9k tokens, $0.00` under a header reading \"spend by model\". No marker, no footnote, and no other measure/runs/show/budget surface names the model as unpriced — only `batten ingest` does, once, at ingest time. cmd/batten/main.go:583 formats every row with an unconditional $%.2f and store.ModelSpend (store.go:1127) carries no unpriced flag, so the surface cannot distinguish the two. This is not Trap 1's by-design case: it is the inverse the brief explicitly names a real defect — an unmeasured quantity reported as a hard 0. The codebase itself already applies the opposite rule on a sibling surface (internal/vault/vault.go:215-220, comment: \"never print \\\"$0.00\\\", which would price the unpriceable as free\", enforced by TestMeasuredButUnpricedRunIsNeverValuedAtZero at vault_test.go:252), so measure is inconsistent with batten's own enforced standard, not with an outsider's opinion.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v32\" && cd \"$SB/sandbox/v32\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init && git add -A && git commit -qm init\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" phase TASK-001 build\n# transcript: 2 requests on claude-sonnet-4-5-20250929 (priced) + 2 on claude-opus-4-9-20260901\n# (no prefix in internal/usage/pricing.go rates -> rateFor ok==false). Timestamps MUST be after\n# the run's started_at: store.RecordUsage (store.go:720) fences pre-run rows out silently.\nTS=$(date -u -d \"+2 minutes\" +\"%Y-%m-%dT%H:%M:%SZ\")   # ...TS2/TS3/TS4 at +3/+4/+5 min\ncat > tr/ses-3333.jsonl <<EOF\n{\"type\":\"assistant\",\"requestId\":\"rq-a1\",\"timestamp\":\"$TS\",\"message\":{\"id\":\"m1\",\"model\":\"claude-sonnet-4-5-20250929\",\"usage\":{\"input_tokens\":100000,\"output_tokens\":20000,\"cache_read_input_tokens\":33312}}}\n{\"type\":\"assistant\",\"requestId\":\"rq-a2\",\"timestamp\":\"$TS2\",\"message\":{\"id\":\"m2\",\"model\":\"claude-sonnet-4-5-20250929\",\"usage\":{\"input_tokens\":20000,\"output_tokens\":3000}}}\n{\"type\":\"assistant\",\"requestId\":\"rq-b1\",\"timestamp\":\"$TS3\",\"message\":{\"id\":\"m3\",\"model\":\"claude-opus-4-9-20260901\",\"usage\":{\"input_tokens\":50000,\"output_tokens\":5000}}}\n{\"type\":\"assistant\",\"requestId\":\"rq-b2\",\"timestamp\":\"$TS4\",\"message\":{\"id\":\"m4\",\"model\":\"claude-opus-4-9-20260901\",\"usage\":{\"input_tokens\":2500,\"output_tokens\":415}}}\nEOF\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" ingest TASK-001 --transcript \"$PWD/tr/ses-3333.jsonl\"\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" measure\n# AMBIGUITY CONTROL: one PRICED request of 4 tokens on claude-haiku-4-5 (~$0.000008, genuinely non-zero)\ncat > tr/ses-4444.jsonl <<EOF\n{\"type\":\"assistant\",\"requestId\":\"rq-c1\",\"timestamp\":\"$TS5\",\"message\":{\"id\":\"m5\",\"model\":\"claude-haiku-4-5-20251001\",\"usage\":{\"input_tokens\":3,\"output_tokens\":1}}}\nEOF\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" ingest TASK-001 --transcript \"$PWD/tr/ses-4444.jsonl\"\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" measure\n# the other surfaces, to test the \"something else already discloses it\" defence\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" runs; \"$BAT\" budget TASK-001; \"$BAT\" show TASK-001",
    "evidence": "=== INGEST (honest — names the model once) ===\nTASK-001: +4 requests, 234.2k tokens total, $0.71 imputed\n  unpriced models (counted as $0, tokens still exact): claude-opus-4-9-20260901\n\n=== MEASURE (not honest — the same fact is gone) ===\nspend by model (imputed, not billed):\n  claude-sonnet-4-5-20250929   2 req, 143.0k tokens, $0.71\n  claude-opus-4-9-20260901     2 req, 57.9k tokens, $0.00\n\n=== MEASURE after the ambiguity control was added ===\nspend by model (imputed, not billed):\n  claude-sonnet-4-5-20250929   2 req, 143.0k tokens, $0.71\n  claude-haiku-4-5-20251001    1 req, 4 tokens, $0.00        <- MEASURED, really ~$0.000008\n  claude-opus-4-9-20260901     2 req, 57.9k tokens, $0.00    <- NOT MEASURABLE, no rate exists\n\nThe two rows are byte-identical in the dollar column. Nothing on this surface separates\n\"measured, and it rounded to zero\" from \"we have no rate for this at all\" — which is exactly\nthe distinction principle #1 exists to preserve (ROADMAP.md:195 \"Never invent a number. What\ncannot be measured is reported as unmeasurable.\"; README.md:194 \"A budget that quietly reports\nzero for what it failed to measure is worse than no budget at all, because it will be believed.\").\nThe $0.00 is a hard zero, not a rounding artifact: 57.9k tokens at any Opus-class rate is ~$0.38.\n\n=== NO OTHER SURFACE DISCLOSES IT ===\n$ batten runs\nUNIT      STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nTASK-001  running  build  —        234.2k  $0.72\n\n$ batten budget TASK-001\nTASK-001  234.2k tokens, $0.72 imputed\n  · tokens       234.2k / 3.0M  [............]\n\n$ batten show TASK-001\nTASK-001  run=TASK-001-1785252761341856100-65d87799  status=running  phase=build  base=3ec8f74\nusage: 234.2k tokens, $0.71 imputed (what this would have cost on the API)\n\nOnly `batten ingest` ever names the unpriced model, and only in the moment of ingestion. Every\nread-only reporting surface — measure, runs, budget, show — is silent. `batten measure` is a\nfirst-class shipped command whose stated purpose is routing verification\n(plugin/claude-code/commands/batten-build.md:149 \"`batten measure` breaks spend down by model —\nso the routing is verified, not just intended\"), and $0.00 makes the unpriced model rank as the\nCHEAPEST tier in the fan-out. The routing conclusion is not merely imprecise, it is inverted.\n\n=== HEAD SOURCE, unchanged ===\ncmd/batten/main.go:583\n  fmt.Printf(\"  %-28s %d req, %s tokens, $%.2f\\n\", name, m.Requests, humanTokens(m.Tokens), m.ImputedUSD)\ninternal/store/store.go:1127-1132  type ModelSpend struct { Model; Requests; Tokens; ImputedUSD }  // no unpriced flag\n\nContrast, same repo, same HEAD — internal/vault/vault.go:215-220:\n  // Tokens were measured but no rate was available (e.g. an unpriced model). Report the\n  // count and say the price is unknown — never print \"$0.00\", which would price the\n  // unpriceable as free. Same rule as the per-node \"not priced\" cell.\n  fmt.Fprintf(b, \"**%s tokens** · imputed cost **not priced** (no API rate for this run's model)\\n\\n\", ...)\nand internal/vault/vault_test.go:252 TestMeasuredButUnpricedRunIsNeverValuedAtZero, whose failure\nmessage is literally \"unpriced run rendered as $0.00 — pricing the unpriceable as free\".\nThe rule is batten's own, already enforced by a test on one surface and absent on this one.",
    "positive_control": "Two controls, both passed. (1) POSITIVE-SIGNAL control: the same `batten measure` invocation prints a correct non-zero figure for a priced model in the same table ($0.71 for claude-sonnet-4-5-20250929), so the dollar column is live and populated — the $0.00 is a rendered value, not an unengaged code path. (2) AMBIGUITY control: I ingested one genuinely-priced request (claude-haiku-4-5-20251001, 4 tokens, true cost ~$0.000008) and it renders as `$0.00`, character-for-character the same cell as the 57.9k-token unpriced Opus row. This is the decisive control: it proves the surface collapses \"measured ≈ 0\" and \"unmeasurable\" onto one indistinguishable string, so no reader or agent can recover the difference. Also ran the 'is it disclosed elsewhere?' control: runs / budget / show were each executed against the same ledger and none mentions the unpriced model.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:583 + internal/store/store.go:1127 — add UnpricedRequests to ModelSpend (SUM(CASE WHEN imputed_usd=0 AND input_tokens+output_tokens>0 THEN 1 ELSE 0 END) in MeasureByModel), and when it equals Requests print `UNPRICED (no published rate; tokens exact)` instead of `$%.2f`; mixed rows get `≥$X.XX`. Same rule vault.go:215-220 already applies."
  },
  {
    "index": 33,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: with 81,227 of 211,227 tokens (38%) on an unpriced model, `batten budget` prints `· imputed_usd  $0.39 / $2.00  [==..........]` and `batten runs` prints `$0.39` with no marker that the figure is a floor. This is NOT the by-design 'NOT MEASURABLE' path of Trap 1 — it is its inverse, the case the brief names a real defect: an unmeasured quantity folded in as a hard 0 and then rendered as a measurement. The same output proves the surface HAS the vocabulary and declines to use it (quota_pct on the very next line says NOT MEASURABLE). Trap-1 disclosure check: the only place batten names the unpriced models is the manual `batten ingest` line, and the hook-driven ingest path (internal/hooks/hooks.go:547, `rows, _, err := usage.Parse(...)`) DISCARDS the unknown-models list, so in the normal async-on-Stop flow the incompleteness is disclosed nowhere the user reads. No commit at HEAD touches this (git log --grep=unpriced is empty), so ALREADY-FIXED does not apply.",
    "repro": "W='$SANDBOX'\nBAT='$SANDBOX'\nmkdir -p \"$W\" && cd \"$W\" && git init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$W/state.db\"\n\"$BAT\" init\n# edit batten.yaml: unit.pattern 'US-\\d+'; budget: tokens_per_run 200000, imputed_usd_per_run 2.00, quota_pct_per_run 40.0, on_exceed block\ngit commit -q --allow-empty -m base\nexport BATTEN_DB=\"$W/state.db\"; \"$BAT\" phase US-001 build\nTS=$(date -u +%Y-%m-%dT%H:%M:%SZ); mkdir -p tr\ncat > tr/ses-2222.jsonl <<EOF\n{\"type\":\"assistant\",\"requestId\":\"req-priced-1\",\"timestamp\":\"$TS\",\"message\":{\"id\":\"m1\",\"model\":\"claude-sonnet-4-5-20250929\",\"usage\":{\"input_tokens\":130000,\"output_tokens\":0}}}\n{\"type\":\"assistant\",\"requestId\":\"req-unpriced-1\",\"timestamp\":\"$TS\",\"message\":{\"id\":\"m2\",\"model\":\"claude-quasar-9-1-20260701\",\"usage\":{\"input_tokens\":81227,\"output_tokens\":0}}}\nEOF\nexport BATTEN_DB=\"$W/state.db\"; \"$BAT\" ingest US-001 --transcript \"$W/tr/ses-2222.jsonl\"\nexport BATTEN_DB=\"$W/state.db\"; \"$BAT\" budget US-001\nexport BATTEN_DB=\"$W/state.db\"; \"$BAT\" runs\nexport BATTEN_DB=\"$W/state.db\"; \"$BAT\" show US-001\n\n# CONTROL B — a run whose cost is ENTIRELY unmeasurable\nexport BATTEN_DB=\"$W/state.db\"; \"$BAT\" phase US-002 build\ncat > tr/ses-3333.jsonl <<EOF\n{\"type\":\"assistant\",\"requestId\":\"req-u-a\",\"timestamp\":\"$TS\",\"message\":{\"id\":\"n1\",\"model\":\"claude-quasar-9-1-20260701\",\"usage\":{\"input_tokens\":90000,\"output_tokens\":0}}}\nEOF\nexport BATTEN_DB=\"$W/state.db\"; \"$BAT\" ingest US-002 --transcript \"$W/tr/ses-3333.jsonl\" && \"$BAT\" budget US-002 && \"$BAT\" show US-002\n\n# CONTROL C — the REAL (hook-driven) ingest path, which is what a user actually runs\ngit checkout -q -b US-003\nexport BATTEN_DB=\"$W/state.db\"; \"$BAT\" phase US-003 build\ncat > tr/ses-4444.jsonl <<EOF\n{\"type\":\"assistant\",\"requestId\":\"req-h-1\",\"timestamp\":\"$TS\",\"message\":{\"id\":\"h1\",\"model\":\"claude-sonnet-4-5\",\"usage\":{\"input_tokens\":40000,\"output_tokens\":0}}}\n{\"type\":\"assistant\",\"requestId\":\"req-h-2\",\"timestamp\":\"$TS\",\"message\":{\"id\":\"h2\",\"model\":\"claude-quasar-9-1-20260701\",\"usage\":{\"input_tokens\":60000,\"output_tokens\":0}}}\nEOF\nexport BATTEN_DB=\"$W/state.db\"\necho '{\"session_id\":\"s-hook\",\"cwd\":\"'\"$W\"'\",\"transcript_path\":\"'\"$W\"'/tr/ses-4444.jsonl\",\"hook_event_name\":\"Stop\"}' | \"$BAT\" hook Stop; echo \"HOOK_EXIT=$?\"\nexport BATTEN_DB=\"$W/state.db\"; \"$BAT\" budget US-003",
    "evidence": "=== INGEST (the ONLY place the incompleteness is ever named) ===\nUS-001: +2 requests, 211.2k tokens total, $0.39 imputed\n  unpriced models (counted as $0, tokens still exact): claude-quasar-9-1-20260701\n\n=== BUDGET — 38% of this run's tokens are unpriced; nothing here says so ===\nUS-001  211.2k tokens, $0.39 imputed\n  ! tokens       211.2k / 200.0k  [============]\n      OVER — on_exceed=block\n  · imputed_usd  $0.39 / $2.00  [==..........]\n  · quota_pct    NOT MEASURABLE — install the statusline (`batten statusline --install`)\n        ^^^ CONTROL A: the same rendering loop, two lines apart, DOES have a vocabulary for\n            'I cannot measure this'. It is simply never applied to a partially-priced dollar total.\n\n=== RUNS ===\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-001  running  build  —        211.2k  $0.39\n\n=== SHOW ===\nusage: 211.2k tokens, $0.39 imputed (what this would have cost on the API)\n        ^^^ a flat assertion of measurement over a number that is missing 38% of the run.\n\n=== CONTROL B — a run whose cost is ENTIRELY unknown ===\nUS-002: +1 requests, 90.0k tokens total, $0.00 imputed\n  unpriced models (counted as $0, tokens still exact): claude-quasar-9-1-20260701\nUS-002  90.0k tokens, $0.00 imputed\n  · tokens       90.0k / 200.0k  [=====.......]\n  · imputed_usd  $0.00 / $2.00  [............]\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-002  running  build  —        90.0k   $0.00\nusage: 90.0k tokens, $0.00 imputed (what this would have cost on the API)\n        ^^^ the unpriceable priced as FREE, with an empty progress bar. internal/vault/vault.go:215-220\n            explicitly refuses to do exactly this ('never print \"$0.00\", which would price the\n            unpriceable as free') and prints 'not priced' instead — the CLI/TUI/MCP never got that fix.\n\n=== CONTROL C — the hook-driven path, i.e. what actually happens in a session ===\n--- STOP HOOK — everything it printed: ---\nHOOK_EXIT=0\n--- proof the hook ENGAGED (the ledger moved; 60% of it unpriced): ---\nUS-003  100.0k tokens, $0.12 imputed\n  · tokens       100.0k / 200.0k  [======......]\n  · imputed_usd  $0.12 / $2.00  [............]\n        ^^^ the hook ingested 100k tokens, 60k of them unpriced, and printed NOTHING.\n            internal/hooks/hooks.go:547 -> `rows, _, err := usage.Parse(...)` throws the\n            unknown-models list away. So in the normal flow the disclosure does not exist at all.\n\n=== CODE THAT MAKES THIS A DEFECT AND NOT A DESIGN DECISION ===\ninternal/usage/pricing.go:11-15  \"A model we have no entry for is UNPRICED, not free. rateFor returns\n  ok == false and the caller emits the row with ImputedUSD == 0 plus the model id, SO THE SURFACE CAN\n  SAY 'we could not price this' instead of quietly reporting a cost that is too low.\"\n  -> the obligation is placed on the surface by design; cmdBudget/cmdRuns/cmdShow/TUI/MCP never discharge it.\ninternal/tui/tui.go:349  \"// totals is the exactly-measured pair.\" — asserts exactness of a figure that\n  is a floor whenever any request in the run is unpriced.\ngit log --oneline -30 --grep=unpriced  ->  (empty)  : nothing at HEAD addresses this.",
    "positive_control": "Three, none relying on silence. (A) The imputed_usd line and the quota_pct line are emitted by the SAME loop in cmdBudget: quota_pct correctly renders 'NOT MEASURABLE' because Ceiling.Available is false, proving the surface can and does report unknowns — so the imputed_usd rendering is a choice, not a limitation. (B) A run with 100% unpriced tokens still renders '$0.00 / $2.00 [............]' and 'show' says '$0.00 imputed (what this would have cost on the API)' — the strongest form of the same defect, and the one internal/vault/vault.go:215-220 explicitly guards against. (C) For the hook claim I did not infer engagement from silence: the Stop hook printed nothing AND the ledger moved from 0 to 100.0k tokens / $0.12, proving the hook ran the ingest and simply never surfaced the unpriced portion. n/a for an ALLOW claim — this finding makes none.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:1117 (cmdBudget imputed_usd) and :419 (cmdRuns), plus internal/tui/tui.go:352,579 and internal/mcp/mcp.go ceilingInfo: add store.Ceiling.UnpricedTokens (SELECT SUM(input+output+cw5m+cw1h+cache_read) FROM usage WHERE run_id=? AND imputed_usd=0) and render '≥$0.39 (38% of tokens unpriced)' when it is > 0, mirroring vault.go's 'not priced'."
  },
  {
    "index": 34,
    "verdict": "CONFIRMED",
    "reason": "On the HEAD binary, `batten measure` prints a full 'headroom effect on THIS project's runs' section in a sandbox whose batten.yaml has NO capabilities.compression at all (the stock `batten init` output never emits it), and renders the `unknown` bucket as a peer data row under that 'effect' heading. The '(insufficient - need >=3 runs)' caveat is keyed only on g.Runs < 3, so at 3+ runs the unknown row is presented with zero qualification, indistinguishable from a measured arm. In the mixed case `unknown 5 run(s)` sits directly beside `without code graph 3 run(s)` under 'code graph effect'.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\n# --- A: headroom section printed although capabilities.compression is absent ---\nmkdir -p \"$SB/sandbox/v34\" && cd \"$SB/sandbox/v34\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\necho hello > README.md && git add -A && git commit -qm init\n\"$BAT\" init            # emits capabilities.graph + memory + obsidian; NO capabilities.compression\ngrep -n compression batten.yaml   # -> no match\n# seed 4 finished runs with real usage (transcript timestamps must be >= run started_at,\n# else store.RecordUsage fences them out and tokens_spent stays 0)\nmkdir -p tx\nfor i in 1 2 3 4; do\n  python - \"$i\" <<'PY'\nimport json,sys,datetime\ni=sys.argv[1]\nnow=datetime.datetime.now(datetime.timezone.utc)+datetime.timedelta(minutes=5)\nwith open(\"tx/t%s.jsonl\"%i,\"w\") as f:\n    for j in range(3):\n        ts=(now+datetime.timedelta(seconds=j)).strftime(\"%Y-%m-%dT%H:%M:%SZ\")\n        f.write(json.dumps({\"type\":\"assistant\",\"requestId\":\"req-%s-%d\"%(i,j),\"timestamp\":ts,\n          \"message\":{\"id\":\"m-%s-%d\"%(i,j),\"model\":\"claude-sonnet-4-5-20250929\",\n          \"usage\":{\"input_tokens\":1000+int(i)*100,\"output_tokens\":500,\n                   \"cache_creation_input_tokens\":0,\"cache_read_input_tokens\":2000}}})+\"\\n\")\nPY\n  export BATTEN_DB=\"$PWD/state.db\"\n  \"$BAT\" phase TASK-$i build\n  \"$BAT\" ingest TASK-$i --transcript \"$PWD/tx/t$i.jsonl\"\n  \"$BAT\" close TASK-$i --status failed\ndone\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" measure\n\n# --- B: unknown rendered as a peer row beside a real arm (same recipe in ./b) ---\n#   runs 1-5 seeded with capabilities.graph.provider: none  -> code_graph NULL -> 'unknown'\n#   runs 6-8 seeded with capabilities.graph.provider: graphify -> code_graph 0 -> 'without code graph'\nexport BATTEN_DB=\"$SB/sandbox/v34/b/state.db\"; \"$BAT\" measure\n\n# --- C: the caveat threshold, same recipe in ./c, 2 runs then 3 ---\nexport BATTEN_DB=\"$SB/sandbox/v34/c/state.db\"; \"$BAT\" measure | grep -A2 'headroom effect'",
    "evidence": "$ grep -n compression batten.yaml\n(capabilities.compression ABSENT)\n\n--- A: 4 runs, compression never declared ---\n$ batten measure\nspend by model (imputed, not billed):\n  claude-sonnet-4-5-20250929   12 req, 21.0k tokens, $0.14\n\nheadroom effect on THIS project's runs (imputed, not billed):\n  unknown              4 run(s): 11.2k tokens, $0.04 imputed (mean)\n\ncode graph effect on THIS project's runs (imputed, not billed):\n  without code graph   4 run(s): 11.2k tokens, $0.04 imputed (mean)\n\n--- B: unknown as a peer row beside a real arm ---\n$ batten measure\nheadroom effect on THIS project's runs (imputed, not billed):\n  unknown              8 run(s): 11.8k tokens, $0.04 imputed (mean)\n\ncode graph effect on THIS project's runs (imputed, not billed):\n  unknown              5 run(s): 11.4k tokens, $0.04 imputed (mean)\n  without code graph   3 run(s): 12.6k tokens, $0.04 imputed (mean)\n\n--- C: the caveat is dropped at n>=3, exactly as reported ---\n=== AT 2 UNKNOWN RUNS ===\nheadroom effect on THIS project's runs (imputed, not billed):\n  unknown              2 run(s): 10.5k tokens, $0.03 imputed (mean)  (insufficient - need >=3 runs to compare meaningfully)\n\n=== AT 3 UNKNOWN RUNS ===\nheadroom effect on THIS project's runs (imputed, not billed):\n  unknown              3 run(s): 10.5k tokens, $0.03 imputed (mean)\n\n--- the code behind it (HEAD) ---\ncmd/batten/main.go:588   if groups, err := st.MeasureByHeadroom(sp.Project); err == nil {   // unconditional\ncmd/batten/main.go:591   if groups, err := st.MeasureByCodeGraph(sp.Project); err == nil {\ncmd/batten/main.go:606-620  for i := range groups { ... }   // every group printed, 'unknown' included\ncmd/batten/main.go:609   if g.Runs < 3 { note = \"  (insufficient ...)\" }   // the ONLY qualifier\ninternal/store/store.go:975  CASE headroom WHEN 1 THEN 'with headroom' WHEN 0 THEN 'without headroom' ELSE 'unknown' END\ncontrast, cmd/batten/main.go:359 (cmdPhase) DOES gate on the declaration:\n  if sp.Capabilities.CompressionEnabled() && sp.Capabilities.Compression.Measure { _ = st.SetHeadroom(...) }\nso the writer knows headroom was never probed while the reader prints an 'effect' heading anyway.\nNo test asserts the current rendering: grep for printFlagComparison / 'headroom effect' across *_test.go returns nothing.",
    "positive_control": "n/a - no ALLOW claim; the analogous control was run and PASSED. To prove the renderer engages in my sandbox rather than silently no-opping: (1) at 2 unknown runs the very same code path DID emit '(insufficient - need >=3 runs to compare meaningfully)', so the missing caveat at 3 runs is the threshold firing, not the renderer being dead; (2) in sandbox b the renderer correctly labelled a real arm 'without code graph  3 run(s)' next to 'unknown  5 run(s)', proving bucket labelling works and that 'unknown' is genuinely being rendered as a peer row rather than being the only thing the code can produce. I also verified the seeding path end to end: `batten ingest` first reported '+0 requests, 0 tokens' because store.RecordUsage fences usage older than the run's started_at (store.go:720), and only after re-timestamping did tokens_spent become non-zero - so the runs feeding measure carry real recorded usage.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:588 - gate the headroom section on sp.Capabilities.CompressionEnabled() the way cmdPhase:359 already gates SetHeadroom; and in printFlagComparison (main.go:606-620) lift the 'unknown' group out of the row loop into a trailing footer, e.g. \"N further run(s) excluded: headroom state was not recorded\"."
  },
  {
    "index": 35,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary. With two open runs and none bound to the session, `batten budget US-013` prints `quota_pct NOT MEASURABLE - install the statusline (`batten statusline --install`)` while `batten doctor` in the same repo, same DB, prints `statusline installed - the quota ceiling is enforced` and the statusline itself prints `5h 12%` / `5h 31%`. The NOT MEASURABLE is correct and by design (Trap 1); the REMEDY names a cause that is provably false. Causal proof: closing the OTHER run - touching nothing about the statusline - flips the same command to `quota_pct 0.0% / 15.0% of the rolling 5h window`. Root cause is statusline.pick() returning (nil,false) for an ambiguous repo (2+ open runs, none ours), so no session is adopted and no baseline is set; store.QuotaBurned then collapses that into a bare ok==false, and cmd/batten/main.go:1111 hardcodes the install hint for every cause.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nD=\"$SB/sandbox/v35\"\nmkdir -p \"$D\" && cd \"$D\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$D/state.db\"\n\"$BAT\" init\n# declare the quota ceiling in batten.yaml (budget: quota_pct_per_run: 15.0)\nsed -i \"s/  tokens_per_run: 3000000/  tokens_per_run: 3000000\\n  quota_pct_per_run: 15.0/\" batten.yaml\necho hi > README.md && git add -A && git commit -qm init\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" statusline --install\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" doctor | tail -1\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" phase US-012 build\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" phase US-013 build\nexport BATTEN_DB=\"$D/state.db\"; echo '{\"session_id\":\"sq\",\"cwd\":\"'\"$PWD\"'\",\"model\":{\"id\":\"claude-opus-4-5\"},\"rate_limits\":{\"five_hour\":{\"used_percentage\":12.0,\"resets_at\":1785240000},\"seven_day\":{\"used_percentage\":33.5}}}' | \"$BAT\" statusline\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" budget US-013\nexport BATTEN_DB=\"$D/state.db\"; echo '{\"session_id\":\"sq\",\"cwd\":\"'\"$PWD\"'\",\"model\":{\"id\":\"claude-opus-4-5\"},\"rate_limits\":{\"five_hour\":{\"used_percentage\":31.0,\"resets_at\":1785240000},\"seven_day\":{\"used_percentage\":34.6}}}' | \"$BAT\" statusline\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" budget US-013\n# causal control - close the OTHER run, change nothing about the statusline\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" close US-012 --status failed\nexport BATTEN_DB=\"$D/state.db\"; echo '{\"session_id\":\"sq\",\"cwd\":\"'\"$PWD\"'\",\"model\":{\"id\":\"claude-opus-4-5\"},\"rate_limits\":{\"five_hour\":{\"used_percentage\":31.0,\"resets_at\":1785240000},\"seven_day\":{\"used_percentage\":34.6}}}' | \"$BAT\" statusline\nexport BATTEN_DB=\"$D/state.db\"; \"$BAT\" budget US-013\n# independent control - fresh DB, identical repo/install, ONE open run\nexport BATTEN_DB=\"$D/control.db\"; \"$BAT\" phase US-013 build\nexport BATTEN_DB=\"$D/control.db\"; echo '{\"session_id\":\"sq\",...\"five_hour\":{\"used_percentage\":12.0}...}' | \"$BAT\" statusline\nexport BATTEN_DB=\"$D/control.db\"; echo '{\"session_id\":\"sq\",...\"five_hour\":{\"used_percentage\":31.0}...}' | \"$BAT\" statusline\nexport BATTEN_DB=\"$D/control.db\"; \"$BAT\" budget US-013",
    "evidence": "$ batten statusline --install\nstatusline installed in .claude/settings.json — it now samples your subscription quota\n\n$ batten doctor   (last line)\n✓ statusline installed — the quota ceiling is enforced\n\n$ batten runs\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nUS-013  running  build  —        —       —\nUS-012  running  build  —        —       —\n\n$ echo '{...\"five_hour\":{\"used_percentage\":12.0}...}' | batten statusline\nbatten · 5h 12% · 7d 34%\n$ batten budget US-013\nUS-013  0 tokens, $0.00 imputed\n  · tokens       0 / 3.0M  [............]\n  · quota_pct    NOT MEASURABLE — install the statusline (`batten statusline --install`)\n\n$ echo '{...\"five_hour\":{\"used_percentage\":31.0}...}' | batten statusline\nbatten · 5h 31% · 7d 35%\n$ batten budget US-013\nUS-013  0 tokens, $0.00 imputed\n  · tokens       0 / 3.0M  [............]\n  · quota_pct    NOT MEASURABLE — install the statusline (`batten statusline --install`)\n\n--- causal control: close the OTHER run, statusline untouched ---\n$ batten close US-012 --status failed\nUS-012 closed (failed). Write-set claims released — files it held are free again.\n$ echo '{...\"five_hour\":{\"used_percentage\":31.0}...}' | batten statusline\nbatten US-013 build · no verdict: commit DENIED · quota 0.0/15% · 5h 31% · 7d 35%\n$ batten budget US-013\nUS-013  0 tokens, $0.00 imputed\n  · tokens       0 / 3.0M  [............]\n  · quota_pct    0.0% / 15.0% of the rolling 5h window  [............]\n\n--- independent control: fresh DB, ONE open run, same install, same payloads ---\n$ batten budget US-013   (after the 12.0 then 31.0 samples)\nUS-013  0 tokens, $0.00 imputed\n  · tokens       0 / 3.0M  [............]\n  ! quota_pct    19.0% / 15.0% of the rolling 5h window  [============]\n      OVER — on_exceed=warn\n\n--- code at HEAD (b7aac14) ---\ncmd/batten/main.go:1111\n\tfmt.Printf(\"  %s %-12s NOT MEASURABLE — install the statusline (`batten statusline --install`)\\n\", mark, c.Kind)\ninternal/store/store.go:833-846  QuotaBurned returns a bare (0,false,nil) for four distinct causes:\n\tr.QuotaStart5h == nil || r.SessionID == \"\"   |   no snapshot for the session   |   d < 0 (window rolled over)\ninternal/statusline/statusline.go:219-244  pick(): \"an ambiguous repo (several open runs, none ours) yields no run rather than a guess\" -> returns nil,false -> attach() never adopts the session and never sets the baseline\ninternal/hooks/hooks.go:378  same wrong remedy in the hook path: \"not measurable (install `batten statusline`)\"",
    "positive_control": "Two controls, both required because the claim is 'this remedy is false', not 'the hook was silent'. (1) CAUSAL: in the very same DB and repo, with the statusline install untouched, closing the unrelated run US-012 flipped `batten budget US-013` from `NOT MEASURABLE — install the statusline` to `quota_pct 0.0% / 15.0% of the rolling 5h window` — proving the cause was the second open run, not a missing statusline. (2) INDEPENDENT: a fresh DB in the identical repo with ONE open run and the identical two payloads produced `! quota_pct 19.0% / 15.0% ... OVER — on_exceed=warn`, proving the sensor→budget loop is fully live in this sandbox. `batten doctor` independently reports `✓ statusline installed — the quota ceiling is enforced` in the exact state where `batten budget` tells the user to install the statusline, so the two commands contradict each other.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:1111 (and internal/hooks/hooks.go:378): make store.QuotaBurned return a reason enum (not-installed / no-baseline / ambiguous-run / rolled-over) beside ok, and print that reason instead of the hardcoded install hint."
  },
  {
    "index": 36,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: a run holding 42,600 measured tokens renders in the SessionStart resume brief as \"0.0M tokens\" and a 68,915-token run as \"0.1M tokens\", while `batten runs` and `batten budget`, reading the SAME db rows, render them correctly as 42.6k and 68.9k. The brief also never prints the declared ceiling, which `batten budget` does show (42.6k / 3.0M). This is a measured quantity displayed as an apparent zero — not the deliberate NOT-MEASURABLE behaviour of principle #1, and not the fail-open/advisory design decision of Trap 1. The offending format string is still at HEAD (internal/hooks/hooks.go:576-578).",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v36\" && cd \"$SB/sandbox/v36\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$SB/sandbox/v36/state.db\"\necho x > README.md && git add -A && git commit -q -m init\n\"$BAT\" init\n\"$BAT\" phase TASK-011 build\nmkdir -p tr\n# NOTE: the usage rows are time-fenced to the run's started_at, so the transcript\n# timestamp must be INSIDE the run window or RecordUsage silently drops the row.\nTS=$(date -u -d '+1 hour' +%Y-%m-%dT%H:%M:%SZ)\ncat > tr/session.jsonl <<EOF\n{\"type\":\"assistant\",\"requestId\":\"req-1\",\"timestamp\":\"$TS\",\"message\":{\"id\":\"m1\",\"model\":\"claude-sonnet-4-5-20250929\",\"usage\":{\"input_tokens\":2600,\"output_tokens\":40000,\"cache_creation_input_tokens\":0,\"cache_read_input_tokens\":0}}}\nEOF\nexport BATTEN_DB=\"$SB/sandbox/v36/state.db\"\n\"$BAT\" ingest TASK-011 --transcript tr/session.jsonl\n# second run, 68,915 tokens\n\"$BAT\" phase TASK-012 build\nTS2=$(date -u -d '+2 hour' +%Y-%m-%dT%H:%M:%SZ)\ncat > tr/s2.jsonl <<EOF\n{\"type\":\"assistant\",\"requestId\":\"r2\",\"timestamp\":\"$TS2\",\"message\":{\"id\":\"m2\",\"model\":\"claude-sonnet-4-5-20250929\",\"usage\":{\"input_tokens\":8915,\"output_tokens\":60000,\"cache_creation_input_tokens\":0,\"cache_read_input_tokens\":0}}}\nEOF\n\"$BAT\" ingest TASK-012 --transcript tr/s2.jsonl\n\"$BAT\" runs\n\"$BAT\" budget\n# cwd MUST be the Windows-style path; a Git-Bash POSIX cwd makes spec.LoadFrom fail\n# and the hook exits 0 SILENTLY (Trap 2).\necho '{\"session_id\":\"s11\",\"cwd\":\"'\"$SB\"'/sandbox/v36\",\"hook_event_name\":\"SessionStart\",\"source\":\"startup\"}' | \"$BAT\" hook SessionStart; echo \"EXIT=$?\"",
    "evidence": "$ \"$BAT\" ingest TASK-011 --transcript tr/session.jsonl\nTASK-011: +1 requests, 42.6k tokens total, $0.61 imputed\n\n$ \"$BAT\" ingest TASK-012 --transcript tr/s2.jsonl\nTASK-012: +1 requests, 68.9k tokens total, $0.93 imputed\n\n$ \"$BAT\" runs\nUNIT      STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nTASK-012  running  build  —        68.9k   $0.93\nTASK-011  running  build  —        42.6k   $0.61\n\n$ \"$BAT\" budget\nTASK-012  68.9k tokens, $0.93 imputed\n  · tokens       68.9k / 3.0M  [............]\nTASK-011  42.6k tokens, $0.61 imputed\n  · tokens       42.6k / 3.0M  [............]\n\n$ echo '{...\"hook_event_name\":\"SessionStart\",\"source\":\"startup\"}' | \"$BAT\" hook SessionStart\n{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"## batten — v36 (TASK)\\n\\n- **TASK-012** — phase `build`, status `running`, 0.1M tokens / $0.93 imputed, **no verdict yet** — the close gate will deny a commit\\n- **TASK-011** — phase `build`, status `running`, 0.0M tokens / $0.61 imputed, **no verdict yet** — the close gate will deny a commit\\n\\n→ this session is working **TASK-011**.\\n\"}}\nEXIT=0\n\nSource still present at HEAD (internal/hooks/hooks.go:575-579):\n  fmt.Fprintf(&b, \"- **%s** — phase `%s`, status `%s`\", r.UnitID, r.Phase, r.Status)\n  if r.TokensSpent > 0 {\n      fmt.Fprintf(&b, \", %.1fM tokens / $%.2f imputed\",\n          float64(r.TokensSpent)/1e6, r.ImputedUSD)\n  }\nNo ceiling is ever appended: sessionStart never calls Store.Budget / h.Spec.Budget.",
    "positive_control": "Two controls. (1) HOOK-ENGAGES control, required by Trap 2: the first invocation with a Git-Bash POSIX cwd (/tmp/claude/.../sandbox/v36) printed NOTHING and exited 0 — proof that silence here means loadForHook's spec.LoadFrom failed, not 'no runs'. Re-running the identical payload with the Windows-style cwd made the hook emit the full banner, so the '0.0M' string is genuinely rendered output, not an artifact of a dead hook. (2) MEASURABILITY control: the same db, same run rows, read through `batten runs` and `batten budget` (both of which go through humanTokens()) print '42.6k' and '42.6k / 3.0M'. So the token count IS measured and IS stored — '0.0M' is a rendering fault in the hook, not the deliberate NOT-MEASURABLE / fail-open behaviour named in Trap 1.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "internal/hooks/hooks.go:577 — replace the hand-rolled `%.1fM` with the shared humanTokens() renderer (currently duplicated, unexported, in cmd/batten/main.go:432 and internal/canvas/canvas.go:224 — promote one to a shared package), and append the ceilings from h.Store.Budget(r.RunID, h.Spec.Budget...) so the brief shows '42.6k / 3.0M tokens'."
  },
  {
    "index": 37,
    "verdict": "REFUTED",
    "reason": "REFUTED AS DUPLICATE of finding 17 (already CONFIRMED). The facts do reproduce on the HEAD binary — `batten --help` prints 16 commands and greps zero for check/close/measure/hook-debug — but this is the identical defect at the identical site (cmd/batten/main.go printUsage, lines 99-118, out of sync with the dispatch switch at lines 49-91), with the identical one-line-scope fix. The `measure` omission is not incremental: v17's own CONFIRMED evidence already enumerates `measure`, `hook-debug` and `version` as likewise dispatched and likewise unlisted, and its fix_hint already says \"(and, optionally, `measure`)\". The deny-message angle (\"Run: batten check US-001\") is also already in finding 17's title and quoted verbatim in its evidence. The `wrong-docs` kind label is additionally inaccurate: no statement in the help text is false — every line it prints is correct — the text merely omits entries, which is exactly what 17 filed as `missing`. Fixing 17 fixes 37 completely; there is no residue.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v37\" && cd \"$SB/sandbox/v37\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" --help; echo \"EXIT=$?\"\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" --help 2>&1 | grep -i -c 'check\\|close\\|measure\\|hook-debug'; echo \"GREPEXIT=$?\"\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" measure; echo \"MEASURE_EXIT=$?\"\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" bogus-subcommand >/dev/null 2>&1; echo \"BOGUS_EXIT=$?\"\n# and the source at HEAD\ncd $REPO && grep -n 'case \"' cmd/batten/main.go | head -30",
    "evidence": "$ batten --help\nbatten — procedural memory as data\n\n  batten init [--from <prose-doc>]   generate batten.yaml\n  batten doctor                      validate spec, report live capabilities\n  batten hook <event>                hook entrypoint (stdin JSON -> stdout JSON)\n  batten phase <unit> <phase>        advance a unit to a phase (records the anchor SHA)\n  batten claim <agent-id> <file>...  declare a subagent's write-set\n  batten verdict [--file v.json]     record a verdict envelope\n  batten runs                        list runs\n  batten show <unit>                 run detail\n  batten canvas <unit> [--out p]     emit the run DAG as JSON Canvas\n  batten budget [<unit>]             governor status (tokens / imputed $ / quota)\n  batten override <unit> --reason    audited escape from the gate\n  batten tui                         review runs in a terminal UI\n  batten mcp                         MCP server (exposes the run graph to the agent)\n  batten statusline [--install]      status line + the only subscription-quota sensor\n  batten ingest <unit> --transcript  price a transcript's tokens into the run ledger\nEXIT=2\n\n$ batten --help 2>&1 | grep -i -c 'check\\|close\\|measure\\|hook-debug'\n0\nGREPEXIT=1                 <-- so the raw facts of f37 are TRUE at HEAD\n\n$ batten measure\nMEASURE_EXIT=0             <-- dispatched (silent, exit 0), not the default branch\n\n$ batten bogus-subcommand\nBOGUS_EXIT=2               <-- default branch control: unknown verbs exit 2 + usage\n\n--- source at HEAD, cmd/batten/main.go: ONE site, ONE fix ---\n 79:\tcase \"measure\":\n 80:\t\terr = cmdMeasure()\n 81:\tcase \"close\":\n 82:\t\terr = cmdClose(os.Args[2:])\n 83:\tcase \"check\":\n 84:\t\terr = cmdCheck(os.Args[2:])\n 85:\tcase \"hook-debug\":\n 86:\t\terr = cmdHookDebug(os.Args[2:])\n 87:\tcase \"version\", \"--version\", \"-v\":\n 89:\tdefault:\n 90:\t\tprintUsage()\n 99: func printUsage() {\n100:\t fmt.Fprint(os.Stderr, `batten — procedural memory as data\n...  (16 entries, none of them measure/close/check/hook-debug/version)\n118: }\n\n--- prior verdict this duplicates: results/v17.json ---\n\"verdict\": \"CONFIRMED\" ... \"`measure`, `hook-debug` and `version` are likewise dispatched and likewise unlisted.\"\n\"fix_hint\": \"cmd/batten/main.go:116 (end of printUsage's list) — add two lines ... (and, optionally, `measure`).\"\n\nOverlap audit f37 vs f17:\n  same binary surface (`batten --help`)          -> yes\n  same source site (printUsage 99-118)           -> yes\n  same root cause (list not synced to switch)    -> yes\n  same fix (append lines to one string literal)  -> yes\n  `measure` claim new?                           -> NO, in v17 evidence + fix_hint\n  deny-message claim new?                        -> NO, in f17 title + v17 evidence\n  `wrong-docs` framing new?                      -> mislabel; nothing printed is false,\n                                                    it is an omission = f17's `missing`",
    "positive_control": "n/a for an ALLOW claim (no hook/gate assertion in this finding), but I ran the dispatch control the brief's spirit requires, because 'the help text lacks X' is only a defect if X really is a command. Control: `batten bogus-subcommand` -> exit 2 and prints the usage block (the `default:` branch). `batten measure` -> exit 0 and prints nothing, i.e. it did NOT take the default branch, so cmdMeasure genuinely ran. Different exit codes prove `measure` is a real dispatched verb rather than an unknown one, so the omission from printUsage is real — it is simply the same omission finding 17 already owns. Also verified `batten --help` is the only help surface: `help`, `--help` and any unknown verb all funnel into the same printUsage()/exit 2, so no second surface could be listing these commands.",
    "already_fixed_at_head": false,
    "severity_adjusted": "not-a-defect",
    "fix_hint": "",
    "notes": "Explicitly NOT refuted on Trap 1 or Trap 2 grounds — the underlying omission is genuine and I reproduced it verbatim on the HEAD binary. It is refuted purely as a DUPLICATE: finding 17 covers the same printUsage/dispatch divergence, was already CONFIRMED, already names `measure` and `hook-debug` in both its evidence and its fix_hint, and already quotes the `Run: batten check <unit>` deny (internal/hooks/hooks.go:333). Counting 37 separately would double-count one string literal as two defects. The only thing 37 contributes is a wrong `kind`: it is filed as `wrong-docs`, but every line `--help` prints is accurate — the defect is omission, which 17 correctly filed as `missing`. Recommendation: fold 37 into 17 and, when fixing, add all four unlisted verbs (`check`, `close`, `measure`, and either list or deliberately mark `hook-debug` internal) so no third duplicate can be filed against the same literal. Isolation: BATTEN_DB was re-exported to sandbox/v37/state.db in every Bash call; no file in the repo under test was modified; ~/.batten/ was never written; no hook-debug tap was enabled."
  },
  {
    "index": 38,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary (batten 0.1.0) in a clean sandbox: with US-005 in build (phase node + subagent, canvas 4 nodes/1 edge), the single command `batten phase US-006 build` collapsed `batten canvas US-005` to 1 node / 0 edges — a bare header — and emptied the `batten tui` detail tree to the literal string \"no nodes recorded yet\". Read-only SQLite says the row was MOVED, not deleted: `p-build`'s run_id was rewritten from US-005's run to US-006's run (INSERT OR REPLACE on a global `node_id TEXT PRIMARY KEY`, store.go:84/404, with the id built as `\"p-\" + phaseID` at cmd/batten/main.go:368). DISTINCTNESS CALL: the root cause is the SAME defect as findings 5 and 14 — one fix at main.go:368 removes all three symptoms, so this is a third surface, not a third bug, and should be deduped for scoring. BUT it is not a pure duplicate: it exposes one genuinely new, separately-fixable sub-defect in the renderers. The store still holds US-005's subagent row `n-a1` (status ok) AND its spawn edge, yet BOTH renderers drop it silently — canvas.go:117/122 and tui.go:440/475 bucket children by `parent[nodeID]` and their explicit orphan safety net only rescues the `\"\"` bucket, never a dangling `p-build` parent. So the canvas/TUI lose strictly MORE than the store lost (canvas 4->1 nodes; TUI 1 node + 1 edge in hand -> \"no nodes recorded yet\"), and the comments that promise the opposite (canvas.go:121 \"so nothing is silently dropped\"; tui.go:473-474 \"Showing them as orphans is better than dropping them\") are falsified. Not by design: batten explicitly supports concurrent units in the same phase (the README multi-session story, the cross-session write-set guard), and this is silent data/observability loss, not a fail-open advisory.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"; WD=\"$SB/sandbox/v38/repo\"\nmkdir -p \"$WD\" && cd \"$WD\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\necho hello > README.md && git add -A && git commit -qm init\nexport BATTEN_DB=\"$WD/state.db\"\n\"$BAT\" init            # then edit sandbox batten.yaml: unit.pattern -> 'US-\\d+'\nW=$(pwd -W)\nexport BATTEN_DB=\"$WD/state.db\"\n\"$BAT\" phase US-005 build\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"a1\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStop\",\"agent_id\":\"a1\",\"agent_type\":\"backend\",\"last_assistant_message\":\"done, 3 tests pass\"}' | \"$BAT\" hook SubagentStop\n\"$BAT\" show US-005 ; \"$BAT\" canvas US-005 --out before.canvas\npython dump.py \"$WD/state.db\"          # read-only sqlite3: SELECT node_id,run_id,kind,label,status FROM nodes\n\"$BAT\" phase US-006 build              # <-- the only thing in between\n\"$BAT\" show US-005 ; \"$BAT\" canvas US-005 --out after.canvas\npython dump.py \"$WD/state.db\"\ncat after.canvas\n\n# --- TUI surface (batten tui needs a TTY; it hung on a piped stdin, EXIT=124) ---\n# so the same Bubbletea model was driven headlessly against the REAL DBs the HEAD\n# binary wrote, using a COPY of the repo sources in the sandbox (repo untouched):\ncp -r <repo>/cmd <repo>/internal <repo>/go.mod <repo>/go.sum \"$SB/sandbox/v38/srccopy/\"\n# + sandbox-only test internal/tui/v38_render_test.go: spec.LoadFrom($WD), store.Open($V38_DB),\n#   New(sp,st); m.Init(); m.Update(tea.WindowSizeMsg{120,40}); select US-005; print m.detailTree(m.det)\nexport BATTEN_DB=\"$WD/tui.db\"\n\"$BAT\" phase US-005 build ; <SubagentStart a1> ; <SubagentStop a1> ; cp tui.db tui-before.db\n\"$BAT\" phase US-006 build ; cp tui.db tui-after.db\ncd \"$SB/sandbox/v38/srccopy\"\nV38_REPO=\"$WD\" V38_UNIT=US-005 V38_DB=\"$WD/tui-before.db\" go test ./internal/tui/ -run TestV38TuiAgainstRealDB -v -count=1\nV38_REPO=\"$WD\" V38_UNIT=US-005 V38_DB=\"$WD/tui-after.db\"  go test ./internal/tui/ -run TestV38TuiAgainstRealDB -v -count=1\n\n# --- negative control (control.db): US-006 enters a NON-colliding phase name ---\nexport BATTEN_DB=\"$WD/control.db\"\n\"$BAT\" phase US-005 build ; <SubagentStart a1> ; <SubagentStop a1>\n\"$BAT\" show US-005 ; \"$BAT\" canvas US-005 --out ctl-before.canvas\n\"$BAT\" phase US-006 verify\n\"$BAT\" show US-005 ; \"$BAT\" canvas US-005 --out ctl-after.canvas",
    "evidence": "$ batten version\nbatten 0.1.0\n\n===== BEFORE: show =====\nUS-005  run=US-005-1785258688952183800-096893c0  status=running  phase=build  base=35686f3\n  [phase] build                    running \n  [subagent] backend                  ok      \n===== BEFORE: canvas =====\nwrote before.canvas (4 nodes, 1 edges)\n===== BEFORE: SQLite (read-only, independent connection) =====\n-- nodes --\n('p-build', 'US-005-1785258688952183800-096893c0', 'phase', 'build', 'running', '', 1785258688)\n('n-a1',    'US-005-1785258688952183800-096893c0', 'subagent', 'backend', 'ok', 'a1', 1785258689)\n-- edges --\n('US-005-1785258688952183800-096893c0', 'p-build', 'n-a1', 'spawn')\n\n===== THE ONLY THING IN BETWEEN =====\nanchor: US-006 base SHA = 35686f3\nUS-006 -> phase build\n\n===== AFTER: show US-005 =====\nUS-005  run=US-005-1785258688952183800-096893c0  status=running  phase=build  base=35686f3\n  [subagent] backend                  ok      \n===== AFTER: canvas US-005 =====\nwrote after.canvas (1 nodes, 0 edges)\n===== AFTER: SQLite -- the ROW MOVED, it was not deleted =====\n-- runs --\n('US-005-1785258688952183800-096893c0', 'US-005', 'build', 'running')\n('US-006-1785258705885345400-6df998e3', 'US-006', 'build', 'running')\n-- nodes --\n('n-a1',    'US-005-1785258688952183800-096893c0', 'subagent', 'backend', 'ok', 'a1', 1785258689)\n('p-build', 'US-006-1785258705885345400-6df998e3', 'phase', 'build', 'running', '', 1785258705)\n-- edges --\n('US-005-1785258688952183800-096893c0', 'p-build', 'n-a1', 'spawn')\n  -> exactly one p-build row exists in the file; its run_id was rewritten from\n     US-005's run to US-006's and started_at overwritten (…688 -> …705).\n     US-005 KEEPS n-a1 and KEEPS the spawn edge.\n\n===== after.canvas, verbatim and complete =====\n{\n\t\"nodes\": [\n\t\t{\n\t\t\t\"id\": \"header\",\n\t\t\t\"type\": \"text\",\n\t\t\t\"x\": -420, \"y\": 0, \"width\": 300, \"height\": 180,\n\t\t\t\"text\": \"# US-005\\n\\nrun `US-005-1785258688952183800-096893c0`\\nstatus `running`\\nphase `build`\\nbase `35686f3`\"\n\t\t}\n\t],\n\t\"edges\": []\n}\n  (before.canvas had g-p-build + p-build + n-a1 + header and edge e0 p-build->n-a1)\n\n===== TUI SURFACE — same Bubbletea model, real DBs written by the HEAD binary =====\n############ BEFORE ############\n=== TUI detail tree for US-005 (db=.../tui-before.db) ===\n  |◐ build|\n  | └─ ✓ backend|\n=== store rows the TUI was given: 2 nodes, 1 edges ===\n  node p-build    kind=phase     label=build      status=running\n  node n-a1       kind=subagent  label=backend    status=ok\n  edge p-build -> n-a1 (spawn)\n--- PASS\n############ AFTER ############\n=== TUI detail tree for US-005 (db=.../tui-after.db) ===\n  |no nodes recorded yet|\n=== store rows the TUI was given: 1 nodes, 1 edges ===\n  node n-a1       kind=subagent  label=backend    status=ok\n  edge p-build -> n-a1 (spawn)\n--- PASS\n  -> DECISIVE for the renderer sub-defect: the TUI was HANDED 1 node and 1 edge\n     and printed \"no nodes recorded yet\". `batten show US-005` still prints\n     \"[subagent] backend ok\" from the same rows, so this is the renderer\n     dropping live data, not the store having lost it.\n\n===== HEAD source (unchanged at HEAD) =====\ncmd/batten/main.go:367-369   NodeID: \"p-\" + phaseID, RunID: run.RunID, Kind: \"phase\", ...\ninternal/store/store.go:84   node_id    TEXT PRIMARY KEY,\ninternal/store/store.go:404  INSERT OR REPLACE INTO nodes ...\ninternal/hooks/hooks.go:494  _ = h.Store.AddEdge(run.RunID, \"p-\"+run.Phase, nodeID, \"spawn\")\ninternal/canvas/canvas.go:117  byPhase[parent[n.NodeID]] = append(...)\ninternal/canvas/canvas.go:121-124  // \"...so nothing is silently dropped\"  if orphans := byPhase[\"\"]; ...\ninternal/tui/tui.go:440-441    p := parent[n.NodeID]; kids[p] = append(kids[p], n)\ninternal/tui/tui.go:473-480    // \"Showing them as orphans is better than dropping them\"  if orph := kids[\"\"]; ...\n  Both safety nets key on \"\" only, so a child whose named parent is missing is dropped.",
    "positive_control": "No ALLOW/deny claim is involved, so the hook-silence rule does not apply; three controls were run instead.\n(1) NEGATIVE CONTROL isolating the cause (control.db) — identical sequence except US-006 enters a NON-colliding phase name (`verify`). US-005's canvas was UNCHANGED:\n    BEFORE: wrote ctl-before.canvas (4 nodes, 1 edges)\n    batten phase US-006 verify\n    AFTER : wrote ctl-after.canvas  (4 nodes, 1 edges)   and show US-005 still lists [phase] build + [subagent] backend\n    SQLite: ('p-build', US-005-...-82880593, 'phase','build') AND ('p-verify', US-006-...-5f86f878, 'phase','verify') both present.\n  This proves (a) the canvas exporter is live and does render phase+subagent nodes, (b) the collapse is caused specifically by the phase-name-derived primary key colliding, not by 'a second run exists' or by `batten canvas` being broken.\n(2) HOOK-ENGAGED CONTROL — `batten hook SubagentStart` prints nothing and exits 0 either way, so silence proves nothing; its effect was verified in SQLite instead: the n-a1 row and the p-build->n-a1 spawn edge exist in the BEFORE dump, so the hook demonstrably engaged (a Windows-style cwd from `pwd -W` was required).\n(3) STORE-vs-RENDERER CONTROL demanded by the angle — every store claim is read with an independent read-only sqlite3 connection (`file:...?mode=ro`), never from batten's own output, and the TUI harness prints the rows it was handed next to what it drew. That separates the two failures cleanly: the p-build row MOVED (store-level loss, shared with findings 5/14), while n-a1 is RENDERED AWAY (renderer-level loss, new).\n(4) `batten tui` itself could not be driven from a pipe — `printf 'q' | batten tui` emitted only altscreen escapes and hung (EXIT=124, timeout). The headless harness above is a COPY of the HEAD sources in the sandbox; the repo under test was not modified.",
    "already_fixed_at_head": false,
    "severity_adjusted": "blocker",
    "fix_hint": "Root cause (dedupe with 5/14, one fix): cmd/batten/main.go:368 — scope the id to the run, `NodeID: run.RunID + \":p-\" + phaseID`, in lockstep with internal/hooks/hooks.go:494 which rebuilds `\"p-\"+run.Phase` for the spawn edge; ideally also make nodes PK `(run_id, node_id)` at internal/store/store.go:84 and scope FinishNode (store.go:413) / NodeByAgent (store.go:419) by run_id. Separate, still-needed renderer fix: internal/canvas/canvas.go:122 and internal/tui/tui.go:475 must route any child whose parent id is not in the phase set — not only `parent == \"\"` — into the 'unattributed' column, e.g. build a set of present phase ids and treat a dangling parent as orphaned."
  },
  {
    "index": 39,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary. Once another unit entered phase `build` (re-parenting the globally-unique node id `p-build` to the new run), `batten show US-005` still listed all four subagents with their write-sets, while `batten canvas US-005` wrote a canvas containing NONE of them — first 1 node (header only), then 4 nodes (g-p-verify, p-verify, verdict, header) after US-005 advanced to verify, exactly as reported. The drop is silent on every surface of that command: stderr was 0 bytes, exit code 0, and the only stdout is `wrote <path> (4 nodes, 0 edges)` — a count with no baseline, no warning line, no 'unattributed' column. The sibling artifact written by the SAME invocation contradicts it: vault run note US-005.md renders a Fan-out table with all four agents and 7 files touched, then embeds `![[US-005.canvas]]` which contains none of them. Cause is as reported: subagents keep a `spawn` edge to `p-build`, so parent[n]==\"p-build\" (non-empty), they land in byPhase[\"p-build\"] — a bucket the render loop never iterates because p-build is no longer in Nodes(run1) — and the orphan safety net at canvas.go:122 only catches byPhase[\"\"]. Not by-design: the code's own comment at canvas.go:121 promises 'nothing is silently dropped', and this is the fail-silent-with-plausible-output mode, not the fail-open-with-a-warning mode the design sanctions.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v39\" && cd \"$SB/sandbox/v39\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\necho hello > README.md && git add -A && git commit -q -m init\n\"$BAT\" init            # then edit batten.yaml: unit.pattern: 'US-\\d{3}'\nW=$(pwd -W)            # NOTE: hook payload cwd MUST be the Windows path, not MSYS /tmp/...\ngit checkout -q -b US-005\n\"$BAT\" phase US-005 build\nfor a in backend frontend ml ml-retry; do echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"a-'\"$a\"'\",\"agent_type\":\"'\"$a\"'\"}' | \"$BAT\" hook SubagentStart; done\n\"$BAT\" claim a-backend src/a.go src/b.go src/c.go\n\"$BAT\" claim a-frontend web/app.js\n\"$BAT\" claim a-ml ml/train.py ml/eval.py\n\"$BAT\" claim a-ml-retry ml/train2.py\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStop\",\"agent_id\":\"a-backend\",\"last_assistant_message\":\"done\"}' | \"$BAT\" hook SubagentStop   # (same for frontend / ml with \"error: it failed\" / ml-retry)\n\"$BAT\" canvas US-005 --out ./before.canvas    # POSITIVE CONTROL -> 7 nodes, 4 edges, all 4 subagents\ngit checkout -q -b US-006\n\"$BAT\" phase US-006 build                     # steals node id p-build from run US-005\n\"$BAT\" show US-005\n\"$BAT\" canvas US-005 --out ./after.canvas 2> ./after.stderr; echo EXIT=$?; wc -c < ./after.stderr\ngit checkout -q US-005 && \"$BAT\" phase US-005 verify\nprintf '{\"gate\":\"qa\",\"result\":\"ok\",\"why\":\"checks passed\",\"evidence\":[\"go test ./... ok\"]}' > v.json && \"$BAT\" verdict --file v.json\n\"$BAT\" canvas US-005 --out ./mirror.canvas 2> ./mirror.stderr; echo EXIT=$?; wc -c < ./mirror.stderr\ngrep -o '\"id\": \"[^\"]*\"' mirror.canvas\n\"$BAT\" canvas US-005          # default export path (canvas + vault note), same result",
    "evidence": "# BASELINE (before the collision) — canvas CAN render these nodes:\nwrote ./before.canvas (7 nodes, 4 edges)\n\"id\": \"g-p-build\" / \"p-build\" / \"n-a-backend\" / \"n-a-frontend\" / \"n-a-ml\" / \"n-a-ml-retry\" / \"header\"\n\n# after: batten phase US-006 build\n$ batten show US-005\nUS-005  run=US-005-1785258729455153100-cdbef693  status=running  phase=build  base=79977d9\n  [subagent] backend                  ok       owns 3 file(s)\n  [subagent] frontend                 ok       owns 1 file(s)\n  [subagent] ml                       failed   owns 2 file(s)\n  [subagent] ml-retry                 ok       owns 1 file(s)\n\n$ batten canvas US-005 --out ./after.canvas 2> ./after.stderr\nwrote ./after.canvas (1 nodes, 0 edges)\nEXIT=0\n--- stderr bytes: 0 ---\n--- node ids in after.canvas ---\n\"id\": \"header\"\n\n# after advancing US-005 to verify + recording a verdict (mirrors the report exactly):\n$ batten canvas US-005 --out ./mirror.canvas 2> ./mirror.stderr\nwrote ./mirror.canvas (4 nodes, 0 edges)\nEXIT=0\nstderr bytes: 0\n\"id\": \"g-p-verify\"\n\"id\": \"p-verify\"\n\"id\": \"verdict\"\n\"id\": \"header\"\n\n# SILENCE, cross-checked: the run note written by the SAME command lists what the canvas dropped\n$ batten canvas US-005\nwrote ...\\vault\\batten\\v39\\runs\\US-005.canvas (4 nodes, 0 edges)\nwrote run note ...\\vault\\batten\\v39\\runs\\US-005.md — open the vault in Obsidian\nEXIT=0   stderr:            (empty)\n\n-- US-005.md --\n## Fan-out\n| agent | domain | status | write-set | tokens | imputed |\n| backend | — | `ok` | 3 files | not measured | not measured |\n| frontend | — | `ok` | 1 file | not measured | not measured |\n| ml | — | `failed` | 2 files | not measured | not measured |\n| ml-retry | — | `ok` | 1 file | not measured | not measured |\n...\n## Run graph\n![[US-005.canvas]]        <-- the embedded canvas contains none of the four\n\n# and the canvas JSON itself carries no marker of the loss:\n{ \"nodes\": [ g-p-verify, p-verify, verdict, header ], \"edges\": [] }",
    "positive_control": "TWO. (1) Canvas control: the identical command run BEFORE the collision printed 'wrote ./before.canvas (7 nodes, 4 edges)' and contained n-a-backend/n-a-frontend/n-a-ml/n-a-ml-retry — so the renderer demonstrably emits these nodes, and only the dangling `spawn` parent suppresses them. (2) Hook-engagement control (Trap 2): my first SubagentStart batch was silent AND produced no nodes because the payload cwd was the MSYS path /tmp/claude/...; re-running with cwd=$(pwd -W) made the PreToolUse commit gate answer with a visible 'batten (warning — not blocking): US-005 has no verdict envelope', proving the hook binary was engaging before I trusted any SubagentStart result. Silence in the canvas run is therefore a real drop, not an unengaged binary.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/canvas/canvas.go:112-118 — build `real := map[string]bool{}` of phase NodeIDs in a first pass, then bucket `p := parent[n.NodeID]; if !real[p] { p = \"\" }; byPhase[p] = append(...)`, so a child whose spawn parent is absent from `nodes` falls into the existing 'unattributed' column at line 122 instead of a bucket nobody iterates. Root cause upstream is the un-scoped phase node id (`\"p-\"+phaseID` at cmd/batten/main.go:368 and hooks.go:494) against `nodes.node_id TEXT PRIMARY KEY`; scoping it to the run fixes the collision, but the canvas guard is worth having regardless since its own comment promises it."
  },
  {
    "index": 40,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary (batten 0.1.0, HEAD b7aac14). On a run that has NEVER had a transcript ingested, `batten budget` prints the hard zero `TASK-2  0 tokens, $0.00 imputed` and draws two empty bars `· tokens 0 / 3.0M [............]` / `· imputed_usd $0.00 / $8.00 [............]`, on the line directly above `· quota_pct NOT MEASURABLE`; MCP batten_budget on the same run returns `\"available\":true,\"spent\":0,\"remaining\":3000000`. Three other surfaces of the SAME binary, on the SAME DB state, seconds apart, report that identical fact honestly: `batten runs` prints `TOKENS — IMPUTED —`, the vault run note prints \"Usage **not measured** for this run (no transcript ingested). Not zero — unknown.\", and MCP nodeInfo.usage is a nullable pointer documented \"null means no usage was recorded for this node — not that it cost nothing\". The cause is exactly the file_hint: store.Budget() passes the literal `true` for Ceiling.Available on the tokens and imputed_usd ceilings (internal/store/store.go:811 and :815) while computing it properly for quota_pct at :822.",
    "repro": "SB=$SANDBOX\nmkdir -p \"$SB/sandbox/v40\" && cd \"$SB/sandbox/v40\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" init\n# edit the SANDBOX batten.yaml only: budget -> tokens_per_run: 3000000, imputed_usd_per_run: 8, quota_pct_per_run: 50; capabilities.obsidian.vault: 'vault'\ngit add -A && git commit -qm init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" phase TASK-2 build      # opens a run; NO ingest, NO measure, ever\n\"$SB/bin/batten.exe\" runs                   # surface 1\n\"$SB/bin/batten.exe\" budget TASK-2          # surface 2  <-- the defect\n# surface 4 (MCP, stdio JSON-RPC, same DB):\nprintf '%s\\n' '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"initialize\",\"params\":{\"protocolVersion\":\"2024-11-05\",\"capabilities\":{},\"clientInfo\":{\"name\":\"t\",\"version\":\"1\"}}}' '{\"jsonrpc\":\"2.0\",\"method\":\"notifications/initialized\"}' '{\"jsonrpc\":\"2.0\",\"id\":2,\"method\":\"tools/call\",\"params\":{\"name\":\"batten_budget\",\"arguments\":{\"unit\":\"TASK-2\"}}}' | \"$SB/bin/batten.exe\" mcp\n\"$SB/bin/batten.exe\" close TASK-2 --status failed   # writes the vault note = surface 3\ngrep -n 'not measured' vault/batten/v40/runs/TASK-2.md\n# POSITIVE CONTROL, separate run TASK-1, same DB:\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" ingest TASK-1 --transcript \"$PWD/gen/t.jsonl\"   # 2 assistant records, ts AFTER the run opened\n\"$SB/bin/batten.exe\" budget TASK-1\n\"$SB/bin/batten.exe\" runs",
    "evidence": "=== SURFACE 2 — batten budget TASK-2 (a run with NO transcript ever ingested) ===\nTASK-2  0 tokens, $0.00 imputed\n  · tokens       0 / 3.0M  [............]\n  · imputed_usd  $0.00 / $8.00  [............]\n  · quota_pct    NOT MEASURABLE — install the statusline (`batten statusline --install`)\nEXIT=0\n\n=== SURFACE 4 — MCP batten_budget, unit TASK-2, same DB, seconds later ===\n{\"available\":true,\"cap\":3000000,\"exceeded\":false,\"kind\":\"tokens\",\"remaining\":3000000,\"spent\":0}\n{\"available\":true,\"cap\":8,\"exceeded\":false,\"kind\":\"imputed_usd\",\"remaining\":8,\"spent\":0}\n{\"available\":false,\"cap\":50,\"exceeded\":false,\"kind\":\"quota_pct\",\"remaining\":null,\"spent\":null,\"unavailable_reason\":\"the share of the rolling 5-hour subscription window can only be sampled by `batten statusline` (hooks never see quota). It is not installed, ... This ceiling is declared but NOT enforced.\"}\n\n=== SURFACE 1 — batten runs, SAME run, SAME moment ===\nUNIT    STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nTASK-2  running  build  —        —       —\nTASK-1  running  build  —        194.0k  $0.15\n\n=== SURFACE 3 — vault/batten/v40/runs/TASK-2.md, line 17 ===\nUsage **not measured** for this run (no transcript ingested). Not zero — unknown.\n\n=== the policy those two surfaces break, in batten's own words ===\nREADME.md:194-195  \"**batten never invents a number.** A budget that quietly reports zero for what it\n                    failed to measure is worse than no budget at all, because it will be believed.\"\ninternal/mcp/mcp.go:102-104 (the batten_budget TOOL DESCRIPTION the agent reads)\n                    \"A ceiling that cannot be measured is reported as unavailable with the reason — never as zero.\"\ninternal/mcp/mcp.go:547  Spent *float64 `jsonschema:\"null when this ceiling cannot be measured — NOT zero\"`\ninternal/mcp/mcp.go:264  Usage *usageInfo `jsonschema:\"null means no usage was recorded for this node — not that it cost nothing\"`\ninternal/vault/vault.go:472-473 \"...distinguish three states that a naive \\\"%d\\\" would collapse into a zero:\n                    not measured, measured but unpriced, and measured. batten never reports an unknown as 0.\"\ninternal/store/store.go:802-803 (the doc comment ON the offending function)\n                    \"A ceiling we cannot measure is reported as unavailable, never as zero — a budget tool\n                     that invents a number is worse than none.\"\n\n=== the two lines that do it ===\ninternal/store/store.go:810-816\n  if tokensCap > 0 {\n    cs = append(cs, Ceiling{\"tokens\", float64(r.TokensSpent), float64(tokensCap),\n      r.TokensSpent >= tokensCap, true})           <-- Available hardcoded true\n  }\n  if usdCap > 0 {\n    cs = append(cs, Ceiling{\"imputed_usd\", r.ImputedUSD, usdCap, r.ImputedUSD >= usdCap, true})   <-- ditto\n  }\n  ...\n  cs = append(cs, Ceiling{\"quota_pct\", burned, quotaCap, ok && burned >= quotaCap, ok})   <-- :822, the honest one",
    "positive_control": "n/a — no ALLOW claim (this is a reporting defect, not a gate claim). I ran one anyway to kill the 'your sandbox was simply dead' alternative: on run TASK-1 in the same DB I ingested a 2-record transcript (claude-sonnet-4-5-20250929, 97k tokens each, timestamps AFTER the run opened). `batten ingest TASK-1 --transcript gen/t.jsonl` -> `TASK-1: +2 requests, 194.0k tokens total, $0.15 imputed`, and `batten budget TASK-1` then printed `· tokens 194.0k / 3.0M` and `· imputed_usd $0.15 / $8.00`, while `batten runs` flipped that row from `—  —` to `194.0k  $0.15`. So the ledger, the pricer and the budget surface all engage correctly in this sandbox. Decisively, that same `batten runs` table printed BOTH rows at once — `TASK-2 ... —  —` and `TASK-1 ... 194.0k  $0.15` — proving batten HAS the discriminating signal and uses it at main.go:416-420 (`if r.TokensSpent > 0`). cmdBudget and store.Budget simply never consult it.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/store/store.go:811 and :815 — replace the literal `true` in both Ceiling literals with a measured flag derived from whether the run has any usage row (the store already has that query: SeenRequests, `SELECT ... FROM usage WHERE run_id=?`; vault.go:473-478 uses exactly this signal). cmdBudget (main.go:1108-1112) and mcp.budget (mcp.go:596-601) already branch on !Available, so both surfaces become honest for free; add a tokens/imputed_usd arm to unavailableReason() (mcp.go:609) reading 'no transcript ingested for this run', and guard the header line at main.go:1092 the way main.go:416-420 does.",
    "notes": "ADVERSARIAL DUE DILIGENCE. (a) NOT Trap 1 by-design. Trap 1's carve-out protects NOT MEASURABLE-instead-of-0; this is the stated converse — an unmeasured quantity printed as a hard 0 — and batten's own README:194-195, the batten_budget tool description at mcp.go:102-104, the `spent` schema doc at mcp.go:547, and the doc comment sitting directly above the offending function at store.go:802-803 all state the opposite of what the code does. Four independent statements of policy; one implementation that breaks it on 2 of its 3 ceilings, in the same six lines of output. (b) DISTINCT FROM FINDING 29 — I checked this specifically. f29 is ingest-side: `batten ingest` reads and PRICES 291k tokens, store.RecordUsage's TS fence silently drops them, nothing is disclosed; its fix lives in RecordUsage's return signature / cmdIngest's printout. f40 is report-side: my TASK-2 run never invoked `ingest` at all, so the f29 fence is not in play, and the zero still prints. Fixing f29 (disclosing fenced rows) would leave f40's output byte-identical; fixing f40 (store.go:811/815) would additionally have made f29's budget block read NOT MEASURED. Same symptom line, two causes at two layers, two different one-line fixes — convergent, not duplicate, and f40 is the more general of the two since it needs no ingest at all. (c) THE STRONGEST COUNTER-ARGUMENT, and why it fails: one can argue 0 is literally true — the run has spent nothing batten knows of. But batten itself rejects that framing on three of its four surfaces for the identical DB row, and the error runs one way only: MCP hands an agent `available:true, remaining:3000000` — full headroom, ceiling reported as ENFORCED — for a ledger that was never fed. /batten-night uses the budget ceilings as its only unattended tripwire (plugin/claude-code/skills/batten-engine/SKILL.md:130 quotes the same README line), so the one consumer that most needs `available:false` is precisely the one handed `true`. (d) SEVERITY: report says major and I agree. Not a blocker — nothing is corrupted, nothing is wrongly denied, no work is blocked, and Exceeded stays correctly false so the gate does not misfire. But it is on the default path (every run before its first ingest), it is the flagship surface of the product's stated first principle, and the MCP contract documents the exact opposite of what it returns. (e) Trap 2 does not apply — no silence and no ALLOW claim; every command above produced output. (f) ISOLATION: BATTEN_DB was re-exported in every Bash call; all writes confined to scratchpad/ft/sandbox/v40; the repo under test was read only."
  },
  {
    "index": 41,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary: I seeded a genuine retry (ml agent fails, second ml agent succeeds) through the real SubagentStart/SubagentStop hooks and the store recorded only two `spawn` edges — `select distinct rel from edges` returns exactly `spawn`. `batten canvas` drew 2 unlabelled edges, MCP `batten_run_graph` returned `\"retries\": 0`, and the vault run note had no Relations section. An exhaustive sweep of the whole repo (Go, SQL, CLI verbs, MCP tool set, plugin commands and skills) finds exactly ONE production AddEdge call site, internal/hooks/hooks.go:494, with the relation hard-coded to the string literal \"spawn\". No CLI command, MCP tool, hook or skill can ever write `retry_of` (or `depends_on`/`supersedes`/`rollback`). All four consumers are alive and correct — proven by injecting the edge by hand, after which the canvas drew an orange labelled `retry_of` edge, MCP reported `retries = 1`, and the vault note grew a `## Relations` section. Only the producer is missing.",
    "repro": "SB=$SANDBOX\nmkdir -p \"$SB/sandbox/v41\" && cd \"$SB/sandbox/v41\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\nmkdir -p src ml && echo x > src/a.go && echo y > ml/m.py && git add -A && git commit -qm init\n\"$SB/bin/batten.exe\" init\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" phase TASK-5 build\nCWD=$(pwd -W)\nexport BATTEN_DB=\"$PWD/state.db\"\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"agent-ml-1\",\"agent_type\":\"ml\"}' | \"$SB/bin/batten.exe\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"SubagentStop\",\"agent_id\":\"agent-ml-1\",\"last_assistant_message\":\"error: the run failed.\"}' | \"$SB/bin/batten.exe\" hook SubagentStop\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"agent-ml-2\",\"agent_type\":\"ml\"}' | \"$SB/bin/batten.exe\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"SubagentStop\",\"agent_id\":\"agent-ml-2\",\"last_assistant_message\":\"Retry of agent-ml-1: fixed the wrapper; 6 tests pass.\"}' | \"$SB/bin/batten.exe\" hook SubagentStop\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" canvas TASK-5 --out canvas.json\npython -c \"import sqlite3;c=sqlite3.connect('state.db');print(list(c.execute('select src_node,dst_node,rel from edges')));print(list(c.execute('select distinct rel from edges')))\"\n# MCP over stdio (mcpcall.py = initialize -> notifications/initialized -> tools/call batten_run_graph)\nexport BATTEN_DB=\"$PWD/state.db\"; python mcpcall.py \"$SB/bin/batten.exe\" TASK-5\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" --help\n# repo sweep (read-only, in $REPO)\ngrep -rn '\\.AddEdge(' --include=*.go . | grep -v _test.go\ngrep -rn 'INSERT INTO edges' -i --include=*.go .\ngrep -rni 'retry|edge' plugin/\ngrep -n 'st\\.\\(Add\\|Record\\|Finish\\|Ensure\\|Set\\)' internal/mcp/mcp.go",
    "evidence": "$ batten show TASK-5\nTASK-5  run=TASK-5-1785259437738591800-467c7591  status=running  phase=build  base=e24fab9\n  [phase] build                    running\n  [subagent] ml                       failed\n  [subagent] ml                       ok\n\n$ batten canvas TASK-5 --out canvas.json\nwrote canvas.json (5 nodes, 2 edges)\nEDGE p-build -> n-agent-ml-1 | label= None | color None\nEDGE p-build -> n-agent-ml-2 | label= None | color None\n(the only difference between the two ml nodes is the colour: '**ml**\\ndomain: `ml`\\n`failed`' color 1 vs '**ml**\\ndomain: `ml`\\n`ok`' color 4)\n\n$ sqlite: select src_node,dst_node,rel from edges\n[('p-build','n-agent-ml-1','spawn'), ('p-build','n-agent-ml-2','spawn')]\nDISTINCT REL EVER: [('spawn',)]\n\n$ MCP tools/call batten_run_graph {\"unit\":\"TASK-5\"}\n{\"edges\":[{\"from\":\"p-build\",\"rel\":\"spawn\",\"to\":\"n-agent-ml-1\"},{\"from\":\"p-build\",\"rel\":\"spawn\",\"to\":\"n-agent-ml-2\"}], ... ,\"retries\":0, ...}\n\n$ batten --help   (no retry / edge / link / relate verb; the full verb list is\n  init doctor hook phase claim verdict runs show canvas budget override tui mcp statusline ingest)\n\n$ grep -rn '\\.AddEdge(' --include=*.go . | grep -v _test.go\n./internal/hooks/hooks.go:494:\t\t_ = h.Store.AddEdge(run.RunID, \"p-\"+run.Phase, nodeID, \"spawn\")\n  -> ONE production call site in the whole tree, relation hard-coded to the literal \"spawn\".\n$ grep -rn 'INSERT INTO edges' -i --include=*.go .\ninternal/store/store.go:454:\t_, err := s.db.Exec(`INSERT OR IGNORE INTO edges (run_id, src_node, dst_node, rel) VALUES (?,?,?,?)`\n  -> AddEdge is the ONLY writer of the edges table.\n$ grep -n 'st\\.\\(Add\\|Record\\|Finish\\|Ensure\\|Set\\)' internal/mcp/mcp.go\n(no output — all six MCP tools batten_runs / batten_run_graph / batten_verdict_status / batten_budget /\n batten_writeset_owner / batten_spec are read-only queries; none is a producer.)\n$ grep -rni 'retry|edge' plugin/\nplugin/claude-code/commands/batten-build.md:99: Do **not**: ... retry the write ...\n  -> the only 'retry' in the whole plugin (commands + skills batten-engine/batten-verdict) and it is a\n     prohibition, not a producer.\ncmd/batten/main.go:367 cmdPhase calls AddNode for the new phase node and NO AddEdge at all.\n\nThe four consumers, all live at HEAD:\n  internal/canvas/canvas.go:79   case \"retry_of\": -> colour 2\n  internal/mcp/mcp.go:334        if e.Rel == \"retry_of\" { out.Retries++ }\n  internal/vault/vault.go:359    renderRelations — 'the interesting ones are exactly the relations a flat trace cannot express — retry_of, supersedes, rollback, depends_on'\n  internal/tui/tui.go:419        detailTree cross-edge annotations\nThe promises: README.md:267 'the fan-out, the retries, the blocked verdict that got fixed and re-verified';\ninternal/mcp/mcp.go:82 '(spawn, depends_on, retry_of, rollback). This is the path actually taken — including retries';\nDESIGN.md:249 'edges tipadas = spawn, depends_on, retry_of, rollback'; store.go:106 schema comment.\n\nNote on DESIGN principle #1: `\"retries\": 0` is an UNMEASURED quantity reported as a hard 0 — the brief's\nown example of a real defect, not the NOT-MEASURABLE case.",
    "positive_control": "The claim is 'the producer never fires', so per Trap 2 I proved the whole consumer chain fires when the edge exists. I injected ONE row by hand into my sandbox DB — INSERT INTO edges VALUES(<run>, 'n-agent-ml-2','n-agent-ml-1','retry_of') — changing nothing else, and re-ran the same three surfaces:\n  batten canvas TASK-5 -> 'wrote canvas2.json (5 nodes, 3 edges)' and\n     EDGE n-agent-ml-2 -> n-agent-ml-1 label= 'retry_of' color 2   (orange, labelled)\n  MCP batten_run_graph -> retries = 1, edges include {'from':'n-agent-ml-2','rel':'retry_of','to':'n-agent-ml-1'}\n  Stop hook with capabilities.obsidian.vault set -> vault/batten/v41/runs/TASK-5.md:33 '## Relations' / ':35 - `retry_of`: ml → ml'\nSo the renderers, the counter and the vault section are all wired and correct; the silence in the first run\nis not a dead pipeline (spawn edges were written by the very same hooks in the same run) but a genuinely\nabsent producer. Control passed — the negative result is meaningful.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/hooks/hooks.go:494 — in subagentStart, before/after the spawn edge, look up the newest finished node in this run with the same agent_type/domain and status 'failed' that has no successor, and emit `h.Store.AddEdge(run.RunID, nodeID, prev.NodeID, \"retry_of\")`. Until a producer exists, internal/mcp/mcp.go:277 should report retries as NOT MEASURABLE rather than a hard 0 (DESIGN principle #1)."
  },
  {
    "index": 42,
    "verdict": "REFUTED",
    "reason": "The mechanical observation reproduces on the HEAD binary (a second node's claim on a path owned by a `failed` node is refused, exit 1; under `enforcement: enforce` the retry's Edit is hard-denied), but the finding's load-bearing claim -- \"there is no way to release a write-set\" -- is false. `batten close <unit> --status failed` releases the claims (\"Write-set claims released -- files it held are free again\") and I proved it works: after the close, a newly registered agent claimed those exact two paths, exit 0. That command is not new (it landed 37 commits before the frozen binary, cmd/batten/main.go:637 \"close: end a run so its claims release (R1)\"); the reporter missed it only because `close` is absent from `--help`, which is a separate, already-filed finding. The requested behaviour (hand the fence to a second agent, `claim --supersede`) is additionally refused BY DESIGN: internal/store/store.go:478-480 (\"the disjointness rule is not advice, it is a constraint\") and plugin/claude-code/commands/batten-build.md:99 (\"Do not: hand the file to a second agent, ask for an override, retry the write ... The fence is load-bearing\"). What survives is an ergonomics gap, not a missing capability: the release path exists but is never named at the point of failure, and it is coarse (run-level, not agent-level) -- a retry inside the still-open run cannot claim even after the close, only a brand-new run can.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v42\" && cd \"$SB/sandbox/v42\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"; W=$(pwd -W)\necho hello > README.md && git add -A && git commit -qm init\n\"$BAT\" init\n\"$BAT\" phase TASK-001 build\n# bind the session (NOTE: cwd in the payload MUST be the Windows path; a /c/... msys path\n# makes loadForHook fail and every hook goes silent -- Trap 2)\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PostToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"batten phase TASK-001 build\"}}' | \"$BAT\" hook PostToolUse\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"agent-ml-1\",\"agent_type\":\"ml\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"agent-ml-2\",\"agent_type\":\"ml\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" claim agent-ml-1 ml/data/extractors.py ml/data/spans.py\n# agent-ml-1 fails\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStop\",\"agent_id\":\"agent-ml-1\",\"agent_type\":\"ml\",\"last_assistant_message\":\"the task failed: tests error: cannot import\"}' | \"$BAT\" hook SubagentStop\n\"$BAT\" show TASK-001\n# the retry cannot claim (the finding's own command)\n\"$BAT\" claim agent-ml-2 ml/data/extractors.py ml/data/spans.py; echo \"EXIT=$?\"\n# exhaustive hunt for a release path\nfor c in release unclaim free disown transfer; do \"$BAT\" $c agent-ml-1 ml/data/extractors.py; done   # all print usage\n\"$BAT\" claim agent-ml-2 --release   ml/data/extractors.py\n\"$BAT\" claim agent-ml-2 --supersede agent-ml-1 ml/data/extractors.py\n\"$BAT\" claim agent-ml-2 --from      agent-ml-1 ml/data/extractors.py\n\"$BAT\" claim agent-ml-2 --force     ml/data/extractors.py\nsed -i 's/^enforcement: report/enforcement: enforce/' batten.yaml\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"agent-ml-2\",\"agent_type\":\"ml\",\"tool_input\":{\"file_path\":\"'\"$W\"'/ml/data/extractors.py\"}}' | \"$BAT\" hook PreToolUse\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"agent-ml-2\",\"agent_type\":\"ml\",\"tool_input\":{\"file_path\":\"'\"$W\"'/ml/other.py\"}}' | \"$BAT\" hook PreToolUse\n# THE RELEASE PATH THE FINDING SAYS DOES NOT EXIST\n\"$BAT\" close TASK-001 --status failed\n\"$BAT\" claim agent-ml-2 ml/data/extractors.py; echo \"EXIT=$?\"   # still collides: close is run-level\n\"$BAT\" phase TASK-001 build\necho '{\"session_id\":\"s2\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"agent-ml-3\",\"agent_type\":\"ml\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" claim agent-ml-3 ml/data/extractors.py ml/data/spans.py; echo \"EXIT=$?\"   # 0 -- released\n# override does NOT relax the fence\n\"$BAT\" override TASK-001 --reason \"retry needs the fence handed over\"\necho '{\"session_id\":\"s2\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"agent-ml-4\",\"agent_type\":\"ml\",\"tool_input\":{\"file_path\":\"'\"$W\"'/ml/data/extractors.py\"}}' | \"$BAT\" hook PreToolUse",
    "evidence": "# 1. the finding's observation DOES reproduce at HEAD\n$ batten claim agent-ml-1 ml/data/extractors.py ml/data/spans.py\nagent-ml-1 owns 2 file(s); any other agent writing them is now denied\n$ batten show TASK-001\n  [subagent] ml                       failed   owns 2 file(s)\n  [subagent] ml                       running\n$ batten claim agent-ml-2 ml/data/extractors.py ml/data/spans.py\nbatten: write-set collision: ml/data/extractors.py is already owned by n-agent-ml-1\nEXIT=1\n\n# 2. no release subcommand, no flag on claim (every `--x` is parsed as a file path)\n$ batten release|unclaim|free|disown|transfer ...   -> prints usage, no such command\n$ batten claim agent-ml-2 --supersede agent-ml-1 ml/data/extractors.py\nbatten: write-set collision: ml/data/extractors.py is already owned by n-agent-ml-1\n(dispatch list in cmd/batten/main.go:49-85 is exactly: hook init doctor verdict claim runs show\n canvas budget override phase mcp tui statusline ingest measure close check hook-debug;\n `grep -rn 'DELETE FROM writesets' --include=*.go` -> no matches;\n internal/mcp/mcp.go exposes only read tools: runs, run_graph, verdict_status, budget,\n writeset_owner, spec)\n\n# 3. the SubagentStop hook does not release either -- it only stamps the node `failed`\n   (internal/hooks/hooks.go:499-513 = FinishNode + ingest); proven by step 1 above, where the\n   collision persists after the stop.\n\n# 4. BUT `close` IS a working release path -- the finding's central claim is false\n$ batten close TASK-001 --status failed\nTASK-001 closed (failed). Write-set claims released - files it held are free again.\n$ batten claim agent-ml-2 ml/data/extractors.py\nbatten: write-set collision: ml/data/extractors.py is already owned by n-agent-ml-1\nEXIT=1                      <- release is run-scoped: the node inside the closed run is stuck\n$ batten phase TASK-001 build && <SubagentStart agent-ml-3> && batten claim agent-ml-3 ml/data/extractors.py ml/data/spans.py\nagent-ml-3 owns 2 file(s); any other agent writing them is now denied\nEXIT=0                      <- the exact two paths the finding calls unreleasable are re-claimed\n\n# 5. by design, stated in the shipped artefacts\ninternal/store/store.go:478  \"The PRIMARY KEY (run_id, path) means the database itself refuses a\n                              second owner for a file - the disjointness rule is not advice, it is\n                              a constraint.\"\ncmd/batten/main.go:637       \"---------- close: end a run so its claims release (R1) ----------\"\nplugin/claude-code/commands/batten-build.md:99  \"Do not: hand the file to a second agent, ask for\n                              an override, retry the write, or 'just this once' edit it yourself.\n                              The fence is load-bearing.\"\n\n# 6. blast radius under the mode `batten init` actually writes (enforcement: report) is a warning,\n#    not a block, so the retry is not even stopped by default:\n{\"systemMessage\":\"batten (warning - not blocking): batten: write-set collision. ml/data/extractors.py belongs to another agent's write-set (n-agent-ml-3); you are n-agent-ml-4.\"}\n\n# 7. not already-fixed-at-head bookkeeping: `close` predates the frozen binary\n$ git log --oneline -S\"cmdClose\" -- cmd/batten/main.go\n512be16 feat(R1-R4): run lifecycle, batten-verified evidence, panic fence, Windows case-fold   (37 commits before HEAD)",
    "positive_control": "Both directions controlled. (a) The write-set gate was proven to FIRE in my sandbox before I trusted any silence: with enforcement=enforce and an ABSOLUTE file_path, agent-ml-2 editing agent-ml-1's file returned {\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: write-set collision. ml/data/extractors.py belongs to another agent's write-set (n-agent-ml-1); you are n-agent-ml-2...\"}; the same payload pointed at ml/other.py (which agent-ml-2 owns) returned nothing, exit 0 = allow. (b) An earlier round where BOTH payloads were silent was correctly discarded, not scored: two independent causes -- a msys /c/... cwd that made loadForHook bail, and a RELATIVE file_path that makes writeSetGuard's filepath.Rel return early (internal/hooks/hooks.go:408). (c) The claim command itself was proven live: the same two paths that fail for agent-ml-2 succeed for agent-ml-3 after the close, exit 0.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": ""
  },
  {
    "index": 43,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary. `batten claim agent-fe-1 frontend/composables/useTrace.ts` stores and then REPORTS the case-folded string: the vault run note's 'Files touched' says `frontend/composables/usetrace.ts`, MCP batten_writeset_owner returns your_write_set=[\"frontend/composables/usetrace.ts\"], MCP batten_run_graph's node n-agent-fe-1 has write_set=[\"frontend/composables/usetrace.ts\"], and the guard's own collision message tells the agent 'Your write-set: frontend/mypanel.tsx' after `claim ... frontend/MyPanel.tsx`. Ground truth from `git ls-files` is `frontend/composables/useTrace.ts`. This is DISPLAY, not storage: in the SAME writeset_owner payload the `path` field (computed from the request, never stored) preserves `useTrace.ts` while `your_write_set` (read back from the store) is folded; likewise the collision message prints the offending path faithfully and the owner's own set folded. The folding-for-MATCHING that the brief calls deliberate is intact and is NOT what is reported here (see positive_control).",
    "repro": "SBW='$SANDBOX'\nBAT='$SANDBOX'\nmkdir -p \"$SBW\" && cd \"$SBW\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$SBW/state.db\"\nmkdir -p frontend/composables && echo 'export const useTrace = () => {}' > frontend/composables/useTrace.ts\ngit add -A && git commit -qm init\n\"$BAT\" init\n# point the spec at a sandbox vault so the run note is emitted\nsed -i \"s|vault: ''|vault: '$SBW/vault'|\" batten.yaml\ngit checkout -q -b TASK-1      # so activeUnit() matches unit.pattern TASK-\\d+\nexport BATTEN_DB=\"$SBW/state.db\"; \"$BAT\" phase TASK-1 build\nexport BATTEN_DB=\"$SBW/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$SBW\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"agent-fe-1\",\"agent_type\":\"frontend\"}' | \"$BAT\" hook SubagentStart\n# THE CLAIM, spelled exactly as the file is spelled on disk:\nexport BATTEN_DB=\"$SBW/state.db\"; \"$BAT\" claim agent-fe-1 frontend/composables/useTrace.ts\n# surface 1 + 2: the vault run note\nexport BATTEN_DB=\"$SBW/state.db\"; printf '%s\\n' '{\"unit\":\"TASK-1\",\"gate\":\"qa\",\"check_id\":\"qa\",\"result\":\"ok\",\"evidence\":[\"ran the suite: 3 passed\"],\"why\":\"criteria met\",\"safe_next_step\":\"close\",\"requires_confirmation\":false}' > v.json\nexport BATTEN_DB=\"$SBW/state.db\"; \"$BAT\" verdict --file v.json\ngrep -A2 'Files touched' vault/batten/v43/runs/TASK-1.md\n# surface 3 + 4: MCP (stdin must stay open or the server exits on EOF)\nRUN=$(export BATTEN_DB=\"$SBW/state.db\"; \"$BAT\" show TASK-1 | head -1 | sed 's/.*run=\\([^ ]*\\).*/\\1/')\ncat > req.jsonl <<EOF\n{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"initialize\",\"params\":{\"protocolVersion\":\"2024-11-05\",\"capabilities\":{},\"clientInfo\":{\"name\":\"t\",\"version\":\"1\"}}}\n{\"jsonrpc\":\"2.0\",\"method\":\"notifications/initialized\"}\n{\"jsonrpc\":\"2.0\",\"id\":2,\"method\":\"tools/call\",\"params\":{\"name\":\"batten_writeset_owner\",\"arguments\":{\"path\":\"frontend/composables/useTrace.ts\",\"agent_id\":\"agent-fe-1\",\"run_id\":\"$RUN\"}}}\n{\"jsonrpc\":\"2.0\",\"id\":3,\"method\":\"tools/call\",\"params\":{\"name\":\"batten_run_graph\",\"arguments\":{\"unit\":\"TASK-1\"}}}\nEOF\nexport BATTEN_DB=\"$SBW/state.db\"; { cat req.jsonl; sleep 4; } | \"$BAT\" mcp > mcp_out.json\n# surface 5: the guard message the agent is shown\nexport BATTEN_DB=\"$SBW/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$SBW\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"agent-be-1\",\"agent_type\":\"frontend\"}' | \"$BAT\" hook SubagentStart\nexport BATTEN_DB=\"$SBW/state.db\"; \"$BAT\" claim agent-be-1 frontend/MyPanel.tsx\nexport BATTEN_DB=\"$SBW/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$SBW\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Write\",\"agent_id\":\"agent-be-1\",\"tool_input\":{\"file_path\":\"'\"$SBW\"'/frontend/composables/useTrace.ts\",\"content\":\"x\"}}' | \"$BAT\" hook PreToolUse\n# downstream consequence\ngit log --oneline -- frontend/composables/usetrace.ts | wc -l   # 0\ngit log --oneline -- frontend/composables/useTrace.ts | wc -l   # 1",
    "evidence": "GROUND TRUTH — what the human actually wrote and what git tracks:\n  $ git ls-files\n  frontend/composables/useTrace.ts\n\nCOMMAND, spelled correctly:\n  $ batten claim agent-fe-1 frontend/composables/useTrace.ts\n  agent-fe-1 owns 1 file(s); any other agent writing them is now denied\n\nSURFACE 1 — vault run note (vault/batten/v43/runs/TASK-1.md, lines 38-40):\n  **Files touched**\n\n  - `frontend/composables/usetrace.ts`\n\nSURFACE 2 — MCP batten_writeset_owner (note `path` vs `your_write_set` in ONE payload):\n  {\"domain\":\"frontend\",\"owned\":true,\"owned_by_you\":true,\"owner_agent_id\":\"agent-fe-1\",\n   \"owner_node\":\"n-agent-fe-1\",\n   \"path\":\"frontend/composables/useTrace.ts\",           <-- faithful (never stored)\n   \"write_allowed\":true,\n   \"your_write_set\":[\"frontend/composables/usetrace.ts\"]}  <-- folded (round-tripped)\n\nSURFACE 3 — MCP batten_run_graph, node n-agent-fe-1:\n  {\"agent_id\":\"agent-fe-1\",\"kind\":\"subagent\",\"node_id\":\"n-agent-fe-1\",\n   \"write_set\":[\"frontend/composables/usetrace.ts\"]}\n\nSURFACE 4 — the collision message the AGENT is shown (after `claim agent-be-1 frontend/MyPanel.tsx`):\n  batten warning:\n  batten: write-set collision. frontend/composables/useTrace.ts belongs to another agent's write-set (n-agent-fe-1); you are n-agent-be-1.\n  Two agents must never write the same file — that is what makes the fan-out safe.\n  Your write-set:\n    frontend/mypanel.tsx          <-- folded; the agent declared frontend/MyPanel.tsx\n    frontend/other.ts\n  If this file genuinely belongs to you, the plan is wrong: fix the plan, do not cross the fence.\n\n  (Same message, one line apart: the offending path — taken from the live payload — keeps its\n   case, the owner's own set — read back from the store — does not. Storage vs display, proven.)\n\nDOWNSTREAM CONSEQUENCE, on Windows itself (git's index is case-sensitive even here):\n  $ git log --oneline -- frontend/composables/usetrace.ts | wc -l\n  0\n  $ git log --oneline -- frontend/composables/useTrace.ts | wc -l\n  1\n  The string batten reports as \"the file that was touched\" resolves to nothing in git.\n\nCODE at HEAD (unchanged): internal/store/store.go:497 INSERTs normPath(p) into the single\n`path` column (schema store.go:112-117 — one column, no path_key/path_display), so\nWriteSet() (store.go:516) and WriteSetsByRun() (store.go:540) — the ONLY read paths behind\nall four surfaces above (internal/vault/vault.go:346, internal/mcp/mcp.go:325 and :673,\ninternal/hooks/hooks.go:448, internal/tui/tui.go:182) — can only ever return the folded form.",
    "positive_control": "TWO controls, both needed because the brief warns the folding itself is deliberate.\n\n(a) THE GATE FIRES — not silence. A second agent writing the claimed file produced a real\n    gate output, exit 0, non-empty (see SURFACE 4 above): \"batten: write-set collision.\n    frontend/composables/useTrace.ts belongs to another agent's write-set (n-agent-fe-1)\".\n    So the write-set machinery is live in this sandbox and my observations are of an engaged\n    hook, not of a no-op.\n\n(b) THE DELIBERATE BEHAVIOUR IS INTACT AND IS NOT WHAT I AM REPORTING. Queried\n    batten_writeset_owner with a deliberately mangled casing, as a foreign agent:\n      path=\"FRONTEND/COMPOSABLES/USETRACE.TS\", agent_id=\"agent-be-1\"\n    ->  {\"owned\":true,\"owned_by_you\":false,\"owner_agent_id\":\"agent-fe-1\",\n         \"write_allowed\":false,\n         \"note\":\"FRONTEND/COMPOSABLES/USETRACE.TS belongs to n-agent-fe-1. A Write/Edit from\n                 you would be DENIED. ...\",\n         \"your_write_set\":[\"frontend/mypanel.tsx\",\"frontend/other.ts\"]}\n    Case-insensitive MATCHING works exactly as documented at store.go:560-565 — a wildly\n    recased path is still correctly attributed and still correctly denied. I am NOT filing\n    that. Note the same response again shows the split: `note` and `path` echo the caller's\n    string verbatim, `your_write_set` is folded. The defect is the REPORTED spelling only.\n\n(No ALLOW claim is made anywhere in this verdict.)",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/store/store.go:497 — INSERT the as-typed path and add a folded `path_key` column carrying normPath() as the PRIMARY KEY (schema at store.go:112-117); keep matching on path_key at :490/:509/:1066, and let WriteSet()/WriteSetsByRun() (:516/:540) SELECT the as-typed `path`.",
    "notes": "Not ALREADY-FIXED: the writesets schema at store.go:112-117 still has a single `path` column and `git log -S path_key -- internal/store/store.go` finds nothing on any branch, so no fold/display separation ever landed. The bug is Windows/macOS-only by construction (normPath is exact on Linux, store.go:568), which is why internal/mcp/mcp_test.go:408 and :514 pass — their fixtures are already all-lowercase, so the fold is invisible to the suite. A regression test should claim a mixed-case path (e.g. `internal/api/Handler.go`) and assert the read path returns it unfolded. Severity kept at the reported 'major' rather than reduced: no gate is weakened and the folded path still opens on NTFS, but the mis-spelled string is handed to an agent as the authoritative list of files it owns, and it does not resolve as a git pathspec even on the platform that produced it."
  },
  {
    "index": 44,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary on both surfaces: after the run advanced build -> verify -> close AND was closed ok with an evidence-bearing verdict, every phase node still renders `running`/purple in the canvas (g-p-build, p-build, p-verify, p-close) and `◐` in the TUI tree, while the same frame's header says status `ok` and the verdict card is green. Same root cause as finding 18 (no FinishNode call for phase nodes; ended_at stays NULL) seen through the canvas + TUI surfaces — a DUPLICATE, not an additional defect: one fix in cmdPhase/CloseRun closes both.",
    "repro": "SB=.../scratchpad/ft; BAT=$SB/bin/batten.exe; D=$SB/sandbox/v44\nmkdir -p \"$D\" && cd \"$D\" && git init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$D/state.db\"\n\"$BAT\" init\ngit add -A && git commit -qm init && git checkout -q -b TASK-5\n\"$BAT\" phase TASK-5 build\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$D\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"a1\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$D\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"a2\",\"agent_type\":\"frontend\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$D\"'\",\"hook_event_name\":\"SubagentStop\",\"agent_id\":\"a1\",\"last_assistant_message\":\"all good\"}' | \"$BAT\" hook SubagentStop\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$D\"'\",\"hook_event_name\":\"SubagentStop\",\"agent_id\":\"a2\",\"last_assistant_message\":\"the build failed\"}' | \"$BAT\" hook SubagentStop\n\"$BAT\" phase TASK-5 verify\n\"$BAT\" canvas TASK-5 --out \"$D/out1.canvas\"\ncat > v.json <<'EOF'\n{\"unit\":\"TASK-5\",\"gate\":\"qa\",\"result\":\"ok\",\"why\":\"checks pass\",\"evidence\":[\"go test ./... -> ok\"]}\nEOF\n\"$BAT\" verdict --file v.json\n\"$BAT\" phase TASK-5 close\n\"$BAT\" close TASK-5 --status ok\n\"$BAT\" runs\n\"$BAT\" canvas TASK-5 --out \"$D/out2.canvas\"\npython -c \"import json;[print(json.dumps({k:v for k,v in n.items() if k in ('id','color','text','label')})) for n in json.load(open('out2.canvas'))['nodes']]\"\npython -c \"import sqlite3;print(list(sqlite3.connect(r'$D/state.db').execute('select node_id,kind,status,ended_at from nodes order by started_at')))\"\n# TUI, headless: a probe module (module github.com/ArthurZizumbo/batten/ftprobe, replace => repo)\n# calls tui.New(sp,st); m.Init(); m.Update(tea.WindowSizeMsg{120,40}); print(strip(m.View().Content))\n\"$D/tuiprobe.exe\" \"$D\" \"$D/state.db\"",
    "evidence": "$ batten runs\nUNIT    STATUS  PHASE  VERDICT  TOKENS  IMPUTED\nTASK-5  ok      close  ok       —       —\n\n$ canvas after close (out2.canvas, nodes trimmed to id/color/text):\n{\"id\": \"g-p-build\",  \"color\": \"6\", \"label\": \"build\"}\n{\"id\": \"p-build\",    \"color\": \"6\", \"text\": \"## build\\n`running`\"}\n{\"id\": \"n-a1\",       \"color\": \"4\", \"text\": \"**backend**\\n`ok`\"}\n{\"id\": \"n-a2\",       \"color\": \"1\", \"text\": \"**frontend**\\n`failed`\"}\n{\"id\": \"g-p-verify\", \"color\": \"6\", \"label\": \"verify\"}\n{\"id\": \"p-verify\",   \"color\": \"6\", \"text\": \"## verify\\n`running`\"}\n{\"id\": \"g-p-close\",  \"color\": \"6\", \"label\": \"close\"}\n{\"id\": \"p-close\",    \"color\": \"6\", \"text\": \"## close\\n`running`\"}\n{\"id\": \"verdict\",    \"color\": \"4\", \"text\": \"## verdict: ok ...\"}\n{\"id\": \"header\",     \"text\": \"# TASK-5\\n\\nrun `TASK-5-...`\\nstatus `ok`\\nphase `close`\\nbase `9b9a710`\"}\n\n$ tuiprobe (ANSI stripped, detail pane):\n TASK-5  ok  close\n ◐ build\n  ├─ ✓ backend\n  └─ ✗ frontend\n ◐ verify\n ◐ close\n verdict OK  qa/\n\n$ sqlite nodes:\n('p-build',  'phase',    'build',    'running', None)\n('n-a1',     'subagent', 'backend',  'ok',      1785259466)\n('n-a2',     'subagent', 'frontend', 'failed',  1785259467)\n('p-verify', 'phase',    'verify',   'running', None)\n('p-close',  'phase',    'close',    'running', None)\n\nCode: cmd/batten/main.go:367 AddNode(status \"running\") is the only write to a phase node; store.go:375 CloseRun updates `runs` only; FinishNode (store.go:411) has exactly one production caller, hooks.go:508 (SubagentStop).",
    "positive_control": "Built into the same run: the two subagent nodes went running -> ok / failed via SubagentStop->FinishNode and both surfaces rendered the transition correctly (canvas color 4 green `ok` and color 1 red `failed`; TUI ✓ and ✗). So the renderers demonstrably paint terminal statuses, and the store demonstrably writes them — the purple/◐ phases are missing data, not a render or a sandbox artifact. Also checked docs/ and DESIGN for any statement that phase nodes are deliberately left open: none exists.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "DUPLICATE of finding 18 — one fix serves both. cmd/batten/main.go:367 (cmdPhase): before AddNode, FinishNode the outgoing run.Phase node; internal/store/store.go:375 (CloseRun): also `UPDATE nodes SET status=?,ended_at=? WHERE run_id=? AND kind='phase' AND ended_at IS NULL`. Scope by run_id — FinishNode keys on node_id alone, so \"p-build\" would finish another run's node."
  },
  {
    "index": 45,
    "verdict": "CONFIRMED",
    "reason": "Reproduced numerically on the HEAD binary in my own sandbox, twice on two unrelated runs. In the emitted JSON Canvas the phase card and its FIRST subagent card share the same x/width and their y-spans intersect by exactly 60px: p-build {x:0,y:0,w:300,h:120} spans y 0..120 while n-agent-be-1 {x:0,y:60,w:300,h:120} spans y 60..180 -> overlap (w=300, h=60), i.e. exactly half a card. Not by design: the Render doc comment says subagents 'stack below' the phase, and no test or DESIGN doc licenses the overlap. Root cause is live at HEAD: internal/canvas/canvas.go:94 padTop=60 is reused as the first child's Y offset (line 152 `y := padTop + j*gapY`) while the phase card is nodeH=120 tall (line 143).",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nD=\"$SB/sandbox/v45\"\nmkdir -p \"$D\" && cd \"$D\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\necho hi > README.md && git add -A && git commit -q -m init\nexport BATTEN_DB=\"$D/state.db\"\n\"$BAT\" init\nCWD=$(pwd -W)\n\"$BAT\" phase US-005 build\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"PostToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"batten phase US-005 build\"}}' | \"$BAT\" hook PostToolUse\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"agent-be-1\",\"agent_type\":\"backend\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"agent-db-1\",\"agent_type\":\"database\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" canvas US-005 --out \"$D/US-005.canvas\"\n# numeric rect-intersection over every non-group node pair (overlap.py, written in the sandbox):\n#   for a,b in itertools.combinations(non_group_nodes,2):\n#     ow = min(a.x+a.w, b.x+b.w) - max(a.x, b.x)\n#     oh = min(a.y+a.h, b.y+b.h) - max(a.y, b.y)\n#     if ow>0 and oh>0: report (a.id, b.id, ow, oh)\npython \"$D/overlap.py\" \"$D/US-005.canvas\"\n# second, unrelated run (different unit, different phase, different agent type):\n\"$BAT\" phase US-099 verify\necho '{\"session_id\":\"s2\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"PostToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"batten phase US-099 verify\"}}' | \"$BAT\" hook PostToolUse\necho '{\"session_id\":\"s2\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"qa-1\",\"agent_type\":\"qa\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" canvas US-099 --out \"$D/US-099.canvas\"\npython \"$D/overlap.py\" \"$D/US-099.canvas\"",
    "evidence": "$ \"$BAT\" canvas US-005 --out \"$D/US-005.canvas\"\nwrote .../sandbox/v45/US-005.canvas (5 nodes, 2 edges)\n\nverbatim excerpt of the emitted US-005.canvas:\n        {\n            \"id\": \"p-build\",\n            \"type\": \"text\",\n            \"x\": 0,\n            \"y\": 0,\n            \"width\": 300,\n            \"height\": 120,\n            \"color\": \"6\",\n            \"text\": \"## build\\n`running`\"\n        },\n        {\n            \"id\": \"n-agent-be-1\",\n            \"type\": \"text\",\n            \"x\": 0,\n            \"y\": 60,\n            \"width\": 300,\n            \"height\": 120,\n            \"color\": \"6\",\n            \"text\": \"**backend**\\n`running`\"\n        },\n\n$ python overlap.py US-005.canvas\nNODES:\n  p-build          x=0      y=0      w=300   h=120    yspan=[0,120]\n  n-agent-be-1     x=0      y=60     w=300   h=120    yspan=[60,180]\n  n-agent-db-1     x=0      y=230    w=300   h=120    yspan=[230,350]\n  header           x=-420   y=0      w=300   h=180    yspan=[0,180]\nOVERLAPPING non-group nodes (id_a,id_b,overlap_w,overlap_h): [('p-build', 'n-agent-be-1', 300, 60)]\n\n$ python overlap.py US-099.canvas   # second, unrelated run\nNODES:\n  p-verify         x=0      y=0      w=300   h=120    yspan=[0,120]\n  n-qa-1           x=0      y=60     w=300   h=120    yspan=[60,180]\n  header           x=-420   y=0      w=300   h=180    yspan=[0,180]\nOVERLAPPING non-group nodes (id_a,id_b,overlap_w,overlap_h): [('p-verify', 'n-qa-1', 300, 60)]\n\n$ batten --version -> batten 0.1.0 ; repo HEAD = b7aac14, working tree clean\n\nsource still live at HEAD (internal/canvas/canvas.go):\n   89 const (\n   90     nodeW  = 300\n   91     nodeH  = 120\n   92     gapX   = 420\n   93     gapY   = 170\n   94     padTop = 60\n   95 )\n  143     ID: p.NodeID, Type: \"text\", X: x, Y: 0, Width: nodeW, Height: nodeH,\n  152     y := padTop + j*gapY\n  164     ID: k.NodeID, Type: \"text\", X: x, Y: y, Width: nodeW, Height: nodeH,\n\nand the doc comment on Render (line 97) states the intended layout, which the numbers contradict:\n  // Render lays the run out as columns: each phase is a group, its subagents stack below it.",
    "positive_control": "n/a — no ALLOW claim; this is a geometry claim, so the control is the detector's discrimination. The SAME rect-intersection routine, over the SAME emitted file, reports NO overlap for the pairs that genuinely do not touch: n-agent-be-1 (y 60..180) vs n-agent-db-1 (y 230..350) -> 50px clear gap, and header (x -420..-120) vs everything -> no x overlap. So the detector is not a blanket 'everything overlaps' oracle; it singles out exactly the phase-card/first-child pair. Group nodes were deliberately excluded, since an Obsidian group is a container and is expected to enclose its children (g-p-build {x:-30,y:-60,w:360,h:440} does enclose them).",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "internal/canvas/canvas.go:152 — the first child's Y must clear the phase card: add a distinct constant `childTop = nodeH + 40` (=160) and use `y := childTop + j*gapY` (leaving padTop=60 as the group's top margin at line 137), then update the group height at lines 131/133 to `childTop + max(1,len(kids))*gapY - gapY + nodeH + 40`. Bumping padTop alone also drags the group's top edge to -160."
  },
  {
    "index": 46,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary in my own sandbox: `batten tui` with stdout redirected to a file emits only 96 bytes of terminal setup (alt-screen on, cursor hidden, title, clear), renders no frame, ignores `q` and Ctrl-C written to stdin, and never exits (killed by timeout at 10s, 20s and 30s — exit=124 every time). The same binary, same DB, same cwd under a real pseudo-console renders the frame and exits 0 on `q`, so the hang is attributable to the missing tty check, not to the sandbox. Code at HEAD confirms it: cmd/batten/main.go:938 cmdTUI calls tui.Run unconditionally and internal/tui/tui.go:683 Run does tea.NewProgram(...).Run() with no options and no terminal detection anywhere in the repo. Not a fail-open/advisory design decision — DESIGN.md says nothing about non-interactive TUI behaviour, and the only mentions of an isatty guard in the tree are the field-test notes themselves.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v46\" && cd \"$SB/sandbox/v46\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\ntimeout 20 \"$BAT\" init < /dev/null\n\n# --- the defect ---\nexport BATTEN_DB=\"$PWD/state.db\"\ntimeout 10 \"$BAT\" tui < /dev/null > out.txt 2>&1 ; echo \"exit=$?\" ; wc -c out.txt ; cat -v out.txt\n\n# --- 'q' and Ctrl-C written to a held-open stdin are ignored ---\nexport BATTEN_DB=\"$PWD/state.db\"\n( sleep 3; printf 'q'; sleep 3; printf '\\003'; sleep 6 ) | timeout 20 \"$BAT\" tui > out_q.txt 2>&1 ; echo \"exit=$?\" ; wc -c out_q.txt\n\n# --- not a slow start: still nothing at 30s ---\nexport BATTEN_DB=\"$PWD/state.db\"\ntimeout 30 \"$BAT\" tui < /dev/null > p3.txt 2>&1 ; echo \"exit=$?\" ; wc -c p3.txt",
    "evidence": "$ timeout 10 \"$BAT\" tui < /dev/null > out.txt 2>&1 ; echo \"exit=$?\" ; wc -c out.txt ; cat -v out.txt\nexit=124\n96 out.txt\n^[[?2026$p^[[?2027$p^[[>4m^[[=0;1u^[[?1049h^[[?25l^[[?2004h^[]2;batten M-bM-^@M-^T v46^G^[[>4;2m^[[=1;1u^[[?u^[[H^[[2J\n\n# 96 bytes vs the reporter's 104 is exactly the project-name delta in the OSC-2 title\n# (\"proyecto_ui\" 11 chars -> \"v46\" 3 chars = 8 bytes). Byte-for-byte the same stream:\n# \\e[?1049h  enter alt-screen\n# \\e[?25l    hide cursor\n# \\e[?2004h  bracketed paste on\n# \\e]2;batten - v46\\a   window title\n# \\e[H\\e[2J  home + clear\n# ...and then nothing. No frame, ever.\n\n$ ( sleep 3; printf 'q'; sleep 3; printf '\\003'; sleep 6 ) | timeout 20 \"$BAT\" tui > out_q.txt 2>&1 ; echo \"exit=$?\" ; wc -c out_q.txt\nexit=124\n96 out_q.txt\n\n$ timeout 30 \"$BAT\" tui < /dev/null > p3.txt 2>&1\nexit=124 elapsed=30s bytes=96\n\n# HEAD source, no terminal detection on the path:\ncmd/batten/main.go:938\n  func cmdTUI() error {\n      sp, st, err := load()\n      if err != nil { return err }\n      defer st.Close()\n      return tui.Run(sp, st)      // <- nothing checks os.Stdout\n  }\ninternal/tui/tui.go:683\n  func Run(sp *spec.Spec, st *store.Store) error {\n      _, err := tea.NewProgram(New(sp, st)).Run()   // <- no tea.With* options\n      return err\n  }\n# grep for term.IsTerminal / golang.org/x/term across the repo hits only\n# docs/field-test/*, i.e. the finding's own suggested fix. No guard exists.",
    "positive_control": "Two controls, both passed, so neither the sandbox nor an empty DB explains the silence. (A) Other subcommands with the identical piped stdout terminate and print: `batten runs` -> exit=0, \"no runs yet\"; `batten doctor` -> exit=0, full checklist. Only `tui` hangs. (B) The discriminating control — the SAME binary, SAME BATTEN_DB, SAME cwd under a real pseudo-console via winpty: `( sleep 4; printf 'q'; sleep 10 ) | timeout 25 winpty -Xallow-non-tty -Xplain \"$BAT\" tui` -> exit=0 and it RENDERED: \"]0;batten - v46no runs yet - run `batten phase <unit> <phase>` to start one\". Same command under winpty WITHOUT sending 'q' -> exit=124 (correctly waits for input), proving the exit in the first case was caused by 'q'. So: real console = renders + honours 'q'; pipe = renders nothing + ignores 'q' + never exits.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:938 (cmdTUI, before `return tui.Run(sp, st)`): if stdout/stdin is not a terminal, return an error instead of starting Bubble Tea — e.g. `if !isatty.IsTerminal(os.Stdout.Fd()) { return fmt.Errorf(\"batten tui needs an interactive terminal; try batten runs or batten show <unit>\") }`. mattn/go-isatty v0.0.20 is already in go.mod (indirect via bubbletea), so no new dependency."
  },
  {
    "index": 47,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary through a real ConPTY: with quota burned 17.0% of the rolling 5h window against a 15.0% cap, the left pane renders `△▓▓▓▓▓▓▓▓ 113% quota` while the right pane, same run and same frame, renders `quota   ▓▓▓▓▓▓▓▓ 17.0% / 15.0% of the 5h window`. The label and the number are demonstrably different quantities: the number is (Spent/Cap)*100 (a ratio-to-cap) while the word `quota` names quota_pct, whose native unit is already a percentage OF THE WINDOW. The positive control proves it — at 7.5% burned of a 15% cap the same line reads `50% quota` next to a detail pane reading `7.5% / 15.0%`: same label, same instant, two numbers that differ by a factor of the cap. 113% of a rolling 5-hour window is not a possible quantity, and three of the four surfaces that print this ceiling (`batten budget`, `batten statusline`, the TUI detail pane) all print the window-share; only tui.go:346 prints the cap-ratio under the same word.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v47\" && cd \"$SB/sandbox/v47\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\nprintf 'placeholder\\n' > README.md && git add -A && git commit -qm init\n\"$BAT\" init\n# add the quota ceiling to the generated batten.yaml:\n#   budget:\n#     tokens_per_run: 3000000\n#     imputed_usd_per_run: 8.0\n#     quota_pct_per_run: 15\n#     on_exceed: warn\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" phase US-005 verify\n# baseline the 5h window at 10%\nexport BATTEN_DB=\"$PWD/state.db\"\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$PWD\"'\",\"workspace\":{\"current_dir\":\"'\"$PWD\"'\"},\"rate_limits\":{\"five_hour\":{\"used_percentage\":10.0,\"resets_at\":1738425600}}}' | \"$BAT\" statusline\n# burn to 27% => 17.0 points burned against a 15.0 cap\nexport BATTEN_DB=\"$PWD/state.db\"\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$PWD\"'\",\"workspace\":{\"current_dir\":\"'\"$PWD\"'\"},\"rate_limits\":{\"five_hour\":{\"used_percentage\":27.0,\"resets_at\":1738425600}}}' | \"$BAT\" statusline\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" budget\n# drive `batten tui` through a real ConPTY (pywinpty), 120x40, capture the frame, send 'q'\nexport BATTEN_DB=\"$PWD/state.db\"; python drive_tui.py   # PtyProcess.spawn([BAT,\"tui\"], dimensions=(40,120)); read; write('q'); strip ANSI\n# CONTROL: drop the burn to 7.5 of 15 and re-render\nexport BATTEN_DB=\"$PWD/state.db\"\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$PWD\"'\",\"workspace\":{\"current_dir\":\"'\"$PWD\"'\"},\"rate_limits\":{\"five_hour\":{\"used_percentage\":17.5,\"resets_at\":1738425600}}}' | \"$BAT\" statusline\nexport BATTEN_DB=\"$PWD/state.db\"; python drive_tui.py",
    "evidence": "$ batten budget\nUS-005  0 tokens, $0.00 imputed\n  · tokens       0 / 3.0M  [............]\n  · imputed_usd  $0.00 / $8.00  [............]\n  ! quota_pct    17.0% / 15.0% of the rolling 5h window  [============]\n      OVER — on_exceed=warn\n\n$ batten tui   (ConPTY 120x40, ANSI stripped — left pane | right pane, one frame)\nbatten  v47 · 1 runs\n│ ▸ ◐ US-005    verify        ││ tokens  ░░░░░░░░ 0 / 3.0M│\n│     ▓▓▓▓▓▓▓▓ 113% quota   ││ imputed ░░░░░░░░ $0.00 / $8.00 imputed│\n│     0 · $0.00 imputed        ││ quota   ▓▓▓▓▓▓▓▓ 17.0% / 15.0% of the 5h window│\n\n(the same two panes, same frame: the left says 113%, the right says 17.0%, and both\n call the quantity \"quota\")",
    "positive_control": "Not an ALLOW claim, so the hook-silence control does not apply; instead I ran the control the crux demands — proving the left-pane number and the label are different quantities rather than a stuck/garbage value. Dropped the burn to 7.5% of the window against the same 15.0% cap and re-rendered the TUI through the same ConPTY harness:\n  $ batten statusline  ->  batten US-005 verify · no verdict: commit DENIED · quota 7.5/15% · 5h 18%\n  $ batten budget      ->    · quota_pct    7.5% / 15.0% of the rolling 5h window  [======......]\n  $ batten tui         ->  │     ▓▓▓▓░░░░  50% quota   ││ quota   ▓▓▓▓░░░░ 7.5% / 15.0% of the 5h window│\nThe left pane moved 113 -> 50 while the detail pane moved 17.0 -> 7.5. The left number therefore tracks Spent/Cap (17/15=113%, 7.5/15=50%) and the label `quota` names the window-share the right pane prints. Different quantities, one word. The render path is live and responsive, so the 113% frame is not a stale or constant artefact.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "internal/tui/tui.go:346 — bindingLine formats every kind as `%3.0f%% <kindLabel>`, which collides for quota_pct because its own unit is a percent. Render the pair for that kind (reuse amounts(c), or emit `17.0/15.0% quota`), or suffix the ratio with `of cap`."
  },
  {
    "index": 48,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on HEAD (b7aac14): the TUI header renders 'batten  proyecto_ui · 1 runs' when the store holds exactly one run. The HEAD binary's string table still contains the literal format '  %s · %d runs', and internal/tui/tui.go:294 is byte-identical to the repo worktree, which is clean at HEAD. Not by-design (Trap 1 does not apply: no fail-open, no advisory, no NOT-MEASURABLE claim) — and the same package already ships a plural() helper that returns exactly '1 run', so the codebase's own convention is being violated one line away from the helper that implements it.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v48\" && cd \"$SB/sandbox/v48\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\necho hello > a.txt && git add -A && git commit -q -m init\n\"$BAT\" phase U-1 build\n\"$BAT\" runs                      # -> exactly ONE run in the store\n\n# (a) the HEAD BINARY itself still carries the ungrammatical format string:\ngrep -a -o -P '.{0,25}%d runs.{0,25}' \"$SB/bin/batten.exe\"\n\n# (b) `batten tui` cannot paint into a pipe (bubbletea v2 emits only the startup\n#     capability queries + the window title, 96 bytes, no frame) and winpty in this\n#     environment refuses with 'stdin is not a tty', so the frame was obtained by\n#     calling the very same Model.render() the binary calls, on a byte-identical\n#     copy of the HEAD worktree:\ncp -r $REPO/. \"$SB/sandbox/v48/repo\"/ && rm -rf \"$SB/sandbox/v48/repo/.git\"\ndiff -q $REPO/internal/tui/tui.go \"$SB/sandbox/v48/repo/internal/tui/tui.go\"   # identical\n# added $SB/sandbox/v48/repo/internal/tui/v48_header_test.go (sandbox copy only; the\n# repo under test was NOT modified): spec project=proyecto_ui, ONE EnsureRun, then\n# strip(m.render()) and print the first line verbatim.\ncd \"$SB/sandbox/v48/repo\" && go test ./internal/tui/ -run TestV48HeaderWithExactlyOneRun -v",
    "evidence": "$ \"$BAT\" runs\nUNIT  STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nU-1   running  build  —        —       —          <- exactly one run\n\n$ grep -a -o -P '.{0,25}%d runs.{0,25}' bin/batten.exe\nd CONTRIBUTING.md  %s · %d runsiters   %d / %d%-7s %s n/     <- HEAD binary, verbatim\n\n$ go test ./internal/tui/ -run TestV48HeaderWithExactlyOneRun -v\n=== RUN   TestV48HeaderWithExactlyOneRun\nV48-HEADER-1RUN>>>batten  proyecto_ui · 1 runs<<<\nV48-HEADER-2RUN>>>batten  proyecto_ui · 2 runs<<<\nV48-PLURAL-HELPER-EXISTS>>>plural(1,\"run\")=\"1 run\" plural(2,\"run\")=\"2 runs\"<<<\n--- PASS: TestV48HeaderWithExactlyOneRun (0.04s)\nok  \tgithub.com/ArthurZizumbo/batten/internal/tui\t1.343s\n\n$ diff -q .../LoopWorkFlow/internal/tui/tui.go .../sandbox/v48/repo/internal/tui/tui.go\nTUI.GO IDENTICAL TO HEAD WORKTREE\n$ git status --porcelain internal/tui/tui.go\n(empty — tui.go is clean at HEAD b7aac1407f6f21065bfa9185751eaaa0b98b6908)\n\ninternal/tui/tui.go:293-294\n\thead := stTitle.Render(\"batten\") +\n\t\tstDim.Render(fmt.Sprintf(\"  %s · %d runs\", m.spec.Project, len(m.runs)))\n\ninternal/tui/tui.go:674-679  (the helper that already exists, unused here)\n\tfunc plural(n int, noun string) string {\n\t\tif n == 1 {\n\t\t\treturn \"1 \" + noun\n\t\t}\n\t\treturn strconv.Itoa(n) + \" \" + noun + \"s\"\n\t}",
    "positive_control": "n/a — no ALLOW claim, no gate involved (Trap 2 does not apply). Two controls were run anyway to prove the render path was live and format-sensitive rather than a canned string: (1) the SAME code path with 2 runs prints 'proyecto_ui · 2 runs', so the count is real and only n=1 is wrong; (2) plural(1,\"run\") in the same package returns '1 run', proving the correct rendering exists and is simply not called at line 294. Negative control on the delivery path: `batten tui` piped to a file produced only 96 bytes of terminal-capability queries plus the OSC window title ']2;batten — v48' and no frame, and winpty refused with 'stdin is not a tty' — which is why the frame was taken from Model.render() directly instead of from a screen scrape.",
    "already_fixed_at_head": false,
    "severity_adjusted": "polish",
    "fix_hint": "internal/tui/tui.go:294 — replace fmt.Sprintf(\"  %s · %d runs\", m.spec.Project, len(m.runs)) with fmt.Sprintf(\"  %s · %s\", m.spec.Project, plural(len(m.runs), \"run\")); plural() is already defined at tui.go:674 in the same package."
  },
  {
    "index": 49,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary, twice, in two clean sandboxes. With enforcement: enforce, a close phase carrying requires_verdict: ok, on branch feature/TASK-002, and ZERO runs in the DB, `batten hook PreToolUse` on a `git commit` payload printed nothing and exited 0; the real `git commit` then succeeded. The paired positive control — the BYTE-IDENTICAL payload replayed in the SAME sandbox with the only delta being that a run row now exists — returned a hard permissionDecision:deny. So the harness was engaged and the silence is a genuine ungated allow, at exactly the site named in the report (internal/hooks/hooks.go:284-286). I also proved the unit WAS attributable in the silent state, ruling out the innocent `unit == \"\"` explanation: a SubagentStart payload sent against the same zero-run DB created a run named TASK-002, and with an empty DB the only possible source of that name is MatchUnit(gitBranch) — i.e. activeUnit() returns \"TASK-002\" with zero runs, and the silent return is the ActiveRun miss. I DO adjust the severity down from blocker to major and reclassify the defect: the fail-open itself is deliberate (docs/field-test/TEST-MATRIX.md:207 VG-D3 pre-declares it), so the defect that survives adversarial scrutiny is the SILENCE, not the allow. That is not covered by the design defence, because the codebase states principle #3 verbatim at hooks.go:343 — 'fail open only with a warning', and 'a gate that silently degrades to trusting the model is worse than no gate, because it will be believed' — and honours it with an advise() under `enforcement: enforce` one branch later for the milder checkless-gate case, while the more dangerous no-run case returns bare nil, nil. Aggravating: SessionStart is ALSO silent at zero runs (hooks.go:565-568 returns early on len(runs)==0), so the banner that would otherwise print 'no verdict yet — the close gate will deny a commit' never appears either. Nothing anywhere tells the newcomer the gate abstained, which is precisely the condition TEST-MATRIX VG-D3 says to classify as a defect.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nrm -rf \"$SB/sandbox/v49b\"; mkdir -p \"$SB/sandbox/v49b\"; cd \"$SB/sandbox/v49b\"\ngit init -q . ; git config user.email t@t.io; git config user.name tester\nWD=$(pwd -W); export BATTEN_DB=\"$WD/state.db\"\nprintf 'hello\\n' > README.md; git add -A; git commit -q -m init\n\"$BAT\" init\nsed -i 's/^enforcement: report/enforcement: enforce/' batten.yaml   # the finding's stated precondition\ngit checkout -q -b feature/TASK-002\ncat > p_commit.json <<EOF\n{\"session_id\":\"newcomer-session\",\"hook_event_name\":\"PreToolUse\",\"cwd\":\"$WD\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\"feat(TASK-002): mark a task done\\\"\"}}\nEOF\ncat > p_session.json <<EOF\n{\"session_id\":\"newcomer-session\",\"hook_event_name\":\"SessionStart\",\"cwd\":\"$WD\",\"source\":\"startup\"}\nEOF\ncat > p_sub.json <<EOF\n{\"session_id\":\"probe-session\",\"hook_event_name\":\"SubagentStart\",\"cwd\":\"$WD\",\"agent_id\":\"a1\",\"agent_type\":\"probe\"}\nEOF\n# --- STATE A: zero runs ---\n\"$BAT\" runs\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" hook SessionStart  < p_session.json ; echo \"exit=$?\"\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" hook PreToolUse    < p_commit.json  ; echo \"exit=$?\"\nprintf 'done\\n' >> README.md; git add -A; git commit -q -m \"feat(TASK-002): mark a task done\"; git log --oneline -1\n# --- proof the unit WAS attributable while the DB was still empty ---\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" hook SubagentStart < p_sub.json\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" runs\n# --- STATE B / POSITIVE CONTROL: byte-identical payload, only delta = the run row ---\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" hook PreToolUse    < p_commit.json  ; echo \"exit=$?\"\n\n# second, independent control in sandbox v49 using the CLI instead of a hook to open the run:\n#   \"$BAT\" runs                      -> no runs yet ; hook PreToolUse -> (silence) exit=0\n#   \"$BAT\" phase TASK-002 build      -> anchor: TASK-002 base SHA = be9650c\n#   hook PreToolUse (same payload)   -> permissionDecision:deny",
    "evidence": "### 0) runs:\nno runs yet\n### 1) SessionStart @ zero runs:\nout=[] exit=0\n### 2) PreToolUse git commit @ zero runs:\nout=[] exit=0\n### 3) the real commit then proceeds:\nCOMMIT SUCCEEDED: b664a7b feat(TASK-002): mark a task done\n\n### 4) SubagentStart on a ZERO-run DB (proves branch attribution alone yields the unit):\nout=[] exit=0\nUNIT      STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nTASK-002  running           -        -       -\n\n### 5) POSITIVE CONTROL: byte-identical p_commit.json replayed, only delta = the run row now exists:\nout=[{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-002 has no verdict envelope. Run the \\\"verify\\\" phase before committing.\\nTo proceed anyway (recorded in the audit log): batten override TASK-002 --reason \\\"...\\\"\"}}]\nexit=0\n\n--- independent second control, sandbox v49, run opened via the CLI rather than a hook ---\n=== STATE A: ZERO RUNS ===\nstdout+stderr=[]\nexit=0\n=== create a run (the ONLY change) ===\nanchor: TASK-002 base SHA = be9650c\nTASK-002 -> phase build\nUNIT      STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nTASK-002  running  build  -        -       -\n=== STATE B: SAME payload, SAME sandbox, run now exists ===\nstdout+stderr=[{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\", ...}}]\n\n--- HEAD source, the site is unchanged ---\ninternal/hooks/hooks.go:280-286\n\tunit := h.activeUnit(in)\n\tif unit == \"\" {\n\t\treturn nil, nil // cannot attribute this commit to a unit: do not block\n\t}\n\trun, err := h.Store.ActiveRun(h.Spec.Project, unit)\n\tif err != nil {\n\t\treturn nil, nil // no run recorded: the gate has nothing to say\n\t}\n\ninternal/hooks/hooks.go:343-347 (the SAME function, one branch later, honouring principle #3)\n\t// But it passes OUT LOUD. Principle #3 is \"fail open only with a warning\", and a gate\n\t// that silently degrades to trusting the model is worse than no gate, because it will be\n\t// believed. This is an advise() even under `enforcement: enforce` ...\n\t\treturn advise(\"PreToolUse\", ...)\n\ninternal/hooks/hooks.go:565-568 (why nothing else warns either)\n\truns, err := h.Store.ListRuns(h.Spec.Project, 5)\n\tif err != nil || len(runs) == 0 {\n\t\treturn nil, nil\n\t}\n\ninternal/hooks/hooks.go:588-590 (the project's own words, violated by the zero-run path)\n\t// ... an unbound session means the gates can't act, and a silent non-gate is worse than a\n\t// loud one.",
    "positive_control": "REQUIRED and RUN, twice, both passing. Control 1 (sandbox v49b): the byte-identical p_commit.json that was silent at zero runs was replayed after a SubagentStart hook opened a run for TASK-002 — it returned permissionDecision:deny ('TASK-002 has no verdict envelope'). Control 2 (sandbox v49, independent DB): same payload silent at zero runs, then `batten phase TASK-002 build` opened the run, then the same payload denied. Because both controls DENY on the same payload, in the same sandbox, on the same branch, with the only delta being the existence of a run row, the hook demonstrably engages and the silence in state A is a real ALLOW, not a store failure, a malformed payload, a recovered panic, an unknown event, or a missing spec. Third control, aimed at the specific innocent explanation the brief warns about: SubagentStart against the still-empty DB created a run whose unit is 'TASK-002', a name that with zero rows in the DB can only have come from MatchUnit(gitBranch) — so activeUnit() != \"\" in state A, and the silent return is provably line 285-286 (ActiveRun miss), not line 281-282 (unattributable). This is also NOT finding 20: `batten runs` printed 'no runs yet' immediately before the silent commit, so there were ZERO runs, not two-or-more unowned ones.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/hooks/hooks.go:284-286 — when activeUnit() resolved a unit but Store.ActiveRun returns no row, return advise(\"PreToolUse\", \"batten: this branch names <unit> but no run was ever opened for it, so the close gate cannot act. Run: batten phase <unit> <phase>\") instead of bare `return nil, nil`; keep the silent path only for unit == \"\". Same one-line treatment for hooks.go:565-568 so the SessionStart banner says something at zero runs."
  },
  {
    "index": 50,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary in a trunk-only repo (branch main, no feature branch ever created). activeUnit never matches the commit message, and verdictGate has `cmd` in hand: (B3) a commit whose message says feat(TASK-002) was denied as TASK-001, and (C2) with the session bound to a batten-verified TASK-001, a `feat(TASK-002)` commit was ALLOWED silently although TASK-002 had an open run with no verdict at all — the byte-identical payload with only session_id changed was DENIED, so the ALLOW is real. SAME ROOT CAUSE as finding 22 (activeUnit ignores the commit message), seen from trunk; two of the finding's own sub-claims do NOT hold: trunk is not ungated per se (with exactly one open unowned run the gate fired on main), and SessionStart does surface the ambiguity at HEAD.",
    "repro": "SB=.../scratchpad/ft; cd $SB/sandbox/v50\ngit init -q .; git symbolic-ref HEAD refs/heads/main   # trunk-only: no feature branch is ever created\nmkdir -p src docs; git add -A; git commit -qm init\nexport BATTEN_DB=$PWD/a.db; $SB/bin/batten.exe init; sed -i 's/^enforcement: report/enforcement: enforce/' batten.yaml; git add -A; git commit -qm spec\n# --- POSITIVE CONTROL A1: does the gate fire at all on trunk? one open unowned run ---\n$SB/bin/batten.exe phase TASK-001 close\ncat > p_a.json <<EOF\n{\"session_id\":\"sess-A\",\"hook_event_name\":\"PreToolUse\",\"cwd\":\"$PWD\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\"feat(TASK-001): first slice\\\"\"}}\nEOF\n$SB/bin/batten.exe hook PreToolUse < p_a.json          # -> DENY (gate proven to fire on main)\n# --- B: two open units on trunk, fresh session, message names TASK-002 ---\nexport BATTEN_DB=$PWD/b.db; $SB/bin/batten.exe phase TASK-001 close; $SB/bin/batten.exe phase TASK-002 close\ncat > p_b.json <<EOF\n{\"session_id\":\"sess-B\",\"hook_event_name\":\"PreToolUse\",\"cwd\":\"$PWD\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m \\\"feat(TASK-002): second slice\\\"\"}}\nEOF\n$SB/bin/batten.exe hook PreToolUse < p_b.json; echo EXIT=$?   # B1 -> silence, exit 0\n$SB/bin/batten.exe hook SessionStart < p_ss.json               # B2 -> the ambiguity IS surfaced (contra the report)\n# CONTROL for B1: identical commit message, cwd = worktree on branch feature/TASK-002\ngit worktree add -q -b feature/TASK-002 wt/f2; cd wt/f2\nBATTEN_DB=../../b.db $SB/bin/batten.exe hook PreToolUse < ../../p_b_wt.json   # -> DENY TASK-002\ncd ../..; $SB/bin/batten.exe hook PreToolUse < p_b.json        # B3 -> DENY, but names TASK-001\n# --- C: the escalation, one session, one branch (main), two tickets in a row ---\nsed -i \"s|^    checks: \\[\\]|    checks: ['git --version']|\" batten.yaml\nexport BATTEN_DB=$PWD/c.db; $SB/bin/batten.exe phase TASK-001 close; $SB/bin/batten.exe check TASK-001   # ok* batten-verified\n$SB/bin/batten.exe hook PreToolUse < p_c1.json     # C1 allow, binds sess-C -> TASK-001\n$SB/bin/batten.exe phase TASK-002 close\n$SB/bin/batten.exe hook PreToolUse < p_c2.json     # C2 -> SILENT ALLOW for an unverified TASK-002\n$SB/bin/batten.exe hook PreToolUse < p_c2_ctl.json # CONTROL: same bytes, session_id=sess-Z -> DENY TASK-002\n$SB/bin/batten.exe hook PostToolUse < p_c2_post.json; $SB/bin/batten.exe runs   # C4 -> the TASK-002 commit closes TASK-001's run",
    "evidence": "== A1 (POSITIVE CONTROL, branch main, one open unowned run) ==\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-001 has no verdict envelope. ...\"}}\n  -> so the gate DOES fire on trunk; the report's blanket \"trunk-based repos get no gate\" is too strong.\n\n== B1 (branch main, 2 open runs, fresh session, message says TASK-002) ==\nEXIT=0            (no output at all)\n== B1 CONTROL (identical commit message, only the cwd's BRANCH differs: worktree on feature/TASK-002) ==\n{\"...\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-002 has no verdict envelope. ...\"}\n  -> same DB, same message: the branch resolves the unit, the commit message never does.\n\n== B2 SessionStart on main, same fresh session ==\n\"## batten — v50 (TASK)\\n\\n- **TASK-002** — phase `close`, ... **no verdict yet** ...\\n- **TASK-001** — ...\\n\\n\\u26a0 2 units are open (TASK-001, TASK-002) and this session isn't bound to one.\\n   The gates can't act until you bind it: ...\"\n  -> REFUTES the report's sub-claim (c): the silence IS made visible at HEAD.\n\n== B3 (branch main, message says TASK-002, TASK-001 is the only unowned run) ==\n{\"...\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-001 has no verdict envelope. ...\"}\n  -> the denial names TASK-001 for a commit that says TASK-002 (this is finding 22's failure, on trunk).\n\n== C1/C2 (ONE session, ONE branch main, TASK-001 batten-verified, TASK-002 open with no verdict) ==\nC2:         EXIT=0   (no output — ALLOW)\nC2-CONTROL: {\"...\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-002 has no verdict envelope. ...\"}\n  -> byte-identical payload except session_id, so C2's silence is a genuine ALLOW, not a dead hook.\n\n== C3 SessionStart, same session ==\n\"- **TASK-002** — phase `close`, status `running`, **no verdict yet** — the close gate will deny a commit\\n- **TASK-001** — ... verdict `ok` (1 evidence)\\n\\n\\u2192 this session is working **TASK-001**.\"\n  -> batten promises in writing that a TASK-002 commit will be denied, then approves it.\n\n== C4 PostToolUse for that same TASK-002 commit ==\nUNIT      STATUS   PHASE  VERDICT\nTASK-002  running  close  —\nTASK-001  ok       close  ok*\n  -> the TASK-002 commit closed TASK-001's run (postToolUse also resolves the unit without reading the message).\n\n== code ==\ninternal/hooks/hooks.go:626-668 activeUnit: MatchUnit is applied to gitBranch(dir) only.\ngrep MatchUnit over the repo: hooks.go:212 (`batten phase <UNIT>` arg) and hooks.go:640 (branch). Never the commit message, and there is no UserPromptSubmit handler either — so of the three sources batten.schema.json:40 advertises (\"a branch name, a prompt, a commit message\") only the branch is implemented.",
    "positive_control": "Three controls, all passed. (1) A1: on branch main with one open unowned run the same shape of payload DENIED, so the hook and the gate are live on trunk. (2) B1's control: the SAME commit message with only the cwd's branch changed (worktree on feature/TASK-002, same b.db) DENIED naming TASK-002, while on main it was silent — isolating the branch as the only resolution signal. (3) C2's control: a byte-identical payload with only session_id changed DENIED with 'TASK-002 has no verdict envelope', proving C2's silence is a real ALLOW and not one of Trap 2's six silences.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "internal/hooks/hooks.go:280 (verdictGate already holds `cmd`) and hooks.go:224 (postToolUse close path): before trusting activeUnit, do `if u := h.Spec.MatchUnit(cmd); u != \"\" && h.Store has an open run for u { unit = u }` — message-named unit wins over branch/fallback; keep activeUnit when the message names none."
  },
  {
    "index": 51,
    "verdict": "REFUTED",
    "reason": "REFUTED AS DUPLICATE of finding 17 (already CONFIRMED); finding 37 was already refuted as the same duplicate, making this the third restatement. The raw facts reproduce verbatim on the HEAD binary — `batten frobnicate` prints the same 16-command usage block and `grep -c 'check\\|close'` returns 0 — but this is the identical defect at the identical source site with the identical fix. f51's own `file_hint` is character-for-character f17's fix site: cmd/batten/main.go:99-118 (printUsage) vs cmd/batten/main.go:79-86 (the dispatch cases), which I re-confirmed at HEAD. The one incremental claim, 'and from the README', does not stand as an independent defect: (a) the README is not a command reference — seven commands that ARE listed in `--help` (hook, phase, runs, show, tui, mcp, ingest) are equally absent from it, so absence-from-README does not discriminate check/close from the documented set; (b) the README's own 'The commands' table (lines 243-250) is the slash-command surface and it does carry `/batten-verify` ('the gate: check the diff against the criteria') and `/batten-close` ('provenance, resolution artifact, and the commit the gate must allow') — the two lifecycle steps this finding says the adoption path lacks; (c) both verbs are documented elsewhere at HEAD — CHANGELOG.md:99-101 ships them as user-facing features, docs/VERIFICATION.md R1/R2 exercises them, and internal/hooks/hooks.go:333 plus doctor's stale-run warning name them at runtime. So the load-bearing half is 17's, and the residue is one README line inside 17's own fix, not a second defect.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v51\" && cd \"$SB/sandbox/v51\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n# 1. the finding's own command, verbatim\n\"$BAT\" frobnicate 2>&1; echo \"EXIT=$?\"\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" --help 2>&1 | grep -c 'check\\|close'; echo \"GREP_PIPE_EXIT=${PIPESTATUS[1]}\"\n# 2. dispatch control — is `check`/`close` really a verb, or the default branch?\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" bogus-verb >/dev/null 2>&1; echo \"BOGUS=$?\"\n\"$BAT\" check >/dev/null 2>&1; echo \"CHECK_NOARG=$?\"\n\"$BAT\" close >/dev/null 2>&1; echo \"CLOSE_NOARG=$?\"\necho hi > README.md && git add -A && git commit -q -m init\n\"$BAT\" init >/dev/null 2>&1\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" check 2>&1; \"$BAT\" close 2>&1\n# 3. the ONLY incremental claim: the README surface\ncd $REPO\nfor c in init doctor hook phase claim verdict runs show canvas budget override tui mcp statusline ingest check close measure; do echo \"README 'batten $c': $(grep -o -E \"batten $c\\b\" README.md | wc -l)\"; done\nsed -n '241,253p' README.md\ngrep -rn -E 'batten (check|close)\\b' *.md docs/*.md\ngrep -n 'case \"measure\"\\|case \"close\"\\|case \"check\"\\|case \"hook-debug\"\\|func printUsage' cmd/batten/main.go",
    "evidence": "$ batten frobnicate\nbatten — procedural memory as data\n\n  batten init [--from <prose-doc>]   generate batten.yaml\n  batten doctor                      validate spec, report live capabilities\n  batten hook <event>                hook entrypoint (stdin JSON -> stdout JSON)\n  batten phase <unit> <phase>        advance a unit to a phase (records the anchor SHA)\n  batten claim <agent-id> <file>...  declare a subagent's write-set\n  batten verdict [--file v.json]     record a verdict envelope\n  batten runs                        list runs\n  batten show <unit>                 run detail\n  batten canvas <unit> [--out p]     emit the run DAG as JSON Canvas\n  batten budget [<unit>]             governor status (tokens / imputed $ / quota)\n  batten override <unit> --reason    audited escape from the gate\n  batten tui                         review runs in a terminal UI\n  batten mcp                         MCP server (exposes the run graph to the agent)\n  batten statusline [--install]      status line + the only subscription-quota sensor\n  batten ingest <unit> --transcript  price a transcript's tokens into the run ledger\nEXIT=2\n\n$ batten --help 2>&1 | grep -c 'check\\|close'\n0\nGREP_PIPE_EXIT=1              <-- f51's raw --help fact is TRUE at HEAD ... and is f17's, verbatim\n\n--- source at HEAD: ONE site, and it is f17's site ---\n 79:\tcase \"measure\":\n 81:\tcase \"close\":\n 83:\tcase \"check\":\n 85:\tcase \"hook-debug\":\n 99: func printUsage() {\nf51 file_hint  : \"cmd/batten/main.go:99-118 (printUsage) vs cmd/batten/main.go:79-86 (the dispatch cases)\"\nv17 fix_hint   : \"cmd/batten/main.go:116 (end of printUsage's list) — add two lines ...\"\n-> same literal, same two lines, same patch.\n\n--- THE INCREMENTAL CLAIM: 'and from the README' — does not discriminate ---\n$ for c in ...; do echo \"README 'batten $c': $(grep -o -E \"batten $c\\b\" README.md | wc -l)\"; done\nREADME mentions 'batten hook': 0\nREADME mentions 'batten phase': 0\nREADME mentions 'batten runs': 0\nREADME mentions 'batten show': 0\nREADME mentions 'batten tui': 0\nREADME mentions 'batten mcp': 0\nREADME mentions 'batten ingest': 0\nREADME mentions 'batten check': 0\nREADME mentions 'batten close': 0\n-> SEVEN commands that ARE in --help are equally absent from the README. The README is a\n   narrative pitch, not a command reference; 'absent from README' is its genre, not a\n   divergence, and it separates check/close from nothing.\n\n--- and the README's actual 'The commands' table DOES carry the two lifecycle steps ---\nREADME.md:241  ## The commands\nREADME.md:248  | `/batten-verify` | the gate: check the diff against the criteria, emit a verdict with cited evidence |\nREADME.md:249  | `/batten-close` | provenance, resolution artifact, and the commit the gate must allow |\nREADME.md:250  | `/batten-night` | unattended: build → verify → fix → re-verify, stopping before the close |\n\n--- and both verbs ARE documented at HEAD, outside README ---\nCHANGELOG.md:99   - **`batten check`** runs a gate's checks for real and records the verdict with `source: batten`.\nCHANGELOG.md:101  - **Run lifecycle** — `batten close`, auto-close when a commit lands in the close phase, and a\ndocs/VERIFICATION.md:72  | R1 `batten close --status ok` sin veredicto | ✓ rechazado |\ndocs/VERIFICATION.md:74  | R2 `batten check` corre checks reales; uno falla → blocked citando el error de git | ✓ |\ninternal/hooks/hooks.go:333  \"Run: batten check %s\\n\"   <-- the runtime deny names it\n\n--- overlap audit f51 vs f17 ---\n  same two commands (check, close)              -> yes\n  same binary surface (the printUsage block)    -> yes, byte-identical output\n  same source site (printUsage 99-118)          -> yes, f51's own file_hint\n  same root cause (list not synced to switch)   -> yes\n  same fix (append lines to one string literal) -> yes\n  `measure`/`hook-debug` claim new?             -> NO — in v17 evidence + fix_hint, and in f37\n  'adoption path' framing new?                  -> rhetoric, not a new surface; f17 already\n                                                   filed the deny-message adoption angle\n  README claim new?                             -> new words, no new defect (see above)",
    "positive_control": "n/a — f51 makes no ALLOW/deny claim, so Trap 2 does not apply. But I ran the control the brief's spirit demands, because '--help lacks X' is only a defect if X is genuinely a command: DISPATCH CONTROL — `batten bogus-verb` -> exit 2 and prints the usage block (the `default:` branch). `batten check` (no args) -> exit 1 and prints its OWN usage, `batten: check: batten check <unit> [--gate <name>]`. `batten close` (no args) -> exit 1, `batten: close: batten close <unit> [--status ok|failed|rolled_back]`. Different exit code AND different text, so cmdCheck/cmdClose genuinely ran rather than falling through to printUsage. The underlying omission is therefore real — it is simply the omission finding 17 already owns. Second control, on the README half: I did not merely grep for check/close (which would have 'proved' the finding); I greped for all 18 dispatched verbs, which is what showed the README omits seven commands it has no complaint about — the test that discriminates rather than confirms.",
    "already_fixed_at_head": false,
    "severity_adjusted": "not-a-defect",
    "fix_hint": "",
    "notes": "Explicitly NOT refuted on Trap 1 (no warn-vs-deny is involved) or Trap 2 (no ALLOW claim) grounds, and NOT as ALREADY-FIXED — the printUsage omission is still present in HEAD source and I reproduced it verbatim. It is refuted purely as the THIRD restatement of finding 17 (37 was the second). Counting 51 would triple-count one Go string literal.\n\nRESIDUE, disclosed so the parent does not lose it: there IS one genuine README defect in this neighbourhood, but f51 does not claim it. f51's `expected` is a pure listing claim ('none appear in printUsage() and none appear in README.md'). The real README defect is different: README.md:58-66 shows the worked path `batten verdict --unit US-034 --file verdict.json` -> `git commit` SUCCEEDING, while the README's own sample spec at README.md:145 declares `checks: ['make check', 'make test']` — and a gate with declared checks makes the commit hook demand a source=batten verdict, i.e. a `batten check` step the worked example omits. That is a wrong-example defect, not a missing-list-entry defect, it is already filed separately (docs/field-test/FINDINGS-RAW.md:497, raw #24, which says verbatim 'Either the commit succeeds as documented, or the README documents the `batten check` step'), and I did NOT reproduce it here because it is outside f51's claim — do not read this note as verification of it.\n\nRecommendation: fold 51 into 17 alongside 37. When fixing 17, add all four unlisted verbs to printUsage (`check`, `close`, `measure`, and either list `hook-debug` or mark it deliberately internal) so no fourth duplicate can be filed against the same literal.\n\nIsolation: BATTEN_DB was re-exported to sandbox/v51/state.db in EVERY Bash call and the 118 KB state.db in that sandbox is the only DB written; ~/.batten/ was never targeted and no hook-debug tap was enabled. No file in the repo under test was modified (all repo access was read-only: Read/Grep/sed/grep). $HOME/Proyectos/MNA/proyecto_ui was never touched. No git push, no remote added."
  },
  {
    "index": 52,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary: with a gate check that exits 1, `batten check TASK-003` prints \"TASK-003: BLOCKED (batten-verified)\" and \"the commit gate will deny until this passes.\" and then exits 0 — the identical status a fully PASSING gate returns. `batten check ... && echo green` therefore prints green on a blocked unit, and the same call under `set -e` does not stop the script. cmd/batten/main.go:786-803 is unchanged at HEAD: cmdCheck computes result=\"blocked\", saves the verdict, prints, and `return nil`. NOT already fixed. Mitigation, measured not assumed: the in-session PreToolUse commit gate does not read this exit code — it reads the persisted verdict from the store and denied TASK-003 correctly — and no CI workflow, script, plugin command or skill in the repo invokes `batten check` today, so the blast radius is user-written CI/Makefile/pre-commit wiring plus anyone driving batten from a terminal, where `batten check` is the only enforcement available. Trap 1 does not rescue this: nothing here is a missing declaration or an unmeasurable quantity — batten DID run the checks, DID observe a failure, and then reported success to its caller.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"; WD=\"$SB/sandbox/v52\"\nmkdir -p \"$WD\" && cd \"$WD\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$WD/state.db\"\n\"$BAT\" init\n# edit batten.yaml: enforcement: enforce; gates.qa.checks:\n#   - \"echo go-vet-ok\"\n#   - \"echo FAIL-TestAddRejectsBlankTitle && exit 1\"\ngit add -A && git commit -q -m init\ngit checkout -q -b feature/TASK-003\nexport BATTEN_DB=\"$WD/state.db\"\n\"$BAT\" check TASK-003; echo \"exit=$?\"\nexport BATTEN_DB=\"$WD/state.db\"\n\"$BAT\" check TASK-003 >/dev/null 2>&1 && echo \"CI SAYS: gate green -> proceed\" || echo \"CI SAYS: gate red -> stop\"\nexport BATTEN_DB=\"$WD/state.db\"\n( set -e; \"$BAT\" check TASK-003 >/dev/null 2>&1; echo \"set -e did NOT stop -> script proceeds\" )\n# downstream check: does the commit gate itself depend on the exit code?\nexport BATTEN_DB=\"$WD/state.db\"\necho '{\"session_id\":\"s3\",\"cwd\":\"'\"$WD\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git commit -m x\"}}' | \"$BAT\" hook PreToolUse",
    "evidence": "$ batten check TASK-003; echo \"exit=$?\"\n  ✓ echo go-vet-ok\n  ✗ echo FAIL-TestAddRejectsBlankTitle && exit 1 (exit 1)\n    FAIL-TestAddRejectsBlankTitle\n\nTASK-003: BLOCKED (batten-verified). one or more gate checks failed\nthe commit gate will deny until this passes.\nexit=0\n\n$ batten check TASK-003 >/dev/null 2>&1 && echo \"CI SAYS: gate green -> proceed\" || echo \"CI SAYS: gate red -> stop\"\nCI SAYS: gate green -> proceed to deploy/commit\n\n$ ( set -e; batten check TASK-003 >/dev/null 2>&1; echo \"...\" )\nset -e did NOT stop -> script proceeds to the next step\n\n--- cmd/batten/main.go:786-803 at HEAD (b7aac14), verbatim, unchanged ---\n\tresult := \"blocked\"\n\twhy := \"one or more gate checks failed\"\n\t...\n\tfmt.Printf(\"\\n%s: %s (batten-verified). %s\\n\", unit, strings.ToUpper(result), why)\n\tif result != \"ok\" {\n\t\tfmt.Println(\"the commit gate will deny until this passes.\")\n\t}\n\treturn nil\n\n--- downstream: the PreToolUse commit gate does NOT use the exit code, and still denies ---\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: TASK-003 verdict is \\\"blocked\\\", not \\\"ok\\\". one or more gate checks failed\\nsafe_next_step: fix the failures, then run batten check again\\nTo proceed anyway (recorded): batten override TASK-003 --reason \\\"...\\\"\"}}\n\n--- no in-repo consumer of the exit code ---\n$ grep -rn \"batten check\" .github/ scripts/ plugin/ README.md DESIGN.md  ->  no matches (only docs/field-test/* and the packaged .exe)\n$ grep -rn \"cmdCheck\" cmd/batten/*_test.go  ->  no matches (no test asserts any exit code for check)",
    "positive_control": "Three, because Trap 2 bit me twice. (1) main() DOES propagate non-zero out of cmdCheck: `batten check TASK-001 --gate nope` -> 'batten: no gate \"nope\" in batten.yaml', EXIT=1; a malformed batten.yaml -> EXIT=1. So exit 0 is a genuine success claim, not a binary that can only exit 0. (2) Contrast control: a gate whose every check passes -> '  ✓ echo lint-ok / ✓ echo test-ok / TASK-002: OK (batten-verified). all gate checks passed', EXIT=0 — byte-identical exit status to BLOCKED, so the status carries zero information about the verdict. (3) For the 'the commit gate still denies' sub-claim: my first two hook invocations printed nothing and exited 0, which per Trap 2 proved nothing — the cause was Git Bash `$PWD` expanding to /tmp/... which the Windows binary cannot resolve, so loadForHook failed silently. With the Windows path, the mandatory-deny control fired: a unit with a run but NO verdict -> permissionDecision \"deny\", 'batten: TASK-009 has no verdict envelope.' Only then did I trust the TASK-003 deny above.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:803 — cmdCheck ends `return nil` for both outcomes. Make the blocked path non-zero, e.g. after the two Printf/Println lines: `if result != \"ok\" { os.Exit(1) }` (or return a sentinel error that main() maps to exit 1 without re-printing 'batten: ...'). Add a test: no *_test.go asserts any exit code for check."
  },
  {
    "index": 53,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary across FOUR independent classes of invalidity (unknown gate reference, malformed YAML, missing required keys, bad enum value) plus the no-spec-found case: `batten doctor` prints `✗ ... invalid spec: ...` and exits 0 every time. `cmdDoctor()` at cmd/batten/main.go:1187 still does `fmt.Printf` + `return nil` in both ✗ branches (1191-1192 and 1196-1197) at HEAD b7aac14, so it is NOT already-fixed. There is no --strict escape hatch: main.go:53-54 calls `cmdDoctor()` with no arguments, so doctor ignores every flag.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v53/repro\" && cd \"$SB/sandbox/v53/repro\"\ngit init -q -b main . ; git config user.email t@t.io ; git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\n# CASE A - unknown gate reference (the finding's own spec)\ncat > batten.yaml <<'EOF'\nversion: 1\nproject: repro\nunit: {name: TASK, pattern: 'TASK-\\d{3}'}\nphases:\n  - id: verify\n    gate: no-such-gate\n  - id: close\n    requires_verdict: ok\nEOF\n\"$BAT\" doctor; echo \"exit=$?\"\n\n# CASE B - malformed YAML\nexport BATTEN_DB=\"$PWD/state.db\"\nprintf 'version: 1\\nproject: repro\\nunit: {name: TASK\\nphases:\\n  - id: verify\\n' > batten.yaml\n\"$BAT\" doctor; echo \"exit=$?\"\n\n# CASE C - missing required keys\nexport BATTEN_DB=\"$PWD/state.db\"\nprintf 'version: 1\\n' > batten.yaml\n\"$BAT\" doctor; echo \"exit=$?\"\n\n# CASE D - bad enum value\nexport BATTEN_DB=\"$PWD/state.db\"\ncat > batten.yaml <<'EOF'\nversion: 1\nproject: repro\nenforcement: banana\nunit: {name: TASK, pattern: 'TASK-\\d{3}'}\nphases:\n  - id: close\n    requires_verdict: ok\nEOF\n\"$BAT\" doctor; echo \"exit=$?\"\n\"$BAT\" doctor --strict; echo \"exit=$?\"   # no escape hatch: flag ignored, still 0\n\n# CASE E - no batten.yaml anywhere up the tree\nmkdir -p \"$SB/sandbox/v53/nospec\" && cd \"$SB/sandbox/v53/nospec\"\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" doctor; echo \"exit=$?\"\n\n# POSITIVE CONTROL - exit-code plumbing is live in this sandbox\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" no-such-command; echo \"control_exit=$?\"",
    "evidence": "=== CASE A: unknown gate reference ===\n✗ ...\\sandbox\\v53\\repro\\batten.yaml: invalid spec:\n  - phase \"verify\" references unknown gate \"no-such-gate\"\nexit=0\n\n=== CASE B: malformed YAML ===\n✗ ...\\sandbox\\v53\\repro\\batten.yaml: yaml: line 2: did not find expected ',' or '}'\nexit=0\n\n=== CASE C: missing required keys ===\n✗ ...\\sandbox\\v53\\repro\\batten.yaml: invalid spec:\n  - project is required\n  - unit.name is required (the work-item noun: US, ticket, issue...)\n  - unit.pattern is required (regexp matching a unit id)\n  - at least one phase is required\nexit=0\n\n=== CASE D: bad enum value ===\n✗ ...\\sandbox\\v53\\repro\\batten.yaml: invalid spec:\n  - enforcement must be \"enforce\" or \"report\", got \"banana\"\nexit=0\n(`doctor --strict` -> identical output, exit=0)\n\n=== CASE E: no batten.yaml anywhere ===\n✗ no batten.yaml found. Run: batten init\nexit=0\n\n=== POSITIVE CONTROL ===\n$ batten no-such-command\nbatten — procedural memory as data\n  ...usage...\ncontrol_exit=2\n\n=== SOURCE AT HEAD b7aac14 (cmd/batten/main.go) ===\n1187 func cmdDoctor() error {\n1189\t path, err := spec.Find(cwd)\n1190\t if err != nil {\n1191\t\t fmt.Printf(\"✗ no %s found. Run: batten init\\n\", spec.Filename)\n1192\t\t return nil\n1193\t }\n1194\t sp, err := spec.Load(path)\n1195\t if err != nil {\n1196\t\t fmt.Printf(\"✗ %v\\n\", err)\n1197\t\t return nil\n1198\t }\n\nmain.go:93-96 proves a returned error WOULD exit non-zero:\n\tif err != nil {\n\t\tfmt.Fprintln(os.Stderr, \"batten:\", err)\n\t\tos.Exit(1)\n\t}\n(A third ✗-and-return-nil exists at 1241-1242 for `✗ store: %v`.)",
    "positive_control": "n/a for an ALLOW claim, but the exit-code plumbing was controlled explicitly: `batten no-such-command` in the same sandbox, same shell call, returned control_exit=2, and main.go:93-96 exits 1 on any returned error. So exit=0 from doctor is a genuine success code, not a swallowed/unobservable status. Additionally `batten doctor --strict` was probed to rule out an opt-in strict mode; doctor takes no args (main.go:54 `err = cmdDoctor()`), so the flag is ignored.",
    "already_fixed_at_head": false,
    "severity_adjusted": "major",
    "fix_hint": "cmd/batten/main.go:1196-1197 — replace `fmt.Printf(\"✗ %v\\n\", err); return nil` with `return err` (main.go:93-96 already prints it and exits 1). Same one-liner for the `✗ store:` branch at 1241-1242; the missing-spec branch at 1191-1192 may stay 0 only if it stops printing ✗."
  },
  {
    "index": 54,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary: in a fresh git repo with no batten.yaml, `batten init --help` prints no usage, WRITES batten.yaml and exits 0 (same for `-h` and `--bogus-flag`). BUT the destructive-surprise framing is refuted: cmdInit has a pre-existence guard (main.go:1455-1458) that fires before any write, so an EXISTING batten.yaml is never overwritten — a hand-edited file survived byte-identical (sha256 unchanged) and the command exited 1. The report's generalisation 'unknown flags are silently ignored on every subcommand' is also refuted: top-level `batten --help` and `batten help` DO print usage (exit 2), and cmdCheck/cmdClose both have `default:` arms and fail loudly with their usage string on `--help` (exit 1); cmdVerdict fails on stdin JSON parse (exit 1). cmdInit is the only one of the four that both lacks a default arm AND has a write side effect. Real defect, wrong severity: creating one new untracked file in a directory the user just pointed at is annoying and teaches the user the binary ignores its own flags (bad right before they type `--from`), but it is not destructive and `rm batten.yaml` undoes it. Downgraded major -> minor. The repo's own docs/field-test/dimensions.json line 25 already concedes the same point (\"It cannot clobber an existing spec (the overwrite guard fires first)\").",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v54/fresh\" && cd \"$SB/sandbox/v54/fresh\"\ngit init -q -b main . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$SB/sandbox/v54/state.db\"\nls -A\n\"$BAT\" init --help; echo \"exit=$?\"\nls -A\n# --- overwrite probe (the part that matters most) ---\nprintf 'MY HAND EDITED SPEC - DO NOT LOSE\\n' > batten.yaml\nsha256sum batten.yaml\n\"$BAT\" init --help; echo \"exit=$?\"\n\"$BAT\" init; echo \"exit=$?\"\nsha256sum batten.yaml; cat batten.yaml\n# --- generalisation probe ---\n\"$BAT\" --help; echo \"exit=$?\"\n\"$BAT\" help; echo \"exit=$?\"\nmkdir -p ../fresh2 && cd ../fresh2 && git init -q -b main .\nexport BATTEN_DB=\"$SB/sandbox/v54/state.db\"\n\"$BAT\" init -h; echo \"exit=$?\"; ls -A\n\"$BAT\" init --bogus-flag; echo \"exit=$?\"\n\"$BAT\" check --help; echo \"exit=$?\"\n\"$BAT\" close --help; echo \"exit=$?\"\n\"$BAT\" verdict --help < /dev/null; echo \"exit=$?\"",
    "evidence": "--- files before:\n.git/\n--- running: batten init --help\nwrote batten.yaml — a working draft in report mode (gates warn, don't block).\n  project=\"fresh\" unit=\"TASK\", 0 domain(s) detected, graphify found, engram found\nNext:\n  1. fill the invariants (the TODOs) — the highest-value part of the file\n  2. run: batten doctor\n  3. flip enforcement: enforce when you trust the gates\nFor a richer draft (invariants mined from AGENTS.md, migration from a prose doc),\nrun /batten-init inside Claude Code instead — it uses `batten init --scan-json`.\nexit=0\n--- files after:\n.git/\nbatten.yaml\n\n=== OVERWRITE PROBE — the claim does NOT hold ===\n--- sha before:\n8ebe1d23bc683edfbb72e20b8a1f332e560347729dff5595c50cb240655cb677 *batten.yaml\n--- batten init --help (existing file):\nbatten: batten.yaml already exists — remove it or edit it by hand\nexit=1\n--- batten init (no flags, existing file):\nbatten: batten.yaml already exists — remove it or edit it by hand\nexit=1\n--- sha after:\n8ebe1d23bc683edfbb72e20b8a1f332e560347729dff5595c50cb240655cb677 *batten.yaml\nMY HAND EDITED SPEC - DO NOT LOSE\n\n=== 'every subcommand' GENERALISATION — does NOT hold ===\n$ batten --help\nbatten — procedural memory as data\n\n  batten init [--from <prose-doc>]   generate batten.yaml\n  ... (full usage block) ...\nexit=2\n$ batten help\n(same full usage block)\nexit=2\n$ batten init -h        # empty repo\nwrote batten.yaml — a working draft in report mode ...\nexit=0\n$ batten init --bogus-flag\nbatten: batten.yaml already exists — remove it or edit it by hand\nexit=1\n$ batten check --help\nbatten: check: batten check <unit> [--gate <name>]\nexit=1\n$ batten close --help\nbatten: close: batten close <unit> [--status ok|failed|rolled_back]\nexit=1\n$ batten verdict --help < /dev/null\nbatten: verdict must be a JSON envelope {check_id, result, evidence[], why, safe_next_step, requires_confirmation}: unexpected end of JSON input\nexit=1\n\n=== SOURCE, cmd/batten/main.go:1429-1462 (HEAD b7aac14) ===\nfunc cmdInit(args []string) error {\n\tscanJSON, from := false, \"\"\n\tfor i := 0; i < len(args); i++ {\n\t\tswitch args[i] {\n\t\tcase \"--scan-json\":\n\t\t\tscanJSON = true\n\t\tcase \"--from\":\n\t\t\tif i+1 < len(args) {\n\t\t\t\tfrom = args[i+1]\n\t\t\t}\n\t\t}\n\t}                       // <-- no default arm: --help falls through\n\t...\n\tdst := filepath.Join(cwd, spec.Filename)\n\tif _, err := os.Stat(dst); err == nil {\n\t\treturn fmt.Errorf(\"%s already exists — remove it or edit it by hand\", spec.Filename)\n\t}                       // <-- the overwrite guard the finding says is absent\n\tif err := os.WriteFile(dst, []byte(facts.ToYAML()), 0o644); err != nil {\n\nmain() (cmd/batten/main.go:47-90) has no \"--help\"/\"-h\" case; unrecognised os.Args[1] hits `default: printUsage(); os.Exit(2)`, which is why top-level --help works by accident while `init --help` does not.",
    "positive_control": "n/a — no ALLOW claim. Two controls run instead. (1) Write-guard control: seeded an existing hand-edited batten.yaml and re-ran `init --help` — it errored exit 1 and the sha256 was unchanged, proving the write path is guarded and the finding's overwrite implication is false. (2) Help-plumbing control: `batten --help` and `batten help` printed the full usage block (exit 2), proving the binary CAN print usage and that `init --help` printing none is a genuine gap in cmdInit, not an absence of any help facility anywhere.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:1431-1440 — add `case \"--help\", \"-h\": printInitUsage(); return nil` plus `default: return fmt.Errorf(\"unknown flag %q\", args[i])` to cmdInit's arg switch (it is the only one of the four cited that lacks a default arm AND writes a file)."
  },
  {
    "index": 55,
    "verdict": "REFUTED",
    "reason": "REFUTED AS DUPLICATE of finding 18, not as wrong. The behaviour reproduces on the HEAD binary exactly as reported (after `batten close TASK-001` -> status=ok, `batten show` still prints build/verify/close as `running`, and every nodes row has ended_at NULL), but it is the same defect, on the same surface (`batten show`), with the same root cause and the same one-line fix as finding 18 — which is already CONFIRMED at major, and of which finding 44 was already judged a duplicate. This is the third restatement of 'phase completion is never recorded': 18 (batten show), 44 (canvas + TUI), 55 (batten show again). It contributes no new failing surface, no new root cause and no new fix: FinishNode still has exactly one production caller (internal/hooks/hooks.go:508, SubagentStop), cmdPhase only AddNodes the incoming phase, and CloseRun writes only the `runs` table. Its `expected` field is strictly about phases, so the incidental `[subagent] general-purpose running owns 2 file(s)` line in its `observed` is not a separate claim — and in my sandbox subagent nodes DO reach a terminal state (see positive control), so that line reflects a subagent whose SubagentStop hook never fired, not a defect. Finding 55 is also strictly weaker than 18: it is filed at `minor` where 18 is `major`, and 18 additionally carries the run-scoping caveat (phase node ids are global, so a naive FinishNode would steal another run's node) that any fix must respect.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v55\" && cd \"$SB/sandbox/v55\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\necho hello > README.md && git add -A && git commit -q -m init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" --version   # batten 0.1.0\n\"$BAT\" init\nexport BATTEN_DB=\"$PWD/state.db\"\nfor p in plan build verify close; do \"$BAT\" phase TASK-001 \"$p\"; done   # 'plan' rejected: not in the default batten.yaml\n\"$BAT\" close TASK-001            # denied: needs a verdict\ncat > v.json <<'EOF'\n{\"unit\":\"TASK-001\",\"gate\":\"qa\",\"result\":\"ok\",\"why\":\"checks pass\",\"evidence\":[\"go test ./... -> ok\"]}\nEOF\n\"$BAT\" verdict --unit TASK-001 --file v.json\n\"$BAT\" close TASK-001\n\"$BAT\" show TASK-001\npython -c \"import sqlite3;c=sqlite3.connect('state.db');[print(r) for r in c.execute('select node_id,kind,label,status,ended_at from nodes order by started_at')];print(list(c.execute('select count(*) from nodes where ended_at is not null')))\"\n# positive control, same DB/renderer:\nW=$(pwd -W)\n\"$BAT\" phase TASK-002 build\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"a1\",\"agent_type\":\"general-purpose\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" show TASK-002\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$W\"'\",\"hook_event_name\":\"SubagentStop\",\"agent_id\":\"a1\",\"last_assistant_message\":\"all good\"}' | \"$BAT\" hook SubagentStop\n\"$BAT\" show TASK-002",
    "evidence": "$ \"$BAT\" phase TASK-001 plan\nbatten: no phase \"plan\" in batten.yaml        # the reported command's first step does not even exist in the default draft\n\n$ \"$BAT\" verdict --unit TASK-001 --file v.json\nverdict recorded: TASK-001 qa=ok (1 evidence)\n$ \"$BAT\" close TASK-001\nTASK-001 closed (ok). Write-set claims released — files it held are free again.\n$ \"$BAT\" show TASK-001\nTASK-001  run=TASK-001-1785261120679143200-e65432ff  status=ok  phase=close  base=49b0274\n  [phase] build                    running \n  [phase] verify                   running \n  [phase] close                    running \n\nverdict qa=ok (agent): checks pass\n  - go test ./... -> ok\n\n$ sqlite (nodes / count of terminal nodes)\n('p-build',  'phase', 'build',  'running', None)\n('p-verify', 'phase', 'verify', 'running', None)\n('p-close',  'phase', 'close',  'running', None)\nterminal nodes: [(0,)]\nRUNS: ('TASK-001-1785261120679143200-e65432ff', 'close', 'ok', 1785261155)\n\n=> byte-for-byte the same state finding 18 already recorded (v18.json: same three phase rows `running`, ended_at NULL, count 0, run row ok+ended_at set).\n\nSame root cause at HEAD, unchanged:\n  $ grep -rn FinishNode (non-test Go)\n  internal/store/store.go:411:func (s *Store) FinishNode(nodeID, status string, cost float64) error {\n  internal/hooks/hooks.go:508:\t_ = h.Store.FinishNode(node.NodeID, status, 0)\n  <- exactly one production caller (SubagentStop). cmdPhase only AddNode()s the incoming phase; CloseRun UPDATEs `runs` only.\n\nOverlap table:\n  #18  surface `batten show` after close      root cause: no FinishNode for phase nodes   CONFIRMED (major)\n  #44  surface canvas + TUI                   same root cause                             CONFIRMED, judged duplicate of 18\n  #55  surface `batten show` after close      same root cause                             == #18, same surface\n\nOn the incidental subagent line in #55's observed (`[subagent] general-purpose running owns 2 file(s)`): #55's `expected` says nothing about subagents, and #44's own evidence shows subagents rendering `ok`/`failed`. My control below shows the same.",
    "positive_control": "Ran the inverse control in the same sandbox and DB, through the one path that DOES call FinishNode, to rule out 'the renderer cannot paint terminal nodes' (which would have made the phase observation an artifact rather than missing data):\n  before SubagentStop:  `  [subagent] general-purpose          running `\n  after  SubagentStop:  `  [subagent] general-purpose          ok      `\n  sqlite: ('n-a1','subagent','general-purpose','ok',1785261179)\nSo `batten show` and the store both handle terminal statuses correctly; phase nodes stay `running` purely because nothing calls FinishNode for them — identical to the mechanism finding 18 established. (Side observation, already covered by v18's fix caveat: TASK-002 entering `build` re-used the global node id `p-build` and took over TASK-001's row — node ids are not run-scoped.)",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "",
    "notes": "Not a Trap-1 by-design refutation and not ALREADY-FIXED: the behaviour is real and still present at HEAD. The refutation is dedup only. Anyone triaging should keep finding 18 as the canonical entry (highest severity, most complete fix hint incl. run-scoping) and close 44 and 55 against it; one change in cmd/batten/main.go cmdPhase + internal/store/store.go CloseRun clears all three. docs/field-test/dimensions.json:797 and :469 already record the same behaviour twice on the project's own side, which is the likely reason it keeps being re-discovered."
  },
  {
    "index": 56,
    "verdict": "REFUTED",
    "reason": "DUPLICATE of finding 26 ('batten canvas emits zero edges for any run without a fan-out'), which was already REFUTED as by-design. The two findings are the same claim with different set dressing: 26 observed the no-fan-out case (0 edges), 56 observed the with-fan-out case (2 spawn edges) and complains about the identical residual absence — no plan->build->verify->close chain. 56's own parenthetical, '(Before the subagents existed it was literally 10 nodes, 0 edges.)', is finding 26's observation restated, and 26's `expected` ('Consecutive phases are joined by edges so the canvas reads as a path') is 56's `expected` ('Edges for plan -> build -> verify -> close at minimum') word for word in substance. I reproduced 56's observation exactly on the HEAD binary — `wrote t1.canvas (12 nodes, 2 edges)` with `p-close -> n-node-1a` and `p-close -> n-node-2b`, the same two node IDs the reporter saw — and then confirmed the same by-design grounds 26 was refuted on. (1) The schema has no phase-sequence relation: internal/store/store.go:106 fixes the edge vocabulary at `spawn|depends_on|retry_of|supersedes|rollback`, and the ONLY production writer of edges is internal/hooks/hooks.go:494, which writes `spawn` and nothing else. There is no relation a phase->phase edge could be recorded under. (2) Phase order is POSITIONAL by design, and canvas.go documents it: canvas.go:97-98 says 'Render lays the run out as columns: each phase is a group, its subagents stack below it. Deliberately simple — a run is small, and the layered structure is already in the data.' canvas.go:119 sorts phases by StartedAt and canvas.go:136 lays them out at x = i*gapX. My canvas shows exactly that chain in the x-coordinates: header -420, plan 0, build 420, verify 840, close 1260, verdict 1680 — a strict left-to-right sequence. The path IS drawn; it is drawn in geometry rather than arrows. (3) The render is faithful, not lossy: the store held exactly [('p-close','n-node-1a','spawn'), ('p-close','n-node-2b','spawn')] and the canvas emitted exactly those two edges. Nothing was dropped. What survives is the same wording gap 26 already recorded, not a defect: cmd/batten/main.go:110 calls the artifact 'the run DAG' and README.md:267 says it 'draws the path the work actually took', which primes a reader to expect arrows between phases.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v56\" && cd \"$SB/sandbox/v56\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" init\n# rewrote batten.yaml: unit TASK-\\d+, 4 phases plan/build(fanout,anchor)/verify(gate qa)/close(requires_verdict ok),\n# 2 domains store{path: store/} and api{path: api/}\nmkdir -p store api && echo hello > README.md && echo x > store/a.txt && echo y > api/b.txt\ngit add -A && git commit -qm init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$BAT\" doctor\ngit checkout -q -b TASK-001\nfor p in plan build verify close; do \"$BAT\" phase TASK-001 $p; done\nCWD=\"$SB/sandbox/v56\"\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"node-1a\",\"agent_type\":\"store\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$CWD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"node-2b\",\"agent_type\":\"api\"}' | \"$BAT\" hook SubagentStart\ncat > vb.json <<'JSON'\n{\"check_id\":\"TASK-001\",\"result\":\"blocked\",\"evidence\":[\"go test ./... FAILED: 2 tests\"],\"why\":\"tests fail\",\"safe_next_step\":\"fix the tests\"}\nJSON\n\"$BAT\" verdict --file vb.json --unit TASK-001\ncat > vo.json <<'JSON'\n{\"check_id\":\"TASK-001\",\"result\":\"ok\",\"evidence\":[\"go test ./... passed\"],\"why\":\"all checks green\",\"safe_next_step\":\"commit\"}\nJSON\n\"$BAT\" verdict --file vo.json --unit TASK-001\n\"$BAT\" canvas TASK-001 --out t1.canvas\npython -c \"import json;d=json.load(open('t1.canvas'));print('nodes',len(d['nodes']),'edges',len(d['edges']));[print('  NODE',n['id'],'| x=',n['x']) for n in d['nodes']];[print('  EDGE',e['id'],e['fromNode'],'->',e['toNode']) for e in d['edges']]\"\n# faithfulness check against the store itself:\npython -c \"import sqlite3;c=sqlite3.connect('state.db');print('EDGES IN STORE:',c.execute('select src_node,dst_node,rel from edges').fetchall())\"",
    "evidence": "$ for p in plan build verify close; do batten phase TASK-001 $p; done\nTASK-001 -> phase plan\nanchor: TASK-001 base SHA = 928c4fe\nTASK-001 -> phase build\nTASK-001 -> phase verify\nthis phase must emit a verdict for gate \"qa\" (evidence required)\nTASK-001 -> phase close\n\n$ batten verdict --file vb.json --unit TASK-001\nverdict recorded: TASK-001 qa=blocked (1 evidence)\nthe close gate will deny a commit while this stands at \"blocked\"\n$ batten verdict --file vo.json --unit TASK-001\nverdict recorded: TASK-001 qa=ok (1 evidence)\n\n$ batten canvas TASK-001 --out t1.canvas\nwrote t1.canvas (12 nodes, 2 edges)          <-- the reporter's exact numbers\n\n$ python -c \"...\"\nnodes 12 edges 2\n  NODE g-p-plan   | group | x= -30   | 'plan'\n  NODE p-plan     | text  | x= 0     | '## plan\\n`running`'\n  NODE g-p-build  | group | x= 390   | 'build'\n  NODE p-build    | text  | x= 420   | '## build\\n`running`'\n  NODE g-p-verify | group | x= 810   | 'verify'\n  NODE p-verify   | text  | x= 840   | '## verify\\n`running`'\n  NODE g-p-close  | group | x= 1230  | 'close'\n  NODE p-close    | text  | x= 1260  | '## close\\n`running`'\n  NODE n-node-1a  | text  | x= 1260  | '**store**\\ndomain: `store`\\n`running`'\n  NODE n-node-2b  | text  | x= 1260  | '**api**\\ndomain: `api`\\n`running`'\n  NODE verdict    | text  | x= 1680  | '## verdict: ok\\n\\n**all checks green**\\n\\n- go test ./... passed'\n  NODE header     | text  | x= -420  | '# TASK-001\\n\\nrun `TASK-001-...`'\n  EDGE e0 p-close -> n-node-1a ''\n  EDGE e1 p-close -> n-node-2b ''\n\n# <-- identical to the finding's observed block, including the n-node-1a / n-node-2b IDs.\n# The x-coordinates ARE the plan->build->verify->close path: -420, 0, 420, 840, 1260, 1680.\n\n# The render is faithful; nothing was dropped:\n$ python -c \"import sqlite3; ...\"\nEDGES IN STORE: [('p-close', 'n-node-1a', 'spawn'), ('p-close', 'n-node-2b', 'spawn')]\nNODES IN STORE: [('p-plan','phase','plan'), ('p-build','phase','build'), ('p-verify','phase','verify'), ('p-close','phase','close'), ('n-node-1a','subagent','store'), ('n-node-2b','subagent','api')]\n\n# No relation exists that could carry a phase->phase edge — internal/store/store.go:106:\n  rel      TEXT NOT NULL,              -- spawn|depends_on|retry_of|supersedes|rollback\n  PRIMARY KEY (src_node, dst_node, rel)\n\n# ...and the sole production writer only ever writes 'spawn':\n$ grep -rn 'AddEdge' --include=*.go . | grep -v _test.go\n./internal/store/store.go:453:func (s *Store) AddEdge(runID, src, dst, rel string) error {\n./internal/hooks/hooks.go:494:\t\t_ = h.Store.AddEdge(run.RunID, \"p-\"+run.Phase, nodeID, \"spawn\")\n\n# The positional layout is stated outright, internal/canvas/canvas.go:97-98:\n// Render lays the run out as columns: each phase is a group, its subagents stack below it.\n// Deliberately simple — a run is small, and the layered structure is already in the data.\n# canvas.go:119  sort.Slice(phases, func(i, j int) bool { return phases[i].StartedAt < phases[j].StartedAt })\n# canvas.go:136  x := i * gapX\n\n# DUPLICATE proof — findings/f26.json, already REFUTED as by-design in results/v26.json:\n  26.title:    \"`batten canvas` emits zero edges for any run without a fan-out — the run \\\"DAG\\\" is a pile of disconnected cards\"\n  26.expected: \"Consecutive phases are joined by edges so the canvas reads as a path, ...\"\n  56.title:    \"`batten canvas` draws no edges between phases — the 'run DAG' is a row of disconnected boxes\"\n  56.expected: \"Edges for `plan -> build -> verify -> close` at minimum. ... (Before the subagents existed it was literally `10 nodes, 0 edges`.)\"\n# Same defect claim, same file_hint (internal/canvas/), same root cause.",
    "positive_control": "REQUIRED and PASSED — this is an absence claim ('no edges'), so before accepting an absence as meaningful I proved edges CAN traverse the whole pipeline in this same sandbox. Two SubagentStart hooks (agent_type=store and api, both declared domains, fired on branch TASK-001 with an absolute Windows-style cwd) each exited 0, wrote a row into the store's edges table, and appeared verbatim in the canvas JSON as `EDGE e0 p-close -> n-node-1a` / `EDGE e1 p-close -> n-node-2b`. So the hook writer, the store round-trip, canvas.go's edge loop (172-187) and the JSON writer all engage. The phase-sequence edges the finding wants are therefore genuinely absent from the store rather than dropped by a dead render path — and store.go:106's fixed relation vocabulary shows they could not be stored even if someone wanted them. Negative control on the same run: the store's edge set (2 rows) exactly equals the canvas's edge set (2 edges), so the renderer emits every edge it is given.",
    "already_fixed_at_head": false,
    "severity_adjusted": "not-a-defect",
    "fix_hint": ""
  },
  {
    "index": 57,
    "verdict": "REFUTED",
    "reason": "REFUTED AS DUPLICATE of finding 40 (already CONFIRMED, major) — not as wrong and not as already-fixed. The behaviour DOES still reproduce byte-for-byte on the HEAD binary (batten 0.1.0, HEAD b7aac14): on a run that never had a transcript ingested, `batten budget TASK-001` prints the hard zero `TASK-001  0 tokens, $0.00 imputed` plus two empty bars, while `batten runs` prints `—  —` for that same run in the same DB at the same moment. But this is the SAME defect at the SAME root cause as finding 40, not a second one: same command (`batten budget <unit>` on a run with no ingested transcript), same three output lines verbatim, same `expected` (distinguish 'measured and it was zero' from 'never measured', quoting the same README line), and the same single cause — internal/store/store.go:811 and :815 pass the literal `true` for Ceiling.Available on the tokens and imputed_usd ceilings, while :822 computes it honestly for quota_pct. f57's file_hint (cmd/batten/main.go cmdBudget) is downstream of, and already covered by, f40's accepted fix, whose fix_hint explicitly includes 'guard the header line at main.go:1092 the way main.go:416-420 does'. The only textual difference between the two reports is that f57's sandbox declared no quota_pct_per_run, so the third `NOT MEASURABLE` line is absent from its paste — a config difference in the demo repo, not a different defect. Fixing store.go:811/:815 makes f57's pasted output correct with no additional change. Nothing in f57 survives f40's fix, so it adds no new work item.",
    "repro": "SB=$SANDBOX\nmkdir -p \"$SB/sandbox/v57\" && cd \"$SB/sandbox/v57\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" --version          # batten 0.1.0\n\"$SB/bin/batten.exe\" init\n# sandbox batten.yaml ONLY: add `imputed_usd_per_run: 8` under budget: (init writes tokens_per_run: 3000000 already)\ngit add -A && git commit -qm init\n\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" phase TASK-001 build    # opens the run; ingest is NEVER called for it\n\"$SB/bin/batten.exe\" budget TASK-001        # <-- the reported output, verbatim\n\"$SB/bin/batten.exe\" runs                   # same DB, same moment: prints \"—  —\"\n\n# CONTROL (proves the sandbox and the budget surface are live):\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" phase TASK-002 build\nmkdir -p gen\nTS=$(date -u -d '+1 hour' +%Y-%m-%dT%H:%M:%S.000Z)\ncat > gen/t.jsonl <<EOF\n{\"type\":\"assistant\",\"timestamp\":\"$TS\",\"message\":{\"id\":\"m1\",\"model\":\"claude-sonnet-4-5-20250929\",\"usage\":{\"input_tokens\":1000,\"output_tokens\":2000,\"cache_read_input_tokens\":94000,\"cache_creation_input_tokens\":0}}}\n{\"type\":\"assistant\",\"timestamp\":\"$TS\",\"message\":{\"id\":\"m2\",\"model\":\"claude-sonnet-4-5-20250929\",\"usage\":{\"input_tokens\":1000,\"output_tokens\":2000,\"cache_read_input_tokens\":94000,\"cache_creation_input_tokens\":0}}}\nEOF\n\"$SB/bin/batten.exe\" ingest TASK-002 --transcript \"$PWD/gen/t.jsonl\"\n\"$SB/bin/batten.exe\" budget TASK-002\n\"$SB/bin/batten.exe\" budget TASK-001\n\"$SB/bin/batten.exe\" runs",
    "evidence": "=== THE REPORTED OUTPUT, REPRODUCED VERBATIM ON THE HEAD BINARY ===\n$ batten budget TASK-001        # run opened by `batten phase`, ingest never called\nTASK-001  0 tokens, $0.00 imputed\n  · tokens       0 / 3.0M  [............]\n  · imputed_usd  $0.00 / $8.00  [............]\nEXIT=0\n\n(f57's `observed`, character for character:\n  TASK-001  0 tokens, $0.00 imputed\n    · tokens       0 / 3.0M  [............]\n    · imputed_usd  $0.00 / $8.00  [............]\n f40's `observed` for the same command on unit US-005 is the same three lines plus the\n `· quota_pct NOT MEASURABLE` line, which appears only because f40's config also declared\n quota_pct_per_run. My sandbox, like f57's demo repo, declared only the first two ceilings.)\n\n=== SAME RUN, SAME DB, SAME SECOND — the surface that is honest ===\n$ batten runs\nUNIT      STATUS   PHASE  VERDICT  TOKENS  IMPUTED\nTASK-002  running  build  —        194.0k  $0.12\nTASK-001  running  build  —        —       —\n\n=== CONTROL — the budget surface is live and does react to real usage ===\n$ batten ingest TASK-002 --transcript gen/t.jsonl\nTASK-002: +2 requests, 194.0k tokens total, $0.12 imputed\n$ batten budget TASK-002\nTASK-002  194.0k tokens, $0.12 imputed\n  · tokens       194.0k / 3.0M  [............]\n  · imputed_usd  $0.12 / $8.00  [............]\nSo the zero on TASK-001 is a real \"never measured\", not a dead sandbox.\n\n=== THE ROOT CAUSE — UNCHANGED AT HEAD, AND IT IS f40's ===\ninternal/store/store.go:802-825 (read at HEAD b7aac14, verbatim)\n  802  // Budget reports every declared ceiling. A ceiling we cannot measure is reported as\n  803  // unavailable, never as zero — a budget tool that invents a number is worse than none.\n  804  func (s *Store) Budget(runID string, tokensCap int64, usdCap, quotaCap float64) ([]Ceiling, error) {\n  ...\n  810   if tokensCap > 0 {\n  811    cs = append(cs, Ceiling{\"tokens\", float64(r.TokensSpent), float64(tokensCap),\n  812     r.TokensSpent >= tokensCap, true})                                <-- Available hardcoded true\n  813   }\n  814   if usdCap > 0 {\n  815    cs = append(cs, Ceiling{\"imputed_usd\", r.ImputedUSD, usdCap, r.ImputedUSD >= usdCap, true})   <-- ditto\n  816   }\n  ...\n  822    cs = append(cs, Ceiling{\"quota_pct\", burned, quotaCap, ok && burned >= quotaCap, ok})  <-- the honest one\n\nThat is exactly the location the CONFIRMED verdict on finding 40 records:\n  v40.json fix_hint: \"internal/store/store.go:811 and :815 — replace the literal `true` in both\n  Ceiling literals with a measured flag derived from whether the run has any usage row ...\n  cmdBudget (main.go:1108-1112) and mcp.budget (mcp.go:596-601) already branch on !Available,\n  so both surfaces become honest for free; ... and guard the header line at main.go:1092 the\n  way main.go:416-420 does.\"\n\n=== WHY f57's DIFFERENT file_hint DOES NOT MAKE IT A DIFFERENT DEFECT ===\nf57 points at cmd/batten/main.go cmdBudget. Both of the lines f57 pasted are produced there,\nbut neither is an independent bug:\n  main.go:1102-1112  for _, c := range cs { ... case !c.Available: print \"NOT MEASURABLE\" }\n    — cmdBudget ALREADY has the correct branch. It prints the two zero bars only because\n      store.Budget handed it Available=true. Fix store.go:811/:815 and these two lines change\n      with no edit to main.go.\n  main.go:1092  fmt.Printf(\"%s  %s tokens, $%.2f imputed\\n\", r.UnitID, humanTokens(...), r.ImputedUSD)\n    — the header line, and the one part of f57's paste that is genuinely in cmdBudget. It is\n      already named inside f40's accepted fix_hint (\"guard the header line at main.go:1092\").\nSo the union of f57 ⊆ the union of f40. There is no residue.\n\n=== NOT 'ALREADY FIXED' — I want this unambiguous ===\nThe defect is live at HEAD; I reproduced it above. already_fixed_at_head is false. This\nREFUTED is a de-duplication of the field-test backlog, not an acquittal of the behaviour.\nThe behaviour remains a real major defect; it is tracked, CONFIRMED and fix-located under\nfinding 40, and closing that one closes this one.",
    "positive_control": "n/a — no ALLOW claim; this is a reporting defect, not a gate claim, and every command produced output (Trap 2 does not apply). I ran a liveness control anyway to kill the 'your sandbox was simply dead / nothing was ever recorded' alternative: on a second run TASK-002 in the same DB I ingested a 2-record transcript with timestamps after the run opened. `batten ingest TASK-002 --transcript gen/t.jsonl` returned `TASK-002: +2 requests, 194.0k tokens total, $0.12 imputed`, and `batten budget TASK-002` then printed `· tokens 194.0k / 3.0M` and `· imputed_usd $0.12 / $8.00`. Decisively, one `batten runs` invocation printed BOTH rows at once — `TASK-002 ... 194.0k $0.12` and `TASK-001 ... —  —` — proving the ledger, the pricer and the budget surface all engage in this sandbox, and that batten holds the discriminating 'never measured' signal it declines to use in cmdBudget.",
    "already_fixed_at_head": false,
    "severity_adjusted": "not-a-defect",
    "fix_hint": "",
    "notes": "READ severity_adjusted CAREFULLY: 'not-a-defect' means there is no defect HERE THAT IS NOT ALREADY FILED — it does NOT mean printing an unmeasured ledger as `0 tokens, $0.00` is acceptable. It is not, it is live at HEAD, and its severity is major as CONFIRMED under finding 40. I set the field this way so triage does not count the same one-line fix twice; if the harness treats this field as the standalone severity of the underlying behaviour, read it as major.\n\nDUPLICATE TEST I APPLIED (all five must hold, and all five do):\n  1. same trigger    — `batten budget <unit>` on a run with no transcript ingested. f57: TASK-001. f40: US-005. Identical precondition.\n  2. same symptom    — the same three lines, verbatim; f40 additionally shows the quota_pct line because its config declared a third ceiling.\n  3. same cause      — store.go:811/:815, Ceiling.Available hardcoded true. Verified unchanged at HEAD b7aac14 by reading the file.\n  4. same expected   — both ask for 'NOT MEASURED / never measured' wording and both quote README.md:194-195 ('batten never invents a number...').\n  5. no residue      — f57's only cmdBudget-specific line (main.go:1092, the header) is already inside f40's fix_hint. Applying f40's fix makes f57's pasted output correct with zero further edits.\n\nWHAT WOULD HAVE MADE THIS NOT-A-DUPLICATE, and why none applies:\n  (a) If f57's run HAD been ingested and the tokens were silently dropped by the TS fence in\n      store.RecordUsage — that is finding 29, a different layer with a different fix. Ruled out:\n      f57's own command comment says 'on a run with no ingested transcript', and my repro never\n      called ingest for TASK-001, so the fence is not in play and the zero still prints.\n  (b) If the demo-repo path reached budget through some other code (e.g. a plugin/skill surface\n      rather than the CLI). Ruled out: f57's `command` field is the plain CLI `batten budget TASK-001`,\n      and that is the invocation I reproduced.\n  (c) If f57 exposed a distinct wording bug f40 does not reach. Closest candidate, and I judge it\n      part of the same fix rather than a new finding: main.go:1111 hardcodes the unavailable text\n      as 'NOT MEASURABLE — install the statusline (`batten statusline --install`)' for EVERY kind,\n      so the moment store.go:811/:815 is fixed, the tokens and imputed_usd rows would advise\n      installing the statusline, which has nothing to do with ingesting a transcript. Whoever\n      implements f40 must give that Printf a per-kind reason (mirroring mcp.go unavailableReason)\n      or they will trade a fabricated zero for a misleading remedy. Recorded here as a fix-completeness\n      note ON finding 40, not as a separate defect — it cannot be observed until f40 is fixed.\n  (d) Severity divergence (f57 says minor, f40 says major) is not a dedup criterion; the reporters\n      simply weighted the same behaviour differently. The CONFIRMED call on f40 is major and I agree\n      with it: it is on the default path (every run before its first ingest) and MCP hands an agent\n      `available:true, remaining:3000000` — a ceiling reported as ENFORCED — for a ledger never fed,\n      which is precisely what /batten-night relies on as its unattended tripwire.\n\nTRAP CHECK. Trap 1 does not rescue this behaviour: Trap 1 protects NOT-MEASURABLE-instead-of-0 and\nexplicitly states the converse — 'reporting an unmeasured quantity as a hard 0 IS a real defect' —\nwhich is this. So the REFUTED here rests solely on duplication, never on by-design. Trap 2 does not\napply: no silence, no ALLOW claim.\n\nISOLATION: BATTEN_DB was re-exported in every Bash call; every write confined to\nscratchpad/ft/sandbox/v57; the repo under test was read-only (only `git log`, `grep` and `Read`);\n~/.batten and proyecto_ui were never touched; no push, no remote, nothing written to ~/.claude/."
  },
  {
    "index": 58,
    "verdict": "CONFIRMED",
    "reason": "Reproduced on the HEAD binary (batten 0.1.0, repo b7aac14). With 'this-command-does-not-exist --at-all' declared BOTH as domains.api.check and gates.qa.checks, `batten doctor` emitted zero warnings about either — while still flagging the missing rules file and the missing skill. Exhaustive audit of cmdDoctor (cmd/batten/main.go:1187-1328) and discovery.Validate (internal/discovery/discovery.go:113-151) confirms the precise gap: gate checks are verified for PRESENCE only (main.go:1220-1230, the 'declares no checks' warning), domain `check:` is not verified at all — not even for presence — and RESOLVABILITY of neither is ever tested. doctor already owns the exact mechanism it fails to apply: have()/exec.LookPath at main.go:1338-1341 is used to resolve capabilities.graph.provider and capabilities.compression.provider, i.e. it LookPath-checks optional degradable binaries but not the commands the verdict's evidence[] rests on.",
    "repro": "SB=$SANDBOX\ncd \"$SB/sandbox/v58\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nmkdir -p api && echo hello > api/main.go\n# batten.yaml written with:\n#   domains.api.rules: api/DOES-NOT-EXIST.md\n#   domains.api.check:  ['this-command-does-not-exist --at-all']\n#   domains.api.skills: ['no-such-skill-anywhere']\n#   gates.qa.checks:    ['this-command-does-not-exist --at-all']\n#   phases: plan, build(fanout), close(gate qa, requires_verdict ok)\ngit add -A && git commit -qm init\nexport BATTEN_DB=\"$PWD/state.db\"\n\"$SB/bin/batten.exe\" doctor; echo \"EXIT=$?\"\n\n# control A - empty gate checks\nsed \"s|    checks: \\['this-command-does-not-exist --at-all'\\]|    checks: []|\" batten.yaml.bogus > batten.yaml\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" doctor\n\n# control B - identical spec, REAL resolvable command\nsed \"s|'this-command-does-not-exist --at-all'|'git --version'|g\" batten.yaml.bogus > batten.yaml\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" doctor\n\n# control C - what the undetected command does at the gate\ncp batten.yaml.bogus batten.yaml\nexport BATTEN_DB=\"$PWD/state.db\"; \"$SB/bin/batten.exe\" check TASK-1 --gate qa",
    "evidence": "$ batten doctor        # bogus command in BOTH domains.api.check and gates.qa.checks\n✓ ...\\sandbox\\v58\\batten.yaml — project \"probe\", unit \"TASK\", 3 phases, 1 domains\n✓ close gate: phase \"close\" requires verdict \"ok\" on gate \"qa\"\n✓ enforcement: enforce — gates block\n✓ store: .../sandbox/v58/state.db\n⚠ domain \"api\": rules file missing: api/DOES-NOT-EXIST.md\n⚠ domains.api.skills references \"no-such-skill-anywhere\", which is not installed\nEXIT=0\n<- not one word about either bogus check command; matches the report verbatim.\n\n$ batten doctor        # CONTROL B: same file, checks changed to 'git --version'\n✓ ...\\sandbox\\v58\\batten.yaml — project \"probe\", unit \"TASK\", 3 phases, 1 domains\n✓ close gate: phase \"close\" requires verdict \"ok\" on gate \"qa\"\n✓ enforcement: enforce — gates block\n✓ store: .../sandbox/v58/state.db\n⚠ domain \"api\": rules file missing: api/DOES-NOT-EXIST.md\n⚠ domains.api.skills references \"no-such-skill-anywhere\", which is not installed\n<- byte-identical to the bogus run: doctor's output does not vary with check validity at all.\n\n$ batten check TASK-1 --gate qa    # CONTROL C: the problem doctor passed as healthy\n  ✗ this-command-does-not-exist --at-all (exit 1)\n    \"this-command-does-not-exist\" no se reconoce como un comando interno o externo,\n    programa o archivo por lotes ejecutable.\nTASK-1: BLOCKED (batten-verified). one or more gate checks failed\nthe commit gate will deny until this passes.\n<- the exact 'surfaces at 3am, not here where it is cheap to fix' failure that the comment at\n   cmd/batten/main.go:1297-1298 says doctor exists to prevent.\n\nAsymmetry in the project's own schema:\n  batten.schema.json:409 (domains.skills) — \"`batten doctor` verifies each one actually exists\n    — a renamed skill should be caught there, not fail silently in the middle of an overnight run.\"\n  batten.schema.json:390 (domains.check) — \"Take them verbatim from the Makefile / package.json\n    — do not invent a command that does not exist in the repo.\"  <- governing rule, NO validator.",
    "positive_control": "CONTROL A (proves doctor is not simply inert on checks): with `gates.qa.checks: []` doctor DOES fire its checks-aware code path — '⚠ gate \"qa\" declares no checks — it verifies NOTHING and approves on the agent's word. Add gates.qa.checks (take them verbatim from your build files).' So the warning family exists and reaches stdout; the gap is specifically declared-but-unresolvable, exactly as the brief framed it. CONTROL B (differential): swapping the bogus command for the resolvable 'git --version' produces byte-identical doctor output, proving doctor never inspects the command's content. CONTROL C (proves the defect is real, not cosmetic): the undetected command genuinely fails — `batten check` returns BLOCKED with 'no se reconoce como un comando interno o externo'. Not an ALLOW claim, so no hook silence ambiguity applies.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:1230 — after the empty-checks loop, LookPath argv[0] of every sp.Domains[*].Check and sp.Gates[*].Checks via the existing have() (main.go:1338), warn if unresolved; skip entries with shell metachars or ./ to avoid false positives."
  },
  {
    "index": 59,
    "verdict": "CONFIRMED",
    "reason": "Reproduced both halves on the HEAD binary (batten 0.1.0). (a) `batten init` writes only batten.yaml — no .gitignore is created and `git check-ignore .batten/...` exits 1 (not ignored). (b) `batten canvas TASK-001` with no vault writes <repo>/.batten/TASK-001.canvas (export.go:85 joins sp.Root), `git status --short` shows `?? .batten/`, and an ordinary `git add -A` stages `.batten/TASK-001.canvas`. Not a mere preference: batten's own repo .gitignore already carries `/.batten/`, and CHANGELOG records that a 'stray canvas' had to be purged from history — the project ignores it by hand but never does it for the user.",
    "repro": "SB=/c$HOME/AppData/Local/Temp/claude/$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v59/clean\" && cd \"$SB/sandbox/v59/clean\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$SB/sandbox/v59/state2.db\"   # DB deliberately OUTSIDE the repo so it cannot pollute git status\n\"$BAT\" init\nls -a\ntest -f .gitignore && cat .gitignore || echo \"NO .gitignore written by init\"\ngit check-ignore -v .batten/x.canvas; echo \"check-ignore exit=$?\"\ngit add -A && git commit -q -m base\n\"$BAT\" phase TASK-001 build\n\"$BAT\" canvas TASK-001\ngit status --short\ngit add -A\ngit diff --cached --name-only\n# control:\ngit reset -q && printf '.batten/\\n' > .gitignore\ngit status --short\ngit check-ignore -v .batten/TASK-001.canvas; echo \"exit=$?\"\ngit add -A && git diff --cached --name-only",
    "evidence": "$ batten --version\nbatten 0.1.0\n\n$ batten init\nwrote batten.yaml — a working draft in report mode (gates warn, don't block).\n  project=\"clean\" unit=\"TASK\", 0 domain(s) detected, graphify found, engram found\nNext:\n  1. fill the invariants (the TODOs) — the highest-value part of the file\n  2. run: batten doctor\n  3. flip enforcement: enforce when you trust the gates\n  (no mention of .gitignore or of where the canvas will land)\n\n$ ls -a\n./  ../  .git/  batten.yaml          <-- HALF (a): only batten.yaml; no .gitignore\n\n$ git check-ignore -v .batten/x.canvas\ncheck-ignore exit=1                   <-- 1 = the path is NOT ignored\n\n$ batten canvas TASK-001\nwrote C:\\...\\scratchpad\\ft\\sandbox\\v59\\clean\\.batten\\TASK-001.canvas (3 nodes, 0 edges)\n\n$ git status --short\n?? .batten/                           <-- HALF (b): batten-generated, inside the work tree\n\n$ git add -A\n$ git diff --cached --name-only\n.batten/TASK-001.canvas               <-- an ordinary `git add -A` stages it\n\nCONTROL (proves the missing entry is the sole cause):\n$ git reset -q; printf '.batten/\\n' > .gitignore\n$ git status --short\n?? .gitignore                         <-- .batten/ no longer reported at all\n$ git check-ignore -v .batten/TASK-001.canvas\n.gitignore:1:.batten/\\t.batten/TASK-001.canvas   exit=0\n$ git add -A; git diff --cached --name-only\n.gitignore                            <-- canvas no longer staged\n\nCorroborating code/docs (read-only, repo under test):\ncmd/batten/main.go:1429-1481 (cmdInit) — writes spec.Filename and prints the 'Next:' block; no .gitignore path anywhere. `grep -rni gitignore --include=*.go .` returns ZERO hits in the whole codebase.\ninternal/export/export.go:85 — res.CanvasPath = filepath.Join(sp.Root, \".batten\", unitID+\".canvas\")  (sp.Root is the repo root; this is the no-vault branch)\nLoopWorkFlow/.gitignore — already contains `/.batten/` and `/*.canvas` with the comment \"Local state ... anything that lands at the repo root is a stray from a manual `batten canvas`\".\nCHANGELOG.md:133-135 — \"A private prompt library, a personal note, a third-party PDF and a stray canvas were purged from the entire history.\"",
    "positive_control": "n/a — no ALLOW claim and no hook involved. I nevertheless ran the equivalent control for a 'did not happen' claim: after manually writing the one line `.batten/` that init omits, `git status --short` stops reporting `.batten/`, `git check-ignore` exits 0, and `git add -A` no longer stages the canvas. That proves my git observation is sensitive to exactly the missing ignore entry rather than to some sandbox artifact. I also placed BATTEN_DB outside the repo so the *.db files could not be confused with the reported untracked path.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "cmd/batten/main.go:1459 (in cmdInit, right after os.WriteFile(dst, ...)): ensure `.batten/` is present in <cwd>/.gitignore (create the file if absent, append idempotently) and print the added line in the 'Next:' block; alternatively default export.Run's no-vault path (internal/export/export.go:85) outside the work tree."
  },
  {
    "index": 60,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary. With a spec carrying one fatal error (bad `reads:`) AND two doctor-level warnings (missing rules file, uninstalled skill), doctor printed ONLY the fatal error and returned. Deleting exactly one line (`reads: [nonexistent_artifact]` -- verified by diff, nothing else on disk changed) made both warnings appear, proving they were true and computable on the first run and were withheld. Not by design: nothing in DESIGN.md/README states doctor stops at the first fatal class, and the loader itself batches ALL of its own errors into one bullet list (control below), so the early return is doctor's, not the loader's. Caveat on the title: fatal spec errors ARE batched, so the real cost is exactly ONE extra round-trip (fatals, then warnings), not 'one round-trip per problem' -- the title overstates it, the observed/expected body is accurate. Hence severity stays polish.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v60\" && cd \"$SB/sandbox/v60\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\n# batten.yaml written by hand with FOUR problems at once:\n#   phases[verify].reads: [nonexistent_artifact]   <- fatal (spec.Validate)\n#   domains.api.rules: api/DOES-NOT-EXIST.md       <- doctor warning (main.go:1289-1295)\n#   domains.api.skills: [no-such-skill-anywhere]   <- doctor warning (main.go:1299-1307)\n#   domains.api.check / gates.qa.checks: this-command-does-not-exist --run\ncp batten.yaml batten.yaml.broken\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" doctor; echo \"EXIT=$?\"      # RUN 1\nsed -i '/reads: \\[nonexistent_artifact\\]/d' batten.yaml               # remove ONLY that line\ndiff --strip-trailing-cr batten.yaml.broken batten.yaml              # proves only line 16 changed\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" doctor; echo \"EXIT=$?\"      # RUN 2\n# control: three fatal errors in one spec -> loader batches them all\nmkdir -p sub && cp batten.yaml.broken sub/batten.yaml   # + version: 2, artifact without {id}\ncd sub && export BATTEN_DB=\"$PWD/../state.db\" && \"$BAT\" doctor; echo \"EXIT=$?\"",
    "evidence": "RUN 1 (fatal + 2 latent warnings present):\n  X ...\\sandbox\\v60\\batten.yaml: invalid spec:\n    - phase \"verify\" reads \"nonexistent_artifact\", which is neither an artifact nor a prior phase\n  EXIT=0\n  <-- nothing else. No store line, no capability lines, no domain warnings.\n\nDIFF between the two runs' specs (only one line, nothing else on disk touched):\n  16d15\n  <     reads: [nonexistent_artifact]\n  $ ls api/     -> ls: cannot access 'api/': No such file or directory   (missing in BOTH runs)\n  $ ls -d .claude -> ls: cannot access '.claude': No such file or directory (missing in BOTH runs)\n\nRUN 2 (identical spec minus that one line):\n  V ...\\sandbox\\v60\\batten.yaml - project \"probe\", unit \"TASK\", 3 phases, 1 domains\n  V close gate: phase \"verify\" requires verdict \"ok\" on gate \"qa\"\n  V enforcement: enforce - gates block\n  V store: .../sandbox/v60/state.db\n  ! domain \"api\": rules file missing: api/DOES-NOT-EXIST.md\n  ! domains.api.skills references \"no-such-skill-anywhere\", which is not installed\n  EXIT=0\n\nCONTROL (loader batches its OWN errors, so 'one report' is the established convention):\n  X ...\\sandbox\\v60\\sub\\batten.yaml: invalid spec:\n    - version must be 1, got 2\n    - phase \"verify\" reads \"nonexistent_artifact\", which is neither an artifact nor a prior phase\n    - artifact path \"docs/plans/static.md\" has no {id} placeholder; every unit would overwrite the same file\n  EXIT=0\n\nSOURCE at HEAD (b7aac14), cmd/batten/main.go:1194-1198 -- unchanged, no fix landed:\n  sp, err := spec.Load(path)\n  if err != nil {\n      fmt.Printf(\"X %v\\n\", err)\n      return nil\n  }",
    "positive_control": "n/a - no ALLOW claim. Two controls run instead: (1) the one-line diff + `ls` of api/ and .claude proving both withheld warnings were TRUE and computable during run 1, so their absence is suppression and not a changed premise; (2) a three-fatal-error spec proving spec.Validate accumulates every error into one bullet list, i.e. the missing report is doctor's early `return nil`, not a loader limitation. Run 2 also proves the domain/skill warning code paths are alive in this sandbox.",
    "already_fixed_at_head": false,
    "severity_adjusted": "polish",
    "fix_hint": "cmd/batten/main.go:1194-1198 - on spec.Load error, print the X, then re-parse permissively (new spec.Parse/LoadPermissive: yaml.Unmarshal + set Root, skip Validate) and run the same warning pass before returning. NOTE re #53: NOT the same fix. #53's minimal fix (`return err` instead of `return nil`) leaves the early exit intact and still hides the warnings; #60's fix leaves the exit code 0. Both are resolved by ONE restructuring of cmdDoctor - permissive parse, print everything (fatals + warnings), then return a non-nil error at the END if the spec was invalid. Do NOT fix by making spec.Load return the parsed spec alongside the error: cmd/batten/main.go:929 (cmdMCP) and :961 discard the error and would start serving an invalid spec."
  },
  {
    "index": 61,
    "verdict": "CONFIRMED",
    "reason": "Reproduced verbatim on the HEAD binary (repo at b7aac14): the deny reason reads \"(n-node-1a/store); you are n-node-2b/api\" — the internal node ids, not the agent ids the operator typed into `batten claim` and the plugin passes as `agent_id`. I could not refute it on any of the three available grounds: (a) it is not already fixed — internal/hooks/hooks.go:450-454 still formats `owner` and `myNode` raw, and no `strings.TrimPrefix(..., \"n-\")` exists anywhere in the tree; (b) it is not deliberate — README.md:77-78 documents this exact message WITHOUT the prefix (`(node-7f3a/ml-C); you are node-2b91/ml-A`), so the shipped behaviour contradicts the project's own advertised output; (c) it is not merely cosmetic — I fed the printed id back to the CLI and `batten claim n-node-2b/api api/api.go` fails with `no subagent \"n-node-2b/api\" on record`, while `batten claim node-2b/api api/api.go` succeeds. The message therefore hands the reading agent an identifier no batten command accepts.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"\nmkdir -p \"$SB/sandbox/v61\" && cd \"$SB/sandbox/v61\"\ngit init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$PWD/state.db\"\nmkdir -p store api && echo x > store/store.go && echo y > store/store_test.go && echo z > api/api.go\ngit add -A && git commit -qm init\n\"$BAT\" init\nsed -i 's/^enforcement: report/enforcement: enforce/' batten.yaml\ngit checkout -q -b TASK-1\nexport BATTEN_DB=\"$PWD/state.db\"; \"$BAT\" phase TASK-1 build\nP=$(pwd -W)\nexport BATTEN_DB=\"$PWD/state.db\"\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$P\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"node-1a/store\",\"agent_type\":\"store\"}' | \"$BAT\" hook SubagentStart\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$P\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"node-2b/api\",\"agent_type\":\"api\"}' | \"$BAT\" hook SubagentStart\n\"$BAT\" claim node-1a/store store/store.go store/store_test.go\n\"$BAT\" claim node-2b/api api/api.go\n# the trespass\necho '{\"session_id\":\"s1\",\"cwd\":\"'\"$P\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"node-2b/api\",\"tool_input\":{\"file_path\":\"'\"$P\"'/store/store.go\"}}' | \"$BAT\" hook PreToolUse\n# is the id it printed usable?\n\"$BAT\" claim \"n-node-2b/api\" api/api.go; echo \"EXIT=$?\"\n\"$BAT\" claim \"node-2b/api\" api/api.go; echo \"EXIT=$?\"",
    "evidence": "$ ... | batten.exe hook PreToolUse   (EXIT=0, raw stdout)\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: write-set collision. store/store.go belongs to another agent's write-set (n-node-1a/store); you are n-node-2b/api.\\nTwo agents must never write the same file — that is what makes the fan-out safe.\\nYour write-set:\\n  api/api.go\\nIf this file genuinely belongs to you, the plan is wrong: fix the plan, do not cross the fence.\"}}\n\nThe ids as the operator typed them (same session, same db):\n$ batten.exe claim node-1a/store store/store.go store/store_test.go\nnode-1a/store owns 2 file(s); any other agent writing them is now denied\n$ batten.exe claim node-2b/api api/api.go\nnode-2b/api owns 1 file(s); any other agent writing them is now denied\n\nThe id the deny message says the agent IS, fed back to the CLI:\n$ batten.exe claim \"n-node-2b/api\" api/api.go\nbatten: no subagent \"n-node-2b/api\" on record; the SubagentStart hook creates it\nEXIT=1\n$ batten.exe claim \"node-2b/api\" api/api.go\nnode-2b/api owns 1 file(s); any other agent writing them is now denied\nEXIT=0\n\nHEAD source, internal/hooks/hooks.go:448-454 (owner and myNode printed raw):\n  mine, _ := h.Store.WriteSet(myRun, myNode)\n  return h.gate(\"PreToolUse\", fmt.Sprintf(\n    \"batten: write-set collision. %s belongs to another agent's write-set (%s); you are %s.\\n\"+\n    ... ,\n    rel, owner, myNode, strings.Join(mine, \"\\n  \")))\ninternal/hooks/hooks.go:479 is where the prefix is minted: `nodeID := \"n-\" + in.AgentID`\n\nThe project's own documented output, README.md:74-84 — no prefix:\n  batten: write-set collision. ml/farslip/train.py belongs to another agent's write-set\n  (node-7f3a/ml-C); you are node-2b91/ml-A.\n\nNothing pins the string and nothing strips the prefix:\n$ grep -rn 'TrimPrefix.*\"n-\"' --include=*.go .      -> (no output)\n$ grep -rn 'write-set collision|you are' --include=*_test.go .  -> (no output)",
    "positive_control": "The finding asserts the CONTENT of a DENY, not an ALLOW, and the deny demonstrably fired (permissionDecision=deny), so the guard engaged — silence-is-not-allow does not apply to the reproducing run. I still ran the paired control required by the brief, the identical payload with ONE field changed: node-2b/api editing its OWN api/api.go produced empty stdout, EXIT=0 (allowed). Deny for the trespass, silence for the legitimate write, from the same binary and same db, proves the guard is live and discriminating rather than blanket-denying. A second control proved the leak is consequential and not a display quirk: `batten claim n-node-2b/api ...` (the id the message prints) is rejected as \"no subagent on record\", while `batten claim node-2b/api ...` (the id the message should print) succeeds.",
    "already_fixed_at_head": false,
    "severity_adjusted": "minor",
    "fix_hint": "internal/hooks/hooks.go:449-454 — the two id verbs must be printed as agent ids, not node ids: `fmt.Sprintf(..., rel, strings.TrimPrefix(owner, \"n-\"), strings.TrimPrefix(myNode, \"n-\"), ...)` (or better, add a `func agentOf(nodeID string) string` next to the `nodeID := \"n-\" + in.AgentID` mint at hooks.go:479 so the two stay symmetric, and use it here). Add a test asserting the rendered string contains `(node-1a/store); you are node-2b/api` and NOT `n-node-`; there is currently no test on this message at all.",
    "notes": "DISTINCTNESS vs finding 8 (asked explicitly): DISTINCT DEFECT, partially-duplicate OBSERVATION. Three grounds. (1) Different code and different failure mode inside the same Sprintf: #8 is the `strings.Join(mine, ...)` argument rendering a blank bullet when the trespasser claimed nothing; #61 is the `owner` and `myNode` arguments rendering unusable ids. In my repro the write-set list was NON-empty (`api/api.go` printed correctly) and the `n-` leak still occurred — so #61 reproduces in a state where #8 does not exist at all. (2) Fixing #8 as its verdict specifies (guard the empty slice) leaves #61 entirely unfixed — the two ids are still wrong. (3) v08's own text draws the line itself: it files the prefix under \"Two adjacent observations, OUT OF SCOPE for this finding\", and its fix_hint's mention of in.AgentID governs only the NEW hint sentence it proposes to add, not the two existing id verbs. Note also that v08's adjacent observation covered only the `you are n-A2` half; the owner half `(n-node-1a/store)` is not mentioned anywhere in v08. Caveat for the ranker: because v08 recorded the 'you are' half in passing, #61 is best treated as a partial duplicate for reporting purposes — it should be MERGED with #8 into one fix at hooks.go:449-454 rather than counted as an independent second bug. What #61 adds that #8 does not is the evidence that the printed id is REJECTED by `batten claim`, and the README:77-78 contradiction. ALREADY-FIXED does not apply. Isolation respected: BATTEN_DB exported in every batten invocation, all work confined to sandbox/v61, repo under test at b7aac14 with `git status --porcelain` empty after my run; no push, no remote, nothing written to ~/.batten or ~/.claude."
  },
  {
    "index": 62,
    "verdict": "REFUTED",
    "reason": "The headline claim is false on the HEAD binary: `batten claim` IS usable from a plain terminal. I opened a run with `batten phase`, registered the subagent with `batten hook SubagentStart` (a command `--help` already lists) piped from bash, and the finding's exact `batten claim node-1a/store store/store.go store/store_test.go` then printed \"node-1a/store owns 2 file(s)\" and exit=0. The claim was not cosmetic: a second agent editing store/store.go was DENIED by the write-set guard. The reporter's own `why` text admits they did this too (\"I did exactly that to produce the demo\"), so the title contradicts the report's own body. What survives is only a small docs gap — `--help` does not name the precondition — but the error message itself names both the cause and the exact remedy (\"the SubagentStart hook creates it\"), and `batten hook <event>` is in the same `--help` block, so the whole chain is discoverable without reading source. Not a defect at the severity or shape reported.",
    "repro": "SB=$SANDBOX\nBAT=\"$SB/bin/batten.exe\"; WD=\"$SB/sandbox/v62\"\nmkdir -p \"$WD\" && cd \"$WD\" && git init -q . && git config user.email t@t.io && git config user.name tester\nexport BATTEN_DB=\"$WD/state.db\"\n\"$BAT\" init\nmkdir -p store && echo 'package store' > store/store.go && echo 'package store' > store/store_test.go\ngit add -A && git commit -qm init\n# (1) the finding's exact command, cold:\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" claim node-1a/store store/store.go store/store_test.go; echo \"EXIT=$?\"\n# (2) open a run, then register the subagent FROM THE SAME PLAIN SHELL:\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" phase TASK-1 build\nexport BATTEN_DB=\"$WD/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$WD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"node-1a/store\",\"agent_type\":\"store\"}' | \"$BAT\" hook SubagentStart\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" show TASK-1\n# (3) the finding's exact command again:\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" claim node-1a/store store/store.go store/store_test.go; echo \"EXIT=$?\"\n# (4) prove the terminal-issued claim is really enforced:\nsed -i 's/^enforcement: report/enforcement: enforce/' batten.yaml\nexport BATTEN_DB=\"$WD/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$WD\"'\",\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"a2\",\"agent_type\":\"other\"}' | \"$BAT\" hook SubagentStart\nexport BATTEN_DB=\"$WD/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$WD\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"a2\",\"tool_input\":{\"file_path\":\"'\"$WD\"'/store/store.go\"}}' | \"$BAT\" hook PreToolUse\nexport BATTEN_DB=\"$WD/state.db\"; echo '{\"session_id\":\"s1\",\"cwd\":\"'\"$WD\"'\",\"hook_event_name\":\"PreToolUse\",\"tool_name\":\"Edit\",\"agent_id\":\"a2\",\"tool_input\":{\"file_path\":\"'\"$WD\"'/README.md\"}}' | \"$BAT\" hook PreToolUse\nexport BATTEN_DB=\"$WD/state.db\"; \"$BAT\" --help",
    "evidence": "# (1) cold, no registration -- the finding's observed output reproduces:\n$ \"$BAT\" claim node-1a/store store/store.go store/store_test.go\nbatten: no subagent \"node-1a/store\" on record; the SubagentStart hook creates it\nEXIT=1\n\n# (2) register from the SAME plain bash shell (no Claude Code anywhere):\n$ \"$BAT\" phase TASK-1 build\nanchor: TASK-1 base SHA = 087c8b0\nTASK-1 -> phase build\nEXIT_PHASE=0\n$ echo '{...\"hook_event_name\":\"SubagentStart\",\"agent_id\":\"node-1a/store\",\"agent_type\":\"store\"}' | \"$BAT\" hook SubagentStart\nEXIT_HOOK=0\n$ \"$BAT\" show TASK-1\nTASK-1  run=TASK-1-1785261451522193600-6fb9bc9c  status=running  phase=build  base=087c8b0\n  [phase] build                    running\n  [subagent] store                    running        <-- node exists, created from a terminal\n\n# (3) the finding's exact command now SUCCEEDS:\n$ \"$BAT\" claim node-1a/store store/store.go store/store_test.go\nnode-1a/store owns 2 file(s); any other agent writing them is now denied\nEXIT_CLAIM=0\n\n# (4) the terminal-issued claim is really enforced, not cosmetic:\n$ ... PreToolUse Edit store/store.go as agent a2\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"batten: write-set collision. store/store.go belongs to another agent's write-set (n-node-1a/store); you are n-a2.\\nTwo agents must never write the same file - that is what makes the fan-out safe....\"}}\n\n# residual (accurate, but only this): HEAD --help still omits the precondition\n$ \"$BAT\" --help\n  batten hook <event>                hook entrypoint (stdin JSON -> stdout JSON)\n  batten claim <agent-id> <file>...  declare a subagent's write-set",
    "positive_control": "Two, both required by Trap 2. (a) DENY control: after the terminal-issued claim, a PreToolUse Edit of store/store.go by a different agent (a2) returned an explicit permissionDecision=deny naming the owning node n-node-1a/store -- so the claim really landed in the store and the guard engages in this sandbox. (b) ALLOW control: the same payload with only file_path changed to an unclaimed README.md was silent, exit 0. Deny fires, allow does not -- the hook is live, so the silence is meaningful. Separately, my FIRST SubagentStart attempt was silent-and-ineffective because I passed an MSYS-style cwd (/tmp/...) that loadForHook (cmd/batten/main.go:195) could not resolve; `batten show` proved no node was created, and re-running with a Windows-style cwd created it. That is a Trap-2 near-miss I caught with `batten show`, not a defect in claim.",
    "already_fixed_at_head": false,
    "severity_adjusted": "polish",
    "fix_hint": ""
  }
]